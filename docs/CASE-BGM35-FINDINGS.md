# BGM35 还原任务：查出的问题全表（按发现顺序）

> 用户 2026-09-16 要求："这些检查出的问题也要写一下，文本预算最后再说"。
>
> 记录**现象 + 实测数据 + 根因 + 修法 + 现状**，供以后复现与自查。
> 这张表本身就是教训：**10 个问题里 9 个是用户先听出来/先指出的**，
> 只有 1 个（BOM）是工具报错。工具的盲区（时间平均统计量）掩盖了大部分问题。

## 1. 逐段乐器配比全错 —— **最晚发现、也最根本**

| | 实测 |
|---|---|
| 原曲（`b35_stem_share.py`，逐段分轨能量占比） | piano **0.0% ~ 20.9%** · guitar **3.7% ~ 24.2%** · bass **0.3% ~ 37.2%** · drums **10.8% ~ 32.6%** |
| 主导乐器序列 | `bass→bass→other→bass→bass→drums→drums→bass→other→…→drums→…→other`（一直在换） |
| 我方（改前） | **每一段所有乐器在场、全曲一套固定配比** |

**根因**：第一版设计就假定"全曲一套配比"，之后十几轮全在调参数（密度/频带/力度/转录），
**从没质疑那个前提**。而"每个地方用的乐器都有所不同"是编曲常识。
**数据其实早就在手上**——第一次跑 Demucs 分轨时就统计过各轨 RMS，但只看**全曲平均**。

**修法**：`b35_segment_mix.py` 把原曲逐段逐轨能量占比换算成 CC7（用实测曲线 `cc^1.9`，线性换算会过冲），
写进 `arr.mix`（引擎在每段起点写 CC7；全曲响度归一化不消除**段间**相对差异）。
只调有分轨依据的轨；Melody 设 ±20% 下限（主旋律不该被压）。

**验收**（`b35_verify_segmix.py`：逐段响度代理 vs 原曲占比的相关）：
Piano **0.93** · Hook **0.91** · Perc **0.90** · Bass **0.82** · Arp 0.57 · 平均 **0.70**
（全曲一套配比时应当≈0）。MIDI 里 9 条轨各 **27 个 CC7**（原来每轨 1 个）。

## 2. 开头 0.4 秒整个丢了

音频 **0.00s** 就有钢琴（36/38/41/44/46/52/77/89），我的转录第一个音在 **0.40s**。
根因：第一版转录参数过保守（`-8dB` / 8 帧 / 每格最多 3 音），弱力度下的内声部进不来。
修法：`b35_retrans.py`（`-15dB` / 3 帧 / 每格 6 音），**命中率 3.5 倍**。

## 3. 转录漏音（逐格漏 3~10 个音）

逐 16 分格对照（`b35_note_audit.py`）：第 1 拍实际 8 个音、我只写 4 个。
同一根因（参数保守），同一修法。

> ⚠ 这里踩过一个**坏尺子**：第一版用"逐格音高集合命中率"，对**单音轨 Bass** 也报
> "每格 4~9 个音"——那其实是**一个音的泛音列**（29/33/36/41/47/52/57 是基频谐波）。
> 用它算出的"命中率 4~7%"**毫无意义**，已撤回。改用**音高重心**（能量加权平均音高，抗泛音）。

## 4. Piano 音高系统性偏高 4.77 半音

`b35_centroid_audit.py`：Piano 平均 +2.99、**中位 +4.77 半音**。
根因：泛音剔除只在**同一格内**做，而最常见形态是"低音在第 4 格、二次谐波在第 5 格才检出"——
跨格泛音全活了下来。
修法：跨格剔除 + **逐轨校准跨度**（piano ±2 → 修好到 **+0.17**；guitar/other 用 ±0 因为
八度叠音是真实内容，跨格 ±2 会压低 3.7~4.6 半音）。
现状：Piano −0.48 · Hook −0.52 · Arp +0.89 · Bass −1.92（中位）。

## 5. MIDI 力度全平 + 整体过重

三轮递进：
① 发现 **Piano/Hook/Arp/Bass 3038 个音全是力度 84**（`notes_extra` 写事件时硬编码），
   而原曲同声部真实跨度 38~54dB → 听感"像打字机"。修：按分轨音频逐音峰值包络补 velocity。
② 用户提供对照模板后发现**整体过重**：模板力度 P10/P50/P90 = **39/51/72**，
   我 45/**82**/**118**（GM 音源 100+ 明显"硬、炸"）。
   修：`B35_VEL_CENTER`（基准 84→**51**）、`arr.perc_vel_max`（127→85）、
   `patterns.mel_vel_center`（96→52）。
③ 现状：全曲力度 **44/59/85**，向模板靠拢。

## 6. 复音度太高（声部堆太厚）

模板复音度中位 **3.0**、我 **5.0~6.0**。修：密度再压（target_scale 0.30→0.20）；
现状中位 **5.0**，仍高于模板。

## 7. 鼓：从第 1 小节就敲满 + 判据过松造成规律性空洞

两级问题：
① 原曲第 1 小节 **0 个鼓点**、第 2 小节 6 个（几乎听不见）—— 是**渐入**；
   我的 `drum_grid` 构建端用 top-k **强制**给每小节选 k 格，把静音小节也填上了。
② 第一版修法用**统一阈值**（`_dn < 8`）→ 把第 5/7 小节（各 5 个点、原曲 7~9 个）也误静音，
   前 12 小节变成 `[0,0,0,11,0,11,0,11]` 的**规律性空洞**。
修法：两档判据（曲首前 2 小节 `< 8` 强制静音、其余 `< 3`）+ 静音小节**不许退回固定套路**。
现状前 12 小节 `[0, 0, 6, 11, 6, 11, 6, 11]`。

> ⚠ **鼓的格数不是独立旋钮**：它挂在 `arr.density` 上（grid 按每段音符数算 density）。
> 改鼓 → 每段音符数变 → density 档跳 → 编配全变（实测 `density` 81.6 → 75.8）。
> 用户口径："原曲有鼓但是控制不好，**最后再解决**" —— 未解决。

## 8. 钟琴撒点（"不流畅"的直接来源）

我把"稀疏小节补高频层"的判据写成 `_dgn < 3`，而网格每小节常只有 0~2 个鼓点 →
**几乎每个小节都被塞了钟琴**。钟琴是**点状**音色，到处撒点 = 不连贯。
修：回到**段级标记**，且 `density == 0` 的极安静段**完全不出钟琴**（原曲第 1 小节实测 0 个 Glock）。

## 9. `notes_extra` 的 target 缩放过采样 / density 档被连带改

按"每段总音数的形状"去缩放 4 条**乐音轨**更差（总分 75.5 → 73.7、density 81.6 → 75.8）：
因为"每段音数"里鼓占约一半，按总音数形状缩放乐音轨会**把乐音分布扭了**，鼓却没跟着动。
修：默认不看形状（全曲一个系数）。

## 10. `PowerShell Set-Content -Encoding UTF8` 写 BOM → studio 读不到

`render.json` 带 BOM → `json.load` 抛 `Unexpected UTF-8 BOM` → 面板里那首歌
`has_ogg=False / ref=''`，看起来像"面板不认我的 ogg"。
修：`studio/server.py` 的 `read_json` 改用 `utf-8-sig`。

## 11. `notes_extra` 抽样把和弦逐个拆散（2026-09-16，用户报"6 个时间段不像"）

用户逐段听出不像的时间窗：0–1s、12–15s、20–35s、61–74s、97–112s、123–125s。
换算成小节（150BPM → 1 小节 1.6s）后与参考 MIDI 比**逐小节音高集合**，
发现**用户报的正是全曲吻合度最低的一批**（bar 8 = 0.18、bar 20 = 0.18、bar 42 = 0.13；
全曲均值 0.32）—— **耳朵比指标先发现问题**。

偏差方向高度一致：**"缺"的全是低音/中声部（MIDI 27–60）、"多"的全是高音（60–96）**。

**根因一（引擎 bug）**：`target` / `_sec_cap` 的抽样按**单个音符**等间隔取，
**把和弦逐个拆散**。实测第 1 小节 1.00 拍处 6 个音（F4+C2+G4+G3+C3+A#4）只留下第 5 个 **G4**；
而该小节的扒谱与原曲 CQT 逐音吻合（C2/C3/G3/A#3/D4/G4）——**扒得对，是抽样毁了它**。
修：抽样改为按**起音时刻整组**（`song_engine._pick_onset`），同一 onset 的音整组留/整组走；
`target` 仍按音符数解释（先按平均组大小换算成组数）。

**根因二（扒谱音区）**：逐小节比"原曲分轨最低音 vs 我的对应轨最低音"，
**Piano +12、Arp +12、Hook +5、Bass +8 半音** —— CQT 扒谱在钢琴低音区抓到更强的**第二泛音**。
修：只在"我的最低音比原曲高 >6 半音"的 **4 小节窗**里整窗降 12
（逐小节判断会八度跳变 = 突兀，用户首要标准是和谐）。

实测（与用户认可的参考 MIDI 比逐小节音高集合）：
**Jaccard 0.321 → 0.364，误差小节 124 → 92**，低音偏差 Piano +7→+3、Arp +9→+2。
同批扫过 `target` 档位 ×1 / ×2 / ×3.15 / ×5 → **×2 最优**（0.353）。

## 12. 低音线怎么量：分轨必须先高通，否则全是次声伪影

`demucs` 分轨在 16–33Hz 有持续能量（C0 = 16.35Hz，人耳听不到）。
直接取 CQT 最低峰会把 `fmin` 那个 bin 当成"最低音"——
实测**所有** stem 的逐小节最低音都被报成 **C1**（恰为 fmin），
于是得出"原曲贝斯比我的低 22 个半音"这种假象（第一版还据此改了方向）。
修：**40Hz 高通后**再取最低显著峰（基频恒为泛音列里最低者，此法对单音/和弦都稳）。
高通后：bass 中位 E1、piano B1、guitar G2、other F2 —— 才是可用参考。

> ⚠ 顺带撤回一个曾经的结论：`b35_render.py` 当年以"160Hz–18kHz 低 5~13dB"为由
> 把 `TR_SHIFT` 归零。现在看那是把**音区偏高**误判成**频段缺失**。
> （不过实测 `TR_SHIFT` **对 `notes_extra` 不生效** —— 引擎直接写原始 pitch，
> 所以归零与否不改变还原结果；真正偏高的是扒谱本身，见第 11 条根因二。）

## 附：两个"过程性教训"（不是 bug，但同样重要）

- **BPM 误判并撤回**：中途据 `beat_track`/自相关说"应该是 152 不是 150"。
  用底鼓起音裁决：**相位集中度 R 150BPM=0.550 vs 152BPM=0.039**（越接近 1 越贴合），
  底鼓间隔主峰 0.378~0.404s（正好 0.4），207 小节 ×1.6s = 331.2 ≈ 音轨 331.9s。
  **150 是对的**。教训：单一工具（`beat_track`）会给出局部最优的错觉，要有独立标尺。
- **"更激进 ≠ 更像"**：逐块 EQ 试了三种口径，只有最保守的那个（4–16kHz 双向 ±6dB）不比现状差；
  逐块×逐频带全匹配反而更差（总分 75.5 → 68.7、RMS 差 8dB）——
  因为它把 `band_match` 已经做准的**全曲平衡**也一起改了。
  教训：**逐块修正只能改"块间相对差异"，不能碰全曲平衡**。

## 附 2：自检抓到的两个"数据污染"问题（2026-09-16）

1. **顶层 `melody.M` 是全局小节号**（12/15/…/197），而每段的 `melody: 'M'` 会取用它 →
   `check_song` 按"段内小节号"解释，于是**每个段都报上千行"小节号越界"**。
   真实旋律走 `sections[i].melody_extra`（段内 0~7，核验过），所以 `melody['M']` 是**死数据**。
   修：`b35_extra.py` 把它清成空表。
2. **实验曲不该进曲库**：把还原曲放进 `songs/99_b35_remake/` 后，自检一次性报 **7 项失败**
   （`render_json_schema` / `audio_health` / `track_ranges_musical` / `accompaniment_harmony` …）
   —— 这些都是"这首曲子本身没做到"的真实检查，不是 bug，但会**淹没曲库的验收信号**。
   修：移出仓库，用 studio 的 `--root` 外置：
   `python studio\server.py --port 8765 --root D:\test\llm_direct\b35_studio`
   （`--root` 指**含 `songs/` 的目录**，不是直接指歌曲目录）。
   移出后自检回到 **115/115**。

## 13. 「逐小节 Jaccard vs 模板」是**坏尺子**：模板是钢琴缩编，不含贝斯/鼓（2026-09-16 晚）

`BGM35 (1).mid`（用户认可的参照）实测：5730 音 · 音高 27–98 · 中位 53 · **48–71 占 3230 音**。
而原曲分轨实测 **bar 7–9 是 bass 占 75.9% 的贝斯主导段**，模板那三小节只有 12 个音、没有低音 ——
于是我的贝斯**响得对**，却被逐小节 Jaccard 记成"多"。

**证据**：`b35_rawjac.py` 报"模板独有音高 = [97, 98]"（模板几乎被我全覆盖，音高集合层面没问题），
但逐小节 Jaccard 只有 0.36；用户指出的 Z2/Z4 两个最差区间，正是"原曲有贝斯而模板没有"的位置。
**结论**：模板只能当"钢琴声部"的参照，**不能当全曲音高内容的判据**。

## 14. 「我的 MIDI 音高集合 vs 原曲**混音** CQT」口径不对等 → 0.119 是假象

同一件事换这个口径只剩 **0.119**，而且偏移扫描 ±3.2s **全程无峰值**（0.12–0.13），
缺口模式整齐得可疑：**"原曲有而我缺"全是 MIDI 26–42、"我有而原曲没有"全是 48–84**。

根因：原曲那一侧是在**完整混音**上做 CQT，低频（贝斯+底鼓）能量占主导，
钢琴/吉他的音根本进不了 **−28dB 显著峰**；我的那一侧是 MIDI 的**存在性集合**。
一个"按能量筛"、一个"按存在列"，不可比。

## 15. 对等判据 = 两边都是音频、分频段比（当前 **0.772**）

`b35_jac_audio.py`：两边都走「40Hz 高通 → 频段带通 → CQT → 逐小节显著峰」，
分 low 40–250 / mid 250–1200 / high 1200–4000：

| 频段 | 逐小节 Jaccard 均值 |
|---|---|
| low 40–250Hz | 0.710 |
| mid 250–1200Hz | 0.851 |
| high 1200–4000Hz | 0.814 |
| **三频段合计** | **0.772** |

用户指出的 6 个区间（Z1–Z6）**全部 ≥0.74** —— 说明"不像"**不在音高内容层**，
而在编配层（谁在响）与音色层，与本文档第 1、5 条的判断一致。

## 16. `audit.py` 的 `octave` 轴**不是"音区/八度"**，是**倍频程频带差**

`similarity.py`：`s['octave'] = 100 − 平均带差(dB)/3×100`（差 3dB → 0 分）。
实测 16.0 分＝平均带差 2.52dB，与频谱层那行"倍频程平均带差"是**同一个量**。
⚠ **别再读成"低音高了/低了"** —— 本次一度据此想整体降八度，方向是错的。

## 17. `b35_lowline.json` 的 "28" 是 **40Hz 高通的下限截断**，不是真实最低音

该文件（第 12 条那次修的产物）用"40Hz 高通后取最低显著峰"，
而 **41.2Hz 恰好是 MIDI 28（E1）** —— 于是低于 41Hz 的内容全被记成 28。
它给出"原曲 bass 中位 E1"，与**模板低音线中位 34（A#1）、P10 29** 对不上；
换模板当标尺后，我的 Bass 最低音与它的差是**中位 +1.0 半音**（全轨合计中位 0.0）——
**低音区其实是准的**。

## 18. 频谱层三项**逐项可对准**，工序固定（总分 71.7 → **81.4**）

```powershell
# ① 频谱对齐（整条倾斜是混音工序，不是凑分）：平均带差 2.52 → 1.11 dB
& $py scripts\band_match.py <渲染.wav> <原曲.ogg> <out.wav> --max-gain 6 --report
# ② 宽度精确校准（口径与 metrics.width 相同：side/mid RMS）
python -c "import render_midi as R; print(R.set_width_exact(wav, 0.473))"   # 实测 0.472
# ③ RMS 对齐 + 峰值保护（软限幅后重新归一化到 −16.10）
```
结果：**octave 16.0→63.0 · timbre 69.6→96.6 · 宽度差 51%→0% · RMS 差 1.8→0.0dB**，
偏差项从 4 个只剩 1 个（variation）。
⚠ 宽度参数**不可线性外推**：`--width 1.4→实测 0.713`、`--width 0.95→0.281`（low shelf 改了 mid/side 比），
所以一律用 `set_width_exact` 在最后一步**精确设**，别猜渲染参数。

## 19. `variation` 的根因是**质心**，不是响度

`variation = 100·min/max`，三维（RMS / 质心 / 起音）块间 std 取平均：
原曲 (1.96 / **265** / 0.95)、我 (1.55 / **526** / 1.22) —— **质心那一维是我的两倍**。
逐段对照（`b35_var2.py`）定位：段 20/21/22 我偏亮 +1000~1267Hz、段 0/12/25 偏暗 810~1834Hz。
**段 25（结尾）是最极端的离群点**：我 1313 vs 原曲 3147，段 24→25 的 Δ 是 2210Hz（原曲 487）。
看 CC7 才明白：该段 `Bass 98 / Glock 60` —— **低频压过高频**。
→ 亮度修正必须**双向**（升高频层的同时降 `Bass/Perc`）；只升高频层，质心几乎不动（实测 533 vs 526）。
**但双向也修不动**（实测 BRIGHT ±3dB：质心 std 526→533；提到 ±6dB：→545，variation 反而 47.5→43.3→40.8）。
根因是 **CC7→音量的换算是指数压缩的**（`cc^1.9`）：亮度系数 ±6dB 只让 CC7 动 ±25%，
而段 25 的质心要抬 1700Hz。**这一步只能改音符内容**（各层的音区 / 音色分配），CC7 层面到此为止。
→ **当前最优成品 = 不做段级 CC7 修正、只做音频后处理的那一版**：
`b35_remake_final.ogg`（= `v_match.ogg`，variation 47.5、audit 总分 81.4、其余五项全绿）。

## 20. 贝斯轨被两件事同时毁掉：**转录偏高** + **全局抽样**（2026-09-16 晚 · goal round 1）

实测能量占比（响度代理 = Σ 力度×时长×CC7^1.9，与 `b35_verify_segmix` 同族口径，26 段平均）：

| 轨 | 我（修前） | 我（修后） | 原曲分轨 |
|---|---|---|---|
| **Bass** | **8.9%** | **10.6%** | **25.5%** |
| Arp | 15.1% | **29.4%** | 26.0% |
| Hook | 21.0% | **14.9%** | 12.4% |
| Piano | 15.8% | 13.7% | 11.0% |
| Perc | 22.3% | 18.1% | 24.3% |

**根因一（转录偏高）**：`b35_crepe.py` 用 torchcrepe，而它的 **fmin 下限是 50Hz（≈G1）**
（脚本自己的注释就写了"贝斯只能扒到 G1 以上"），可是原曲贝斯主体在 **E1–A1（41–55Hz）** ——
扒出来的音被顶到 **G3 附近**（`bass_notes.v4v.json` 样例 `[0, 1.5, 1.875, 55, 40]`，55 = G3）。
→ 重扒：`b35_bass_re.py` 用 **pyin（fmin = E1 41.2Hz, fmax = C3）** 在 **bass stem** 上扒，
得到 779 音 · **音域 28–46** · 音高分布以 E1/A1/A#1/C2 为主（这才是贝斯）。
这也是"40–80Hz 一直差 5dB"的另一半原因。

**根因二（全局抽样）**：`b35_extra.py` 的 `SCALE=0.63` 是**全轨一个系数**，
把本已稀疏的贝斯一起砍了 —— **段 0 的 target 只剩 4 个音**（扒谱本身 6 个）。
→ 改成**按轨系数**：`TR_SCALE = {'Bass': 1.0, 'Piano': 0.45, 'Hook': 0.37, 'Arp': 1.05}`。
⚠ 注释里那句"target 档位 ×2 最优"是拿**坏尺子**（vs 模板的 Jaccard，第 13 条）评出来的，**已作废**。

**效果**：段 0 的 bass 占比 11.2% → **19.7%**（原曲 37.4%）、段 1 8.3% → 12.2%（原曲 27.4%）；
Arp 15.1% → **29.4%**（原曲 26.0%，已对齐）；Hook/Piano 从超标回落到接近原曲。
`variation` 47.5 → **48.3**（质心 std 526 → 514、起音 std 1.22 → 1.14）。
**仍未达标**：Bass 总时长只有 229.8s（全曲 331s 的 69%），而原曲贝斯接近全程 —— 音符还要补。

### 20b. 三版对照 + 一处**判据混淆**（务必别被 grid 误导）

| 版本 | audit 总分 | variation | Bass 占比（原曲 25.5%） | Arp 占比（原曲 26.0%） |
|---|---|---|---|---|
| 旧（贝斯被顶到 G3） | **81.4** | 47.5 | 8.9% | 15.1% |
| 贝斯修正 + Arp 全保留 | 79.0 | **48.3** | 10.6% | **29.4%** |
| **贝斯修正 + Arp 0.55（默认成品）** | **79.6** | 47.0 | **12.2%** | 18.6% |

⚠ **`grid` 从 77.1 掉到 70.8 是判据混淆，不是退步**：三版的 **Perc 音符数一模一样（3849）**、
鼓的 `drum_grid` 一个字节没改 —— 鼓没动，变的只有贝斯。
而 `grid` 靠**频段分离**判 kick/snare/hat，贝斯（E1–A1 = 41–55Hz）与底鼓落在同一低频段，
**贝斯一变密就被读成"kick 变了"**。同理 `density`（块起音率）也会被更密的贝斯抬高。
→ 结论：**以内容正确性为准**（贝斯音区与占比是硬缺陷），audit 总分的小幅下降已知来源；
最终取舍请用面板 A/B 同位置切，看听感。
`b35_remake_oldbass.ogg` 保留了旧贝斯版供直接对照。

## 21. CC7 **修不动 `variation`** —— 四种写法全失败（2026-09-16 晚 · goal round 2）

目标：把块间**质心跳动**从 556Hz 压到原曲的 265Hz（`variation` 的分母是三维 std 的平均，
质心那一维是主因；段 24→25 我 Δ2443Hz 而原曲 487）。四次尝试，全部回滚：

| 写法 | 质心 std | variation |
|---|---|---|
| 基线（不调 CC7） | 526 | 47.5 |
| 亮度系数 ±3dB（双向：高频升、Bass/Perc 降） | 533 | 43.3 |
| 亮度系数 ±6dB | 545 | 40.8 |
| **直接设定目标 CC7**（k=1.5，clamp 40–122） | **587** | **38.6** |

⚠ **其中一轮白跑，是链顺序的坑**：`b35_seg_bright.py` 读写的是 **studio 的 `song.json`**，
而流水线里 `Copy-Item b35_remake\song.json → studio\song.json` 排在它**后面** →
改动被整份覆盖（校验发现段 25 的 Glock 仍是 52、预期 113，与上一版逐字节相同）。
**修正顺序后 CC7 确实生效**（段 25：Glock 47→98、Arp 58→114、Pad 65→122、Bass 96→40、Perc 83→40），
但 variation **反而从 47.0 掉到 38.6**（质心 std 556→587、RMS std 1.60→1.14、起音 std 1.17→1.76）。
→ **教训**：`variation` 量的"块间一阶差分"对**段内**响度再分配极敏感 ——
把某段的低频压到底、高频拉满能挪动该段质心，但同时改掉段内 RMS 与起音率，
另外两维的差分被拉大，**净值更差**。**要修它只能改音符内容（音高分布本身），混音推子这条路到此为止。**

## 22. 贝斯"补音符"的正确姿势：补覆盖率没用，缺的不是音（goal round 2）

`b35_bass_cov.py` 报"有声但缺 56.5s"，看着像漏扒 —— 实际是**帧标记用了截断而非向上取整**
（`int(t1/0.1)` vs `ceil`），真实覆盖是 **96%**。
按**能量掩码**补齐（`b35_bass_fill.py`：缝隙有声就延长、否则插入）后到 **98%**、
音符 779→827、总时长 230→240.5s —— 指标几乎不动。
→ **贝斯能量占比低（12.2% vs 原曲 25.5%）不是"音符不够"，而是"每个音的响度/音色能量"**，
最终由 `band_match` 的 EQ 在频谱层兜住（40–80Hz 带差 1.0dB）。
**教训**：覆盖率是"有没有"，占比是"响不响"，两者别混。

## 23. **修 `variation` 的正解：给安静段补"少而响"的高频（hat）**（2026-09-16 晚 · goal round 3）

第 19/21 条把 variation 的主因定位到**质心**（块间 std 556 vs 原曲 265），第 21 条又证明 CC7 修不动。
这一轮换方向：**先看那段到底缺哪个频段**。

**诊断**（`b35_sec25_band.py`，段 25 = 结尾，我 1273Hz vs 原曲 3147Hz）：

| 带 | 原曲 | 我 | 差 |
|---|---|---|---|
| 20–40 | −19.9 | −11.8 | +8.1 |
| 630–1250 | 4.9 | 15.6 | **+10.7** |
| 5000–10000 | −3.4 | −13.7 | **−10.2** |
| 10000–18000 | −16.3 | **−40.8** | **−24.5** |

而**音高集合几乎完全重合**（low 32/32、mid 28/28、high 21/21）——
缺的**不是"音"，是高频的量**。再看原曲 drums stem：段 25 全程只有 **−35.0dB**（全曲最静），
但**高频占比 −7.4dB 是全曲最高之一**（全曲中位 −13.2）→ **原曲那点鼓几乎全是镲片/踩镲的高频**。
而我的 `drum_grid` 在 bar 200–207 **只有 1 格**（全曲逐小节中位 21 格）。

**修法**（`b35_sec25_hat.py`）：该段每小节补 **8 个 8 分 hat 格**（力度 45）+ 段级 `perc_vel_max=55`
—— 原曲那一段本身极轻，不能补成"敲鼓"。
⚠ `arr.perc=0` **不能**用来表达"没鼓"：那是"全曲极简"总闸，会把贝斯/钢琴/琶音一起削掉（song_engine 1256 行）。

**效果**：

| | 修前 | 修后 |
|---|---|---|
| 段 25 的 10–18kHz | −40.8dB | **−27.4dB**（+13.4） |
| 段 25 频段平均 \|差\| | 7.88dB | **5.70dB** |
| 块间质心 std | 556 | **458** |
| **`variation`（audit 口径）** | **43.7** | **52.2** |
| audit 总分 | 79.6 | **80.4** |

→ 离参考线 **55 只差 2.8**。同一做法已推广到段 16（bar 128–135：原曲 **7.5 格/小节**且均匀，
我只有 **2.9 格**且中间 5 小节全空，该段质心也偏暗 2782 vs 3068）。
**教训**：variation 的质心维度靠"**在安静段补高频内容**"来修，且补什么要对照
**原曲分轨的逐段×逐频段能量**（"少而响"优于"多而轻"），不是靠混音推子（第 21 条）。

⚠ **补多少必须看差额，不能照抄别的段的量**：段 16（bar 128–135）质心只差 **286Hz**（2782 vs 3068），
却按段 25 的量补了 8 格/小节 hat → **过冲**：质心 std 458 → **476**、variation **52.2 → 48.3**（退步）。
已回滚（段 16 恢复 23 格、段 25 保留 65 格，MIDI 已校验 23/65 一致）。
**可用的档**：Δ质心 **1800Hz** 量级 → 8 格/小节（力度 45）；Δ质心 **300Hz** 量级 → **2~3 格/小节**或干脆不补。
⚠ 回滚时注意：`per_bar` 里 **hat 与 kick/snare 是并列的键**，直接 `hat=[]` 会把**原有的 hat** 一起删掉
（段 16 从 23 格掉到 8 格）—— 要按备份整小节替换，别只清一个键。

## 24. 质心双向调鼓的**边界**：补有效、削有害（2026-09-16 晚 · goal round 4）

第 23 条已证明"给安静段补高频"能把质心 std 压下来（556 → 458）。这一轮试**双向**：

| 轮次 | 动作 | 质心 std | variation(audit) | 整体质心差 |
|---|---|---|---|---|
| v9 | 基线 | 458 | 52.2 | 9% |
| **v11** | **补**：段 25/12/16 加 **open hat（46）**、段 16 hat 1.9→4；**削**：段 21 hat 11→9、段 22 11→10 | **394** | **53.0** | 9% |
| v12 | 再削：段 3/4/5/6/7/9/10/11 的 hat 各减 2~3 格 | **514** ✗ | **42.0** ✗ | **6%** ✓ |

v12 的教训很干净：**削 hat 让"整体质心差"变好（9% → 6%）却让 variation 崩掉（53.0 → 42.0）**。
因为 variation 量的是**块间一阶差分** —— 把**连续成片的段群**（3–11）一起缩放，虽然每段都更贴原曲，
但段与段之间的**落差形状**被改了，std 反而上升。已回滚到 v11。

→ **可复用规则**：① 只对**孤立**的离群段动手（段 25 孤立在结尾、段 12 孤立在低谷），
**不要对连续段群做统一缩放**；② **只补不削** —— 补是"加内容"，削是"破坏已有的连续性"；
③ 量要用**块间 std**（variation 的口径）而不是"整体平均差"，两者会给出相反的结论。

**顺带**：v12 那轮把 `octave` 抬到 70.3、`timbre` 98.0、倍频程带差 0.89dB（全曲频谱更准）——
若以后以"整体频谱"为目标，那批改动有价值；但本轮目标（variation）不认它。

## 25. **`variation` 达标：53.0 → 62.1，audit 六轴全绿**（2026-09-16 晚 · goal round 5）

第 24 条之后 variation 停在 53.0（参考线 55）。这一轮先**纠正了一个读数错误**：
`b35_variation.py` 打印的"块间差分标准差"是 **|diff| 的 std**（给人看的"跳多大"），
而 variation 用的是 **diff 的 std（带符号）** —— 两个不同的量，前几轮我一直用错的那个判方向。
改成逐维分解后才看清主因：

| 维度 | 原曲 | v11 | 我/原曲 |
|---|---|---|---|
| RMS(dB) | 2.70 | 1.99 | 0.73（起伏不够） |
| **质心(Hz)** | **365.7** | **664.1** | **1.82** ← 主因 |
| 起音率 | 1.65 | 1.94 | 1.18 |

于是一心修"质心维"：它大的原因是**三个低谷段** —— 段 12（我 2847 vs 原曲 3326，低 479）、
段 16（2592 vs 3068，低 476）、段 25（2748 vs 3147，低 399），
相邻落差因此异常大（段 11→12 我 1334 而原曲 96、段 15→16 我 757 而原曲 73）。
照第 23 条的"少而响"做法，把这三段的 **open hat（46）从 2 格加到 4 格**（段 16 再 hat 4→5），
**其余段一律不碰**（第 24 条已证明削 hat 有害）：

| | v11 | **v13** |
|---|---|---|
| 质心维（我/原曲） | 1.82 | **1.52** |
| variation（similarity 口径） | 55.4% | **66.1%** |
| **variation（audit 口径）** | 53.0 | **62.1** ✓ |
| **audit 偏差项** | 1 个 | **0 个** ✓ |
| audit 总分 | 80.5 | **81.4** |

→ **audit 六轴全部达标**（objective ⑥ 达成）。
鼓的逐段活跃度仍落在原曲区间内（我 **7.0~28.0** 格/小节 vs 原曲 **2.2~26.5**；
段 2/3 的 28 比上界高 1.5 格，属"格"定义与静音守卫的口径差）。

**这一轮的方法论**：当比值型指标（min/max）卡住时，**先把它逐维摊开**再动手 ——
前四轮我在"整体平均差"和"块间 std"之间来回用错了尺子，摊开后一轮就到位。

## 26. **两份 `song.json` 并存** —— 重跑引擎前必须先确认基准（2026-09-17，用户"你怎么更差了"）

**根因**：同一首曲子有两份 song.json，而**后续脚本只写其中一份**：

| 路径 | 角色 |
|---|---|
| `b35_studio\songs\99_b35_remake\song.json` | **活跃工作区** —— `b35_group_trim.py` / `b35_instrument.py` / `b35_melprog.py` 等**全部硬编码写这一份** |
| `b35_remake\song.json` | **过时副本** —— 停在更早状态 |

拿过时那份重跑 `song_engine.py`，产出 **11291 音**（Hook 1350 / Bass 579 / Perc 4010），
把 `b35_remake.mid` 和面板里的 mid **一起覆盖**，丢掉了 **11916 音**那版（Hook 2070 / Bass 299 / Perc 3849）。

**三条纪律**：
1. **改之前先确认基准**：`b35_prog.py`（读 style/programs/melody_prog）+ `b35_midstat.py`（读逐轨音符数）。
   **两份 song.json 都要看**，不要假设它们同步。
2. **覆盖前先记 SHA256 指纹**。本次靠 `Get-FileHash` 留下的 `9D1E62DA…F6E1A`，
   逐个备份跑引擎比对，才精确找回真身（`pre_r2.bak` = `pre_segtune.bak`）。
   注意**只有 SHA256 可靠**：两版都可能同为 11916 音、甚至同样 102814 字节（我一度以为 97378 字节相同就没事）。
3. **恢复时不要叠加后处理**。第一次"恢复"在 `pre_r2.bak` 之上又跑了 `b35_instrument.py` +
   `b35_melprog.py`，字节变成 `F31CD02…` —— 音色与 CC7 被改，所以用户仍说"比之前差"。
   **v17 真身根本没写 `programs`**（音色走 `style: dance` 预设 + `melody_prog` 逐段轮换），
   正对应用户说的「**我就听 midi 不关音源的事**」→ 换音色那条路（`b35_instrument.py`）是弯路。

**顺带纠正一处我自己的判据错误**：MIDI 音高 **C4 = 60，所以 84 是 C6 而不是 C7**。
据此算出的"C7+ 超标 19 倍"是错的，真实为 **C6+ 12.3% vs 参考 7.2%（1.7 倍）** ——
结论被夸大，也是我误判"该给 Glock 降八度"的原因（客观上也更差：带差 1.20 vs 0.94 dB）。

**只改一处的正确姿势**（本次 Glock 换音色用的）：引擎里 `progs.update(song.json 的 programs)` 是**合并**
（`song_engine.py` line 459-461），所以写 `{"Glock": [12, 7]}` 就**只改 Glock**，
其余轨与 `melody_prog` 一个字节不动（改后音符数 11916、大小 102814 字节均不变，可逐项核对）。

## 27. 「不连续 / 点状」的三轮排查：两条被证伪、两条真因（2026-09-17）

用户："为什么听起来不是连续的" → 追问后明确为「**声音断断续续（点状）**」。
按顺序查了四条假设，**两条被数据证伪、两条是真因** —— 记下来免得下次重走：

**① 段 23「Piano 只有 2 音」不是坑，是正确还原**（证伪"要重跑转录"）
测原曲 stem 能量：段 23 的 **piano = −77.7 dB（静音）**，而邻段 22/24 是 −34.1/−39.1、
全曲中位 −29.3；同段 guitar/bass/drums 全部正常（−20 上下），原曲混音 −14（全曲最响之一）。
→ 那是「**吉他主导、钢琴不参与**」的段落，所有转录版本给 0–2 音都对，补钢琴才是造假。
**更一般的判据**（`b35_realgap.py`）：逐段比「原曲 stem 能量 > 中位+1dB」与「我方该轨音符数」，
**只有"原曲有能量、我方缺席"才算真缺口**。五条轨跑完，**一条都不缺**。
⚠ 对照物别用 Demucs 转录总数（不完整，段 12 甚至算出 −13 音），要用参考 MIDI 的钢琴缩编。

**② `patterns.section_gap` 不是主因**（证伪）
三个取值（1.0 / 0.25 / 0）下，**Pad 时值分布完全相同**（370 个全是 4.1 拍完整长音）。
原因：`dd = min(dd, _left)` 只在 `_left < dd` 时截断，而 Pad 起音都在小节头（`_left = 4 > gap`）。
它只影响段末 1 拍内起音的 2–4% 的音。

**③ 真因一：同轨同音高重叠 → 音源吞音**（已修，已固化）
`b35_midi_feel.py` 早列为"声音怪/卡"三大来源之一。实测 **340 处**（Bass 单次最长 1.00 拍、
Arp/Hook/Piano 0.88 拍；Pad 因 `B+0.1` 时值 vs 4.0 拍小节必然叠 0.1 拍）。
修法：把**前音的松键提前**到后音按下之前 1ms —— 不改起音/不删音/不改力度，
**340 → 15 处**（剩下的都是同 tick 重复，无解）。
已固化为引擎 opt-in **`patterns.legato_trim`**（默认关 = 全库逐字节不变），
落在 `song_engine.write_midi` 开头。单独后处理脚本 `b35_deoverlap.py` 已证等效。

**④ 真因二：补垫底时把音量压过头**（已修）
补 Pad 时为防"变厚"把 `mix.Pad` 压到 **45**（原设计 60–68）。但按
「发声量 = Σ(时值 × 力度)」算，Pad 占 **20.9%** 的发声量（4.1 拍长音 ×370 个），
是最大的持续层 —— **CC7=45 让它实际听不见**，等于把刚补的垫底又按回去。
→ 改回 **62**。**教训：加层与压音量要分开验证，不能一次同时做。**

**「点状」诊断法**（`b35_staccato.py`）：按**包络类型**给轨分类，
算 `Σ(时值 × note_on 力度)` 的发声量占比。我方：
`percussion`（鼓+木琴）18.1% · `pluck`（Arp 87 Lead 8 + Hook 4 Elec Piano）**36.2%** ·
`sustain` 45.7%。**点状类合计 54.3%** —— Arp 单轨就占 21.2%，是最大单一来源。
⚠ 参考 MIDI 是单轨钢琴（100% sustain），**口径不对等**，不能直接比该比例。

## 28. **「坏尺子」总清单（8 轮里造了 6 个）** —— 每一个都骗过我一次（2026-09-17）

用户："吃过的苦让以后不要再犯了"。以下每条都曾让我做出**方向性错误的决定**，
共同点是**拿一个和「像不像」无关的量去判断「像不像」**，或者**两侧口径不对等**。

| # | 坏尺子 | 怎么骗的我 | 正确做法 |
|---|---|---|---|
| 1 | **CQT 谱峰数当「每个起音的音数」** | 测出「原曲每起音 7–8 音」，据此做了三轮"补和弦音"加厚。而 **bass stem 测出「1684 onset × 5 音 ≈ 8420 谱峰」，实际转录只有 827 音** —— 贝斯是单音线条，**高估 10 倍**（泛音被算成独立音高） | 用**分离后 stem 的转录**按 16 分格聚簇，才是对等口径 |
| 2 | **「音符数/秒」当「起音率」** | 据此认为"我是单音点串"。实际那是音符数÷秒数再÷起音点数，**口径混淆** | 起音率 = **起音点数÷时长**；每起音音数 = 音符数÷**起音点数** |
| 3 | **逐帧 onset 包络相关判「节奏像不像」** | 全部版本 r ≈ 0（连用户认可的 v17 −0.0039、模板 −0.0098），一度以为"所有版本节奏都不像" | 逐帧（11.6ms）相关对时移极端敏感，**差 10ms 就归零** → 用**逐段起音率**（粗粒度） |
| 4 | **音高重心用 CQT 算** | 得「原曲重心 39.1」，据此以为我偏高 16 个半音 | CQT 受**能量**影响（原曲贝斯低频强）→ **真值 64.3**（四条 stem 转录逐音） |
| 5 | **模板 MIDI 直接线性移植** | 以为"用户认可模板 → 移植它最像"，做了互相关（最佳 r=+0.322）才发现不成立 | 模板 162.4 小节 vs 原曲 207 小节，**结构有简化/重排**，时间轴非线性 |
| 6 | **频谱指标（质心/带差）判「像不像」** | 拿它否定"纯钢琴+弦乐"方案、并据此降 Glock 八度 —— 两次都被用户否掉 | **用户认可的模板自己质心 1035（参考 3522）、带差 14.11dB**，比"更对齐"的版本差一个数量级 → 见 PITFALLS 165 |

**两条被证伪的「病因」**（不是尺子，是事实判断错了）：
- **段 23 的 Piano 只有 2 音 = 坑？** 原曲该段 piano stem 仅 **−77.7 dB（静音）**，转录给 0–2 音是**对的**。
- **`patterns.section_gap` 是不连续主因？** 三个取值下 Pad 时值分布**完全相同**，它只影响段末 2–4% 的音。

**元教训**：本任务里**唯一可靠的判据是用户认可的版本**（`v17pure.mid` / `BGM35 (1).mid`）。
我自造的指标几乎每一个都骗过我；每次都是靠"与认可版本对照"才发现。
→ **新指标必须先拿认可版本自检**：若它在认可版本上给出"差"的读数，那是**指标错**，不是版本错。

## 还没解决的

1. **鼓**（用户指定"最后再解决"）：原曲鼓活跃度逐段 2.2~26.5 格，且它是中高频的主要承担者；
   去掉鼓会让音高交叠率从 0.522 升到 **0.790**（超过模板的 0.682）但质心掉到 923（原曲 3522）。
   → 鼓要**保留但控制**，不是去掉。
2. ~~中高频缺口~~ → **已解决**（第 18 条工序）：`band_match` 把平均带差从 2.52 压到 **1.11dB**，
   630–1250 / 1250–2500 / 2500–5000 分别只差 2.9 / 1.6 / 0.2dB，**octave 轴 16.0 → 63.0**。
   当初"EQ 补不了"的判断是因为**只试了逐块匹配**；全曲整条倾斜用 band_match 一次到位。
3. 复音度仍 5.0（模板 3.0）。
4. **`variation` 40.3 → 43~50（参考线 55）**：**根因已定位**（第 19 条：质心块间 std 533 vs 原曲 265）。
   已对齐的：RMS 维度（1.68 vs 1.96）、逐段电平（段 8/13/16 这些原曲极静段跟上了：−20.2/−19.5/−18.0dB）。
   剩下的**质心跳动**（段 24→25 Δ2030Hz 而原曲 487、段 11→12 Δ1600 而原曲 96）——
   属"相邻段的编配/音色落差"，要动**音符内容**（各层音区与音色分配），
   不是 CC7 能修的：CC7 换算是指数压缩的（`cc^1.9`），亮度系数 ±6dB 只让 CC7 动 ±25%。
   第 20 条修完贝斯后 variation 43.7~48.3（取决于 Arp 密度），**质心那一维仍是主因**。
   **第 21 条已把 CC7 这条路线彻底关闭**（四种写法全失败，最好 47.5、最差 38.6）——
   下一步只能改**音符内容**（各层音高分布），例如段 25（结尾）我 1273Hz vs 原曲 3147Hz，
   它缺的不是音量而是**高频内容本身**。
7. **贝斯还要补音符**（第 20 条只修了音区与抽样）：占比 12.2%（原曲 25.5%）、
   总时长 229.8s（全曲 331s 的 **69%**），而原曲贝斯接近全程 —— pyin 的 voiced 帧只占 73%，
   间隙需要补（长音延展 or 低置信度帧用能量法回溯）。
8. **一处判据混淆**（第 20b 条）：`grid`/`density` 会把"更密的贝斯"读成"kick 变了"
   （贝斯 E1–A1 与底鼓同频段）→ 这两个轴的下降**不能直接当退步**，要与鼓轨自身的字节对照着看。
5. **两段编配与原曲乐器构成不符**（改抽样+音区后仍是低谷）：
   - bar 7–9（12–15s）：原曲分轨占比 **bass 75.9%**（贝斯主导的过渡段），我这边是满编配；
   - bar 38–46（61–74s）：原曲 **drums 39.7% + piano 39.4%**，我的 Piano 密度不够。
   → 属**编配层**（谁在响），不是音高层；`b35_zones.py` 已能量出逐段分轨占比。
6. ~~主旋律轨基本不对（0.088）~~ → **那个数是坏尺子的产物**（第 13/14 条：拿单轨去比模板全集）。
   换对等判据（两边音频、分频段）后，音高内容逐小节 Jaccard 是 **0.772**，
   用户指出的 6 个区间**全部 ≥0.74** —— 主旋律轨没有"基本不对"，是尺子错了。

## 第 29 条：音符级 F1 —— 判「像不像」的决定性判据（2026-09-17）

**尺子**（`b35_segalign.py`）：同 16 分格（0.1s）+ 同音高的一对一匹配，逐段算 P/R/F1；
每段另外独立搜时间偏移 dt(±3s) 与移调 dp(±12 半音)。**先自检再使用**：
把模板 1:1 转出的文件与模板互比，F1 = **0.753–0.943** —— 这就是本尺子的天花板
（16 分格边界抖动所致），**不是 1.0**。

| 被评 | vs 模板（逐段 F1 范围 / 中位） |
|---|---|
| 模板内容版（恒等自检 = 上限） | 0.753–0.943 / **0.809** |
| **分轨 Basic Pitch 转录（原曲的客观转录）** | 0.095–0.411 / **0.298** |
| 我的 pure（纯钢琴+弦乐） | 0.060–0.162 / **0.123** |
| 我的 v17pure（用户认可基线） | 0.066–0.155 / ~0.12 |

**结论（内容层，与音源无关）**：
1. **时间轴本来就对**：所有对比的每段最佳 dt ≈ 0（|dt| ≤ 1.0s）→ 差异**纯在音高内容**。
2. **我的引擎版本与原曲的音高重合度只有转录的一半**（0.123 vs 0.298，天花板 0.809）；
   按天花板归一化：转录 **37%**、我的版本 **15%**。
   **这就是"不像"的根本原因** —— 与用户"不是音源的问题"的判断完全一致。
3. → **逐段微调音色/力度/织体不可能修好**；要像，就必须把内容换成扒谱（模板或转录）。

**同时修正两处旧结论**：
- 时间比例是 **1.0，不是 1.0246**。模板 0.41–329.6s 与我的 332.8s 轴**同速**；
  之前按总时长比例拉伸模板，让末段错位 3.3s（≈ 一整小节）。见 PITFALLS 166。
- 上文第 6 条那句"音高内容逐小节 Jaccard 0.772"是**音频层**判据（已被 PITFALLS 165 否掉），
  与 MIDI 层 F1 15% 矛盾 → **以 MIDI 层为准**。

**本轮据此产出的两个交付版本**（音符内容全部取自用户认可的模板）：
- `b35_tpldirect.mid` → `v_tpldirect_fin.ogg`：模板内容 1:1（单轨钢琴 · 5730 音 · 332.96s）
- `b35_tplarr.mid` → `v_tplarr_fin.ogg`：内容同上，按**原曲分轨实测**重新编配
  （Piano 3520 / Bass 1952 / Strings 258 + 我的鼓 3854；**不含** Arp/Glock/Hook 电音层）

## 第 30 条：模板自身的缺陷 = 把「重复起音」拉成长音（2026-09-17，用户听感定位）

用户认可模板（"模板比你的像多了"），但**模板不是完美的**：它在多处把原曲的重复起音
合并成一个长音，节奏被抹平 → 用户能听出"不像"。

用户听 `v_tplarr_fin.ogg` 的反馈：**"全部好多了，只有 3:35–4:03 有一点、还有 4:20 那段不像"**。

**逐段量化**（`b35_retrig_scan.py`，不取平均）：模板每段的"长音（≥0.25s）占比"与
"重触发机会数"（模板音持续期内、音高 ±5 半音处的原曲起音次数）：

| 段 | 秒 | 模板音 | 长音占比 | 重触发机会 |
|---|---|---|---|---|
| 16 | 204.8–217.6 | 211 | **34%（全曲最高）** | 91 |
| 17 | 217.6–230.4 | 223 | 24% | 68 |
| 18 | 230.4–243.2 | 280 | 17% | 73 |
| 19 | 243.2–256.0 | 286 | 24% | **128（全曲最高）** |
| 20 | 256.0–268.8 | 216 | 19% | 65 |

用户点出的 **3:35–4:03** 正落在段 16 尾部 + 段 17 + 段 18；**4:20** 落在段 20 —— 逐段指标与听感吻合。

**修法**：窗口内按原曲转录的起音**重触发**模板长音（只补节奏，不改音高）
→ `b35_tplfix.mid`（+226 音）。修后对原曲转录的 F1：
段 17 `0.312→0.327` · 段 18 `0.301→0.323` · 段 20 `0.310→0.343`；
**未触碰的段数值一字不变**（局部修的验收标准）。

**可复用结论**：把"模板长音 vs 原曲密集起音"当成一类**可量化、可局部修**的缺陷类型；
段 19（4:03–4:16）是全曲机会最多处（128），若用户继续指出，用同一条命令即可处理。

## 第 31 条：不依赖模板的自主路线 —— 多轨交叉验证转录（2026-09-17）

用户问："**能不能提升到根据原曲写出这样效果的 MIDI**。"

**答案：能，已有量化证据。** 关键一步是**多轨交叉验证**：
对 htdemucs 四条分轨分别跑 Basic Pitch → 同 0.1s 格同音高合并 → **只保留有非 `other` 轨支撑的音**。

| 内容来源 | 音符数 | 对模板 F1（整体） |
|---|---|---|
| 单轨最优（piano 转录） | 2404 | 0.333 |
| 四轨合并（未过滤） | 8261 | 0.438 |
| **四轨合并 + 交叉验证（support=1）** | **4733** | **0.482** |
| 四轨合并 + 严格双轨（support=2） | 1191 | 丢 86%，太少 → 弃用 |
| 模板内容（自检上限） | 5730 | 0.75–0.94 |

`other`（arp）轨是**假音主源**：被过滤掉的 3528 个音里 **2390 个只有它支撑**。
→ 推论：**分轨转录必须交叉验证**，单轨（尤其"其他"类）不可直接信。

成品 `b35_trans1.mid` → `v_trans1_fin.ogg`：逐段对模板 F1 **0.35–0.46**，
**19/26 段判定"本来就对"**（dt=0、dp=0）；相比我的引擎版（0.06–0.16）高 3–4 倍。

**可复现流程**：
① `b35_bp.py` 对四条分轨转录 → `bp_*.mid`
② `b35_transarr.py --support 1` → 交叉验证 + 按**来源轨**分配音色 + 搬已对齐的鼓 → `b35_trans1.mid`
③ `render_midi.py`（宽度 1.4 / RMS −16.0 / 搁架 6.0 / 高通 25 / 低调 2.0 / 驱动 1.2 / 中频 4.0）→ `b35_pipe.py` → ogg

**剩余差距**（0.48 vs 上限 0.81）来自转录本身：模板有而转录漏的 **1364 音（24%）**，
以段 5（135）、段 13（111）、段 18（84）最多 → 下一步用**音频能量验证**（音符必须在其来源分轨音频里有能量）
与**更长窗口的和声一致性**去补。

## 第 32 条：转录能力提升 —— 多分离模型集成 + 软交叉验证（2026-09-17）

用户要求："**转录本身提升一下能力**"。共 6 组实验，全部以**对模板的音符级 F1** 为同一指标：

| 实验 | 结果 |
|---|---|
| ① BP 参数扫描（默认 / 敏感 / 很敏感） | 敏感把 R 从 0.439 拉到 **0.743**，但 P 崩到 0.288 → F1 **0.415 < 0.481** |
| ② 音频能量验证（f0 + 2/3/4 次谐波 ÷ 帧谱 RMS） | 单独用只值 **+0.003**（假音多有真实泛音能量，不是"没能量"） |
| ③ 碎片合并（同音高 ≤40ms 合并） | **大幅变差 0.481 → 0.283** → 那些"碎片"是**真实的重复起音**，不能合 |
| ④ 硬交叉验证 support ≥ 2 | 太严（1191 音）→ F1 0.232 |
| ⑤ 第二方法 HPS（谐波求和显著度）补漏 | 失败：显著度分不清泛音，补进来的几乎全是假的 |
| ⑥ **多分离模型集成 + 软评分** | **F1 0.481 → 0.527**（5490 音 · P 0.538 · R 0.516 · 逐段中位 0.486 → 0.532）✅ |

**⑥ 为什么有效**：
- 来源从 4 条扩到 **8 条**：`htdemucs`(4-stem) 与 `htdemucs_6s`(6-stem) 是**两个不同分离模型**，
  同一首曲子的分轨互为**独立证据**；再加上两个模型的 vocals 轨。
- **一个关键单点事实**：`htdemucs`(4-stem) 的 **other** 轨单轨 F1 = **0.415**，
  是 6s 版 other（0.189）的 **2.2 倍**（也高于 piano6 的 0.333）。
  → **分离模型的版本选择本身就是转录质量的一大变量，别默认 6s 一定更好。**
- **软评分**取代硬布尔规则：`score(g,p) = Σ_{来源∈集合} w[来源]`，`w` = 该来源的单轨精度 P；
  阈值 0.50 命中最佳（5490 音）。
- **权重可以无监督化**：改用"跨来源支持率"当权重时，排序与监督 P **完全一致**
  （bass4 0.907 > bass6 0.901 > piano6 0.880 > other4 0.873 … > other6 0.525）
  → **配方可迁移到没有模板的新曲子**（`b35_ens.py --weight unsup`）。

**可复现**：`b35_bp.py` × 8 条来源 → `b35_ens.py --thr 0.50 --weight sup` → `render_midi.py` → `b35_pipe.py`
（研究脚本：`b35_bp_scan.py` 参数扫描 · `b35_energy.py` 能量验证 · `b35_combo.py` 组合实验 ·
`b35_missdiag.py` 漏音诊断 · `b35_hps.py` 第二方法 · `b35_multi.py` / `b35_ens.py` 集成）

**仍未解决**：模板有而集成转录漏的 1364 音里，约 **682 音证据充分却抓不到** ——
这是单一模型（Basic Pitch，2019）的能力上限；本机无网络（pypi 连接超时）无法安装新模型，
下一步要么换更强模型、要么自训练/微调。

## 第 33 条：网络与模型接入 —— 中转站打通 SOTA 模型（2026-09-17）

用户提示"看看有没有中转站"后测得的**可用通道**（这决定了整条技术路线的上限）：

| 目标 | 结果 |
|---|---|
| github.com / codeload / objects.githubusercontent | ✅ 0.5s（直连可用） |
| **hf-mirror.com**（HuggingFace 镜像） | ✅ **1.25s** ← 打通整个 HF 模型生态 |
| ghproxy.net / gh-proxy.com / ghfast.top | ✅ 1.2s（GitHub 加速） |
| pypi.org / mirrors.aliyun.com | ✅ 但慢（~8s 首字节） |
| **huggingface.co（原始）** | ❌ 超时 |
| **zenodo.org** | ⚠️ 通但**只有 38KB/s**（172MB 模型下到 9.6MB 就断流）→ 必须找镜像 |
| modelscope.cn | ✅ 302（正常） |

**关键做法**：zenodo 上那个 172MB 的钢琴 CRNN checkpoint，在 HF 上有同名副本
（`asigalov61/bytedance_piano_transcription`）—— 换镜像后 **17 秒下完（9.9MB/s）**。

**实际接入并被评估的模型**：

| 模型 | 来源 | 对模板 F1（单模型） | 结论 |
|---|---|---|---|
| Basic Pitch（原有） | 本地 | 0.333（piano 轨） | 基线 |
| **ByteDance CRNN 钢琴**（MAESTRO Note F1 0.967） | HF 镜像 172MB | **0.273** | ❌ 独奏域训练，用在**分离后的 stem** 上反而更差 |
| Conformer2D（作者新版 628MB） | HF 镜像 | 未测（同上域问题） | 备查 |
| omnizart music_piano-v2 ONNX（84MB） | GitHub Release | — | ❌ onnxruntime 加载失败（ConvTranspose 负 padding，TF2ONNX 兼容问题） |
| **YourMT3+ YPTF+Multi (PS)**（多乐器 SOTA） | HF 镜像（Space 源码 + 541MB 权重） | **0.261**（全混音 9 轨） | ✅ 视角独立，**加入集成后 F1 0.527→0.532、recall 0.516→0.575** |

**YourMT3 落地踩的 4 个坑**（都在本机解决，未污染生产环境）：
1. `transformers` 环境里是 5.17，YourMT3 需要 **4.45.1** → 用 `pip install --target` 装到独立目录，
   脚本里 `sys.path.insert(0, ...)` 前置（**不降级 site-packages**）；
2. 连带需要 `tokenizers==0.20.3`、`huggingface-hub<1.0` → 同样进独立目录；
3. `torchaudio 2.11` 的 `load()` 改用 torchcodec（未装）→ 用 **soundfile 打补丁**替换解码；
4. `model_helper.py` 在 repo 根目录、`save_dir='amt/logs'` 是相对路径 → `sys.path` 与 `chdir` 都要设对。
   权重须放到 `amt/logs/<project>/<exp_id>/checkpoints/model.ckpt`（project 是 `2024`，不是默认 `ymt3`）。

**推理成本**：RTX 5060 8GB 上，5.5 分钟全曲 **11分50秒**（bf16，perceiver-TF + multi-T5）。

## 第 34 条：应用 batch 加速（并行会话成果）与 MoE 权重测试（2026-09-17）

**加速内容**（来自并行会话的 `b35_speed.py` 实测，已固化进工具链 `scripts/transcribe_ymt3.py`）：
官方 `model_helper.transcribe()` 把推理 batch **硬编码成 `bsz=8`** → 0.688 s/段；
提到 32 → 0.297 s/段。**根因不是硬件**：小 batch 时 GPU 利用率虚高 99%、功耗只有 35W，
时间全耗在 kernel 启动开销上 —— 加大 batch 是这里唯一有效的旋钮。

**本会话的独立复现**：

| 跑法 | 推理耗时 | 音符 | 对模板 F1 |
|---|---|---|---|
| 官方 `transcribe()`（bsz=8） | **711.7 s** | 8667 | 0.261 |
| `scripts/transcribe_ymt3.py --bsz 32` | **72.1 s（9.9×）** | 8679（+0.14%） | 0.261 |
| **MoE 权重**（759MB）· bsz=32 | 205.3 s | 9050（13 类乐器） | 0.270 |

→ 加速**无精度损失**：接进集成后 F1 同为 **0.532**，与并行会话记录的"+0.1% 音符差"一致。

**MoE 权重结论：无增益**。把 MoE 的 4 条主轨加入集成后 F1 **0.532 → 0.524**
（来源数变多使软评分阈值语义漂移、弱音混入）；它单模型 0.270 vs cross_v6 0.261 的微弱优势不足以补偿。
**决定保留 cross_v6 版**（`b35_ens3.mid` / `b35_ens4.mid`，13 来源，thr=0.50，6638 音，F1 0.532）。

**加速的真实价值**：单次转录从 12 分钟压到 75 秒 → 实验迭代从"一天几版"变成"几分钟一版"；
本轮的 MoE 测试、以及后续"对分轨各跑一次 YourMT3"这类多视角实验，都是加速的直接产物。

## 第 35 条：BGM34（带人声）vs BGM35 —— 谁更适合当还原源（2026-09-17）

用户说 `BGM34.ogg` 是 `BGM35.ogg` 加人声版，要求对比。结论：**说法成立，但 BGM34 不是更好的还原源。**

**① 说法验证**：YourMT3 的 13 通道里，BGM35 → `Singing Voice = 0`；BGM34 → `Singing Voice = 368 音`，
**音域 57–74（A3–D5，典型人声）**、力度中位 100、覆盖 2.6–316.9s（几乎全程）。

**② 但 BGM34 作为转录源全面更差**（同一指标：对模板的音符级 F1）：

| 项目 | BGM35 | BGM34 | 结论 |
|---|---|---|---|
| 全轨 F1 | **0.261** | 0.249 | BGM35 更好 |
| Piano 轨 | **0.291**（R 0.206） | 0.252（R 0.170） | BGM35 更好 |
| Bass 轨 | **0.177** | 0.169 | BGM35 略好 |
| Strings 轨 | **0.058** | 0.048 | BGM35 略好 |
| Guitar 轨 | 0.067 | 0.067 | 平手 |
| 器乐音符总数 | **4871** | 3993 | BGM35 多 22% |
| 加入集成后 F1 | **0.532** | 0.518 | BGM34 无增益 |

**根因**：人声占 **57–74** 中音区，正好压住钢琴右手/旋律区 —— 一方面掩蔽中音乐器，
另一方面被 YMT3 判给 `Singing Voice` 轨，于是 BGM34 的器乐轨直接少了 878 个音。

**③ 物理证据（严格对齐后）**：时间偏移 **0 ms**、增益 0.95（同一演奏），但逐样本相关仅 **0.716**；
逐段残差常**超过参考能量**（段 13 达 166%）。频带上 —— **20–200Hz 残差只有 −22.5 dB（低频骨架几乎相同）**，
残差集中在 200–4000Hz。→ **BGM35 更像"BGM34 去掉人声后的版本"**（或同一曲的器乐混音版），
这也解释了为什么 BGM35 的器乐**更清晰、更好转录**。

**结论**：还原继续用 **BGM35**。BGM34 的唯一独有价值是**人声轨**（368 音，全程），
若要做"器乐还原 + 原曲人声"的完整版，可用它叠加。

## 第 36 条：**对第 35 条的修正** —— 背景音乐确实相同，是我的判据错了（2026-09-17）

用户质疑："背景音乐肯定是一样的，为什么你检查会不一样" —— **用户是对的**。

**我的错在哪**：只做**全频段逐样本相减** + 一个全局增益，看到"相关 0.716、残差 ≈ 原曲能量"
就推断"器乐不同 / BGM35 是另一版混音"。实际上 **能量占比大的成分会主导残差**，
而这里那个成分就是**人声**。

**决定性实验**（`b35_sep34.py`）：用 htdemucs 把 BGM34 分离成人声/非人声，再与 BGM35 比：

| 对比 | 相关 | 残差占参考 |
|---|---|---|
| BGM34 vs BGM35（原样） | 0.7158 | 97.6% |
| **BGM34 去人声 vs BGM35** | **0.9492** | **33.2%** |
| BGM34 的人声 vs BGM35 | 0.0754 | 1323%（完全无关 → 人声确实不在 BGM35 里） |

分频段（去人声 vs BGM35）：**20–200Hz 0.9978**（残差 6.7%）· 200–800Hz 0.9260 ·
800–2000Hz 0.7939 · 2000–4000Hz 0.8761 · 4000–9000Hz 0.8834。
逐秒动态对齐后 P90 相关 **0.998**（纯器乐处几乎逐样本相同）。

**修正后的结论**：
1. ✅ **用户说的对**：BGM34 = BGM35（器乐）+ 人声，**背景音乐是同一套**（低频相关 0.9978）。
   剩下那 5% 的差距来自"去人声/demucs 分离本身的误差 + 母带电平差"，不是内容不同。
2. ✅ 但**用 BGM35 当转录源仍然更好**这个结论不变 —— 只是**理由要改**：
   不是"器乐不同"，而是 **BGM34 里人声占了 57–74 中音区、把钢琴右手/旋律掩蔽掉**，
   且被模型判给 `Singing Voice` 轨（368 音），于是器乐轨少了 878 个音、Piano 轨 recall 从 0.206 掉到 0.170。
3. 附带发现：既然两者是精确的"器乐 + 人声"关系，**人声可用 `BGM34 − 0.948×BGM35` 直接差分得到**，
   比 demucs 分离更干净（demucs 的人声轨与 BGM35 相关 0.075 也已经很干净）。

## 第 37 条：**逐带"带差"曾把 96kHz 参考与 44.1kHz 候选直接比 —— 低频差被算大一倍**（2026-09-17）

**怎么发现的**：给 BGM29 做完低音补强后，band_match 报"补偿后 ≈ 0.33 dB"，
而我的体检脚本却报"20-40Hz 仍差 −7.4 dB"。两个数字不能同时对 → 查口径。

**根因**：`metrics._avg_spec` 用**固定 8192 点窗**（不按采样率折算）：
44.1kHz 下窗长 186ms、96kHz 下只有 85ms（频率分辨率 5.4Hz vs 11.7Hz），
而 20-40Hz 带在两种采样率下分别只有 ~4 个 / ~2 个 bin。实测同一段音频：

| 素材 | @44.1kHz | @96kHz | 差 |
|---|---|---|---|
| 参考 BGM29 的 20-40 带 | −9.1 dB | −7.8 dB | **1.3 dB** |
| 参考 BGM35 的 20-40 带 | −17.0 dB | −14.4 dB | **2.6 dB** |

BGM29/BGM35 的参考都是 **96kHz**，而我方渲染一律 44.1kHz ——
所以此前所有"20-40Hz 差 7~9 dB"的结论里都掺了 1~2.6 dB 的**纯口径假象**。
`band_match.py` 早就处理过这件事（比较前把参考降到候选采样率），**是我的体检脚本没跟上**。

**顺带查出的第二个混淆**：`band_energy_db` 返回的是**绝对 dB**，所以
"把成品 RMS 从 −15.0 归一到 −16.1"会让十条带**一起**掉 1.1 dB，看起来"频谱全歪"，
其实音色平衡一点没变。→ 判断平衡必须看**相对带差**（每带先减本曲 80-160Hz），
判断响度看 RMS 差。

**产物**：`scripts/band_grade.py` —— 统一降到 44.1kHz 再比，并**同时**给
"绝对带差 / 相对带差（平衡）/ 质心 / 宽度 / RMS 差"。记入 PITFALLS 174。

## 第 38 条：BGM29 低音缺失的根因是"全混音里低音被盖住"，不是模型不行（2026-09-17）

用户的诊断（"都修一下"）指向三处：低音缺失、只用了单一模型、主奏音色。
第一处的实测证据链：

| 来源 | 低音音符数 | 平均音高 |
|---|---|---|
| YourMT3 在**全混音**上 | **279** | 42.7（≈93Hz） |
| YourMT3 在 **bass 分轨**上 | **818** | 39.0 |
| Basic Pitch 在 htdemucs 的 bass 分轨 | 1307 | 37.0 |
| Basic Pitch 在 htdemucs_6s 的 bass 分轨 | 1384 | 37.4 |
| **三源交叉验证集成（thr 0.90）** | **1416** | 37.1 |

参考曲的 bass 层能量里 **20-40Hz 占 16.6%**（drums 只占 0.53%）——
低音是"面"不是"点"，279 个音根本铺不满。而且原来的渲染链还挂着 `--hp 25`（三阶高通），
**在 20-40Hz 带又削掉 3~5 dB**。

**修法与逐项收益**（统一口径，44.1kHz，平均|相对带差|）：

| 版本 | 平均\|相对带差\| | 20-40 相对差 | 说明 |
|---|---|---|---|
| 旧版（仅 YMT3 + hp25） | 2.61 dB | −6.70 | 基线 |
| v2（低音集成，仍 hp25） | 2.16 dB | −7.49 | 音符补齐了，但高通还在削 |
| e2A（低音集成 + **hp 0**） | — | −3.39 | 单是"不切低频"就 +3.3 dB |
| **e2B（+ 低八度 sub 层 + hp0）** | **0.95 dB** | **−0.67** | 交付版 |
| e2B 再迭代一遍 band_match | 0.76 dB | −1.23 | 还能小幅再压 |
| **参照：BGM35 交付版 v5_fin（用户认可）** | **1.57 dB** | +2.68 | 同口径重算 |

**另一条方法论**：**band_match 跑两遍**（对齐后的再对齐）能把残差再压 0.1~0.2 dB；
`--max-gain` 从 6 放到 9 **没有任何变化** —— 说明残差不是"补偿量不够"，
而是"EQ 实现（STFT + 平滑）不精确"，这条路已经到顶。

**音色候选（改主奏 Synth Lead 的 GM 号）**：带差最好的是**现状 80（方波）**，
换成 81/4/11/24 都略差（0.84~0.94 vs 0.76）→ 指标上不动它。
但**指标不等于听感**，所以三个版本都入库让用户 A/B 挑（见下）。

**踩到的实现坑**：只改 `model['tracks'][i]['program']` **无效** ——
`midi_file.export_midi` 在轨里**有 `program_changes` 时优先用它、忽略 `program`**，
实测"换音色"前后渲染出的 WAV **SHA256 完全相同**。必须两条都写
（`scripts/bass_ensemble.py --program` 已修）。

## 第 39 条：BGM29 新交付版与试听入口（2026-09-17）

| 文件 | 内容 | 统一口径平均\|相对带差\| |
|---|---|---|
| `99_bgm29_remake/0_旧版_仅YMT3.ogg` | 改进前（对照） | 2.61 dB |
| **`99_bgm29_remake/1_定版_v3.ogg`** | 低音集成 + sub 层 + hp0 + band_match×2 | **0.95 dB** |
| `99_bgm29_remake/2_候选_电钢主奏.ogg` | 主奏换 Electric Piano 1 | 0.92 dB |
| `99_bgm29_remake/3_候选_颤音琴主奏.ogg` | 主奏换 Vibraphone | 1.01 dB |

宽度与参考对齐（0.521）、RMS 统一定在 −16.10（与 BGM35 交付同口径，不抄商业母带的 −14.8）。
源 MIDI：`bgm29_v3_source.mid`（bass 1416 音 + Sub 715 音 + 其余 YMT3 原样）。

面板：`http://127.0.0.1:8765`（曲库 `99_bgm29_remake`、`99_b35_remake`）。

## 第 40 条：流水线跑第二首曲子当场翻车 —— 通用化的真正门槛是"参数从参考曲推"（2026-09-17）

把 BGM29 调好的那条链（低音集成 + 低八度 sub + hp 0 + band_match×2 + 母带收尾）
固化进 `scripts/imitate_ref.py` 后，**第一件事就是拿 BGM35 从头跑一遍**（这才是通用性验证）。
结果直接暴露问题：

| 曲目 | 参考曲 "20-40 相对 80-160" | 流水线版平均\|相对带差\| | 手工/旧版 |
|---|---|---|---|
| BGM29 | **−7.9 dB**（有 sub） | **0.82 dB** | 2.61 dB（旧链） |
| BGM35 | **−18.2 dB**（母带切了 sub） | ~~6.46 dB~~ → **1.60 dB** | 1.57 dB（人工调优的交付版） |

第一版 BGM35 的 20-40 相对 **+10.95 dB**（低频爆表），而且 160Hz 以上全线 +4~+7.5 dB
—— 因为 sub 把 40-160 抬高后，`band_match` 又把它整体压了 4.5 dB，于是中高频"相对"就高了。

**修法**：低频策略改成**从参考曲自己推**（不看曲名、不看人工设定）：

| 参考曲 20-40 相对 80-160 | 高通 | sub 层 |
|---|---|---|
| < −15 dB | 36 Hz | 不加 |
| −15 ~ −10 | 28 Hz | 不加 |
| −10 ~ −6 | 18 Hz | 不加 |
| ≥ −6 dB | 0（不切） | 加 |

改完 BGM35 = **1.60 dB**（与人工调优版 1.57 持平 —— 也就是说**全自动流水线已经追平了人工**），
BGM29 保持 0.82 dB。记入 PITFALLS 175。

**剩余已知空间**（记录备查，未做）：
- BGM35 的 **315-630 相对 +3.36 dB**、BGM29 的 **160-315 相对 +2.7 dB** —— 都是"中低音偏厚"。
  实测**按轨调音量能改善它**：BGM29 把 Acoustic Piano 降 6 dB → 平均相对带差 0.86 → **0.74 dB**
  （315-630 从 +1.97 掉到 +1.17），但 160-315 几乎不动（+2.72 → +2.69）
  → 说明这条带偏高**不是某一件乐器的音量问题**，是低音区音符密度/编配本身。
- `band_match` 报"补偿后 ≈ 0.00 dB"而实测总有 1~1.6 dB 残差：`--max-gain` 从 6 放到 9 **毫无变化**，
  说明残差来自 EQ 实现（STFT 平滑 + 峰值保护的整体缩放），不是"补偿量不够"。

## 验证入口（studio，用户 2026-09-16 定：所有音乐相关的活都要用）

```powershell
python studio\server.py --port 8765 --root D:\test\llm_direct\b35_studio
# → http://127.0.0.1:8765  曲目 99_b35_remake（ref=BGM35 已配好）
# 试听本段 1~3 秒 · 参考曲 A/B 同位置切 · 分轨独奏找"哪件乐器在捣乱"
```


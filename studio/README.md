# BGM Studio —— 可视化面板（引擎面板 + MIDI 编辑器）

**位置**：`<music-gen>\studio\`（就是本仓库的一个子目录，一切都在同一个文件夹里）。
驱动的是**本仓库自己的脚本**（`scripts\*`），面板不重写任何音频逻辑：改的永远是 `song.json`，
所以面板与 CLI（`make_song.py`）两条路结果一致。

**而 CLI 现在默认就走面板**（2026-09-19 起的 A′「面板是唯一入口」）：手敲 `new_song.py` /
`make_song.py` 会把任务**委托给面板 API**（`/api/new`、`/api/job?kind=render-tune|render`），
于是任务在 GUI 里全程可见、产物立刻能听。这是被"同一条规矩**文档写了四遍、用户当场问了五次**
仍没执行"逼出来的：规则不落成代码就不算规则。两个开关 —— 面板自己调脚本时带
`BGM_STUDIO_INNER=1`（走原生实现，**防递归**：`/api/new` 背后正是起 `new_song.py`）；
批量 / CI 用 `BGM_CLI_DIRECT=1` 直连（面板无等价任务时，如 `make_song --check`，也自动不委托）。

**两个页面**：
- `/` **引擎面板**（下表的全部能力）：作曲引擎 + 混音台 + 指标体检，面向"做一首自己的曲子"
- `/editor` **MIDI 编辑器**：导入任意 `.mid` → 钢琴卷帘编辑 → 导出 `.mid`，面向"编辑标准 MIDI 文件"
  （对标 miditoolbox 的功能面：全轨卷帘 / 网格吸附 / 1/4–1/32 量化 / 力度与曲线 / 半音移调 /
  多轨管理 / Type 0&1 导入导出；另外多出"把成品接进引擎渲染与体检"这一步）

## 启动 / 停止

```powershell
studio\start.cmd          # 双击也行：起服务 + 开浏览器（已在跑则只开浏览器）
studio\stop.cmd           # 按端口杀进程树
# 手动：<仓库>\.venv\Scripts\python.exe studio\server.py --port 8765 [--open] [--root <曲库>]
```
`http://127.0.0.1:8765`，只监听本机。交付物默认导出到 `<仓库>\export\<曲目>\`。

## 它能干什么 ↔ 背后是哪个工具

| 能力 | 面板操作 | 背后 |
|---|---|---|
| 按音轨制作 | 音轨卡：GM 音色号 / 音量 CC7 / 声像 / 音符数；段落里逐轨开关 + 逐段 CC7、力度 | `song_engine` 的 `programs/mix/arr` |
| **实时混音台** | 「⬇ 载入分轨」→ 分轨同时播放，推子/声像/静音/独奏/电平表；「↩ 写回」；「🔊 以当前混音渲染」 | `bridge stems` + WebAudio |
| **⚡ 试听本段** | 只渲染**当前段落切片**并循环（实测 1~3 秒）：改一版立刻听 | `tools/preview_job.py` |
| **🎚 自动配平** | 解方程求每轨 CC7（±6dB 护栏）→ 真渲染 → **实测成绩单** → 更好才留、更差自动回滚；到限幅时给"该改哪一轨"的建议 | `bridge mixfit` + `tools/mixfit_job.py` |
| **🧬 候选搜索** | 选段 + 速度档 → 切片渲染 + 多路并行 + 贪心搜索；结果出**逐项采纳表**（织体类预勾选、性格类标红），**默认不写回** | `tools/search_job.py` |
| 可视化写旋律 | 卷帘：全轨音符着色；**Melody 可增删拖**、拖右缘改时值、Alt+拖改力度；**和弦内音导引带 + 强拍违规描红** | `scripts\song_events.py` + `song.json` |
| 段落/和弦编辑 | 时间轴选段 → 小节数 / 每小节和弦 / 编排开关 / 混音；「🎹 和弦表」改和弦定义（实时校验可解析） | `song.json` 契约 |
| 指标 | 绝对倍频程差 + **占用率**（墙/点）+ RMS/宽度/质心；可按**时间窗**（引子/安静段/主体段/尾奏） | `bridge metrics`（同 `scripts\bands_abs.py` 口径） |
| 参考曲 A/B | 传输条切「主混音 / 分轨混音 / **参考曲**」，同位置、同循环区间 | `/api/ref-audio` |
| 播放/循环/波形 | 波形拖动定位，Shift+拖设循环，播放头走独立叠加层 | `engine.js` |
| 作曲/渲染/调参 | 🎼 作曲 / 🔊 渲染（+宽度校准）/ ✨ 渲染+调参（自动 EQ） | `song_engine` / `make_song.py` |
| 合并/分轨导出 | 📦 合并交付（MIDI+OGG+WAV+notes+song.json）/ 🎚 分轨（`stems/*.ogg`） | `bridge export --stems` |
| ➕ 新建曲目 | 脚手架：选**主题模板包**（同主题 ≥8 首白名单模板）+ 频谱画像 | `new_song.py --theme` |
| 保存/校验/撤销 | 💾 规范写回 · ✅ 数据契约检查 · Ctrl+Z/Y（含音符级） | `json_io.py` / `check_song.py` |

## 速度（实测，都是这台机器上的真数字）

| 动作 | 耗时 |
|---|---|
| 候选搜索单次渲染 | **0.36 s**（8 小节切片；150BPM 歌） |
| 搜一轮（标准档 48 次预算 / 4 路并行） | **11 秒**（慢歌 67.6BPM 切片更长 ≈60 秒） |
| ⚡ 试听本段（4~8 小节） | **1~3 秒** |
| 整曲渲染（含宽度校准） | **18 秒**（238 秒的歌）~40 秒 |
| 整曲渲染 + 自动调参 | 2~3 分钟（只在定稿跑） |

速度靠三件事：**只渲染切片** + 多路并行 + 渲染预算/墙钟上限/⏹ 停止。

## 边界（诚实）

- **只有 Melody 轨能手写音符**，其它轨由引擎按和弦+编排生成（改和弦/开关/CC7 才是正确改法）。
- 混音台推子是线性增益，写回按 `CC7 = gain×100` 映射；**写回后要重新渲染**才进 MIDI/成品。
- 🧬 搜索的适应度只衡量"更像参考曲"，不衡量音乐性 → **性格参数（律动/移调）必须人工确认**（默认不写回）。
- 指标只证明"像不像"，**好不好听只能靠耳朵**。
- 10-18k 的"连续空气墙"是音源天花板（见 `PITFALLS.md` 103）。
- 中间产物（分轨缓存/切片）都在 `%TEMP%\bgm-studio-audio\`，不进仓库；
  **启动时按预算自动清理**（`prune_tmp_audio`：>7 天的删、总量压到 512MB 以内，最旧先删；
  `--keep-tmp-audio` 可跳过）。以前从不清理，实测涨到 0.93GB（见 `PITFALLS.md` 125）。

## 回归

```powershell
node studio\tools\smoke_ui.js   # 引擎面板：初始化/混音台写回/撤销/分轨播放/卷帘拖删缩放/搜索采纳表
node studio\tools\smoke_ed.js   # 编辑器：导入真实 .mid（走真实 HTTP）→ 编辑 → 和弦 → 导出往返
node studio\tools\browser_check.js  # 真浏览器：音轨卡/残影/播放头定位
```

## MIDI 编辑器（`/editor`）

一份**可编辑的 MIDI 模型** + DAW 式界面，导入**任意标准 MIDI**（Type 0/1、`.mid/.midi/.kar`；
拍号 2/2、3/4、6/8 都支持）。

| 能力 | 操作 |
|---|---|
| 加载 | 默认**只读**（`🔒 只读`）；点右上角切 `✏️ 编辑中` 才能改音符 |
| 钢琴卷帘 | 拖=移动 · 右缘拖=改时值 · 空白拖=新建 · 右键删 · 滚轮缩放 · Alt+拖=力度 · Ctrl+点多选 |
| 量化 | 网格 1/1~1/32（含三连）+ 吸附 + **强度**（<1 部分量化）与**摇摆** |
| 力度 | 力度通道拖动 · ×/+/=/夹取/人性化 · **渐变（渐强渐弱）** |
| 移调 | 半音（整轨或选中）；越界自动夹取并计数 |
| 多轨 | 独奏/静音/隐藏 · 换乐器（GM program）· 通道 · 复制轨 · 删轨 |
| 编辑 | 加/删音符 · 复制区间 · 粘贴 · 去重 · 修剪同音高重叠 |
| 导出 | Type 1 / Type 0，导出后**当场做往返校验** |
| 和弦 | 🎵 按小节识别（带置信度）· 🎼 写成和弦轨 |
| 播放 | 进度条定位 · 标尺 Alt+点定位 · 标尺拖选 A–B 循环 · 🎧 真音源（见下） |
| 其它 | Ctrl+Z/Y 撤销 · Delete 删选中 · Q 检测和弦 / W 生成和弦轨 |

**口径**：音乐语义全在 `scripts/midi_ops.py`，前端只发 `{op, params}`、服务端用同一份函数执行 ——
"面板做的"和"自检测的"是同一件事（`selftest` 的 `midi_ops_semantics` / `midi_file_editor_roundtrip`）。

**听感（真音源）**：点「🎧 渲染音频」→ 编辑结果导出 MIDI → **与引擎面板同一条 `render_midi.py`
管线**（GeneralUser GS）渲染 OGG → 播放它（123 秒约 7 秒、后台跑；改了音符才重渲）；未渲染/失败时
退回 **WebAudio 合成音**（6 音色族·多泛音+滤波+混响）。两条音源共用同一套时钟语义。

**边界**：导入的 `.mid` 不进曲库（会话在 `%TEMP%`）；同音高重叠无法唯一还原配对；和弦识别是
模板匹配（和弦音被省略时会差一个音）；**还没有** 键盘录制、CC 曲线编辑、弯音轮导出。


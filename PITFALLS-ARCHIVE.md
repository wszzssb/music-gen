# 坑台账·归档（1–40、81–88、89–92、93–104、109–113、114–120、121–126）

从 `PITFALLS.md` 拆出来的早期条目，**编号保持原样**（正文一字未改）。
拆的原因：PITFALLS 有 token 预算，只能留"新近 + 高频"的坑；归档区不设预算（按号查才翻）。


> 从 `README.md` 第 4 节拆出来的：**出现症状时按编号查**，不要整篇读。
> 前 10 条最常踩的留在 README 里；编号与历史文档中的"见坑 NN"引用一致。
> 每条格式：现象 → 根因 → 修法/守卫。想直接看"修过哪些 bug"读 `HISTORY.md`。

1. **GitHub 直连在国内不通**（超时/重置）。用镜像：`https://ghproxy.net/https://github.com/...`。
   `raw.githubusercontent.com` 反而能直连（音源就是直连下的）。
2. **母带 `tanh` 必须在归一化之后**。合成后原始混音峰值能到 2.4，直接 `tanh` = 硬削波，
   产生刺耳交调失真（这就是"很多杂音"的真凶之一）。正确顺序：量峰值 → 归一化 → 轻限幅。
3. **libsndfile 的 Vorbis 编码器一次性写超大 float32 数组会崩**（进程静默退出、0 采样）。
   改 64k 帧分块，或直接用 ffmpeg（`to_ogg.py` 现在走 ffmpeg）。
4. **噪声层 ≠ 真实感**。曾加过"击弦噪声/弓噪/房间底噪"想模仿实录，结果就是可听的沙沙声。
   要提亮用 EQ 搁架和更多高次分音，**不要用噪声**。沙锤算打击乐（短促离散）不算底噪。
5. **`bright` 参数要作用在高次分音上**。写成 `a * bright`（所有分音等比缩放）只是改音量，音色不变。
6. **扒谱的音名基准必须是 C**（`12*log2(f/261.6256)`）。用 A=440 做基准会让所有音名整体偏移。
7. **MIDI 轨名必须写在 End-of-Track 之前**（tick 0），否则 DAW 读不到轨道名。
8. **写 MIDI 建议带 CC10 声像 / CC7 音量**，否则渲染出来所有乐器都在正中间（宽度只有 0.08）。
9. **编配里第 1 拍要落根音**。分解和弦从和弦音 index 1 开始会让根音只存在于贝斯，和声听觉含糊、
   连扒谱都对不上。
10. **高潮段不要把旋律升八度**。GM 钢琴高音区能量少，升八度后高潮反而成为全曲最轻的段。
11. **不要用 `Select-Object -First N` 接在生成脚本后面**（尤其 `... | Select-Object -First 2`）：
    PowerShell 拿到 N 条就**终止管道并杀掉上游进程**，会把正在写的 MIDI 截成 0 字节。
    要少看输出就用 `> $null` 或 `Select-Object -Last N`。
12. **别用 PowerShell 的 `Get-Content`/`Set-Content` 改写含中文的 UTF-8 脚本**（会按 GBK 解码再存，直接乱码）。
    改文件用编辑工具，或 Python 读写时显式指定 `encoding='utf-8'`。
13. **节奏型要"留白"才读得出来**：底鼓+贝斯每拍打「正拍+十六分」双踩（`★★◇·`）才有驱动感；
    踩镲只放反拍且音量要压过其他高频元素（正拍也加满的话 7-12kHz 会糊成一片，律动全丢）。
14. **不要靠低八度 sub 层找低频**：实测它只往 20-40Hz 堆轰鸣（参考曲该段 −21.6dB，直接多出 20dB），
    40-80Hz 的力度应该用贝斯本身的音区 + 母带低频搁架做。
15. **中侧加宽与软限幅的顺序有影响**：软限幅会先压掉侧向分量（约 −0.1 宽度），
    所以先限幅再加宽；加宽量大时单声道回放会变薄。
16. **换参考曲一定要先改 BPM**：脚手架现在会按 `refs/<画像>.json` 自动写入 BPM（`new_song.py`
    已修），否则小节类指标（节奏型/调式/结构）全部算错，白烧一轮。
17. **不是所有参考曲都以低频为主**：BGM18b 的最强频段是 315–1250Hz、40–80Hz 只有 −11.8dB。
    照搬"厚贝斯"会整体错位——先看 `refs/*.json` 的倍频程再定配器。
18. **反拍驱动 vs 正拍**：低频型 `◇★◇★` 表示重音在每拍第 2、4 个十六分（0.25/0.75 拍），
    写成正拍长音会变"稳"而不"跳"；但 sub 层要用 0.3 拍短音，写 1 拍长音会把节奏型糊成连续块。
19. **成绩单测速会倍频误判**：低音周期性常把速度测成 1/2 或 2 倍 → `scorecard.py` 已加吸附逻辑。
    （更狠的一条见坑 56：连奏编配根本不是倍频问题，是**测错**。知道真实速度就给 `--bpm`。）
20. **`perc_style: none` 会让高频塌掉**：这类小编配里**沙锤是 5–18kHz 的唯一来源**，
    关掉后顶频比参考暗 26dB（已踩两次）。要"无鼓组"用 `perc_style: light` + `perc: 1`（只有沙锤）。
21. **`render.json` 的 `mid` 是 MIDI 文件名**，中频增益参数叫 **`mid_db`**（别撞名）。
22. **GM 音源 1.2–6kHz 偏薄**：`shelf` 从 3kHz 起才抬，管不到 1.2–3kHz → 用 `mid_db`。
23. **自动调参不要追大差距**：`LIMITS` 已收到保守值；差 >4dB 就该换音色/加层（如吉他 24→25、
    加 `arr.arp`），否则 EQ 推爆只会刺耳。旧参数载入时会先夹到范围内（避免早前推爆的值残留）。
24. **`make_song.py` 默认自动调参**：想只渲染一轮看现状用 `--no-tune`。
25. **`new_song.py` 固定生成 `song.json`**（不是 `<名字>.json`）——命令里别写错文件名。
26. **自动调参的 EQ 有上限**：到顶时会打印"该改哪一轨、幅度多少"（`+` 表示本曲偏厚 → 要降）。
    照着改 `song.json` 的 `mix` 比继续推 EQ 有效得多；风格预设里的配比就是这些结论的沉淀。
27. **`voicing_shift` 是改变"厚度"的快刀**：中低频偏厚时把和弦声部 +12（`"voicing_shift": 12`），
    比一轨一轨降音量快。
28. **自动调参夹紧的是"结果值"不是"变化量"**（`cur+delta ∈ [lo,hi]`）。
    把 delta 夹进 `[lo,hi]` 是错的：width 的 lo=0.8、hp 的 lo=18，会把 +0.3 的小修正抬成 +0.8/+18，
    参数瞬间冲到上限（这个 bug 让宽度一度跑到 0.997）。
29. **宽度只做一次比例拟合**（响应实测严格线性：每单位倍数 ≈ 0.409 宽度），拟合后永久冻结；
    上限收到 **2.6**。反复拟合/高倍数区会失控。
30. **改完引擎（song_engine / write_midi）要重出所有曲目**：交付的 MIDI 会"过期"。
    `selftest.py` 会直接报"已交付的 X.mid 与当前引擎输出不一致 → 需要重跑 make_song.py"。
31. **`write_midi` 对越界数据报错而不是静默回绕**（原来 `note & 0x7F` 会把 128 变成 0，音高悄悄错掉）。
32. **遗留测试品在 render.json 标 `"legacy": true`**，不参与对标（它们不是交付物）。
33. **对标判据不含 20–40Hz**：sub 低频各家音源/生产差异极大、多数回放设备也听不到，
    只作提示打印。
34. **和弦音必须自检**：`selftest.py` 会把和弦名解析成期望音级集合，与音表逐音比对
    （song.json 与代码式脚本的 `CHORDS` 都查）。这条查出过 4 处错音：`A7` 的最高音写成
    C（b3，与 C# 撞半音）×3、`B7` 的七音写成 A#（应为本位 A）、`Dmaj7` 里含 9 音（改名 maj9）。
    **手写和弦表打错音时，音频听起来只是"有点脏"，不报任何错——只能靠这条检查。**
35. **通道不能重复**：两轨用同一 MIDI 通道时，后一个 program change 会盖掉前一个（音色错乱）。
36. **旋律的"小节偏移"必须落在该段范围内**，越界会悄悄串到别的段（现在自检会拦）。
37. **风格预设只改音色、不碰通道**：通道用固定表（`CH`）。踩过：4 套预设的通道号在
    `DEFAULT_PROGRAMS` 改动后互相冲突（如 Arp 与 Pad 都占通道 3）→ 两轨的 program change
    互相覆盖、音色错乱。现在 `selftest.py` 会逐套检查。
38. **宽度不用迭代**：加宽在整条链最后，前面 EQ 每轮都变会让"比例系数"漂移，
    线性拟合必然失准 → 收尾用 `render_midi.set_width_exact()` 从成品**反推精确校准**
    （实测 0.814 vs 0.814，差 0.000），并重新编码 OGG。
39. **自动调参的日志只打印"实际应用"的改动**：之前把 tune_step 的建议原样打印，
    即使该参数已冻结/到顶（日志显示在调、其实没动），会误导后续判断。
40. **"该改哪一轨"的建议必须按当前状态给**：早期是静态文案，会对已换过音色的歌建议
    "吉他 24→25"（其实已经是 25）。现在会带上当前值，并跳过已生效的项。

## 续：41–80（第二次归档，编号不变）

41. **`eighth` 贝斯也要 sub 层**：之前只有 simple/offbeat 有，快歌用八分贝斯时 20–40Hz 会缺 20dB。
42. **响度目标与峰值上限会互相打架**：加宽抬高峰值 → 天花板把整体增益拉小 → 响度可差 **4.3dB**。
    正确做法是**用限幅压峰值**（`render_midi.render` 现在会迭代提高限幅强度、每轮重新归一），
    而不是整体降增益。修好后预演的峰值不再恒为 0.970、对齐从 8.1dB 降到 3.0–4.3dB。
43. **比一小节还短的文件**会让 `metrics.rhythm/quiet_chroma` 崩（numpy `a cannot be empty`）
    → 现在返回中性值；短音频也能体检了。
44. **自动调参的"偏厚"判据不能用宽阈值**：原来是 `> TOL+1`，导致 **+1.5~2.5dB 的过厚永远不修**、
    却报"✓ 达标"（和之前的夹紧 bug 同一类：假装收敛）。现在两个方向都用 `TOL`。
45. **编配建议要按单频段给**：按组平均会把"低频厚 6dB、中频薄 4dB"互相抵消成 0，不给建议。
    现在 |单频段差| > 4dB 会单独提示；且频段→轨道映射必须**按名字**（区间判断会让
    315–400 重叠、把 40–80Hz 的错误指向钟琴）。
46. **`__pycache__` 不算残留**（Python 正常行为）；但 `*.raw.wav`、`_*` 测试目录、`*.log` 要清。
47. **人声歌做参考曲时不能逐频段对齐**：这种人声混音最强段在 315–630Hz（人声基频区）、
    **20–40Hz 可低到 −68dB、10–18kHz 只有 −41dB**，器乐编配照抄等于把贝斯和空气感全砍掉。
    `metrics.profile` 现在会判定 `character: vocal_forward` 并给出 `align_bands`
    （只对齐 160–10kHz 的中频音乐平衡），scorecard 会打印豁免说明。
48. **夹紧只能有一层**：`tune_step` 先夹一次、autotune 再夹一次，会让"想要 −8.24dB、
    被夹成 0"的信息丢失 → 日志谎报"✓ 达标"而实际还差 10dB。
    **这是"假装收敛"这类 bug 的第三次出现**：现在 `tune_step` 只给原始建议，
    夹紧与"到顶"报告统一在 autotune 一层，并有自检项 `autotune_reports_caps` 守着。
49. **FLAC 直接支持**（libsndfile），扒谱/渲染无需特殊处理；`profile_ref.py` 对
    找不到文件/格式不支持现在给友好报错而不是裸 traceback。
50. **改调用处必须同时改函数签名**：加 `analyze_chords.py --brief` 时只改了调用方，
    签名没改 → 这个工具**从此每次调用都崩**，而因为不再用它，一直没人发现。
    现在自检里有一条"所有分析/体检脚本冒烟测试"（10 个工具逐个跑）守着这类静默损坏。
51. **画像加了新字段要就地补齐旧画像**：`character` / `align_bands` 只依赖 `bands`，
    不必重扒音频（一条命令即可，12 个画像全部补齐）。缺字段时行为会退化成"全部频段对齐"。
52. **预演必须校验"调参有效果"**：只检查"没崩/不削波"的话，**调参完全失效也会 PASS**
    （踩过：一次预演里对齐 19–25dB 却全绿）。现在会打印 `调参前→后` 并断言确实改善。
53. **建议必须跟对标口径一致**：scorecard 一边打印"人声曲不做逐频段对齐，已跳过 20-40"，
    一边照样建议"20-40Hz 多 29.8dB → 提高高通" —— 照做会把配器调得**比所有真实 BGM 都薄**
    （实测 13 首真实 BGM 的 20-40Hz 在 −9.9 ~ −34.4dB，那首人声曲是 −68.7dB：低频是它
    在母带切掉的，不是我们的失真）。现在 `suggest()` 只在 `align_bands` 内出建议，
    组名也如实写成实际参与对标的频段（`5-10kHz` 而不是 `5-18kHz`）。
54. **成绩单是跑在"成品"上的，参数建议必须是相对当前的**：成品已经过一轮 EQ，若按
    "从零起步"给绝对值，**已到限的参数会得到反方向建议**（实测 shelf 已 −3 下限、
    高频仍偏亮时，它建议改成 −2.8 = 让成品更亮）。现在按"当前值再走一步"算，
    走不动就明说"已到下限，靠编配：降低 mix.Glock/mix.Perc（当前 44/52）"。
55. **预演小样必须用参考曲的 BPM**：写死 120BPM 却按 `ref['bpm']`（84~163）切小节，
    节奏型/调式/结构全算在**错位的小节网格**上 —— 那种验证等于没验证。
    现在 `run_style` 自动取参考速度，并多一条不变量：小样写进 MIDI 的速度必须等于参考速度。
56. **速度要读 MIDI 的 tempo，不能靠音频测速**：`detect_bpm` 对**连奏编配**（竖琴/弦乐/
    合唱，没有明显起音）会误判 —— 实测 gorgeous 编配的 106BPM 样带被读成 **154.3**
    （而它自相关峰值正好落在**一小节**上，音频确实是 106BPM；置信度得分也分不开对错：
    误判 0.289 vs 正确却更低的 0.239，所以设阈值没用）。
    速度判错 → 小节类指标全错位、成绩单还会误报"!! 速度不一致，先改 BPM"。
    现在：`make_song` 从 MIDI 读真实速度（`midi_bpm`）传给 `measure` 与 `scorecard --bpm`，
    只有拿不到速度时才退回测速。
57. **渲染尾巴是近静音的混响余韵，可以不管**（实测本曲 79.3s 里音乐 75.3s、余下 3.9s
    均 −85dBFS、峰 −50dBFS，人耳不可闻；掐掉尾巴重测：各频段 0.0dB、质心 1Hz、
    RMS 0.3dB 的差别）。参考曲自己的尾巴 1.0~1.3s，比我们短，但**不影响对标**。
58. **"换个目录/换台机器就崩"是编码问题，不是玄学**：Windows 默认控制台是 GBK，
    脚本打印 `✓`（U+2713，不在 GBK 里）直接 `UnicodeEncodeError` —— **崩在自动调参中途**。
    本地没暴露是因为每次都设了 `chcp 65001`/`PYTHONIOENCODING=utf-8`；
    别的对话/目录/普通终端一跑就必现。
    修法：所有入口脚本调用 `cli_utf8.setup()` 把 stdout/stderr 切成 UTF-8 + `errors='replace'`
    （编码不了就降级成 `?`，绝不为一个装饰性字符中断渲染）；捕获子进程输出也统一
    `encoding='utf-8'`（否则父进程按 GBK 解码子进程的 UTF-8 → 乱码，检查项会误判）。
    守卫：自检项 `console_encoding_safe` —— 起真进程（`PYTHONIOENCODING=gbk`）验证救得回来、
    **反向对照**验证不加固确实会崩（防止检查空转）、并静态要求每个入口脚本都调用它。
59. **手写旋律会带上"我自己的习惯"，要用画像驱动生成**：实测我手写的旋律**级进只占 14%、正拍占 73%**，
    而例曲的旋律语言是**级进 59%、正拍仅 34%（切分明显）、乐句碎、时值长短相间**。
    照着"我的手感"写，越写越不像。现在的做法：`melody_profile.py` 扒出语言（音级/音程/时值/落点/乐句），
    `melody_gen.py` **按画像抽样生成**（时值/落点/级进率/音域都来自画像，强拍仍强制和弦音）。
    生成结果：级进 59%、正拍 34%、音域 72–87、强拍和弦贴合 96% —— 四项都命中画像。
60. **"检查不通过"有两种，别用放宽阈值解决**：一种是我们真的漂了（跑 `make_song.py` 收敛），
    另一种是**参考曲本身**的母带特征不该照抄。后者要在该曲 `render.json` 里**显式声明**：
    `"align_exempt": {"5000-10000": "理由…"}`，**空白理由视为没写**，自检照样拦下。
    实例（`10_b146_night_meal`）：参考曲 5–10kHz 仅 −47.3dB，比游戏里最暗的器乐 BGM（−29.5dB）
    还低 17dB；本曲 −33.0dB 已暗于全部 15 首真实 BGM，照抄只会更闷 → 显式豁免 + 理由进 `notes.md`。
    **反面教材**：直接调大 `alignment_vs_refs` 的 8dB 阈值 —— 真正的漂移也会被一起放过。
61. **"永远不会红的检查"比没有检查更糟**：
    ① `t_unused_chords_warn` 只打印、从不 FAIL（装饰性绿灯）；
    ② **13 条与曲目有关的检查里有 11 条**在 `song_dirs()` 返回空列表时**照样 PASS**
    （`for d in []` 什么都不做）—— 目录名一改、glob 一坏，它们集体空转，你还以为"全绿"。
    现在统一走 `songs_or_fail()`：先断言"发现数非空"，真实曲目时再核对"发现数 == 磁盘数"；
    提示类检查则**两个方向都验**（该响要响、不该响不许响）。
    **注入临时曲目目录时跳过数量核对**——否则变异注入会因为"数量不对"被抓，
    而不是因为那条检查本身有效，变异测试就变成了自欺。
62. **静默夹断 = 听感变了却没人报错**：`build_events` 末尾原来写着
    `max(0, min(127, int(m)))`，注释还自欺"write_midi 会报错，这里先兜住"——
    结果 song.json 里写个 200，声音变了、所有工具都沉默。
    现在：**输入数据越界在 `load()` 就报错**（点名是哪个和弦/旋律/段落的哪个音）；
    **派生声部**（±12 八度加倍、sub）越界则**丢弃该声部**（而不是夹成另一个音）。
63. **同一件事写两遍，口径必然漂**：`aligned_bands()`/频段分组/容差以前在 `make_song`
    和 `scorecard` 各有一份，连 "mid" 的含义都不同（1250-5000 vs 315-1250），
    于是出现"成绩单说没问题、调参还在动"。现在全部收进 `metrics`：
    `aligned_bands()` / `GROUPS` / `TOL` / `judge_gaps()` 是**全链唯一口径**，
    调参、成绩单、编配提示都从这里取数。
64. **变异测试的覆盖度就是检查层的可信度**：只有 10 条注入用例时，45 条检查"只验证过现在是绿的"，
    没验证过"坏了会不会红"。扩到 **53 条**后立刻抓出上面 3 个真问题
    （质心测量只测"低很低/高很高"、人声判定只读画像里存好的字、引擎静默夹断）。
    新增检查时的准入门槛：**必须同时给出"注入故障 → 必须被抓到"的用例**。
65. **"渲染很久"有一半是被前台超时杀掉的**：前台命令上限 120s（硬顶 600s），autotune 跑到第 2 轮
    就被杀在半路，`render()` 收尾的 `os.remove(raw.wav)` 没跑到 → 留下几十 MB 的 `*.raw.wav`。
    **长曲（>3 分钟）一律后台跑**，迭代阶段用 `--no-tune`。守卫：自检 `hygiene_no_leftovers` 拦残留 raw。
66. **逐样本 Python 循环 = 白烧 CPU（音频 DSP 尤其致命）**：`render_midi` 的四个 EQ 曾是
    `for i in range(len(ch))` 递推，313 秒的歌要 **63s/轮**；`metrics.detect_bpm` 的 `np.correlate(e,e,'full')`
    把 5.4万×5.4万 对全算了一遍（**19.1s**），而真正用到的只有前 ~1800 个 lag。改批量/频域后
    单轮 **101.8s → 28.7s**。**风险不是"崩"，是"听着差不多但响应偏了"**（方向检查对此完全不敏感）→
    守卫：自检 `dsp_fft_equivalent`（与独立写的时域递推逐样本比对）+ 两条注入用例。
    （完整改动与实测见 `HISTORY.md` 第 9 轮。）
67. **"不好听"是可以查的**：11_dn75_neon v1 被用户直接否掉。解剖发现不是口味问题——
    **强拍只有 54% 落在和弦音上**（早期曲目 78~87%），同期的 10_b146_night_meal 也是 55%。
    旋律越写越"自由"、不再检查"这个音在这小节的和弦里成不成立"，听感就发飘发脏。
    现在自检项 `melody_chord_fit` 把它钉死：强拍（第 1、3 拍）和弦音占比 ≥70%、
    根音上方半音（♭9）≤2 处；**弱拍不限制**（经过音/倚音本来就该在弱拍）。
    附带结论：段落**复用旋律但换了和弦**时强拍会撞音 → 正确解法是**让和弦配合旋律**
    （10 号曲的 B'/C 段就是这么修的），不是硬改旋律。
68. **改了 `song.json` 的配器/混音后，必须重置 `render.json` 里旧的极端 EQ**：
    调参器在"源头就不平衡"的混音上会把 `shelf`/`mid_db` 拉到 **+10dB 上限**、`low` 压成 **−2.7**，
    新一代从这些极端值起步就越调越偏（实测 40–160Hz 塌 12~14dB，听起来"低音没了"）。
    改完配器先把 EQ 重置成中性（`shelf 3.5 / low 0 / mid_db 0`）再跑 `make_song.py`。
    还有一条容易误判的：**最强频段决定归一化基准**——主奏太响时其它频段会被相对压低，
    于是"低音消失"其实是"主奏太抢"。
69. **同一个仓库可能被两个对话同时改**：本轮就撞上了（`render_midi.py`/`make_song.py` 被另一处
    改出 `_pad_len`/`dsp_fft_equivalent`，坑号也从 64 直接跳到 66）。所以：**只追加、写完立刻验证**，
    报数前重新数一遍（自检项数/变异条数随时在变），别用记忆里的旧数字。
70. **OGG 解码后峰值可能超出满刻度（Vorbis 过冲）**：`audio_health` 报 `warm_run_sf.ogg 削波 1.006`
    —— **WAV 是好的**（天花板 0.97），是**有损编码把峰值抬上去**，触发条件是"母带很响"。
    处置：该曲响度目标降 0.5dB 留编码余量。**更根本的修法在 `render_midi.encode_ogg`**：
    编码前留 0.5~1dB 余量（或提高 q）；`audio_health` 是这条的守卫。
71. **"参考曲的真实编制"比"风格偏好"更重要**：同参考同 BPM 的两版 —— 合成音色版最大偏差 6.7dB、
    质心差 −333Hz；换成钢琴/电吉他/原声贝斯后**最大偏差 3.2dB、质心差 +87Hz**，630–1250Hz 的
    顽固偏亮也从 +6.7 降到 +0.7dB。**遇到追不上的音色差，先怀疑音色选错了**，再怀疑 EQ。
72. **想"像例曲"，先把例曲的律动量出来，照它做引擎律动**：BGM33 的低频型是
    `◇★◇★◇★◇★·★◇★◇★◇★`（每拍的 e/a 都推、正拍留空），而旧引擎只会"四踩/双踩" ——
    音符再多也对不上律动。新增 `bass_style: pump16` 后变成 `◇★·★·★·★·★·★·★·★`（★ 位置与例曲一致）；
    高频同理，例曲的密集段就是**每 4 小节的十六分过门** → 新增 `perc_style: pump`。
    **律动型是可以逐格对齐的硬指标，比"听起来像不像"可靠得多。**
73. **两个密度指标会互相矛盾，别拿它们当靶子**：同一首歌，"频谱通量峰值"说例曲 4.6/秒 vs 我们
    2.5/秒（显得太稀），"20ms 短窗能量瞬态"说我们 5.4~9.6/秒 vs 例曲 2.3/秒（显得太密）——
    两者测的是不同的东西（频谱变化 vs 能量冲击）。追单一代理指标会走偏，最后仍靠
    "律动型逐格一致 + 频段对齐 + 耳朵"。
74. **"一模一样"的最后一道墙是音源，不是音符**：律动/频段/宽度/响度都能对齐，但峰值/峰值因数/
    20ms 起伏 σ 都追不上真实录音（0.72 vs 1.002、13.5 vs 15.9dB、9.7 vs 21.9dB）——
    GeneralUser GS 的鼓组瞬态软（换音源的路见 80）。**MIDI 才是上限最高的交付物**。
75. **"像不像"由织体的疏密决定，不是频段比例**：用户听完 14_d75_pulse 说"一点都不一样"，
    而所有标量指标（倍频程/质心/宽度/响度/律动型）**全对上了**。画出频谱才看到：
    **例曲 40Hz–8kHz 是连续的墙，我们是一根根孤立竖条 + 空洞**（占用率 87/90% vs 37/30%）。
    根因是我自己造的：为对律动把伴奏做成断奏（`staccato: 0.55`）、又为"腾空间"撤掉 pad。
    **教训：只调频段比例调不出"像"，得看频谱的疏密结构**（怎么补见 81）。
76. **"我做不到"的事大半有现成工具 —— 但要用对那一个**：本机是 RTX 5060 + Python 3.13，
    主 venv 是 3.14（PyTorch 无 cp314 轮子）→ **另建 `.venv-ml`，绝不碰主 venv**。
    细节全在 `ML.md`，这里只留要点：**Demucs 分离是突破口**（扒谱调内率 66%→93%）、
    **matchering 只解决母带那一层**、**librosa.pyin 不如自己写的**。
77. **别拿"单声部绝对值"当靶子：听众听的是总和**。按逐声部靶子改完鼓与贝斯后，单看 drums 声部
    仍然"差得远"（低频占用 31/27% vs 例曲 82/76%），一度以为没改对。把**鼓+贝斯加起来**再量：
    40–160Hz 例曲 **97%/17dB**、本曲 **83%/21dB** —— 已经很接近。原因是 **Demucs 把例曲低频的
    连续感（真实底鼓的长 body + 房间声）归到了 drums 轨**，而我们的底鼓采样短而干、能量落在 bass 轨。
    **跨声部比较看"组合"，单声部只看"趋势"**。另外先验分离可信度（我们 vocals 轨 −72.8dB
    vs 例曲 −49.6dB → 分离干净），否则逐声部对比不成立。
78. **混响是 opt-in 且要"小步调"**：`render.json` 的 `reverb` 覆盖 FluidSynth 参数，默认 None =
    字节不变。默认(0.78/0.35/0.80) 尾巴 4.6dB/300ms、例曲 6.0dB；**一次调到 0.92/0.20/0.95
    直接飙到 12~14dB**，最后落 **0.80/0.30/0.88**。整体感参数一次只动一点，用单指标验收。
79. **matchering 只能解决"母带那一层"**：`scripts/master_match.py`（走 `.venv-ml`）按参考曲做
    频谱/响度/动态匹配 —— 峰值 **0.909→0.993**（例曲 1.042）、峰值因数 **16.6→17.8**、
    占用率差之和 **62%→55%**。但**频段/织体/旋律这些"内容"问题它一点都解决不了**。
    定位：交付前的可选最后一步。
80. **换音源先确认真的拿得到**：本机可达的网络只有 PyPI/npm（GitHub、archive.org、huggingface
    全部超时），PyPI 的 `arachno`/`sf2` 是同名无关库、npm 的 `generaluser` 就是我已有的音源 →
    "换个更好的 sf2"**走不通**。口子：`find_sf2()` 认 `.sf2/.sf3` 且优先 mscore/arachno/timbres，
    丢进 `vendor/` 即生效；读音源用 `sf2_lib.py --drums`。

## 归档：治旋律同质化（109–113，2026-09-13 归档）

> 守卫已覆盖（melody_lang_diverse / melody_matches_profile）；完整经过见 HISTORY.md。

109. **"每首歌一个说话方式"不能靠换画像解决 —— 生成器的固定纪律会把画像差异吃掉**。
     用户听感"这么多歌都像"→ 直觉解法是"每首换一份旋律画像"。**实测这条基本无效**：
     同一首曲子分别用 12 份画像生成（同 seed），结果两两语言重合 **83%**，而"生成结果保住
     画像方言"的比例只有 **42%**。拆到维度上，罪魁一目了然：
     句长 **2%**（生成器**从不产生休止**，末段把音间空隙全部归零 → 每句都填满整段）、
     落点 42%（只用 `onbeat_pct` 做**二值**判正拍/反拍，画像 `onset16_hist` 的 16 格方言没用）、
     音程 63%（级进率用 `max(0.62, stepwise_pct)` 全局下限，`interval_hist` 没用）、
     时值 60%（吸附到全局固定 `GRID`，画像的三连音/十六分值全被削平）。
     **诊断顺序**：先量"换画像能拿到多少差异"，再决定改哪 —— 别直接开始扒新画像。
     修法：`melody_gen` 改成读画像的**分布**（句长切乐句 + 句末休止、落点按 `onset16_hist`
     抽样、时值按 `dur16_hist`、音程按 `interval_hist`），另配每首一组个性参数 + 候选去重。
     实测：承接度 42%→63%，句长 2%→58%，换画像后差异 83%→69%，全库孪生对 5→0。

110. **画像里的"绝对量"多数是扒谱伪影，只有"相对分布"能用**。同一件事踩了四次：
     ① **`phrase_bars` 的 0.5 小节**（碎片）不是乐句 —— 混音 F0 只抓到有起音的稳定段，
     照抄当句长后旋律密度掉到 0.9 音/小节（16 小节只出 29 个音、句间空 2–4 拍）。
     ② **`interval_hist` 的 ±8 以上**多是八度误判 —— 照抄生成 20 处 >7 半音的大跳（旧版只有 6 处）。
     ③ **`iv=0`（同音重复）被系统性放大** —— F0 在每个稳定音上连续出帧、量化后全变同音，
     照抄写出 64% 同音重复的旋律（听感＝原地踏步）。
     ④ 连 **`notes_per_bar` 也不能当密度**（BGM33 画像 1.09 音/小节，可那是首 75BPM 舞曲）。
     规矩：**密度/绝对长度服从曲目本身，画像只提供相对形状**；凡是"照抄会让某一维极端化"的
     都要设上下限（跳进 ≤7 半音、同音重复 ≤30%、句长 ≥1.5 小节、密度沿用原旋律）。

111. **"只留高频档"的噪声过滤，会把音乐上必须有、频次却不高的值滤掉**。
     为滤掉扒谱长尾，落点分布按"累计 80%"截断 —— 结果把**格 0/8（第 1、3 拍）**
     当长尾扔了，13 号因此生成出一条**完全没有正拍音**的旋律（正拍 0%）。
     同类：**落点是逐段抽样的，段的位置会把强拍格挡在候选之外** —— 30 号画像格 0/8 各占 6%，
     生成后 **36 小节只剩 3 个强拍音**，自检 `melody_chord_fit` 因样本 <8 直接空转报错。
     结论：概率过滤之后必须补**音乐纪律的保底**（第 1/3 拍落点有最小权重 + 每 2 小节至少一个
     强拍落点），并且**用自检的样本量反查**（"样本太少"往往不是检查的问题，是生成的问题）。

112. **调式写死一种 = 大调曲上大面积"修正"成同音**。`melody_gen` 只有自然小调/多利亚两个音阶，
     大调曲（23 号 D 大调）的三度/七度音被当成调外音，每次生成都 `±1` 硬修 ——
     实测 65% 的音被压成同音重复（旋律原地踏步）。修法：按**本曲自己的和弦**判大小调
     （大调与自然小调只共享 0/2/5/7 四个音级，按和弦音级匹配率就能分清），
     段落 `mode` 仍可显式覆盖。**别拿画像的 `mode` 当本曲调式** —— 那是参考曲的。

113. **段落"按名复用旋律"时，逐段生成会互相覆盖**。同一个旋律名被多个 section 引用
     （A' 复用 A 是设计意图），若每个 section 各生成一遍，最后一个会覆盖前面的，
     且彼此拿**对方的和弦**去对 —— 实测 11 号留下 1 处强拍错音。修法：同名旋律**只生成一次**
     （用第一次出现的段落），其余引用段落只**核对并报告冲突**，候选评分里给冲突加权 —— 冲突不能静默。



## 81–88（从 PITFALLS.md 归档，正文一字未改）

81. **"连续性"是时间覆盖率，不是能量**：例曲 5–10kHz 占用 96~100%、40–80Hz 92~97%，而我们的
    频段**能量**早对齐了 —— 差的是"有没有一直响"（GM 底鼓采样只有 0.13~0.18 秒，垫不满一拍）。
    修法 `patterns.perc_layers`（opt-in）：`kick` 低音嗵鼓（位置**由底鼓派生**，逐点对齐）
    + `air` 开镲**铺满十六分格**（改八分掉到 84%）。实测 5000Hz 78→91/96、40Hz 87→93。
    ⚠ **律动型与颗粒感会打架**：追律动型要拉平力度、把底鼓骨架移到反拍
    （`kick_pos: "offbeat"` + `bass_vel`）。**引子别做稀疏**：例曲引子与 A 段同响度，稀疏引子会让前 40 秒的 40/80/630Hz 占用掉到 66/61/79。
82. **FluidSynth 会连 MIDI 之后的 voice 尾巴一起渲染**：重叠的镲（0.4 拍 > 0.25 格距）让它多渲**15.7 秒** −72dBFS 死气（听不见但真实：4:42 → 5:03）→ `render_midi.trim_tail()`（尾巴 >3 秒才裁）+ 三道守卫。铁律：**量指标先切窗口再算**，一次对比只用同一脚本同一版音频。


83. **速度是"层级"不是单值**：60/75/80 与 120/150/160 八度关系、两层都能自圆其说（BGM19：
    ac(80)=0.197 / ac(151)=0.182；本管线自渲的 150BPM 曲反而是 ac(75)=0.448 > ac(150)=0.165）。
    危害不是"测错一个数"，而是**画像按 150、成品按 75 → 一路假报速度差 100%**。
    → `check_audio.py` 报各层支持度；`analyze_chords` 与 `metrics` 口径不一致时会显式提示。
84. **70% 松门会放过新曲的单点错音**：`melody_chord_fit` 是全库 ≥70%（老曲目 78~100%），
    注入 1 个错音仍 PASS → 新曲自查用 `check_song.py --strict`（100% 严判据）。
    但**别把它当默认门**：库里 7 首老歌各有 7~15 处正当的强拍经过音。
85. **"跳过哪些检查"要写成共享常量**：`check_song.py` 把自检跑在沙箱里，若沙箱又跑到
    "沙箱检查"自己 → **无限递归**（实测自检 300s 超时）。自指检查必须显式跳过。
86. **`chord_names_match_notes` 是子集判据**（`got <= want`）：voicing 允许省略和弦音，
    所以"少写一个音"不算错、只有"含不属于该和弦的音级"才报。注入故障时别指望它抓前者。

87. **速度的"层"不能被工具自动决定**（实测两轮都翻过车）：试过用"每拍音数 ∈ 1.0~3.0（八分~十六分）"
    自动定层，结果 `ひとりごはん`（145.6 层只有 0.94 音/拍）改判对，却把 BGM16c 的 150BPM
    （十六分音型 3.35 音/拍）判成 75、BGM33 的 75BPM（5.73 音/拍）判成 150 ——
    **根因："每拍音数"区分不了"慢歌走八分"和"快歌走十六分"**（都是 ~3 个/拍）。
    现方案：`detect_bpm` 只报"自相关层 + 该层起音/拍"，异常时（<1.0 或 >8.0）直接建议
    `--bpm` 试哪一层，**由人定层**；`profile_ref.py --bpm N` 把选定层写进画像。
88. **`check_song --fix` 的和弦替换"能用但难听"**：它为凑"强拍弦内音"会写出 `Fm→F7`（丢三度）、
    `Cm7→Csus4`。正确用法是**读它的诊断、手工重写** —— 校验器的价值在"2 秒告诉你哪儿错"，
    不在"替你决定怎么改"。新曲写完后建议 `--strict` 看强拍，再人工判断要不要动。


## 89–92（从 PITFALLS.md 归档，正文一字未改）

89. **`character_of` 会把"分解吉他"误判成人声**：判据只有频段（315-630Hz 最强 + 无 sub + 无顶频），
    而扫弦/分解吉他的频谱重心同样落在 315-630Hz。实例：`ひとりごはん` 被 Demucs 判为
    vocals/bass/drums **全静音**（真·器乐），却被标成 `vocal_forward` 只对齐中频。
    后果不致命（中频本来最重要），但**别再拿这个字段当"有没有人声"的结论**。

90. **"清脆/像不像"主要来自段间对比，不是整体更亮**：实测参考曲 5–10kHz 在暗段 −5.4dB、
    亮段 +7.7dB（**差 13dB**）；第一版成品把整曲抬成一条直线（起伏仅 4.5dB）→ 听感"亮却闷"。
    → 用 `analyze_sections.py` 拿分段目标 + `new_song.py --from-sections` 自动写段落曲线
    （现成起伏 9dB，人工微调可到 13+）。
91. **`refs/*.json` 是参考曲画像、自检会按字段校验**：分段画像必须放 `refs/sections/`
    （与 `refs/melody/` 同理），否则报"缺字段 bpm/bar/…"（踩过）。
92. **预渲染校验要"干跑一次引擎"**：`arr.mix` 写成 `{"Strings":[54,76]}`（该收**单整数 CC7**）
    时数据检查全绿、渲染到 `write_midi` 才 `int(list)` 崩 → 白跑一轮。
    `check_song` 现在加 `compose_dry_run`（真调 `song_engine.compose`），归入数据契约批。


## 93–100（从 PITFALLS.md 归档，正文一字未改）

93. **项目自检不能硬依赖"宿主级"文件**：技能卡在 `~/.dsh/skills/`（用户目录），不在仓库里。
    原来 `t_docs_budget_and_skill_intact` / `t_skill_routes_resolve` 直接 `open(sk)` →
    **换台机器/换用户，整套自检 `FileNotFoundError` 崩掉**（不是给出可读提示）。
    现按 `token_audit.HOST_LEVEL` 区分：宿主级缺失 → 跳过 + 提示；本机想强制 → `DSH_REQUIRE_SKILL=1`。

94. **`melody_profile` 报"调内率低、别用"时，先怀疑自己给的 `--tonic`**：低有**两种**原因 ——
    真扒错了，或主音给错了。实测 BGM16c：`--tonic C#` 得 **9.6%**（像扒错），换 `--tonic F`
    得 **90.4%（完全可用）** —— 我因此白判了一次"旋律扒取失败"。
    更难的一点：**不能只取"调内率最高"的主音**，那组音级（F G A A# C）被 **F 与 A#(Bb)**
    两个大调同时 100% 覆盖 → 必须与该曲的**和声分析**（`refs/*.json` 的 `quiet_chroma`）对齐。
    （D 大调不含 A#，压根不是候选 —— 手算容易想当然。）
    工具现已自动提示"换个主音试试（与和声分析一致）"；判据由 `t_melody_profile_tonic_hint` 守着。

95. **移调/编辑编配时的"多移一次"与"音区不合法"**：我把和弦整体移低 12 半音修"声部挤"，
    但**低音被移了两次** → Bass 落到 **F-1–D2（22~73Hz）**，成了次声波轰鸣
    （表现为"低频厚 +9dB、听着乱"）。当时**所有既有检查都是绿灯**（MIDI 值合法、和弦音级也合法）。
    → 新增 `t_track_ranges_musical`：按库里 17 首成品校准的**每轨合理音域**，越界即 FAIL。
96. **自动调参会反制你的编配改动，而且当时毫无提示**：把 Strings 提 22 想让 1250–2500 变厚，
    结果它为了压别处把**整体**拉低，该频段反而更薄（−8.2dB）。→ 现在 `make_song` 会把上次实测
    频段存进 `render.json` 的 `last_bands`，下次渲染时若某段**比上次差 >1.5dB 就报警**，
    并提示"优先收小冲突源"。**正确做法是收小冲突源**（如打击音量 ×0.45），不是加大别的轨。
97. **spec 与 song.json 会静默漂移**：手工编辑 spec 时把 4 元 `[小节,拍,时值,音高]` 当 3 元处理，
    **把时值 +7 了**；另一次两者差整八度 —— 复现命令全失效。→ `build_song` 现在把 spec 落到
    **曲目目录** `songs/<曲名>/spec.json`，并由 `t_song_spec_sync` 验证"spec 能重建出同一份 song.json"。

98. **旋律生成只看强拍 → 弱拍大量半音冲突（最刺耳的"不协调"）**：`melody_gen` 原先只把
    **强拍**吸附到和弦音，弱拍自由游走。实测 **20 号有 42% 的音**与某弦内音差 1 个半音
    （♭9/♮7 —— 最刺耳的和声关系），用户听感"不协调、很难受"；手写的 17 号只有 4.9%。
    → 生成器已加**弱拍半音回避**（原地 ±1/±2 微调，保轮廓；退不到就落弦内音）。
    复查：20/21 号冲突率 **42%/17% → 0%**，且频谱贴合反而更好。
    **判据**：`与和弦的半音冲突率` 应 < 5%（17 号是 4.9%）。

99. **旋律"卡卡的"= 时值没网格化 + 有空白间隙**：`melody_gen` 从画像的时值直方图抽样，
    而画像的 16 分格统计里含**三连音值**（0.75/1.25/1.75 拍）→ 生成出非 4/4 常用音值，
    叠加相邻音之间 **>0.5 拍的空白**（实测 20/21 各有 40+ 处）→ 听感"卡、断续、不流畅"。
    对照：手写的 17 号只有 **3 种时值（1/2/4 拍）、0 处空隙**。
    → `melody_gen` 已加**严格网格化**：音值吸附 0.5/1/1.5/2/3/4，且每个音的时值 = 到下一个音的
    间隔（**无空隙、无重叠**）。判据：`>0.5 拍大间隙 = 0`、时值种类 ≤ 6。

100. **旋律"主体太奇怪"= 净在跳四五度**：`melody_gen` 的跳进分支取 `±3~5`，而 `±5` 就是
     **纯四/五度** —— 叠加"强拍吸附和弦音"（窗口 ±6）后，生成出的旋律有 **32% 的音在跳四五度**
     （实测 22 号：+5 出现 35 次、−5 出现 21 次），而手写的 17 号以**同音(22%)+级进(42%)**为主。
     听感就是"东跳西跳、没有句子"。
     → 修复：① 跳进幅度收敛到 `±3~4`（不用 ±5）② 强拍吸附窗口 ±6→±3（别为凑弦内音大跳）
     ③ 级进概率设下限 0.62。**修后级进率 23%→64%、同音 13%→29%、五度跳进 56→15 次**。
     **判据**：`最常音程应为同音或 2 度`、`级进率 ≥ 55%`。


101. **旋律"不够自然连接"= 乐句没有拱形 + 跳进不解决**（本轮按对位法/旋律写作的经典规则修）：
     量化两个**手写 17 号 vs 生成 22 号**的差异 —— ① **每 8 小节拱形跨度**：17 号 **24 半音**、
     22 号只有 **11**（太平，没有"起伏雕塑"）② **跳进后是否反向级进**（教科书的"跳进要解决"）：
     22 号 79% 未解决。修法：**乐句级拱形雕刻**（每 8 小节按"起→高点(0.62 处)→落"的包络
     微调 ±3 半音）+ **跳进解决**（跳进 ≥4 后强制反向级进 2 度）+ 事后**冲突/强拍兜底**。
     修后：拱形 17、跳进未解决 **18%**、级进 46%、最常音程 = 同音（与 17 号一致）。
     注意：**雕刻会引入新的半音冲突与拱形偏移**，必须"雕刻 → 冲突修复 → 强拍兜底"三步收尾。

102. **旋律落点撒在十六分奇数格 = "跟伴奏错位、节奏混乱"**：生成器把落点铺满 16 个十六分格
     （实测 22 号格 1/3/5/…/15 都有音），而**伴奏与打击在"拍/八分"网格上** → 听感"时不时跳跃、
     跟伴奏错位、很混乱"。对照：手写的 17 号**只在格 0/4/8/12**（拍与八分）。
     → `melody_gen` 已加**落点八分网格吸附 + 去重 + 网格化后的强拍兜底**（和弦音可移八度落进音域）。
     **判据**：`奇数格落点 = 0`、`八分格 ≥ 95%`、`强拍和弦贴合 = 100%`。
     注意：网格化会把音从强拍挪走 → 必须**最后再做一次强拍兜底**，否则贴合率会掉到 80%。

103. **"倍频程只差 1~3dB"可能是假的：成绩单那列是"相对本曲最响频段"归一的**。低频一厚，10 个数字
    整体平移 —— 实测连改 5 版编配那张表几乎不动，真问题（5–18k 差 10~18dB、40–160Hz 厚 4~6dB）
    被完全掩盖。**判编配改动只看绝对口径**：`bands_abs.py`（平均谱带内总功率的绝对 dB + 与参考的差）。
    另加一列**占用率**（帧能量 > 本带峰值 −20dB 的占比）——"高频是连续的墙还是稀疏的点"原口径里
    根本没有：实测 10–18k 电平差 0.2dB、占用率差 26 点（点 44% vs 墙 70%）。选"空气层"音色同用此表
    （`probe_timbre.py`，等响度下）：GM95 Pad Sweep 10–18k 15.9dB / 占用 95%，Glock 20.0dB / 21%、
    Celesta 7.4dB / 19% —— 只看电平会选错。⚠ **改弱轨等于没改**：`probe_timbre.py --solo` 一眼看出
    力度 42~72、CC7 60 的轨在整混里听不见（本轮就是这么浪费了 5 轮）。

104. **速度"静默折半"：窗口外的曲子被折上来，`info` 里不留任何痕迹**。`detect_bpm` 的候选窗口
     写死 `[60,180]`（`while bpm<60: *2` / `while bpm>180: /2`），实测（底鼓每拍+踩镲每半拍）：
     **55BPM→111.1、45BPM→90.7、180BPM→60.4**，而返回值只有 `low_level`，没有一句"这已经超出
     自动定层窗口"—— 扒新参考曲的人会**直接把 111.1 写进画像**。
     修法（**零行为变更**：折叠窗口不动，14 首参考曲读数与修复前逐个相同）＝把**窗口外同样成立的
     层级显式报出来** → `info['window_alt']`（八度 ×2/÷2/×4/÷4 + 三拍 ×3/÷3，支持度 ≥0.85×本层），
     `check_audio` 列进 `levels` 并打印"请用 `--bpm` 核对"。
     **>180BPM（窗口上界之上）**：上界只是"折叠归一"的历史取值，**不是判断依据** —— 所以
     **高于 180 的层级若支持度严格更高，就直接报它**（44100Hz 的 210BPM 素材实测报 **209.4**，
     旧代码报 69.8）。为什么只对 >180 生效：向下的 ÷2/÷4 是经典八度歧义、两种读法在音乐上都成立
     —— 实测"谁支持度高就报谁"会让 14 首参考曲里 **4 首**读数被翻动（90→45、168→42、110→55…，
     后两个还是 0.23 vs 0.22 的近平分）且**没有证据**说明变好了，故收窄。
     ⚠ **仍未解决**：当那一层支持度并没有更高时（22050Hz 合成 180/210/230 实测报 60.4/70.3/116.1），
     它只出现在备注与层级阶梯里 —— 仍须人工 `--bpm` 钉死。要再进一步得换判据（宽带起音包络 /
     层级结构判别），本项目的经验是这类启发式**反复失败**（`pick_level`、onset 密度都栽过）。
     守卫：自检 `bpm_out_of_window_reported`（45/55 必须命中真值；120 窗内速度**不许**触发）
     + 变异用例"关掉阈值 → 必须被抓"。


114. **"出口处另立一套约束"是隐蔽 bug 的温床：裁剪点与计算点不一致，调试打印还印在裁剪之前**。
     用户反馈新曲 31 的主旋律"镫镫地卡着不规律"。量出来：**0.25 拍的十六分碎音占 11%**
     （参考画像里是 0%）。查这条碎音**绕了三轮**，每次改完数据**逐格一模一样**：
     改了"时值计算"（放开 `dur_cap`、去掉"不许出小节"），可 `gen_section` **末尾还留着一句
     兜底裁剪 `e[2] = min(e[2], SPB - e[1])`** —— 又把跨小节的长音截回"到小节末的长度"，
     落在 16 分格 15（3.75 拍，画像里很常见的方言落点）的音一律变 0.25 拍。
     **最坑的是调试打印写在裁剪之前**：输出说"684 个音、零个碎音"，落盘却有 9 个 —— 两边对不上，
     白查三轮。规矩：① 约束只在一处生效，出口别再来一刀；② **验证要读落盘的数据**，
     别信生成过程中的中间变量；③ 改动无效且输出"一模一样"时，先怀疑**根本不是你改的那段在起作用**。

115. **旋律"卡卡的"有三个独立来源，要分别量**（都以参考画像的时值分布为尺子）：
     ① **长音被砍** —— `dur_cap` 设 1.5 拍，而画像 48% 是 2 拍音 → 长音全没，剩一堆中短音；
     ② **密度过高挤碎落点** —— 新曲默认 2.0 音/小节，而画像本身是 1.51（稀疏长音向），
        落点排密后时值被 `nxt-on` 截短（跨拍时值 69%、"跨拍+反拍"56%）；
     ③ **落点贴小节末/句末** —— 到边界只剩 0.25 拍。
     修完的实测：碎音 11%→**0%**、2 拍长音 16%→**33%**、"跨拍+反拍" 41%→**26%**、
     时值承接度 66%→**81%**。判据：**拿画像的时值直方图逐档对**（`probe31_melody.py` 式体检），
     别只看"平均每小节几个音"。

116. **同类 bug 会反复出现，因为它是结构性的 —— 三层防护已落地**。同日在一个生成器上连踩五次
     （判错调式 / 正拍格被滤 / 强拍保底缺失 / 长音被砍 / **出口裁剪**），共同结构是：
     **改错了没有任何东西会喊** —— 频段、响度、结构类守卫全绿，
     而"旋律统计是否偏离画像"这一层**当时一项守卫都没有**。三层防护（均已落地 + 真注入回归）：
     ① **出口不变量**（`gen_section` 末尾）：相邻落点 ≥0.5 拍、音不许重叠，破了当场 assert ——
        **一个约束只在一处生效**，出口只做一件事（坑 114 就是出口又裁了一刀）；
     ② **落盘重读复核**（`melody_gen.main`）：写盘后读回逐音比对，不一致直接报错；
     ③ **统计层守卫 `melody_matches_profile`**：带 `melody_gen` 元数据的曲子，落点/时值
        与画像承接度须 ≥55%（画像 <80 音走 40% 档）。做过真注入：把出口裁剪改回去 → 实测 FAIL。
     两条最费时间的习惯教训：**验证一律读落盘数据**（别信生成中的中间变量，调试打印骗了三轮）；
     **改动无效、输出"逐字一样"时先怀疑"起作用的是另一段代码"**。

117. **用户描述听感时，"量错指标"和"找错声部"会连续骗过你**（本例整整四轮）。
     · **第一层：量错指标**。用户说 "d d d d ddd"，melody_health 量"相邻连续同音"→ 报 0/26
       问题；真身是"**小步打转**"：生成器产物 **|iv|≤1 占 26~44%**、手写曲 **6~15%**、
       画像 **39~48%**（**画像的 iv=0/±1 本身就是扒谱伪影**）。修：同音权重 30%→**8%**、
       ±1 **×0.25**、级进率上限 0.92→**0.52**、**强拍吸附别吸回 prev**、调内/半音回避
       **优先 ±2**、同音第 2 个 65% 概率换掉 → |iv|≤1 **44%→12~18%**、同音 **28%→3~9%**。
     · **第二层：找错声部**。用户第四轮仍说"32 还是很明显"。打印 **MIDI 各轨音符**才看见
       Perc 453音  A#5@0.00 A#5@0.50 A#5@1.00 …：**沙锤每 0.5 拍一个、力度只有两档、
       56 小节一动不动**，出自 perc_part 的 light 分支一行 or k in range(NB*2)；
       **所有用 light 的曲子都是同一条** —— 这才是用户说的"怎么**都是**这个"。
       修：按小节轮换四种落点型（铺底/抽格+切分/加十六分/留白）+ 4 小节力度起伏，
       每小节仍保 ≥7 个沙锤（5–18kHz 连续性不塌）。
       ⚠ 改引擎 → 全库 MIDI 失效 → **必须全库重渲染**（本例 23 首≈40 分钟）；
       ⚠ 筛选取**实际 patterns.perc_style**，别按 style 名（21/22 号是 gorgeous
       却显式设了 light，第一轮就漏掉了）。
     规矩：**① 听感描述先翻译成 2~3 个指标全量一遍；② 逐轨看音符序列，别只在用户点名的
     声部里找；③ "所有曲子都一个样"的第一嫌疑是引擎里的公共音型。**

118. **"多模板聚合"最容易混进三类垃圾 + 两处静默跳过**（做主题模板包时一次踩齐）。
     用户口径：**一次生成要依据很多个同主题模板**（≥8 首），来源只许 `refs/midi2/` 或网络权威数据
     —— 于是把同主题模板 MIDI 聚合成"主题模板包"（`theme_pack.py`）。第一版看着像数据，其实全是垃圾：
     · **拍号混进 4/4 统计**：库里的 [5,4]/[7,4]/[3,8] 进来后 16 分格对不上，共识节奏型算成
       `0.1 音/小节`（像"这个主题很稀疏"，其实是不同小节的格被重合）。修：主题声明 meter，
       候选按拍号过滤。
     · **密度口径错**：拿 `midi_ref` 的 `rhythm_low/high`（**全曲合并**的"该格出现过没有"）
       除以小节数当密度 → 长曲几乎每格都出现过，密度变 0.1 音/小节、共识型成 `★★★★★★★★`（无信息）。
       修：按**每小节**统计占用率，密度**按轨角色**分别算（贝斯轨/鼓轨起音数）—— 否则钢琴左手
       八度被算成贝斯、琶音被算成鼓点，民谣主题被推成 `bass=sixteenth perc=dance`。
     · **和弦窗口被"同和弦换延伸音"占满**：`I6 → Imaj7` 不是进行。修：按**音级序列**计数窗口，
       要求窗口内 ≥3 个不同音级、≥2 首模板共有；不够就降级到 2 小节窗口、再不够用"级数转移链"兜底
       （**每层都记进包里**，不许拿"只有一首有"的进行冒充主题进行）。
     两处**静默跳过**（守卫全绿、其实没查）：① 主题旋律画像写在 `refs/themes/`，而
     `probe_melody_health` / `melody_matches_profile` 硬拼 `refs/melody/` → 主题曲的
     "碎音对照画像"与"旋律像不像画像"两条**都没跑**；② 旋律轨挑成钢琴织体 → 画像音域 46–88、
     5.3 音/小节（真旋律 1.5~3）。修：画像解析统一走 `melody_profile.find_profile`
     （refs/melody → refs/themes）；旋律轨挑完做**最高声部单音化**（重叠裁到下一音起始）。
     ⚠ 还有一条**判据顺序**坑：`validate_pack` 先查"模板在不在索引里"、`continue` 掉了来源检查
     → 非白名单来源**永远查不到**（判据自证当场抓到，把来源检查提到索引检查之前）。

119. **"量的量"和"验的量"不是同一个量：归一化用双声道 RMS，验收用单声道 RMS**（响度永远差 2dB）。
     `autotune_idempotent` 长期红着，先怀疑的是"到顶没报"，实测根因有两层：
     · **第一层（真 bug）**：`render_midi.render` 的响度归一化用的是**双声道** 2D RMS
       （`np.sqrt((y**2).mean())`），而全链路的验收口径 —— `metrics.profile` / scorecard /
       自检 —— 量的是**单声道(mid)** 的 RMS（`metrics.load` 取 `x.mean(axis=1)`）。
       加宽把 side 放大 → 双声道 RMS 虚高 → 成品单声道响度比目标低：**宽度 2.2 差 −2.09dB、
       宽度 1.0 只差 −0.25dB**（实测四个宽度/驱动组合）。于是自动调参**每轮重设同一个 rms、
       永远追不上**，6 轮渲染白烧（≈150 秒）——日志里"调 响度差 +1.3"重复 5 次就是它。
       修：归一化改用**单声道(mid) RMS**（与验收同量），宽度 1.0 直接命中 −16.90（目标 −16.9）。
       **教训**：口径一致不只要求"两个工具一样"，还要求"优化的量 = 验收的量"（坑 105 的第二形态）。
     · **第二层（机制该认账）**：峰值上限 0.97 挡住响度目标时（宽 + 稀材料的正常代价），
       重设多少次都不会变 → 现在 `render_midi.LAST` 自报"峰值受限 + 差多少"，自动调参据此
       **冻结 rms 并报"响度到顶"**；同时给其他参数加"**没进展也冻结**"（误差 <0.1dB 就停手），
       不再每轮白烧一次渲染。
     · **守卫跟着改口径**：`autotune_idempotent` 原来一律要求"残差 ≤2.5dB"，而夹具的 1.2–5kHz
       比参考薄 9.7dB、`mid_db` 上限 10 是**故意保守**的（顶到 9.84 再推反而更差 → 机制冻结）——
       把"如实报告的到顶"判成失败，等于逼实现假装收敛。现在判据是：**收敛，或"到顶/冻结"里有名有姓**
       （且加了"第一轮必须真调过"的防假通过断言）。

120. **变异测试的"漏了"有两种，别修错那一种：检查坏了 vs 检查空转**。
     `mutation_check` 有 4 条长期"漏了"（成品音频削波/时长只有一半/有声段落被静音/画像偏移 20dB）。
     它们**不是检查坏了**：仓库按用户要求"只留 MIDI"（`.gitignore` 里 `songs/**/*.wav|ogg`），
     `songs/` 里一个音频都没有 → `audio_health`/`audio_semantics`/`alignment_vs_refs` 的
     `for d in ...` 遍历到空集合，**整条静默跳过**（"空集合假通过"的又一形态：这次不是实现漏了
     非空断言，而是**样本本来就没有**）。
     修法不是"把空转判成失败"（那会让"只留 MIDI"这个合法状态永远红），而是**让变异用例自带夹具**：
     `mutation_check.audio_fixture()` 现场造一首 16 小节小曲 + 真渲染的 `_sf.wav` + render.json，
     再叠故障注入 → 三条检查真有东西可查 → 4 条全部"抓到"（95/95）。
     ⚠ 夹具**长度要够**：1 小节（2s）的夹具配 `audio_semantics` 那条"+8s 混响尾"的宽松窗口，
     "时长只有一半"落不进窗口（第一版就是这么漏的）→ 16 小节（32s）砍半必然越界。


121. **同一个坑 92 又踩了一次：`arr.mix` 的值是单整数 CC7，不是 `[pan, level]`**。
     做"段间能量曲线"时我把段落混音写成 `{"Melody": [76, 77], ...}` → 数据检查全绿、
     `make_song` 到 `write_midi` 才 `int(list)` 崩（`TypeError: int() argument ... not 'list'`）。
     **根因不是"忘了坑 92"，而是生成路径没有干跑**：`check_song` 的 `compose_dry_run` 只在
     **渲染前**跑，`new_song.py --theme` 生成完就交棒 → 结构性错误要等下一步才暴露。
     修：生成路径加 `dry_compose()`（写 MIDI 到临时目录，<1 秒，与 `build_song.py` 同一招），
     编配跑不通就**非零退出并指向上面的报错**。**原则：谁能最快发现问题，谁就跑那一步的干跑。**

122. **主题画像会把它自己的"密度语言"带进生成 —— 密集模板的主题必须体检 + 退密度**。
     `battle`（rock/game16/game32）的模板多是十六分跑句，主题画像 `notes_per_bar` 2.58 →
     `melody_gen` 生成出来**碎音 37%**，连它自己的画像宽容档（`probe_melody_health` 允许到 30%）
     都判"卡卡的"。这不是画像错，而是"照抄模板密度"在成品上就是缺陷（用户口径里"卡卡的"要修）。
     修：`new_song --theme` 生成后**当场体检**，不合格按 1.0→0.75→0.6→0.5 密度阶梯重试
     （实测 2.58 碎音 37% → 1.94 通过），全都不行就如实报告剩余问题，不许当"已修好"。

123. **CC7 不是线性幅度：按线性换算会把"段间曲线"做过一倍**。
     做段间能量曲线时我按"CC7 = 线性幅度"写成 `cur × 10^(ΔdB/20)` → 实测 `battle` 目标起伏
     6dB、成品 **12dB**（正好一倍）。真身：FluidSynth + GeneralUser GS 的 CC7 是 **GM 凹曲线**，
     实测 `dB(cc, cc₀) ≈ 38·log₁₀(cc/cc₀)`（幅度 ∝ **cc^1.9**；线性模型算 104→77 是 −2.6dB，
     实测 −3.9dB）。修：`new_song.cc7_at()` 走实测曲线（表见该常量注释）。
     测法本身也有坑：**同一渲染里必须放一个参考段** —— 渲染器会把整首归一到同一响度，
     跨渲染比绝对电平会串味（第一版就是这么测出 `CC7 74 → +1.16dB` 这种鬼值，还差点当成"声卡怪"）。
     规矩：**凡是要"按 dB 写进 MIDI 控制器"的地方，先把那条控制器的 dB 曲线量出来**，
     别拿"线性幅度"想当然；同时留意控制器上限（CC7=127 → 高电平轨的 +dB 会被夹掉）。
     · **同一处的第二个坑：CC7 是"持续状态"，不写的段落会继承上一段**。我图省事把
       `|Δ| < 0.5dB` 的段落跳过（不写 mix），结果只有第 1 段有偏移的主题（seaside/daily）
       让第 2~8 段**全都继承了那个偏移** → 整首被压低、段间对比归零（表现为"目标 3dB、
       成品 2dB、形状相关 −0.25"，看着像"映射不准"，其实是根本没生效）。
       修：**只要决定写曲线，就每段都写**（偏移 0 的段也写回预设电平）。
       规矩：**控制器类"状态量"要么全写、要么全不写**，别用"跳过默认值"省字节。

124. **"模板里没有这个乐器" ≠ "音乐里没有这个声部"：按角色缺失关掉基础声部，低频整条塌掉**。
     主题包的配器规则"角色占比 0% → 关"本身没错（用来关吉他/垫子/钟琴很合适），
     套到**基础声部**上就出事：classical/baroque/public_domain 的模板是钢琴/古钢琴，
     **没有独立贝斯轨** → `arr_off=['bass', …]` → 生成出来**根本没有贝斯**：
     40–80Hz 实测 **−33dB**（参考 0dB），成品只剩中频（315–630Hz 成了最响段）。
     低音其实就在那些模板的低音区里（左手）：**8.8 个起音/小节** —— 证据用错了维度。
     修：① `FOUNDATION=('bass','piano')` 不按角色缺失关（交给风格预设）；
     ② `new_song` 里 `arr['bass']` 恒为 True；③ `validate_pack` 守"低音区有内容（≥2 起音/小节）
     却把 bass 判成关"→ 报问题（配变异用例）。修完 classic 主题 40–80Hz **−33 → −2.6dB**。
     值得记住的形态：**"某个维度为零"只说明那个维度没测到，不说明对象不存在** ——
     换维度（这里是"低音区音符密度"）才是证据。
     （同轮的小事故：新写的 `_corr(a, b)` 与 `_key_of` 用的 `_corr(v, prof, tonic)` **同名**，
     静默覆盖 → `--selftest` 当场 TypeError。**长文件里的同名私有函数＝定时炸弹**，
     加函数前先 grep 一遍名字。）

125. **工具往系统临时目录丢东西却从不清理 —— 一天几百 MB，最后是用户先发现"C 盘怎么变小了"**。
     真身：`selftest.py` 的 `TMP` 是**模块级**创建的（`import` 即 `mkdtemp`），而 `selftest` 被
     `check_song` / `build_song` / `midi_ref` / `theme_pack` / `mutation_check` 到处 import ——
     于是**每次 `new_song.py` / `check_song.py` / `theme_pack.py` 运行都会在 `%TEMP%` 留一个
     `selftest_*` 目录**（里面是渲染出来的 WAV/OGG），而它**从不清理**：实测积了
     **1601 个 / 7.5GB**（`mutation_*` 另有 600+ 个），把系统盘吃掉一大块。
     `rehearsal` / `check_song` 都清理了自己的，唯独被 import 最频繁的这个没清。
     修：`selftest` / `mutation_check` 注册 **`atexit` 清理**（`DSH_KEEP_TMP=1` 保留，便于回看
     失败用例）+ 新守卫 `tmp_hygiene`（本项目前缀的临时目录：**>24 小时的残留当场自愈清掉**
     —— 进程被杀时 atexit 跑不到；**总量 >512MB 则 FAIL**「有工具在漏」；
     判据自证"伪造 3 天前的目录必须被抓"；并用**子进程**验证退出后目录真消失）。
     **同类第二处**：面板的音频缓存 `%TEMP%\bgm-studio-audio`（试听/搜索/配平/分轨每次丢文件）
     也从不清理，实测 0.93GB / 12 个 job 目录 → 加 `prune_tmp_audio()`（启动时：>7 天删、
     总量压到 512MB、最旧先删、`--keep-tmp-audio` 可跳过）+ 守卫 `studio_cache_prune`
     （功能测试：过期必删 / 新鲜必留 / 超预算最旧先删 / 容器目录不删）。
     ⚠ 变异用例还有个隐蔽坑：检查与变异用例**各自 import 了一次 `studio/server.py`**
     （模块名不同）→ patch 打的是另一个副本，检查照样通过、变异报"漏了"。
     修：两边共用 `st.load_studio_server()`（缓存到 `sys.modules`）。
     规矩：**凡是 `mkdtemp` 就必须有对应清理**（`try/finally` 或 `atexit`），
     而且**"被 import 的工具"比"直接跑的脚本"危险得多** —— 泄漏概率与**调用次数**成正比，
     与代码行数无关。

126. **"频谱全达标但不好听"：旋律缺的是结构三层（动机 / 期待 / 终止式），不是音色**。
     用户听完 36 号说"这个不是很好听"，而它当时**所有守卫全绿**（响度差 0.0、宽度 0.01、
     段间起伏 5.8 vs 目标 6.0、旋律形态 0 问题）—— 因为守卫量的全是**分布/频谱**，
     而听感上"像人写的"靠的是**结构**：动机（记忆点）、期待（大跳后反向/回填）、终止式（句读）。
     理论来源（都查过、可查）：Narmour 的旋律期待模型（[I-R 规则综述](https://lbj.utexas.edu/sites/default/files/file/faculty/Expectations%20as%20learned%20heuristics.pdf)）、
     Huron 的期待-情绪理论（[Sweet Anticipation 书评](http://www.psy.mq.edu.au/me2/uploads/publications/review_of_david_huron_sweet_anticipation.pdf)）、
     动机发展手法（[Ithaca DevTech](https://musictech.ithaca.edu/MusicTech/Form/Handouts/DevTech.html)）、
     乐句=带终止式的完整乐思（[Navy 音乐手册](https://www.navyband.navy.mil/documents/navedtra-15026b-musician2.pdf)）。
     **实现时踩的四个坑（每个都让指标先"看起来对了"再暴露）**：
     · **句末休止把终止音截成 0.5 拍**：短语末尾原本要留休止（呼吸，坑 99 的纪律），
       图式照原样铺到小节末时，最后一个音的时值被 `end - on` 砍到 0.5 → **收束率 0%（实测 38%）**。
       修：句末小节只铺**动机前半**（fragmentation），末音自然延长 = 分裂 + 扩时的收束写法 → 88%。
     · **小节接缝不受动机音程约束**：cell 只管小节**内部**，上一小节末音 → 本小节首音是自由的
       → **跳后反向率被接缝拉到 52%**。修：小节根音也按期待规则挑（在"能落该小节和弦音"的
       移调里选代价最小的：离 prev 近 + 大跳后反向 + 反向要回填）→ 80%。
     · **动机必须"可循环"**：落点若间距 <0.5 拍或太靠小节末，会被出口的"落点间距"规则
       **在小节之间**悄悄删掉/挪位 → 每小节的图案不再一致，动机重复率只有 20%。
       修：抽动机时加约束（间距 ≥2 个 16 分格、末落点 ≤12 格），并在 8 个候选里挑
       **落点分布最像画像方言**的那个（`_cell_fit`）→ 重复率 70%，且落点承接度回到 63%。
     · **指标自己会骗人**：① "节奏动机重复率"若**跨段**统计（8 段各一个动机），
       会把 8 个动机平均成 16% 的假低值 → 必须**逐段算再平均**（→81%）；
       ② 结构层判据的**单样本粒度太粗**（16 小节只有 4~6 个大跳，反向率只能取 0/25/50/…），
       判据要用**多 seed 平均**，并断言样本量（大跳数）够，否则"检查会空转"。
     **A/B（同骨架、只换旋律）**：动机重复 19%→**70%**、句末收束 0%→**81%**、
     跳后反向 71%→80%、回填 75%→92%；**混音层持平**（段间起伏 5.7 vs 5.8dB、
     最大频段差 4.2 vs 3.9dB）—— 说明结构层改动没有破坏已对齐的频谱。
     新守卫 `melody_motif_rules`（4 条下限 + 判据自证"关掉动机层必须掉到门以下"）+ 2 条变异用例。
105. **判据与被优化的量口径不一致 = 假绿灯（预演曾拿"最大频段偏差"当调参判据）**。
     `autotune` 走的是 `make_song.target_gaps()` 的**分组**误差（low / 1.2–5k / 5–18k / 20–40，
     `tune_step` 只看这四个），而预演原来判 `max_gap`（单个频段最大偏差）——**不在它的目标里**。
     实测：五套风格里多组"调参前后一模一样"（4.3→4.3），判据靠 `gap_after<=max(3.5,gap_before)`
     的**等号**勉强成立；另有一条 `or gap_after<=8.0` 的绝对逃逸口，等于给"小样本来就差 5–20dB"
     开后门。修法：① 判据改判 `tune_error`（分组误差之和）——
     `err_after <= 3.0（本来就够好）or err_after < err_before（确实变小了）`；
     ② 对齐 dB 只打印并标**仅供参考**（4 小节骨架 vs 完整制作，18.8→15.0dB 属正常）。
     **注入验证**：把 `make_song.autotune` 换成空操作 → 预演 **9 项 FAIL**（唯一 PASS 的是
     `2.8→2.8`，"本来就够好"），证明这道门不是装饰。**通则：判据必须与被优化的量同口径。**

106. **把 `detect_bpm` 的输出当"速度真相"**。本轮第一次把它量出来（两组带标注集；判据：
     精确 = ±2%，层级 = 含 ×2/÷2/×3/÷3 关系后 ±5%）：

     | 集合 | 精确命中 | 层级命中 |
     |---|---|---|
     | 本仓库自有曲目（`song.json` 有精确 bpm，音频是管线渲染的） | **7/17** | **15/17** |
     | 外部参考曲（`refs/*.json` 里人工钉死的值） | **1/23** | **3/23** |

     即：**外部素材上基本不可用**（不是"差一点"，是给了一个无关的数）；自有曲目上只到
     "层级级别可靠"（多数错例是 ÷2 慢一倍）。应对不是"修好它"，而是**不让人误用它**：
     ① `check_audio` 打印**层级阶梯**（本层 + ×2/÷2/×3/÷3 + 支持度）→ 人工核对＝挑一个；
     ② 画像写 `bpm_source: auto(未核对)` + `bpm_ladder`，`profile_ref.py` 打提醒；
     ③ 速度**一律人工 `--bpm` 钉死**（工具不猜，与 `pick_level` 立场一致）。
     ⚠ 别拿"提高命中率"当验收：这组数字的**真值本身来自人工**，它是参考、不是金标准。
     ⚠ 统计前先确认"画像名 → 音频"是**精确同名**：`BGM15b/15c/15d` 没有独立音频，
     用前缀匹配会全指到 `BGM15.ogg`，几首曲子读数必然相同（本轮第一版就踩到）。

107. **`midi_probe` 的拍号**永远**读不出来**：`timesig = None` 写在了**每轨循环内**，
     而拍号元事件（FF 58）只写在第一轨 —— 后一轨一进循环就把它抹成 None，
     于是 `res['timesig']` 恒为 None，打印时退回硬编码的 `'4/4'`。
     后果：**外部 MIDI 参考曲**（`refs/midi/`）与任何非 4/4 文件的拍号都被当成 4/4，
     `midi_ref.analyze` 又按 `bar_ticks = div*4` 切小节 → 小节/和弦/节奏型全错位，且**不报错**。
     修法：初始化提到循环外；`midi_ref` 按解析出来的拍号算 `bar_ticks` 与十六分格数；
     写侧（`bgm_synth.write_midi`）新增 `meter=` 参数写对 FF 58（原先硬编码 `04 02 18 08`）。
     守卫：自检 `meter_34_68` 要求 MIDI 拍号读回正确 + 音符不越出小节 + 强拍口径随拍号
     （变异用例：把 `strong_beats` 退回写死的"第 1、3 拍" → 3/4 的第 2 拍经过音被误报）。

108. **"配置写了、声音里没有"：`perc_layers` 在不支持它的 `perc_style` 下静默不生效**。
     垫层代码当初写在 **pump** 分支里，而 `light` 恰恰是最需要它的一档（这一档只有沙锤一个高频
     来源，5–18kHz 连续性全靠垫层 —— 坑 81 说的就是这个）。实测**库里 5 首**声明了不生效的垫层：
     **16/23/27/28（`perc_style: light`）与 26（`orchestral`）** —— 那一层从来没响过，
     而所有检查都是绿灯（MIDI 合法、频段达标、成绩单只报"节奏型不同"）。
     修法：把垫层接进 `light` 分支（`pump` 本来就支持）；并在 `load()` 加两道**声明校验**告警：
     ① `perc_layers` 出现在不支持它的 `perc_style` 下 → 报"不生效"；
     ② `air` 的时值 ≥ 重复间隔（条目数 × 0.25 拍）→ 报"同音高重叠"（那是坑 82 的悬空 voice 源头）。
     ⚠ **但"激活"并没有变好（实测）**：16/23 激活后 10–18kHz **+4.1/+4.6dB 过亮**、质心各涨 200+；
     新曲 30 用同样方案也 **+4.0dB**、质心 3240→3666。按"没有证据变好就不收"→ **全部撤回**，
     并把那 5 首的**死配置删掉**（数据要与声音一致：它们的交付音频本来就没有那一层）。
     通则：**声明了就要么生效、要么报错** —— 三行没生效的配置，比没有这三行更危险；
     而"加垫层"这种事要按 `bands_abs.py` 的**绝对口径 + 占用率**量过再收，光看成绩单相对那两行不够。

109–113 已归档到 PITFALLS-ARCHIVE.md（"治同质化"那一批的完整经过与实测数据，
     正文只留 §117 的结论 —— 那几条的守卫现已存在：melody_lang_diverse / melody_matches_profile）。


127. **"不如普通的曲子"：旋律的节奏「形态」没人量过 —— 结构层全绿也拦不住**。
     用户对 37 号的评价是"好了一点，但还是不如普通的曲子"，而它**所有守卫全绿**：病在**形态**
     —— 它的落点是小节 `0/0.5/1.5/2.0` 拍（三个音挤在前 2 拍、之后空 1.5~2 拍），于是
     **每小节都被切成一句**（断句 74 处 / 64 小节）。
     **这一轮最值钱的一步：先量真值，再定判据**。用 `theme_pack._melody_notes` 从
     `refs/midi2/` 的 **150 首模板**里提旋律，与生成端同一套定义量出对照表：
     | 指标 | 真实（中位 / cheerful） | 旧版 37 号 | 新 38 号 |
     |---|---|---|---|
     | 末落点 ≥8 格（跨过第 2 拍）的小节占比 | 90% / 79% | 61% | 77% |
     | 小节内最大空档中位 | 1.03 / 1.40 拍 | 2.00 | 1.50 |
     | 格 0（小节第 1 拍）落点占比 | 12.9% / 14.8% | 25.7% | 5~9% |
     | **小节节奏签名重复率** | **23% / 33%** | 66% | 30% |
     | 密度（音/小节） | — / 2.63 | 3.41 | 2.6 |
     **① 判据方向错了**：126 定的门是"动机重复率 **≥55%**（下限）"，真实旋律中位只有 **23%**
     —— 旧版的 66%（每小节复刻同一 figure）正是"呆板"的量化真身。改判**上限 45%**，并新增
     `melody_form_rules`（末落点 / 空档 / 格 0 / 密度 / 句内高点位置，全部带真实对照值）。
     **通则：拍脑袋的门要拿真值复核一遍。**
     **② 落点铺满 ≠ 声音铺满**：只铺满落点后断句**反而更多**（74→95 处）—— 时值仍照画像
     直方图抽（中位 0.5 拍，那是 F0 量的"发声时长"），间隔 1.5 拍就空 1.0 拍。真实模板的
     "无声空档"中位只有 **0.12 拍**（音是连着的）→ 时值改为"覆盖到下一个落点的
     `CELL_DUR_FILL`"；**但别拉满**（fill=1.0 时值方言承接度掉到 33%，门 55%）。
     **③ 变体层**：动机不再逐小节复刻，逐小节铺变体（移位/压缩/扩展/稀疏长音），
     **不许连续两小节用同一个**。**④ 接缝候选太窄会废掉期待规则**：小节首音原先只在**和弦音**
     里挑，而 `Am6 = A C E F#` 可用的方向只有一两个（F# 还是调外音，随后被"调内"再挪一次）
     → 接缝大跳反向率只有 43%（整曲 48%）。修：弱拍首音放开到音阶内自由音（代价 +4）+ 调外音
     预先计入代价 + **所有修正之后再一道期待兜底**（⑨）→ **82%**（真实 62~66%）。
     **⑤ 拱形在动机模式里从未生效**（旧版也如此）：⑤ 只写在"非动机"分支里。修：拱形落在
     **骨架音（小节首音）**上（代价 1.0 vs 平滑 0.45），小节内的音程 cell 不动 —— 加在每个音上
     会被随机游走淹掉（句高点中位仍只有 0.13）。度量要用**三音滑动平均的轮廓**（单音尖峰 0.40 /
     平滑后 **0.67** = 目标 2/3）；`aim` 方向别对半随机（一半句子"倒拱"，高点必然在句首）
     → 85% 上拱 + 段末句下拱。
     **⑥ 终止音会被后续修正打回**：终止式（拉向根音/主音）之后还有 ⑧ 同音去重与 ⑨ 期待兜底
     → 收束率在 50~69% 抖。加 `cad_tone` 标记让 ⑧⑨ 跳过它 → **81%**。
     **⑦ 和声要收束、材料仍须来自模板**：旧版把 4 和弦进行原样循环整段 → A 段永远停在 `B7`
     （属）= 一直悬着。`new_song.cadence_pair` 段末 2 小节改 V→I（三层退让：进行里真实的 V→I
     相邻对 → 同一进行里的属+主 → `chord_pool` 的 degree 7+0；`_stable` 挡掉 sus/dim）
     → cheerful 得 `G7→Cmaj7`。
     **⑧ 密度口径**：画像 `notes_per_bar` 是 F0 的**上界估计**（cheerful 3.23），旧代码还 ×1.3
     夹到 3.2 → 实测 3.41（音长被压短）。改 `DENS_MAX = 2.6`（`melody_gen` 单一出处）。
     **⑨ 力度**：旋律原只有硬编码两档（96 / 低八度 62）→ `patterns.melody_dyn`（**opt-in**，
     缺省时引擎一行不改、老曲字节不变）给乐句级包络（高点在句 2/3、句末收）。
     **变异用例的教训**：四个注入里"只把 `CELL_LAST_MIN` 设 0"**抓不到** —— `_cell_fit` 里
     "末落点越靠后越好"那一档仍会把落点挑到小节末。**注入必须打在真机制上**（覆盖下限与打分
     偏好一起关，或用 `VARIANTS_ON` 这类消融开关）。
     守卫（**每一层改动都配判据 + 注入用例**，selftest 92→**95**、mutation 101→**108**）：
     `melody_form_rules`（末落点 / 空档 / 格 0 / 密度 / 句内高点位置 / 音域 六条 + 自证
     "旧形态至少破两条"）· `melody_motif_rules` 重复率改上限 · **`theme_cadence`**（15 个主题包
     120 个段落段末必须"属→主"，材料取自模板）· **`melody_dyn_optin`**（缺省只有 2 档 = 老行为、
     打开 ≥6 档、显式 `false` 与缺省逐字节相同）；变异用例 **7 条**（关变体层 / 关铺满机制 /
     关间隔上限 / 关拱形 / 收窄音域 / 关 `cadence_pair` / 关包络函数）。
     ⚠ 两条判据的**度量口径**别弄错：**音域**要跨 seed 合并算（16 小节夹具单样本撑不开，
     实测单 seed 8~19 / 4 seed 合并 19，而 64 小节的真实曲目才用满 100%）；**句内高点**要用
     三音滑动平均的轮廓。实现与调参过程见 HISTORY。

128. **"旋律和伴奏配合不好" = 伴奏整轨在和弦外（`TR_SHIFT` 用了非八度移调）**。
     用户听完 38 号说"主旋律和伴奏没有很好配合"。探针（旋律 vs 同时发声的伴奏，同口径拿
     `refs/midi2/` 80 首真实模板作对照）量出两条硬差距：**旋律与同拍伴奏的半音冲突 45%**
     （真实 6%）、**旋律反被伴奏盖住**（"旋律在下" 59%，真实 1%；音区分离 −2 半音 vs 真实 +12）。
     根因是 `song_engine.TR_SHIFT`：音区分工表是 `Pad −5 / Hook −5 / Piano +4 / Strings −3 /
     Arp +3 / Melody +7`，注释写着"只改 MIDI 音高、不动和声：整轨同移不改变和弦内的音程关系"
     —— **那句话是错的推理**：轨内音程关系不变，但**与和弦的关系全变了**。
     实测各轨"音的 pc 落在当小节和弦音集里"的比例：Bass **98.6%**（无移调，作对照）·
     Hook **15.1%** · Arp 31.6% · Piano 37.5% · Strings 43.2% —— **伴奏 60~85% 是和弦外音**。
     修：① `TR_SHIFT` **只允许纯八度**（伴奏整体 −12 让位、旋律 +12 独占最高）；② 加**边界
     保护**（整轨移调后越出乐器音域就整轨不移 —— 不许轨内八度跳变）；③ 乐器合理音域表从自检
     移进引擎 `TR_RANGE`（**单一出处**：引擎的保护与自检的守卫必须用同一张表，否则互相打架）。
     修后：伴奏贴合 **100%**、音区分离 **+16**（真实 +12）、半音冲突 **8%**（真实 6%）、
     旋律在下 **0%**（真实 1%）。
     **两个附带教训**：① **判据别写死音区** —— `harmony_layer` 原来写"音区 65~76 才算副旋律"，
     `TR_SHIFT` 一改那些音就掉出区间、**检查当场空转**（变异用例漏抓）；而且它的夹具旋律
     （76/79）比和弦最高音高一个八度，`harmony_below` 根本不产音，`assert harm` 是**假通过**
     （Hook 的普通伴奏音恰好撞进断言集合）。改成"机制级单元判据 + 端到端必须真写进 Strings"。
     ② **改引擎行为 = 全库 MIDI 过期**：`determinism_and_bytes` 逐首比对已交付 MIDI，TR_SHIFT
     一改 30 首全部要重跑 `make_song`（不是"跑一首就行"）。
     守卫 `accompaniment_harmony`（纯八度 / 伴奏贴合 ≥95% / 音区分离中位 ≥6 / 旋律在下 ≤15%）+
     变异用例"换回非八度表"。⚠ Bass 的**句尾半音引导音**（`bass_part` 末尾：`i%4==3` 时朝下一
     和弦根音走半音）本来就落在和弦外，是正确手法 —— 判据不把 Bass 算进伴奏贴合。
     探针口径（真实模板侧用 `theme_pack._melody_notes` 挑旋律轨、其余轨当伴奏）见 HISTORY。

129. **"伴奏太满"：旋律没有"独唱"的时刻**（网上编曲手法的第一条 "creating space for a melody"）。
     用户"配合不好"在修完和声（坑 128）之后还剩一层：**伴奏密度**。同口径探针（真实侧
     = `refs/midi2/` 80 首模板）量出 **伴奏起音 45.0 音/小节 vs 真实 19.8（2.3 倍）**
     —— 真实模板非鼓轨**每轨中位只有 3.6 音/小节**，我们 Hook 14.7 / Bass 12.1 / Arp 8.0 /
     Piano 7.2 全部超；后果是旋律"独唱率"只有 **15%**（真实 43%）。
     修（`patterns.space`，**opt-in**，主题路径新歌默认开）：
     ① **减薄**：有鼓的段落 Arp 每半拍→每拍（时值 0.28→**0.9 拍**，靠"持续"而不是"音数"占高频）、
        EP 每拍 3→2 音、Piano 反拍 3→2 音、Bass 十六分三连→两条（12→8 音/小节）；
        **无鼓段落不减**（那里本来就稀，实测 rehearsal 的"无打击乐段落"夹具调参误差卡在 3.5）。
     ② **运行时让位**：**旋律起音的同一刻，伴奏最多 3 条轨发声**、每轨该刻最多留 1 个音。
        ⚠ **只做①不够**：减薄降的是"平均密度"，而"旋律响的那一刻仍挤着 6 条轨"只能靠② ——
        实测减薄后"旋律起音处的伴奏音数"**一点没降（6.5 → 6.5）**；让位后 **6.50 → 2.50**
        （真实模板 3.0，直接可比）。修后 38 号：密度 45 → 26 音/小节。
     判据 `melody_space`：**开关两版就地对照**（密度 ≤85% / 旋律起音处伴奏音数必须下降 /
     无鼓不减）+ 自证（`space_on` 恒 False 时两版必须完全一致）+ 变异用例。
     **两条纪律**：① **改引擎行为一律 opt-in** —— 第一版我直接改，结果 30 首 MIDI 全部过期、
     rehearsal 的夹具也被牵连（边界用例调参误差 3.5 卡住、连换两次补偿都不行）；改成
     `patterns.space` 之后老曲字节不变、rehearsal **立刻全绿**。
     ② **"独唱率"不能当判据** —— 它要求伴奏放弃整个八分网格（真实模板靠"旋律节奏自由"实现），
     在我们"16 分格全覆盖"的编配里**恒为 0**，是个永远红灯的假判据；改用
     "**旋律起音处的伴奏音数**"（可升、且与真实 3.0 直接可比）。

130. **"参照系选错，比没有参照更糟"**：四条手法里，**先量真值**这一步直接否掉两条、纠正一条。
     · ❌ **微时序人性化（每轨 ±10~30ms）——不做**：真实模板 MIDI 的 onset 偏差**中位 0.0ms**、
       **88% 的轨是量化的**（<1ms）；它们的"味道"不来自微时序。我们本来就是严格量化，
       加了反而**偏离参照系**。（网上讲 humanization 的文章说的是**人演奏的录音/演奏**，
       与"MIDI 模板"不是一类对象 —— **引依据时先看清对象**。）
     · ❌ **拿 MIDI 模板的"编配密度曲线"当张力真值 ——不成立**：真实模板"每 8 小节密度"的
       **相对起伏中位 0.00**（四分位 0.00~0.57，一半以上完全平）—— MIDI 模板库在编配层是
       **扁平**的（多是钢琴/小编制），它回答不了"副歌该比主歌厚多少"。
     · ✅ **换成"混音目标画像的段间能量块"（来自真实音频的每 8 小节响度）后成立**：
       `new_song.arr_level` 让能量高于均值的段开第二梯队（替换旧的 `level = i % 3`）——
       高/低能量段的编配厚度**排序相关 0.87**（机械轮换 0.64）。38 号因此从
       `[0,1,2,0,1,2,0,1]` 变成"能量高的段厚、能量最低的 C 段薄"。
     **判据也踩了一次（同一课）**：先用"排序相关 >0.5"当门，结果 `i % 3` 与**周期性的能量
     曲线**（8 段循环）撞出**伪相关 0.64**；改成**机制级判据**（`arr_level` 必须是能量的
     **单调不减**函数）后，机械轮换**立刻失败**（在 `[-3,-1,0,1,3]` 上给出 `[0,1,2,0,1]`）。
     **通则：判据尽量直接测机制（函数性质），少用统计量 —— 相关系数会被周期性骗。**
     ⚠ 另一条边界：目标起伏 <0.5dB 的主题包**整首不给曲线**（`ENERGY_MIN_DB`），
     那时 `arr_level` 全 0 —— 那是**设计**（不造假变化），不是"编配没生效"。



131. **"平行五/八度"是四声部合唱的规矩，不是流行/舞曲的** —— 拿古典禁忌当守卫门之前，先量真值。
     真实模板（`refs/midi2/` 54 首）平行五度占比**中位 0.000**（75% 分位 0.006）、平行八度
     **中位 0.002**（75% 分位 0.035），**但最大到 0.829** —— 舞曲的低音跳动天然会撞八度。
     我们实测 0~0.009 / 0.007~0.010（3/4 圆舞曲 29 号 0.109：它的贝斯是 oom-pah-pah，
     与旋律撞八度属预期）。→ 这条**不加约束**（旋律生成本来就守着"大跳后反向/回填"的期待规则），
     只加**防退化守卫** `melody_voice_leading`（门 0.15 / 0.20，只抓"整条旋律跟着低音走八度"
     那种极端退化）+ 变异用例"旋律全部 = 低音 + 12"（实测被抓：42~50% 平行八度）。
     **通则：把古典规则搬进流行语境之前先量真值 —— 门开太紧等于给正常写法判红。**

132. **混音参考要从"挑一份"改成"多份聚合 + 逐份可溯源"**（用户口径："混音要参考权威音源，
     **也要多方参考**"）。
     旧做法：`theme_pack.mix_target` 从 46 份真实画像里**挑一份**最像的，评分只有
     速度 0.55 / 打击感 0.30 / 调式 0.15 —— **没有亮度**。实测后果：`tender`
     （ballad 编配：钢琴＋尼龙吉他＋弦乐，中频天生厚）挑到 **BGM04**（315–1250Hz 在 −10~−14dB
     的**亮薄**参考）→ 成品中频厚 **9.6dB**，成绩单直接报"先改 BPM 再谈其它"。
     而且 46 份画像**都没有出处字段** —— "权威"无从追溯。
     现在：① `tag_ref_sources`（幂等）给每份画像补 `source` —— 如实写"项目素材包 `Bgm`
     （提取自 galgame 素材的 50 首 BGM，真实商业混音），**厂商/作品名未标注**"，
     **不假装权威**；以后引入带 URL 的来源写 `source.kind='web'` + `url` 即可；
     ② `mix_target` 改成**合格的前 N 份（≥3）逐维度取中位数**（`aggregate_refs`），
     落成 `refs/mix_targets/<主题>_mix.json`，`render.json` 的 ref 指向它；
     ③ `validate_pack` + 新增守卫 `mix_target_aggregate`：必须是聚合、成员 ≥3、**逐份带 source**，
     并**逐频段核对"聚合值 = 成员中位数"**（机制级，不是只看字段在不在）。
     实测（tender）：质心 2281 → **3284**、1250–2500Hz −20.9 → **−9.4** —— 单份的极端个性被削掉，
     成品中频差 **9.6 → 5.2dB**。
     **三条踩过的坑**：① 聚合画像**文件名必须与 `ref` 同名**（第一版写成 `<主题>.json` 而 ref 是
     `<主题>_mix` → `new_song` 按 ref 名扑空，`render_json_schema` 报"参考画像不存在"）；
     ② 离散字段（`rhythm_low`/`rhythm_high`/`quiet_chroma`）**不能取中位** —— 取分数最高那份
     成员的（`scorecard` 要打印它们）；③ 聚合后目标曲线**更平** → `theme_arrangement_dynamic`
     的可判主题数从 10 掉到 8（门按实际校准：15 个主题约一半目标本身平坦，那是
     `ENERGY_MIN_DB` 的设计）；另外 `gorgeous` 的第二梯队被 `arr_on` **强制全开**（`[5]*8`）
     → 判据跳过"没有更厚的档可加"的情况，否则误报"没跟张力走"。
## 续：133–134（并入：加入 142/143 条时超预算，按 LIMITS 注释拆，编号不变）

133. **"好多部分都是一样的"：两头都反了 —— 旋律该重复的没重复，编配该变的没变**。
     用户反馈后**先量**（`_same_probe` 把"一样"拆成旋律 / 编配 / CC7 三层），发现两个方向都错：

     | 层 | 38 / 39 号（修前） | 该有的样子 |
     |---|---|---|
     | 旋律 | **8 段 8 支完全不同的旋律**（A/A2/A3/A4/A5 各一支） | A 段复现**共用**主题旋律（记忆点） |
     | 编配 | 39 号**只有 1 种组合**、CC7 全曲同一套 | 主歌薄、副歌/桥段厚（张力） |

     · **旋律那头的根因是一行命名**：`build_from_theme` 给每段 `mname = 'm%d' % (i + 1)` ——
       而 `melody_gen` 本来就是**按旋律名分组生成、同名共用**的；每段一个新名字，等于把复用
       机制**掐掉了**。改成按**角色名**（去掉尾部数字：A2/A3/A4/A5 → A）后，8 段只用
       **3 支旋律**（A×5 / B×2 / C×1）—— 这才是曲式（AABA）该有的样子，也才有"主题"可言。
     · **编配那头的根因是"兜底太保守"**：`arr_level` 在能量曲线平坦时返回全 0（当时的理由是
       "不造假变化"），而 `tender` 的目标画像**本身平坦** → **8 段编配一模一样**。改成
       **曲式角色兜底**（非 A 段开第二梯队、A 段关）—— 用的是**已有信息**（`form.plan` 的
       段落名就是从模板分析来的），不是编造数据。
     **顺带修一处判据口径错**：`melody_form_rules` 原来要求"单曲音域 ≥ 画像 `range` × 0.85"，
     而画像是**同主题多首模板的并集**（tender 34 半音）—— 旋律复用后 39 号只剩 18 半音（53%）
     被误判"不达标"。改成"落在**合理区间**"（`FORM_SPAN_MIN` = 12 ≤ span ≤ 画像 range）。
     **通则：拿"多首聚合的画像"当"单曲的下限"是口径错**（坑 105 的同一族）。
     守卫 `theme_arrangement_dynamic` 的"曲线平坦"分支随之改成"**编配不许全平**"。

134. **"每个曲子都一样"的第二层：编配是"同一套乐器从头铺到尾"**（133 只修了"每段给不给第二梯队"，
     没修"整套乐器是否换"）。量法：段落的乐器组合两两 Jaccard + 各乐器在场率 + 真实分段画像
     （`refs/sections/*.json`，以 160–315Hz 为基准的相对谱）。
     实测（修前）：231 个段落 bass **100%** / piano 98% / perc 97% 在场，段间 Jaccard **中位 0.86**
     （36~39 号全 0.86，18/20/24/25/27 号 **1.00** = 整曲一套乐器）；对照 13 首真实商业 BGM：
     高频 5–10k 段间起伏 **7.8dB**、10–18k **9.1dB**，我们只有 **0.5 / 0.5dB**。
     修法 `song_engine.arr_by_role`（opt-in `patterns.arr_by_role`）：引子/尾声=钢琴+贝斯+垫子、
     主歌=+吉他、副歌=+钟琴/琶音/打击/弦乐、桥段=换电钢（另一条频段），副歌按出现次序升级；
     `perc` 变成 0/1/2/3 **层次**（不只是开关）；**兜底**：若整首没有任何一段有打击则强制开一段
     （否则 5–18kHz 塌，坑 81 系）。
     实测（41 号 vs 旧 39 号，同主题同速）：Jaccard 0.86 → **0.62**、strings 在场 100% → **25%**、
     高频段间起伏 1.1/1.0 → **4.6/5.0dB**；`melody_matches_profile` 不动。
     守卫 `arr_role_variety`（机制级 6 条性质 + 15 个主题端到端 Jaccard ≤0.78）+ 变异用例
     "段落编制不随角色变"。**注意**：门取 0.78 不是 0.75 —— waltz 的 A/B 交替最密、曲线又把
     末尾两个 A 段抬了一档，实测 0.76；抬门要有实测理由，不许为消警告抬。

## 续：135–136（并入：加入 145/146 条时超预算，按 LIMITS 注释拆，编号不变）

135. **"每首曲子的刚弦吉他都一个节奏音调"：跨曲硬编码 + 曲内逐小节复读**。
     两条都量出来才算清：
     ① **跨曲**：`patterns.arpeggio` 是引擎**硬编码**的 `[0,2,3,4,3,2,4]`，而 15 个主题包
        **全都没有这一项** → 每首歌的吉他都是同一组落点 + 同一组和弦音序（只有和弦不同）。
     ② **曲内**：`arp[k % len(arp)]` 对每个小节都一样 → **和弦相同的小节音高序列逐音完全相同**
        （实测 39 号 **94%** 的小节重复、38 号 56%）。
     修法（全部 opt-in）：音型按主题包 `arrangement.role_range` 的**真实吉他音域跨度**分三档
     （`retro` 31–77 跨度 46 → 宽琶音；`night` 50–67 跨度 17 → 窄音型；`new_song.theme_guitar_arp`）；
     落点按主题包**真实高音区占用率** `rhythm.high_slot_share`（`song_engine.guitar_beats`，
     gorgeous 16 分铺满 vs night 7 个八分）；曲内加**相位轮换 + 同和弦换把位**
     （`guitar_rot` / `guitar_arpeggio(prev_chords=…)`，`patterns.guitar_vary`）。
     实测：15 个主题 → 音型 3 种 / 落点 **12 种**（修前 1 种）；同和弦 6 小节音高序列
     **1 种 → 6 种**；42 号音高重复小节 94% → **75%**（剩下的重复来自相同的和弦+相同的落点，
     属正常）。
     **两条纪律**：① 换把位必须**夹在音域内**（`>96 → −12`）且**第 1 拍永远是根音**
     （不参与轮换，否则和声含糊）；② `vary=False` 必须与旧行为**逐音一致**（有自检断言
     `[48,55,59,59,59,55,59]`）—— 老曲字节不能变。
     守卫 `guitar_variation`（跨主题音型 ≥3 种、落点 ≥6 种、同和弦 ≥3 种序列、和弦内+根音在拍 1）
     + 2 条变异用例。**判据别合并**：第一版把"音型×落点"合成一个组合数，注入"所有主题共用
     同一组音型"时组合数照样过门（落点仍各不相同）—— 拆开判才抓得住。

136. **"上一条检查过了"不等于"这一层没问题"** —— 133/134/135 是同一个用户反馈的三层，
     每层都要重新量。用户说"怎么感觉你写的好多部分都是一样的"时，第一轮只查了旋律与
     段间能量（133），结论"已修"；用户接着说"都有"，第二轮查出编配（134）；再问
     "怎么每个曲子的刚弦吉他都是这个节奏音调"，第三轮查出吉他音型（135）。
     **通则：听感反馈"像/一样"必须拆成"哪个乐器、哪一层"逐个量**（乐器组合 / 落点 / 音高序列 /
     音量曲线是四个独立自由度），量到一个维度就宣布"修好了"会连着三轮返工。

137. **"只有开头一秒有声音"：WebAudio 调度里"音在时钟上的绝对时刻"算错了**（用户听感反馈）。
     症状：点播放，开头一小段有声，之后彻底静音（进度条还在走）。
     根因在 `studio/web/ed.js` 的 `scheduleWindow`：音符绝对时刻写成了
     `startedAt + (音位置 − **当前播放位置**) × spb`，而正确值是
     `startedAt + (音位置 − **视窗起始拍 x0**) × spb`
     （由 `pos = x0 + (now − startedAt)/spb` 反解而来）。两者差 `now − startedAt`：
     **播放越久差越大**（实测 2 秒后差 2 秒）→ 窗口里每个音的 `at` 都落在 `now` 之前，
     被"已经过去的音不追"全部丢掉 → 听感就是"放了一小段后彻底没声"。
     同一族还有三个坑（都在同一条链上，逐个修过来）：
      ① **AudioContext 要等 `resume()`**：Chrome/Safari 用户手势前是 suspended、`currentTime`
         不动；不等就取基准 → 播放头永远停在起点（"点了播放没反应"）。
      ② **并发保护不能"拒绝新音"**：原来 `if (S.live.size > 48) return;`，而 `S.live` 靠
         `onended`/`setTimeout` 回收 —— 回收不及时就**永久静音**。改成"到上限淘汰最老/最弱"。
      ③ **自动滚屏改了 `x0` 必须重算基准**（位置表达式里的常数项会整体跳变）。
     **副作用一条**：冒烟测试里若把 `setTimeout` stub 成"永不执行"，`drop` 不回收 →
     `S.live` 涨到上限 → 也会复现"0 发声"。夹具必须让 `setTimeout` 真的跑（测试自身也可能是凶手）。
     判据（`studio/tools/smoke_ed.js`，**逐帧推进虚拟时钟**）：第 1/2/3 秒每秒发声数都 > 0、
     调度窗口 `late < inWin`、`S.live` 未触顶。修前实测 `62/0`，修后 `180/152`（真曲 `1768/2362/1974`）。
     另：进度条拖动"卷帘不定位"的根因是 `seek()` 只改位置不重画、视窗也不跟随 —— 一并修。

138. **"播着播着直接跳到最后"：自动滚屏的时钟补偿写成了恒等式**（用户听感反馈，紧接坑 137）。
     `tickPlay` 里播放头走到视窗右侧就自动滚屏。位置公式 `pos = x0 + (now − startedAt)/spb`：
     `x0` 一改、若不动 `startedAt`，下一帧位置就**凭空多出 Δx0 拍** → 播得越久窜得越快
     （实测 40 秒就能跑完 120 秒的曲子，用户看到"从 10 多秒直接跳到最后"）。
     **我第一版的"补偿"是恒等式**：写的 `tau = (now − startedAt)/spb; startedAt = now − tau*spb`
     —— 代回去就是原值，等于没补偿（这是最隐蔽的一类错：看起来在做补偿、实际啥也没干）。
     正确写法是**直接要求"位置在新 x0 下不变"**、反解基准：
     `startedAt = now − (pos − x0_new) × spb`（**先算 startedAt 再改 x0**，顺序别反）。
     另一条护栏：视窗宽度 `clientWidth` 在 canvas 未布局时可能是 0/极小 →
     `pos > x0 + 宽×0.85` 每帧都成立 → 每帧滚一次（同样窜）。所以宽度要 `Math.max(8, …)` 夹下限，
     并按 wall-clock 给滚屏限流（rAF 叠加 / 切标签页补帧时不连滚）。
     判据（`smoke_ed.js`）：**滚屏那一帧最大单帧位移 < 0.5 拍**（修前 5.8 拍/帧；修后 0.073 = 正常一帧）、
     以及"音轨时间 `pos×spb` 与音频时钟差 < 1 秒"（连播 33 秒、滚屏 6 次后实测 33.0 vs 33.0）。

139. **WebAudio 合成音"听起来奇怪，不如其它播放器"**：一个音 = 一个振荡器 + 线性包络直连输出。
     用户口径"声音听起来有点奇怪，不如其它播放器"。原来的实现只有三件事：
     `createOscillator()` 一个 `triangle` + 线性增益包络 + `connect(destination)` ——
     没有泛音结构（像"电子豆子"）、没有滤波器（力度/音区不改变明亮度）、没有空间感（干声贴耳）。
     改成：① 每音 2~4 个**泛音/失谐**正弦振荡器 + 低通滤波（截止随音区与力度变化）；
     ② 按乐器族给 ADSR（拨弦快起快落 / 弦乐缓起 + 揉弦 / Pad 更缓）；
     ③ 主输出串 **压缩器**（防叠加削波）+ 低通（去刺）+ **程序生成脉冲响应的混响**；
     ④ 鼓组分开做（底鼓=正弦下滑、军鼓=带通噪声、踩镲=高通噪声），不再用方波糊弄。
     音色族按轨的 GM program 自动推断（`familyOf`），顶栏「🎨 音色」可强制成一种并存 localStorage。
     实测：一次 3 秒播放里 **3066 个音共 8035 个泛音振荡器（平均 2.62 个/音）**。
     **测量教训**：判据别"在沙箱外数 createOscillator 调用" —— 沙箱内外两个计数器实测对不上，
     第一版因此把"多泛音"误判成"0 个泛音"；改用实现事实（`FAMILIES` 的泛音层数、
     主链是否挂了 wet/comp、`playTone` 自己记的账）就稳定了。**判据要测"事实"，不是测"我数得对不对"。**

140. **"还是比不上主界面"：编辑器在用合成音，而引擎面板放的是真音源渲染**。
     用户把两个页面一比就听出来了。教训：**先看"隔壁那条链在用什么"** —— 引擎面板走的是
     `make_song → render_midi.py`（GeneralUser GS + FluidSynth + 混响），编辑器却自己写 WebAudio 合成。
     正确解法不是继续堆合成算法，而是**复用同一条渲染管线**：编辑模型 → 导出 MIDI →
     `render_midi.py` → OGG → 前端 `<audio>` 播放；合成音退为兜底。
     三条工程约束（都是实测踩出来的）：
      ① **渲染必须异步**：整曲 40~60 秒（FluidSynth + OGG 编码），同步会把面板卡死 + 请求超时 →
         `render-audio` 提交后台任务返回 `task`，前端轮询 `render-status`，期间继续用合成音。
      ② **路径要短、名字要唯一**：`render_midi.py` 会写 `<base>.raw.wav` 中间文件；
         面板会话路径（带 eid + 指纹）太长 → FluidSynth 写不出来 → `os.remove(raw)` 崩；
         短名若被两个任务共用 → 互相删对方的 raw.wav。两个症状都是"手工渲染成功、走面板必失败"。
      ③ **播放时钟抽成锚点对** `(anchorBeat, anchorT)`：`synth` 看 `ctx.currentTime`、
         `media` 看 `player.currentTime`；换音源/seek/滚屏只改锚点，位置天然连续 ——
         这是坑 137/138 那一族的**通用解**（不用再为每种音源各写一套基准补偿）。
     判据：渲染出音频（123 秒曲子约 7 秒）· 音源切成 media · **播放头与音频时间对齐（2.00s vs 2.00s）**
     · 停止时媒体也暂停 · 媒体时钟推进播放头前进。

141. **引擎修好了、全库没重渲、也没提交 → 三个批次后分不清哪些产物是新引擎的**。
     这轮为了查"主界面卷帘是空的"，顺手扫了全库的 determinism，结果发现 **34 首的音符内容全都变了**
     （同数量、内容不同）——引擎里 `TR_SHIFT` 的非八度 bug（坑 128）早已修成纯八度，
     但**既没重渲全库、也没提交**，于是 `songs/*.mid` 全是坏引擎的产物（伴奏音高系统性偏 7 个半音）。
     教训与做法：
      ① **改引擎 = 改行为**：哪怕新参数是 opt-in，只要动了 `build_events` 的默认路径，
         就必须**当场全库重渲**（实测 `make_song --no-tune` 34 首共 **5.2 分钟**，成本很低）。
         "等会儿再渲" = 下次来的人（包括自己）分不清哪些产物是哪个引擎出的。
      ② **别用 `song_engine.py <song.json> <out.mid>` 重渲**：命令行**忽略第二个参数**，
         MIDI 会直接写到 `song.json` 同目录 → **覆盖交付物**（我这么干过一次，67 个文件被改）。
         正确做法是调 API `song_engine.compose(song_json, out_mid)`（它认第二个参数）。
      ③ **全库一致性可以秒扫**：在**临时目录**里 `compose` 每一首再与 `songs/*.mid` 比音符
         （比字节宽松，能抓"引擎变了"而不是"元数据变了"），几分钟就能拿到完整清单，
         比反复跑 `selftest` 一次只报第一首快得多。

142. **面板"什么都没有"≠ 渲染坏了：UI 读的是 song.json 里没有的字段**。
     用户报"左侧音轨面板只有产物和大小"。实测 `/api/song` 返回的 `song` 键里**没有**
     `programs`/`mix`（42_gtr_tender 就是这种形状），而 `renderTracks()` 用
     `Object.keys(S.song.programs||{})` 枚举音轨 → `[]` → **一张卡都不渲染**，面板里只剩"产物/大小"表。
     同根因的还有：混音台 strips 空；`applyMixToSong()` 里 `if(!(t in (S.song.mix||{}))) continue;`
     → **拖动混音台后"应用到曲目"静默什么都不写**。为什么一直没被发现：
     `fixtures/song.json` 自带 programs/mix —— **smoke 的假数据比真数据"完整"**，正好绕过这个形状。
     解法（三处都要）：
      ① 统一取数 `trackRows()`：**events 实测值**（＝默认＋风格预设＋song.json 覆盖的结果）
         > song.json 显式覆盖；面板/混音台/段落 CC7/数字快捷键全走它；
      ② **惰性写回**：只在用户真改某轨时才建 `S.song.programs[t]`，且**只改被编辑的那一维**
         （通道号必须沿用 events 的真实值 —— 退化成 0 会和别的轨撞车）；
      ③ 断言必须跑在**真实形状**上：先把 fixture 的 programs/mix **删掉**再渲染，音轨卡必须 = 轨数。
     判据：缺字段时音轨卡 **9/9**（旧版 0/9）、strips 9 条、"应用到曲目"能写回、改音色不动通道号。
     ④ **"按需建字段"要精确到维度**：无条件把 `programs`/`mix` 都建成 `{}`，会让"只拖了一下
     音量"也往交付物里塞一个空的 `"programs": {}`（实测污染 `42_gtr_tender/song.json`）——
     空对象没有任何语义。改成"改哪个建哪个"。
     ⑤ **改了 `mix`/`programs` 只动 song.json，交付 MIDI 不会自动跟着变**：`selftest` 的
     `determinism_and_bytes` 会报"已交付的 X.mid 与当前引擎输出不一致 → 需要重跑 make_song.py"——
     **这正是它该报的**。处理只有两条：重跑 `make_song.py`（认下这个改动），或撤掉改动
     （不是它想要的）；**不要去改检查**。

143. **常驻 rAF 往画布上叠画"每帧变化的东西"＝在攒残影**（拖进度条＝满屏红线）。
     用户报"拖动进度条之后还会有显示bug"，截图里卷帘上几十条红色竖线。根因：`drawPlayhead()`
     每帧在**卷帘画布**上补一条 2px 红线、**从不擦除上一帧的** —— 播放时位置连续、旧线叠成红带，
     拖动时位置大跨度跳跃、旧线全部留下。反证：5 次 `seek()`＋`drawPlayhead()` 在旧版留下
     **5 条红色满高矩形**，新版 0 条。做法：播放头改成**独立 DOM 叠加层**（`#rollPh`，只改
     `transform`，画布上零播放头像素）；位置用**同一次绘制**的几何（`paintRoll` 记进 `S.rollG`）算，
     保证与音符严格对齐。配套两条：smoke 的 canvas 替身必须**记录绘制**
     （原来 `getContext()` 返回空壳 → 画了什么都测不到），并加**探针自证** ——
     手工画 2 条红线必须被数出来，否则"0 条"是"测不到"而不是"没有"。
     **验收侧的第二个坑：headless 浏览器里 rAF 不会自己跑** —— 实测 `posInfo` 恒停在
     `0.0 / 0.0s`、播放头 `transform` 一直是拖动前那一帧，一度被误判成"定位没跟上"。
     用 CDP 验 UI 时：① 先 `Page.captureScreenshot` 逼它合成一帧来驱动 rAF（或直接调一次
     绘制函数，`tools/browser_check.js` 两种都做）；② 判据写成"播放头比例 ≈ **几何算出的**
     比例"，别拿"拖动比例"当期望值 —— 音频尾巴比音乐长（120BPM/64 小节 = 128s 音乐 vs
     132.9s 音频），拖到 80% 音频位置对应 **83%** 的卷帘宽度，**这是对的**不是偏差。

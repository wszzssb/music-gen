# 开发记录：从素材分析到 BGM 生产线

> 这份文件回答两件事：**① 前面那些对话到底做了什么 ② 一共修了哪些 bug（含根因与守卫）**。
> 想直接用工具链的人读 `README.md`；想写新歌的人读 `~/.dsh/skills/bgm-studio/SKILL.md`。
> 最后更新：2026-09-13（自检 **79/79** / 变异 **78/78**；**当前状态见文末「三、现在的验证状态」**，
> 本文按时间倒序堆叠，早期的轮次数字是当时的口径）。

---

## 一、经过（四个阶段）

### 阶段 1｜素材提取（原始任务的第 1 步）

原始需求：从一组**本地游戏素材**里学习 UI 制作方法、还原一个完整的 UI 界面，
"剧情和角色音乐背景等之后再说"。（素材的具体来源与路径不记入本仓库。）

| 类别 | 做法 | 结果 |
|---|---|---|
| `pna` | 用对应格式的提取工具 | 解出 `Graphic / SysGraphic / Chip1-5 / Effect / Fonts / Script / Sound…` |
| `mos` | 与 pna 同格式，改后缀后同样处理 | 同上 |
| `psp` | 用 EmtConvert / FreeMoteViewer 系列工具 | 立绘/贴图 |
| `dat` | 确认基本是视频/动画容器，按需处理 | `OP.dat`、`ED.dat`、`EFFECTS_*.dat`、`FIREWORKS_MOV_*` |
| 批量脚本 | `extract_pna.ps1`（放在素材目录里） | 可重跑 |

关键收获：`Sys_Title`（标题画面）、`Sys_Msw`（对话框）、`Chip1+`（背景）、
`PNA_Extracted`（角色立绘）、`Bgm`（50 首 BGM）是整个 UI/音乐素材的来源。

### 阶段 2｜UI 还原（`ui-v2`）

基于提取出的贴图还原出一套可交互的 galgame UI（HTML/CSS/JS，并做成 DSH 插件在 GUI 里直接看）：

- 标题画面（`Sys_Title` 贴图）、游戏画面（`Chip1` 背景 + 立绘 + `Sys_Msw` 对话框）
- 点击推进、AUTO / SKIP / LOG / CONFIG / TITLE
- 设置画面（文字速度、BGM/SE/语音音量、全屏）、画廊（立绘与背景缩略图）
- 产物：`web/index.html` + `web/app.js` + `style.css` + `gallery-data.js`（163KB 资源索引）
  + `lib/index.js|client.js`（DSH 插件壳）；面板地址 `（本地 GUI 面板）`

### 阶段 3｜音乐子项目（`music-gen/`）—— 从"你能生成歌吗"开始

用户提问"你能生成歌曲吗" → 试做 → 用户连续给出**很具体的听感反馈**，每一步都推动了工程化：

| 用户原话（要点） | 触发的改动 |
|---|---|
| "感觉电音有点强了，正常一点" | 去掉 supersaw / EDM 乒乓延迟 / 鼓组，转向原声小编制 |
| "有很多杂音" | 查出真凶是**归一化之前就 tanh 限幅**（硬削波交调失真）；噪声层（击弦/弓噪/房间底噪）全部删除 |
| "不要生成太长，现在只是测试" | 测试曲一律 16–40 小节；正式曲 60–120s |
| "这两个有很多杂音" | 同上 + 限幅/归一化顺序固定；`noise_probe.py` 成为体检工具 |
| "写个符合风格的歌" | 5 套风格预设（acoustic / daily / gorgeous / dance / ballad） |
| "不同的歌整理成文件夹" | `songs/<NN_名字>/` 一歌一目录（song.json + compose.py + MIDI/WAV/OGG + notes.md） |
| "写歌消耗 token 太多了，怎么省" | 数据驱动：新歌只写一个 `song.json`；`make_song.py` 一条命令闭环；参考曲画像缓存 |
| "怎么把方法运用到每一个要写歌的对话" | 立 `~/.dsh/AGENTS.md` 全局约定 + `bgm-studio` 技能 |
| "把发现的全部 bug 都修一下" / "再检查" ×N | 六轮系统审计（见下） |

技术骨架：**纯标准库 Python 作曲**（加法合成 + GM MIDI 写出）→ **FluidSynth + GeneralUser GS**
真音源渲染 → 母带链（高通 → 搁架 → 中频提升 → 软限幅 → RMS 归一 → 立体声加宽 → ffmpeg OGG q8）
→ **客观对标**（倍频程平衡 / 质心 / 宽度 / RMS / 16 分节奏型 / 安静段调式 / 结构曲线）→ 自动调参闭环。

### 阶段 4｜六轮审计（每轮都真的修出东西）

| 轮次 | 修出 | 代表性发现 |
|---|---|---|
| 1 | 5 | tanh 限幅顺序、噪声提亮、`bright` 只改音量、chroma 基准 A→C、轨名写在 EOT 之后 |
| 2 | 4 | CC10/CC7 缺失（宽度 0.08）、分解和弦不落根音、libsndfile Vorbis 崩溃、高潮升八度反而变轻 |
| 3 | 4 | 5 套风格里 4 套通道冲突、`dance` 预设缺 Piano 轨、`arrange_probe` 迁移后静默损坏 |
| 4 | 4 | 夹紧两层丢信息（"假装收敛"）、宽度线性拟合失准 → 反推精确校准、限幅与响度目标打架差 4.3dB |
| 5 | 3 | `analyze_chords` 签名/调用不匹配（该工具**每次调用都崩**）、画像缺 `character`/`align_bands`、预演缺"调参有效性"断言 |
| 6 | 5 | 建议与对标口径自相矛盾、成品上给"反方向"参数建议、速度靠音频测速会测错、预演小样 BPM 写死 120、**GBK 控制台崩** |
| 7 | 3 | **写歌变贵**这件事本身：`song.json` 被写成"一个数字一行"（868 行/首）、技能与 README 内容重复（两份都加载）、重写技能时**覆盖掉 frontmatter → 技能从可用列表消失** |
| 8 | 5 | **只加固、不加功能**的一轮：11 条检查对空曲目列表集体空转（假绿灯）、1 条检查只打印从不 FAIL（装饰性绿灯）、质心测量只测"低很低/高很高"（×2 也过）、人声判定只读画像里存好的字（逻辑坏了也过）、**引擎静默夹断越界音高**（写错音听感变了却无人报错） |
| 9 | 2 | **只提速、不改行为**的一轮：逐样本 DSP 63s/轮 → <2s、`detect_bpm` 自相关 19.1s → 0.05s、每轮重复的 OGG 编码；顺带确认"前台 120s 超时会把长曲渲染杀在半路"（残留 `*.raw.wav` 的症状来源） |

### 第 9 轮：渲染提速（5 分钟的歌 112.6s → 31.3s）

起因是用户问："另一个对话里 5 分钟的歌渲染要很久，**能在本地渲染吗？会省 token 吗？**"
先量再答（工具链没有 git，动脚本前先把 4 个文件备份到仓库外 `_musicgen_backup_pre_speedup\`）：

| 结论 | 依据（313.5 秒的歌实测） |
|---|---|
| 本来就是**纯本地**渲染 | `.venv` + `vendor\fluidsynth.exe` + GeneralUser GS sf2，全程本机 CPU，无任何云端调用 |
| 渲染本身**不消耗 token** | token 花在对话轮次上；渲染慢只占墙钟，还会**因为被超时杀掉而逼出额外轮次** |
| 瓶颈是**逐样本 Python 循环 + 每轮整曲重算** | FluidSynth 只占 5.6s；高架 12.3s + 三阶高通 50.6s + OGG 8.1s + 成绩单 3.7s |

改动（**不改行为，只改速度**）：

| 文件 | 改动 | 效果（313 秒的歌） |
|---|---|---|
| `render_midi.py` | 四个 EQ（高架/低架/高通/中频）从逐样本递推改成**频域实现**（H(w) 由递推系数直接写出） | 63s → **<2s** |
| `render_midi.py` | 新增 `encode_ogg(out_base)`；`render(ogg=False)` 供中间轮次跳过编码 | — |
| `make_song.py` | autotune 循环内不编码 OGG，**定稿只编一次**；去掉不再用的 `import to_ogg` | 8.1s×轮数 → 8.1s |
| `metrics.py` | `_band_env()` 批量窗 FFT 替掉逐窗列表推导；`_avg_spec`/`octave_bands`/`centroid` 共用一次频谱 | rhythm 3.4s → ~2.5s |
| `metrics.py` | `detect_bpm` 自相关只算用得到的 lag（原来 `np.correlate` 把 5.4万×5.4万 对全算） | 19.1s → **0.05s**（扒参考曲 23.3s → 1.3s） |

**等价性是量出来的，不是"看着像"**（专门写了一次性比对脚本，逐样本/全字段比）：

- DSP：旧（备份副本）vs 新，4 种信号 × 9 条滤波链 → **最大逐样本差 2e-15**（double 精度地板）；
- `metrics.profile`：3 首真实成品**全字段一致**（带 bpm / 不带 bpm 都比过）；
- `detect_bpm`：4 个用例的 bpm 与 periodicity **完全相同**；
- 端到端：新旧各跑一遍完整 `make_song`，autotune 打印**逐字相同**（第1轮 质心3139 宽度0.643、到顶 mid_db/shelf、收尾 0.404）。

| 实测（同一首歌、同一台机器、顺序跑，避免互相抢 CPU） | 之前 | 之后 |
|---|---|---|
| 单轮渲染 + 成绩单（`--no-tune`） | 101.8s | **28.7s** |
| 完整 `make_song`（autotune 1 轮收敛 + 收尾 + 成绩单） | 112.6s | **31.3s** |
| `profile_ref.py` 扒 5 分钟参考曲 | 23.3s | **1.3s** |

守卫：新增自检 `dsp_fft_equivalent`（与**独立写的**时域递推逐样本比对；长 2 万 / 短 400 **两种长度**
都要测 —— 短信号守"零填充"，长信号上看不出循环卷积绕回），并配两条注入用例
（高通忽略 order、频域滤波不做零填充）；变异测试 **53 → 55**，两条都被抓到。
顺带修掉一个**这一轮自己引入的 bug**：把编码搬进 autotune 后，两条给 `render` 打桩的检查
（`autotune_convergence` / `autotune_reports_caps`）拿不到 WAV → ffmpeg 报错；
现在它们同时给 `encode_ogg` 打桩（检查对象是调参算法，不是编码器）。

验收：`selftest` **57/57**、`mutation_check` **55/55**、`rehearsal` **全通**、等价性比对全一致。
（后来检查层又各自加了一条检查与两条注入用例，最终一起跑出 **58/58** 与 **57/57**。）
**回滚材料与验证日志**：改动前的 6 个脚本原件、等价性脚本 `equiv_check.py`、绿灯日志、
DSH 插件注册表快照（当时工具链还没有 git，只能靠手工归档回滚；现在本仓库本身就是回滚点）。

> **给另一个对话 / 接手的人**：渲染管线已提速 3.5×，**接口有两处行为变化**：
> ① `make_song` 只在**定稿**写 `.ogg`（中间轮次只有 `.wav`）；
> ② `render_midi.render()` 多了 `ogg=` 开关，并新增 `render_midi.encode_ogg(out_base)`。
> **长曲（>3 分钟）一律后台跑** —— 前台 120s 上限会把 autotune 杀在半路并留下 `*.raw.wav`。
> 另：5 分钟的歌仍在 README 记录的"未验证区间"（>3 分钟未测）；这一轮只证明它**更快、数值不变**，
> **不证明它更好听**。

### 第 8 轮：检查层加固（"改了这么多次怎么还有 bug"）

用户的质问是对的：问题不在"同一个地方反复错"，而在**bug 的产地换了**——
音频管线（第 1–4 轮，17 个）早已收敛，第 5 轮起的错都在**验证与文档层**，
而那一层没有"坏了会响"的机制，只能靠主动去戳。

这一轮不加任何功能，只做两件事：

**① 把"检查自己会不会红"从 10 条扩到 53 条注入用例**，于是立刻抓出 3 类真问题：

| 抓到的 | 性质 |
|---|---|
| **11/13 条与曲目相关的检查在空曲目列表下照样 PASS** | `for d in []` 什么都不做 → 目录名一改、glob 一坏就集体空转。现在统一走 `songs_or_fail()`（非空 + 与磁盘数量一致；注入临时目录时跳过数量核对，避免变异测试自欺） |
| `unused_chords_warn` **只打印、从不 FAIL** | 装饰性绿灯，让"55 项通过"变成假话。改成双向契约（该响要响、不该响不许响），并加静态守卫：**每条检查必须有断言**（新写的空转检查靠它拦） |
| 质心测量只测"低很低/高很高" | 把质心整体 ×2 也照样通过 → 改成对照**真值**（60/1000/12000Hz 正弦 ±20%） |
| 人声判定只读 `refs/*.json` 里**已经存好**的字段 | 分类逻辑坏了它照样绿 → 改成 ① 用合成画像直接测分类器 ② 已存画像必须与当前逻辑一致 |
| **引擎静默夹断越界音高**（`min(127, max(0, m))`） | 写错一个音（比如 200）声音变了却无人报错 → 输入数据越界在 `load()` 报错并点名；派生声部（±12 加倍/sub）越界**丢弃该声部**而不是夹成另一个音 |

**② 把三套口径合并成一套**（"同一件事写两遍，口径必然漂"）：

| 项目 | 之前 | 之后 |
|---|---|---|
| `aligned_bands()` | `make_song` 与 `scorecard` 各一份 | `metrics.aligned_bands()` 唯一来源 |
| 频段分组 | 两处 **`mid` 的含义都不同**（1250-5000 vs 315-1250） | `metrics.GROUPS` + `metrics.judge_gaps()` |
| 容差 | 调参 ±1.5、成绩单 -2/+2.5 | `metrics.TOL = 1.5`，两边共用 |

验收：`selftest` **56/56**、`mutation_check` **53/53**、`rehearsal` **全通**、
全量自检确认**已交付的 MIDI 与引擎输出仍然逐字节一致**（没有偷偷改动任何成品）。

### 第 7 轮：token 成本治理（"为什么写歌消耗的 token 变多了"）

量出来的三笔账（`scripts\token_audit.py`，CJK≈1token/字、ASCII≈1token/3.6 字符 粗估）：

| 项目 | 之前 | 之后 | 做法 |
|---|---|---|---|
| `SKILL.md`（**每个音乐任务都加载**） | 3629 tok | **1456 tok** | 去掉与 README 重复的坑清单/曲目表/参考曲画像细节，只留"会改变行为"的规矩 + 指针 |
| `README.md`（技能让 agent"先读"） | 8971 tok | 6313 tok（常规只读 1~2 节 ≈1500） | 坑清单 1–58 拆到 `PITFALLS.md`（出症状才查）+ 顶部加"读哪一节"地图 |
| `song.json`（读一遍是最普通的一步） | 05/06/09 约 1800–1978 tok（815–868 行） | **1091–1217 tok（171–187 行）** | 新增 `json_io.py`：**按小节分行**（一个音符仍是独立子串，可精准 edit）；`new_song.py` 改用它写 |
| 一个典型写歌对话 | ≈17.4k tok | **≈6.6k tok** | 上面三项 + 一开始就读地图而不是整篇读文档 |

新守卫：`song_json_canonical`（song.json 必须是规范格式且数据逐字节等价）、
`docs_budget_and_skill_intact`（技能/文档体积上限 + **SKILL.md 必须有 frontmatter**）。
新工具：`json_io.py`（规范化，可命令行跑）、`token_audit.py`（成本审计，随时回答"贵在哪"）。

---

## 二、修过的 bug 全集（按类别，含根因/修法/守卫）

> 编号沿用 `README.md` 的"踩过的坑 1–58"。**守卫**=现在由哪条自检/断言盯着，防止复发。

### A. 听感类（"杂音/电音/闷"的真实原因）—— 12 条

| # | 现象 | 根因 | 修法 | 守卫 |
|---|---|---|---|---|
| 2 | 刺耳"杂音" | 母带 `tanh` 写在归一化**之前**，混音峰值 2.4 被硬削 | 先量峰值→归一→轻限幅 | DSP 方向自检 |
| 4 | 沙沙底噪 | 用噪声层（击弦/弓噪/房间）冒充实录感 | 删掉；提亮改用搁架 + 高次分音 | 谱平坦度自检 |
| 5 | 音色不亮 | `bright` 写成 `a*bright`（等比缩放=只改音量） | 作用到高次分音 | 音色自检 |
| 14 | 低频轰鸣 | 靠低八度 sub 层找低频，只往 20–40Hz 堆 | 用贝斯音区 + 低频搁架 | 20–40Hz 只作提示 |
| 15 | 宽度不够 | 加宽放在软限幅**之前**，侧向分量被压掉 | 限幅后再加宽 | 顺序自检 |
| 29 | 宽度失控 0.997 | 反复拟合 + 高倍数区非线性 | 只拟合一次 + 上限 2.6 | 参数范围自检 |
| 38 | 宽度不准 | 前面 EQ 每轮都在变，比例系数漂移 | `set_width_exact()` 从成品反推 | 实测 0.814 vs 0.814 |
| 42 | 响度差 4.3dB | 加宽抬高峰值 → 天花板压整体增益 | 迭代提高限幅强度而不是降增益 | 响度契约自检 |
| 20 | 高频塌 26dB | `perc_style: none` 把沙锤（5–18k 唯一来源）也关了 | 用 `light + perc:1` | 配器警告 |
| 33 | 对标被低频带偏 | 各家音源/设备的 sub 差异极大 | 对标判据不含 20–40Hz | `align_bands` |
| 47 | 照人声曲调会把配器调废 | 人声混音最强段在 315–630Hz、sub 被母带切掉 | 判定 `vocal_forward`，只对齐 160–10kHz | 分类自检 + 豁免说明 |
| 57 | 尾巴 3.9s 疑似问题 | 其实是 −85dBFS 混响余韵（不可闻） | 不改；掐尾重测差 ≤0.3dB 已记录 | — |

### B. MIDI 写出类 —— 9 条

| # | 现象 | 根因 | 修法 | 守卫 |
|---|---|---|---|---|
| 6 | 音名整体偏移 | chroma 基准用了 A=440 而非 C | `12*log2(f/261.6256)` | 和弦音自检 |
| 7 | DAW 读不到轨名 | 轨名写在 End-of-Track **之后** | 统一写在 tick 0 | MIDI 往返自检 |
| 8 | 全在正中间（宽 0.08） | 没写 CC10 声像 / CC7 音量 | 逐轨写入 | 宽度自检 |
| 9 | 和声含糊、扒谱对不上 | 分解和弦从 index 1 起，根音只存在于贝斯 | 第 1 拍落根音 | 和声自检 |
| 10 | 高潮反而最轻 | 高潮段旋律升八度，GM 钢琴高音区能量少 | 不升八度 | 段落响度自检 |
| 31 | 音高悄悄错（128→0） | `write_midi` 用 `note & 0x7F` 静默回绕 | 越界直接报错 | 越界自检 |
| 34 | 4 处错音 | 手写和弦表打错（A7 最高音写成 C、B7 七音写成 A#、Dmaj7 含 9 音） | 改正 + 名字改成 maj9 | **和弦名↔音集**自检（听感只像"有点脏"） |
| 35 | 音色错乱 | 两轨共用同一 MIDI 通道，program change 互相覆盖 | 固定 `CH` 通道表 | 通道唯一性自检 |
| 36 | 旋律串到别的段 | 小节偏移越界 | 越界报错 | 段内范围自检 |

### C. 引擎/数据类 —— 10 条

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 17 | 配器整体错位 | 以为参考曲都以低频为主（BGM18b 最强段其实是 315–1250Hz） | 先读 `refs/*.json` 倍频程再定配器 |
| 19/56 | 速度判断错 | 音频测速倍频误判；连奏编配更是**测错**（106→154.3） | 速度取自 MIDI tempo（`--bpm`），测速只作兜底 |
| 21/22 | 参数撞名/管不到 | `render.json` 的 `mid` 是文件名，中频增益叫 `mid_db`；1.2–6kHz 要单独抬 | 参数改名 + `mid_db` |
| 23/26 | EQ 推爆 | 追大差距只会刺耳 | `LIMITS` 收紧；到顶改提示"该改哪一轨" |
| 27 | 中低频偏厚难修 | 一轨一轨降太慢 | `voicing_shift +12` |
| 30 | 交付物过期 | 改了引擎但没重出曲目 | 自检报"X.mid 与当前引擎输出不一致" |
| 32 | 遗留测试品混入对标 | 早期脚本式曲目不是交付物 | `render.json` 标 `legacy` |
| 37 | 4 套风格通道冲突 | 预设改了音色却撞通道（Arp 与 Pad 都占 3） | 预设只改音色，通道用固定表；逐套自检 |
| 41 | 快歌 20–40Hz 缺 20dB | `eighth` 贝斯漏了 sub 层 | 补齐 |
| 16/55 | 小节类指标全错位 | 换参考曲没改 BPM；预演小样还写死 120 | 脚手架自动填 BPM；预演用参考速度 + 断言 |

### D. 自动调参类（"假装收敛"这一族，出现 4 次）—— 6 条

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 28 | 宽度瞬间冲到 0.997 | 夹紧夹的是"变化量"而非"结果值"（width lo=0.8、hp lo=18 把小修正抬成大值） | 改成夹结果值 |
| 39 | 日志显示在调、其实没动 | 打印的是建议而非已生效的改动 | 只打印实际应用的 |
| 40 | 建议无效 | 静态文案会对已换音色的歌建议"24→25" | 按当前状态给，并带当前值 |
| 44 | +1.5~2.5dB 的过厚永不修却报达标 | "偏厚"判据用了宽阈值 `TOL+1` | 两个方向都用 `TOL` |
| 48 | 想要 −8.24dB 被夹成 0 → 谎报达标（实际差 10dB） | 夹紧做了两层，信息丢失 | 只夹一层；到顶必须报告（含"被夹成 0"） |
| 45 | 低频厚 6dB 与中频薄 4dB 互相抵消 | 建议按组平均给 | 单频段 |差|>4dB 单独提示；频段→轨道按**名字**映射 |

### E. 测量/对标类 —— 7 条

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 43 | 比一小节还短的文件直接崩 | `a cannot be empty` | 返回中性值 |
| 51 | 老画像行为退化 | 新增 `character`/`align_bands` 后旧画像没有 → 退化成"全部频段对齐" | 一条命令就地补齐 12 个画像 |
| 53 | **建议跟自己的对标口径打架** | 一边打印"已跳过 20-40"，一边建议"20-40 多 29.8dB → 提高高通"（照做会比所有真实 BGM 都薄） | `suggest()` 只在 `align_bands` 内出建议；组名如实写实际频段 |
| 54 | **成品上给出反方向建议** | 成绩单跑在成品上（EQ 已生效），却按"从零起步"给绝对值 → shelf 已到 −3 下限时建议 −2.8（更亮） | 按"当前值再走一步"算；走不动就明说"已到下限 → 靠编配" |
| 19/56 | 速度不一致误报 | 见 C 类 | MIDI tempo + `--bpm` |
| 47 | 人声曲当低频标尺 | 见 A 类 | 已豁免 |
| — | 参考曲池被预演逐个消耗完 → 永久 FAIL | 每跑一次扒一首 | 用完自动重扒旧画像的原始音频，且不写 `refs/` |

### F. 工具/流程类（静默损坏｜假检查）—— 13 条

| # | 现象 | 根因 | 修法/守卫 |
|---|---|---|---|
| 1 | 下载音源失败 | GitHub 直连不通 | 走 `ghproxy.net` 镜像；raw 可直连 |
| 3 | OGG 编码静默崩 | libsndfile Vorbis 写大 float32 | 分块 / 改走 ffmpeg |
| 11 | MIDI 被截成 0 字节 | `Select-Object -First N` 提前终止管道杀掉上游进程 | 用 `> $null` / `-Last N`；已写入技能铁律 |
| 12 | 中文脚本变乱码 | PowerShell `Get-Content/Set-Content` 按 GBK 解码再存 | 用编辑工具或显式 UTF-8 |
| 13 | 律动糊成一片 | 踩镲正拍也加满 | 节奏型留白、踩镲只放反拍 |
| 18 | 反拍驱动写成正拍长音 | 不理解节奏型记号 | sub 用 0.3 拍短音 |
| 24/25 | 用错命令 | `make_song` 默认就会调参；`new_song` 固定生成 `song.json` | 写进文档 |
| 46 | 误判"有残留" | `__pycache__` 是 Python 正常行为 | 白名单 |
| 49 | 裸 traceback | 找不到文件/格式不支持 | 友好报错 |
| 50 | **一个工具从此每次调用都崩** | 加 `--brief` 时只改调用方、没改函数签名，而之后没人用这个工具 | 自检新增"10 个分析工具逐个冒烟" |
| 52 | **调参完全失效也全绿** | 预演只检查"没崩/不削波"（踩过：对齐 19–25dB 仍 PASS） | 断言"调参确实改善"+ 打印 前→后 |
| 58 | **换个目录/换台机器就崩** | Windows 默认 GBK 控制台打印 `✓` → `UnicodeEncodeError`，崩在调参中途；本地因为都设了 UTF-8 而没暴露 | 26 个入口脚本调用 `cli_utf8.setup()`；子进程输出统一按 UTF-8 解码；自检 `console_encoding_safe`（真进程 + 反向对照 + 静态覆盖） |
| — | 变异测试自身 2 处误报 | 元组解包写错、替换了 `glob` 模块 | 修正；也说明"检查本身也会错" |

### 关于"检查本身出错"（同类踩过 3 次）

1. 第 4 轮：2 条检查项因脚本 bug **从未真正验证过**；
2. 预演缺"调参有效性"断言 → 19–25dB 的错位也全绿；
3. 第 6 轮：我新加的"速度网格"不变量一开始用**音频测速**当判据 → 在连奏编配上误报 FAIL。
   **结论写进 README：判据本身不可靠时，检查比没有检查更糟**（既掩盖真问题又制造假问题）。

---

## 三、现在的验证状态（诚实版）

| 手段 | 结果 | 说明 |
|---|---|---|
| `scripts\selftest.py` | **79/79** | 含和弦音、MIDI 往返、DSP 方向、交付物是否过期、自动调参收敛/到顶上报、编码安全、song.json 规范格式、文档预算、**每条检查必须有断言**… |
| `scripts\mutation_check.py` | **78/78** | 注入 78 种故障全部被抓到（第 8 轮从 10 条扩到 53 条，立刻抓出 3 类真空转的检查） |
| `scripts\rehearsal.py` | **全部通过** | 全新参考曲扒谱 → 5 套风格 → 5 种边界情况端到端；判据＝管线通 / **调参有效**（`tune_error` 变小或本来就够好）/ 小样速度=参考速度；**对齐 dB 不进判据**（坑 105） |
| 跨目录/跨控制台 | **已验证** | GBK 控制台 + 用户目录（路径含中文/空格）：自检全绿、完整出曲 exit 0 |
| `scripts\token_audit.py` | 常规路径 ≈6.6k tok | 每项都有预算上限，超了自检会报 |

**没验证 / 不能保证的**：好不好听（只有耳朵能判）；GM 音源自身音色；非 4/4 拍号；
**<60 或 >200 BPM**（慢端只做到"报出窗口外层并要求 --bpm"，>180 仍可能落三拍关系上，坑 104）；
只在 Windows 验证过；01/02 老曲目（已标 `legacy`）的音乐性；
`detect_bpm` 仍不可靠（扒新参考曲时要人工核对速度）；检查项本身可能有盲点。
**"零 bug"不做承诺**：六轮分别查出 5/4/4/4/3/5 个真问题。

---

## 四、产物清单

```
music-gen\
├── README.md            总索引（顶部有"读哪一节"地图，别整篇读）
├── PITFALLS.md          踩坑 1–58（出症状时按编号查）
├── HISTORY.md           本文件（经过 + bug 全集）
├── scripts\             29 个工具（作曲引擎 / 渲染管线 / 度量内核 / 扒谱 / 体检 /
│                        自检 / 预演 / 变异 / json_io 格式 / token_audit 成本审计）
├── songs\               01–09（05 起为 song.json 数据驱动；每首含 .mid / _sf.ogg / notes.md）
├── refs\                16 个参考曲画像（含 1 个人声曲 + 预演新扒的）
├── vendor\              FluidSynth 2.6.0 + GeneralUser GS v1.471.sf2
└── .venv\               numpy / soundfile / imageio-ffmpeg
```

顺带：UI 还原是另一个子项目（不在本仓库）；素材与批量提取脚本也只在本机，**不进仓库**。

## 2026-09-12 文档架构重构（用户问"SKILL 是不是不够用，能不能分离"）

**诊断**：SKILL.md 已顶死在预算上限（1700/1700，上一轮为塞进"分段拟合"不得不删内容）；
但真正的臃肿在 README（≈6968 tok）—— 它一半是"只有写歌时才需要"的内容（song.json 结构、
风格预设、画像/渲染参数），而 agent 写歌时往往整篇读它。另发现 README 里三处**指针腐烂**
（指向已搬走的"PITFALLS 附录"、"完整 58 条"过时说法）。

**拆分原则**：不是拆得越多越好，而是"**同一流程步骤能被一份文档覆盖**"：
- 操作细节 → 去工具自己的 `--help`/专门文档（不做成文档）
- 干活的步骤 → 独立文档（写歌只要 1 份）
- 导航与铁律 → 留在技能卡（它每次加载，必须小且准）

**新分工**（都用 token 预算守着）：
| 文档 | 体量 | 读者/时机 |
|---|---|---|
| `~/.dsh/skills/bgm-studio/SKILL.md` | 1636/1700 | 常驻：**路由表** + 工具链 + 4 道硬门 + 解读口径 |
| `README.md` | 5247/5600 | 总索引：目录/工具清单/验证状态/产物 |
| `docs/SONG-FORMAT.md` | 1624/2000 | **写/改歌才读**：song.json 结构、风格预设、段落曲线、画像与渲染参数 |
| `CHEATSHEET.md` | 630/1600 | 要动手/看开关时：完整命令与示例 |
| `PITFALLS.md` + `-ARCHIVE.md` | 6671/8000 | 出症状按编号查（1–40 在归档） |

**加了防腐烂守卫**（新检查必须配注入用例 → 已加第 52 条）：
- `t_skill_routes_resolve`：技能卡路由表指的每份文档必须存在（指向不存在的 → FAIL）
- `t_docs_paths` 扩到**全部文档** + 文档之间的 `.md` 指针（搬走正文没改指针 → FAIL）
- 变异验证：故意把 `PITFALLS-ARCHIVE.md` 改名 → 抓到（README/PITFALLS 两处指针都点名）

**收益**：典型写歌路径的必读量 2872 → 2254 tok（且不再需要整篇读 README）。
自检 67→68 项、变异 65→66 条，全绿。

## 2026-09-12 `20_d73_quiet_night` 交付（首个"七项标准全达标"的曲目）

**背景**：用户对 20/21 号连续反馈四轮问题（不协调/不融合/卡卡的/错位混乱），
22 号按"把每次反馈固化成量化标准"的方式重做，一次通过用户验收（"这次还行"）。

**固化下来的七项旋律标准**（全部由 `melody_gen` 内建，生成即达标）：

| 标准 | 判据 | 22 号实测 |
|---|---|---|
| 落点网格 | 奇数格 = 0 | 0 |
| 八分格对齐 | ≥95% | 100% |
| 强拍和弦贴合 | 100% | 100% |
| 半音冲突率 | <5% | 0% |
| 间隙 / 时值种类 | 0 / ≤6 | 0 / 6 |
| 拱形跨度 | +8~12 半音 | +11 |
| 最常音程 | 同音或二度 | 同音 |

**对应的坑条目**：94（主音给错）、95（音域越界）、96（调参反制）、97（spec 漂移）、
98（弱拍半音冲突）、99（时值未网格化）、100（跳进全用四五度）、101（乐句无拱形）、
102（落点撒在十六分奇数格）。

**流程**：体检 → 整曲/分段识别 → 旋律语言画像 → 写 spec（1767 字）→ build_song →
melody_gen → check_song → make_song（3 轮调参）→ **一致性核验 + 封存**。全程约 14 分钟。

**验收数据**：宽度 0.800 vs 参考 0.793；315–630Hz 差 **0.0**；RMS 差 0.4；
零削波、无 NaN；自检 73/73、变异 72/72。

## 2026-09-13 P0-1：速度自动测速不再"静默折半"（坑 104）

**缘起**：清单式排查"已知天花板"时，把 `<60 / >200 BPM 未测` 升级成 bug ——
`detect_bpm` 的候选折叠窗口写死 `[60,180]`，窗口外的速度被折上来且**不留痕迹**。

**实证**（合成信号：底鼓每拍 + 踩镲每半拍；纯等幅脉冲串**不能**用作判据 ——
所有整数倍周期等强，自相关分不出"拍"与"4 拍"）：

| 真值 | 修复前 | 修复后主读数 | `window_alt` |
|---|---|---|---|
| 45 BPM | 90.7（+100%） | 90.7 | **45.4** ✔ |
| 55 BPM | 111.1（+101%） | 111.1 | **55.5** ✔ |
| 180 BPM | 60.4（−66%） | 60.4 | **181.2** ✔ |
| 120 BPM（窗内） | 120.2 | 120.2 | 无（不该响不许响）✔ |
| 210 / 230 BPM | 70.3 / 116.1 | 同前 | **不触发**（已知边界） |

**方案**：折叠窗口与读数**一个都不动**（逐首核对 14 首参考曲：与修复前**逐个相同**，
零行为变更），只把"窗口外同样成立的层级"显式报出来（`info['window_alt']`，
八度 + 三拍关系，支持度 ≥0.85×本层），`check_audio` 列进 `levels` 并提示用 `--bpm` 核对。
> 中途试过"直接把窗口放宽到 [40,240]" —— 慢端修好了（45/55/230 全对），但**真实参考曲读数整体
> 漂移**（bgm01 107.3→210.3、BGM19 161.9→234.4…）且无从判断优劣，等于拿一套不确定性换另一套。
> 故退回零变更方案：**报出来让人钉**，而不是替人猜（与 `pick_level` 的既有立场一致）。

**守卫**：自检 `bpm_out_of_window_reported` + 变异用例"关掉 `WINDOW_ALT_RATIO` → 必须被抓"
（实测抓到：`45BPM 既没报对也没报窗口外层：bpm=90.7`）。
**已知边界（不宣称解决）**：>180BPM 自相关峰落在三拍关系上，`window_alt` 不触发 ——
那一档仍只能 `--bpm` 人工钉死。

## 2026-09-13 P0-3：预演的"对齐 dB"退出判据（坑 105）

**缘起**：诚实清单第 10 条早就写着"预演的对齐 dB 只能当参考"，但它**还在 PASS/FAIL 里** ——
`gap_after <= max(3.5, gap_before) or gap_after <= 8.0`。

**测量**（先跑一次未改动的预演拿数，10 组）：

| 场景 | 调参前→后（最大频段偏差） | 备注 |
|---|---|---|
| 五套风格 | 4.3→4.3 / 4.3→4.3 / 9.5→4.3 / 6.6→3.6 / 5.0→4.3 | 两组**一模一样**，靠 `max(3.5,…)` 的等号过关 |
| 无打击乐段落 | **18.8→15.0** | 典型"5–20dB 属正常"，旧写法靠 `<=8.0` 之外的相对项才没红 |

**根因**：`autotune` 优化的是 `target_gaps()` 的**分组**误差（low / 1.2–5k / 5–18k / 20–40），
`max_gap` 是**单个频段**的最大偏差 —— **不在它的目标里**，所以调参"有效"与否它几乎不动。

**改法**：判据换成 `tune_error`（分组误差之和）＝`err_after <= 3.0 或 err_after < err_before`；
对齐 dB 只打印并标注"仅供参考"。**注入验证**：`make_song.autotune` 换空操作 → **9 项 FAIL**
（唯一 PASS 是 `2.8→2.8`，"本来就够好"），证明这门不是装饰；正常预演全绿。
好处顺带可见：调参效果从"4.3→4.3 看不出变化"变成 **8.0→4.0 / 10.9→2.4 / 17.5→12.7**。

## 2026-09-13 P0-2：第一次把 `detect_bpm` 到底有多不准量出来（坑 106）

**缘起**：诚实清单一直写着"音频测速仍不可靠"，但**没有数字** —— 于是先量再决定改不改。

**两组带标注集**（判据：精确 = ±2%；层级 = 含 ×2/÷2/×3/÷3 后 ±5%）：

| 集合 | 精确命中 | 层级命中 |
|---|---|---|
| 本仓库自有曲目（`song.json` 的 bpm 是精确真值，音频由本管线渲染）17 首 | **7 (41%)** | **15 (88%)** |
| 外部参考曲（`refs/*.json` 里人工钉死的值，精确同名才计入）23 首 | **1 (4%)** | **3 (13%)** |

**结论**：外部素材上它**基本不可用**；自有曲目上只到"层级级别可靠"（错例大多是 ÷2 慢一倍）。
因为"改成准的"不是本轮预算内能做的（八度/三拍歧义是这个问题的固有难点），**改法改成"不让人误用"**：

1. `detect_bpm` 返回 `info['level_ladder']` —— 本层 + ×2/÷2/×3/÷3/×4/÷4 的支持度一览（`window_alt` 由它派生，语义不变）；
2. `check_audio` 把阶梯并进 `levels` 打印（`60.1BPM[60.1 30.1 120.2 180.3]`）→ 人工核对＝挑一个；
3. `metrics.profile` 写 `bpm_source: forced(--bpm) / auto(未核对)` + `bpm_ladder`；
4. `profile_ref.py` 在自动测速时打醒目提醒 + 候选表 + 重写画像的命令。

**统计口径的坑（本轮第一版踩到）**：按前缀匹配"画像名 → 音频"会把 `BGM15b/15c/15d` 全指到
`BGM15.ogg` 上，多首曲子读数必然相同 → 改成**只认精确同名**，23 首。

### 追加：>180 BPM 从"不可表示"改成"能报就报"（同坑 104）

排查 `>180 怎么修` 时发现真正的缺口不在搜索范围（lag 下界只影响候选枚举，阶梯里本来就有 209.4），
而在**报的是折上来的那个值**：44100Hz 的 210BPM 素材实测"报 69.8/支持度 1.295，而 209.4/1.553"——
最成立的层已经在手里却没报。

修法：**高于窗口上界（>180）的层级若支持度严格更高，就直接报它**。
为什么**只对 >180 生效**：先试了"谁支持度高就报谁"，14 首参考曲里 **4 首**读数被翻动
（BGM11 90→45、BGM12 168→42、BGM14 110→55、BGM16 90→45，后两个是 0.23 vs 0.22 的近平分），
而**没有证据**说明那 4 处变好了（按人工钉死值看 BGM12 反而更远）—— 按项目铁律收窄。

**验收**：210BPM 探针报 **209.4**（旧：69.8）；14 首参考曲读数**变化 0/14**；
A 组 7/17·15/17、B 组 1/23·3/23 不变；自检 79/79、变异 **78/78**。
**仍未解决**：那一层支持度没更高时（22050Hz 合成 180/210/230 → 60.4/70.3/116.1）只进备注与阶梯，
仍须人工 `--bpm`。

## 2026-09-13 拍号 A 档：3/4 与 6/8 打通作曲侧（坑 107）

**缘起**：清单纯粹写着"引擎到处假设 4/4（`bar = 4 * beat`），3/4、6/8 未支持"。分步做，
每步都拿"20 首既有曲目重新编配后 **MIDI 逐字节相同**"当回归（md5 清单，改一次比一次）。

| 步 | 做了什么 | 验证 |
|---|---|---|
| A1 | `song.json` 加 `meter`（缺省 `[4,4]`）+ `bar_beats()`；非法写法当场报错，不许静默按 4/4 | 载入既有曲目 = 4 |
| A2 | `song_engine` 全部声部按小节长度展开（arpeggio/piano/bass/ep/pad/strings/glock/arp/shimmer + 4 处 bar→时间 + `build_song` 时值 + `melody_gen` 网格） | **20/20 逐字节相同** |
| A3 | 打击乐格数 = `beats×4`（不再是写死 16）；过门/收句位置按 `B-…` 定位 | 同上 |
| A4 | 写 MIDI 拍号元事件（`bgm_synth.write_midi(meter=…)`，原先硬编码 4/4） | 3/4→(3,4)、6/8→(6,8)、4/4→(4,4) |
| A5 | **强拍口径唯一来源** `strong_beats(meter)`：4/4→[0,2]、3/4→[0]、6/8→[0,1.5]；`check_song` 严判/自动修、`selftest.melody_chord_fit` 都改用它 | 自检 80/80 |
| A6 | 新自检 `meter_34_68`：拍号读回 + 音符不越出小节 + 强拍口径随拍号；配注入用例 | 变异 **79/79**（注入"退回写死 4/4"→ 抓到 3/4 第 2 拍误报） |
| A7 | 分析侧画边界：`profile_ref.py --meter` 拒绝非 4/4、画像写 `meter_assumed`；`melody_gen` 遇非 4/4 直接拒绝；`midi_ref` 改按解析出的拍号算小节 | 全绿 |

**顺带修掉一个静默 bug（坑 107）**：`midi_probe` 的 `timesig = None` 写在**每轨循环内**，
而拍号只在第一轨 —— 于是它**从来没读出过拍号**、打印时退回硬编码 `'4/4'`；
`midi_ref` 再按 `div*4` 切小节，外部 MIDI 参考曲的小节/和弦/节奏型全错位且不报错。

**明确不做（B 档）**：分析侧（参考曲画像的小节网格、`melody_gen` 落点/拱形、结构曲线）
仍只支持 4/4 —— 收益低（现有 41 个画像全是 4/4），且要重新定义"强拍/节奏型/结构"整套判据。

## 2026-09-13 华尔兹律动：3/4 不能只是"把 4/4 的落点按拍缩放"（坑 107 的续集）

**怎么发现的**：`29_meter34_waltz` 第一版渲染出来后量了一下落点 —— 贝斯踩"第 1 拍 + 第 3 拍"、
钢琴落在反拍 0.5/1.5。那是把 4/4 的落点**按拍号机械缩放**的结果：拍号对了、小节对了、
音符一个都没越界，**但听感没有圆舞曲的推动感**（一个"技术上通过的错的音乐"）。

**修法**（全部 opt-in，4/4 输出逐字节不变 —— 回归：原 20 首重新编配后 md5 **变化 0 首**）：

| 新增 | 内容 | 实测落点 |
|---|---|---|
| `bass_style: waltz` | 根音只踩第 1 拍（"oom"）+ 第 3 拍一个轻五度 | `{0.0, 2.0}` |
| `perc_style: waltz` | 底鼓只踩第 1 拍 + 侧棒点第 2、3 拍 + 八分沙锤 | 底鼓 `{0.0}`、侧棒 `{1.0, 2.0}` |
| 钢琴/电钢的**奇数拍分支** | 和弦落在第 2、3 拍（"pah-pah"），不再用 4/4 的反拍写法 | `{1.0, 2.0}` |

**验收**：三拍起音强度 2.98 / 2.73 / 2.51；自检 **81/81**、变异 **80/80**
（新增 `waltz_groove` + 注入用例"和弦退回 4/4 反拍写法 → 必须被抓"）。

### 追加：用户听出来的"没有停顿" —— 口径最后定成"**连续太长才需要换气**"

用户问"没有停顿是正常的吗"。量了一下初版探针曲：**24 音 / 8 小节（每拍一个）、最长 2 拍、
占空比 100%、间隙 0** —— 正是自检 `melody_breathing` 描述的症状。**不是引擎的问题，
是我手写探针旋律时写满了**。重写：句尾 3 拍长音 + 第 6 小节第 2 拍留白 → 2.12 音/小节、
最长 3 拍、占空比 33%。

**然后用户又校准了判据本身**："不一定每一段都要停顿，而是**太长的话**要停顿换气"。
于是把这条检查从"过满（密度 + 无长音 + 无静音）"改写成量"**最长不间断段**"：

- 相邻音缝隙 < 0.5 拍 = 连着（连奏不算换气）；≥ 0.5 拍 = 换气
- 阈值按**秒**不按小节（慢曲 8 小节 = 30 秒、快曲 = 9 秒，按小节会随速度漂）：**> 20 秒没换气**才提示
- **短句写满不提示**（这正是用户纠正的点）

**全库量出来的结果**（这是本条的真正价值 —— 一个可排的待办清单）：

| 曲目 | 最长不间断 |
|---|---|
| 11_dn75_neon | **307 秒 / 96 小节**（整曲都没有换气） |
| 13_d75_rising | 282 秒 / 88 小节 |
| 12_d75_warm | 256 秒 / 80 小节 |
| 16_d150_bright_day | 112 秒 / 70 小节 |
| 05 / 22 / 20 | 54 / 60 / 30 秒 |
| **29_meter34_waltz（新曲）** | **9 秒** ✓ 不提示 |

共 **12 首**命中 → 旧库的旋律普遍"一口气唱到底"，与新曲的标准不一致（旧曲重写要动旋律，
所以这条**只提示不判错**，清单供人工排期）。

**同轮踩的小坑**：给这条检查加 helper 时，把 `_breath_runs` 插在了 `@check` 与
`def t_melody_breathing` **之间** → 装饰器挂到了 helper 上：检查静默消失、helper 被当检查调用。
**自检立刻报 `80/81` + `TypeError`**，一眼看穿（守卫有效）。另给这条提示类检查补了
**判据自证**（3 条 assert 证明"缝隙怎么算"）+ 注入用例"无视缝隙 → 必须被抓"，变异 **81/81**。

### 追加：全库按这条口径改了一遍（用户要求"整个库都要按照这个方式修改"）

**做法**：新增判据模块 `breath.py`（**唯一口径**，自检与修复工具共用）+ 修复工具 `fix_breathing.py`：

- 换气点落在**乐句边界**（小节号是 N 的倍数；N 由速度推：取时长 ≤ 0.9×阈值的最大 4 小节倍数）
- **只收短音符时值**，不动音高/落点/和弦 → 强拍贴合、节奏型、旋律轮廓全不变
- 有 `spec.json` 的曲目同步把该音写成 4 元显式时值（3 元音的推导时值只看下一个落点，收短一个音不影响别的）
- 同一处有多个可选方案时取**改动最小**的那个（第一版会砍出 `4.00→0.50`，太狠）

**结果**：**12 首 / 101 处**改动，全库达标 —— 最长不间断从 **307 / 282 / 256 / 237 / 211 / 112 秒**
降到阈值以内；二次运行 **0 处**（幂等）；全库强拍贴合 **1827/1827 = 100%**（落点一个没动）。
12 首全部重作曲 + 重渲染。

| 曲目 | 改前最长 | 换气点 |
|---|---|---|
| 11_dn75_neon | 307 秒 | 19 处 |
| 13_d75_rising | 282 秒 | 21 处 |
| 12_d75_warm | 256 秒 | 19 处 |
| 17_b73_slow_evening | 237 秒 | 9 处 |
| 22_g68_mellow | 211 秒 | 14 处 |
| 16_d150_bright_day | 112 秒 | 4 处 |
| 05 / 10 / 19 / 20 / 21 / 28 | 25–54 秒 | 各 1–5 处 |

**守卫**：新工具按约定配了 ① README 工具清单一行 ② 自检 `breath_fix_works`（合成 40 小节连奏 →
必须被判需要换气 → 修完达标 → **落点音高一个不许动** → 再跑幂等）③ 注入用例
"把 `BREATH_SEC` 抬到天上 → 工具不出手 → 必须被抓"。
（`long_runs` 的默认值特意改成**调用时取常量**，否则注入改不动阈值 —— "默认值 def 时捕获"
会让变异测试静默失效。）

## 2026-09-13 新曲 `30_d150_sunbeam`（欢快·快·1 分钟）+ 一个"死配置"的发现

**需求**：欢快的歌、速度快、约 1 分钟。
**选型**：参考 **BGM16c**（150BPM、质心 3250 亮）、`style: acoustic`（区别于已用 BGM16c 的
16/23/25 那三首 daily）、结构 Intro4+A8+B8+**C12**+Outro4 = 36 小节 = 谱面 57.6s / 成品 62.4s。

**结果**：D 大调、强拍贴合 **63/63 = 100%**、最长不间断 < 20 秒（新换气标准）、
自动调参 **1 轮收敛**；宽度 **0.510 = 参考**、质心 3152（参考 3250）、各频段 ≤2.7dB。

**途中发现的真问题（坑 108）**：给这首加"高频连续性"垫层时发现 **`perc_layers` 只在
`perc_style: pump` 分支里生效** —— 而 **16/23 号**声明的 `perc_layers.air` 用的却是 `light`，
**那一层从来没响过**（所有检查都是绿灯）。修了引擎（垫层接进 `light`）+ 加了两道声明校验告警
（不支持的样式下声明 → 报"不生效"；air 时值 ≥ 重复间隔 → 报"同音高重叠"，那是坑 82 的悬空 voice 源头）。

**但"激活"并没有变好**：16/23 激活后 10–18kHz **+4.1/+4.6dB 过亮**、质心各涨 200+；
新曲 30 用同样方案（两条开镲交替铺十六分）也 10–18kHz **+4.0dB**、质心 3240→3666。
按项目规则"**没有证据变好就不收**"→ 三个都**撤回**：16/23 回滚重渲染并把**死配置删掉**
（数据要与声音一致），30 也撤掉垫层。
**留下的**是引擎能力和两道告警 —— 下次要用垫层，得先按 `bands_abs.py` 的**绝对口径 + 占用率**
量一轮再决定（这次只看了成绩单的相对那两行，不够）。

## 2026-09-13 "怎么这么多歌的主旋律都是一样的"（用户听感 → 三层量化）

**用户的问题**：听下来觉得"很多歌的主旋律是一样的"。**分三层量，结论和直觉不一样**：

| 层 | 量法 | 结果 |
|---|---|---|
| ① 是不是同一支旋律 | 连续 8 个音的**音程+时值形状**（转调/变速不变）跨曲比对 | **1%**（14/2611 个窗口）→ **不是照抄** |
| ② 有没有句子级重复 | 找出共享窗口落在哪 | **12/13 是唯一一对**：第 40–50 拍共用 `75 79 82 79 82 74 77 82`（各 16 个窗口） |
| ③ 那"像"在哪 | 落点/时值/音程**分布**两两重合度 | **落点 93–99%、时值 87–98%** —— 25/26 综合 93%、25/27 92%、24/25 91%… |

**根因（可量化）**：**10 首歌只用了 2 份旋律画像** ——
`BGM33` 画像养了 11/12/13/14/15（5 首）、`BGM16c` 画像养了 16/23/25/27/30（5 首），
而 `melody_gen` 的落点/时值就是**从画像的直方图抽样**的 → 同一个画像出的歌，
节奏语言天然 90%+ 重合；再加上固定纪律（强拍和弦音、级进为主、八分网格、拱形雕刻），
音高不同但"说话方式"一样。**听感上的"一样"来自语言，不是来自旋律本身。**

**已做**：
1. 改写 13 号那句重复乐句（强拍仍是弦内音，98%）→ 共享窗口 **16→5**、全库 **1%→0.5%**，13 号重渲染；
2. 新增自检 `melody_distinct`：窗口形状跨曲共享率 ≤5%，**判据自证**（整体移调=同形、换节奏=不同形）
   + 注入用例"阈值改坏 → 必须被抓"。

**没做（治本，成本高）**：给 `melody_gen` 加多样性 —— ① 让每首用**不同画像**（现在 5 首共用一份）；
② 生成时对"与库里已有旋律的相似度"做去重筛选；③ 画像补几个个性维度（切分比例/跳进率/
乐句长度/拱形幅度）按曲随机取。这条要重写 10 首歌的旋律 + 重渲染，收益是"每首有自己的说话方式"。

## 2026-09-13 治本「每首歌要有自己的说话方式」（用户：**做**，模板目录已给）

**用户提供的素材**：`仰望夜空星辰 FINE DAYS\Bgm`（69 首）、`なついろレシピ …\mp3`（23 首）、
`ピュアソングガーデン！解包\Bgm`（50 首）→ 可作画像模板。

### 第一步：先别动手改，先量"原方案值多少" —— 结论颠覆了方案

原方案①是"每首换不同画像"。**先做对照实验**（同一首曲子的和弦/结构不动，分别用 **12 份画像**
生成，同 seed），两个数字把方案否了：

| 维度 | 生成结果保住画像方言的比例 | 换画像后结果之间仍重合 |
|---|---|---|
| 句长（呼吸） | **2%** | **100%** |
| 落点（律动） | 42% | 88% |
| 音程（走向） | 63% | 90% |
| 时值（长短） | 60% | 80% |
| 合计 | **42%** | **83%** |

即：**换画像只值 42%，剩下 58% 由生成器的固定纪律决定**。逐条对得上代码：
句长 2% —— 生成器**从不产生休止**（末段把音间空隙全部归零）；落点 42% —— 只用 `onbeat_pct`
做**二值**判正/反拍；音程 63% —— 级进率用 `max(0.62, stepwise_pct)` 全局下限、且**从不产生同音
重复**（而参考曲 BGM33 画像里 `iv=0` 占 **35%**）；时值 60% —— 吸附到全局固定 `GRID`。
→ **先修生成器，再谈换画像**（坑 109）。

### 工具改造（`melody_gen.py` 重写，纪律一条没删）

保留：强拍吸附和弦音、弱拍半音（♭9/♮7）回避、音域折返、只支持 4/4。
把"说话方式"还给画像：**句长**按 `phrase_bars` 切乐句 + **句末留休止**（呼吸）；
**落点**按 `onset16_hist` 的 16 格方言抽样；**时值**按 `dur16_hist`；**音程**按 `interval_hist`
（同音/级进/跳进的比例与方向都听画像）；**拱形**每句给起伏目标；外加**个性四维**
（切分/跳进/句长/拱形，每首随机取一组）+ `--avoid`/`--candidates` **候选去重筛选**
（与库里已有旋律算形状共享率 + 语言重合度，取最不像的）。

改造后的同一实验：承接度 **42%→63%**（句长 **2%→58%**、落点 42%→72%、音程 63%→72%），
换画像后结果之间的重合 **83%→69%**（落点维度 88%→51%）。

### 途中踩的坑（都写进台账了）

密度先崩过一次：照抄画像的 `phrase_bars`（中位数 0.5 小节是**扒谱碎片**）→ 16 小节只出 29 个音；
改为"密度服从曲目（沿用原旋律）、画像只给相对形状"。另外四坑：`interval_hist` 的 ±8 以上是
八度误判（照抄出 20 处大跳）、`iv=0` 被 F0 连续帧放大（照抄 64% 同音重复）、
`_top_hist` 的 80% 截断把**第 1/3 拍落点当长尾滤掉**（13 号正拍 0%）、
**调式写死自然小调**（D 大调的 23 号 65% 音被"修正"成同音）—— 见坑 110–112。
最后两个是"结构级"的：段落**按名复用旋律**时逐段生成会互相覆盖（11 号留 1 处强拍错音，坑 113）；
强拍**样本太少**会让自检空转（30 号 36 小节只剩 3 个强拍音）→ 加"每 2 小节至少一个强拍落点"的保底。

### 十首重写（每首一份**不同**画像）

| 曲目 | 画像 | 曲目 | 画像 |
|---|---|---|---|
| 11_dn75_neon | `BGM33_mix`（原） | 16_d150_bright_day | `bgm16c_new_other`（原） |
| 12_d75_warm | `hitorigohan2_raw` | 23_d150_skip_along | `psg_BGM16b`（新扒） |
| 13_d75_rising | `fine_BGM11`（新扒） | 25_d150_joy | `shimijimi_new_other` |
| 14_d75_pulse | `fine_BGM16`（新扒） | 27_d150_revisit | `psg_BGM12`（新扒） |
| 15_d75_dark_drive | `psg_BGM10`（新扒） | 30_d150_sunbeam | `hitorigohan_new_F` |

新扒 10 份画像（`melody_profile.py` 直扒混音，3–5 秒/首；先看"调内率"自检，低于 80% 的
按它建议的主音重扒一遍）。十首**强拍和弦贴合全部 100%**，复用段冲突 0。

### 结果

| 指标 | 治本前 | 治本后 |
|---|---|---|
| 全库孪生对（语言重合 ≥85%） | **5 对**（12~13 87%、25~26 87%、23~25 85%…） | **0 对** |
| 最像的一对 | 87% | 79%（19~20，不在重写名单内） |
| 落点维度两两平均 | 58% | **41%** |
| 跨曲共享旋律窗口（形状级） | 0.5% | **0.0%** |
| 落点格数（每首） | 手写多为 2–4 格 | 9–15 格 |

十首重渲染后曲目指标仍在参考区间（宽度/质心压在参考上、一轮收敛）。
**验收工具**：把分布级体检收编为 `probe_melody_lang.py` + 自检 `melody_lang_diverse`
（孪生对 ≤2，判据自证 + 两道注入用例）。**还没验证的**：听感 —— 客观指标只能证明"十首
不再说同一种话"，好不好听得由人听（尤其是 14 号 18% 正拍的切分向、13 号 76% 正拍的方整向）。

## 2026-09-13 「为什么总有这种 bug」——把三类结构性问题变成守卫

**用户的问题**：一天内同一个生成器上连出五次同类 bug（属七和弦判错调式 / `_top_hist` 滤掉正拍格 /
强拍保底缺失 / `dur_cap` 砍长音 / 出口裁剪），问"能不能预防"。

**共性诊断**：这五次的共同结构是 **"改错了没有任何东西会喊"**。工具链的 84 项自检全在管
频段/响度/结构，而 **"旋律统计是否偏离画像"这一层一项守卫都没有** —— 所以碎音 11%、
长音 0%、正拍被滤光，频谱照样全绿。加上两条工程习惯没做到：**验证看的是内存不是落盘**、
**改动无效时继续调参数而不是怀疑"起作用的是别的代码"**（坑 114 那次白查三轮）。

**三层防护（本轮全部落地）**：
1. **出口不变量**（`gen_section` 末尾）：相邻落点 ≥0.5 拍、音不许重叠 → 破了当场 assert；
   规矩写进注释：**一个约束只在一处生效**，出口只做一件事。
2. **落盘重读复核**（`melody_gen.main`）：写盘后读回来逐音比对，不一致直接报错并指向"出口裁剪"。
3. **统计层守卫 `melody_matches_profile`**（自检 84→85 项）+ `melody_gen` 元数据
   （`song.json` 记 `profile/seed/dens/candidates`，让"这首该像哪份画像"变成可查事实，
   不再靠人翻 notes）。判据：落点/时值两维承接度 ≥55%，判据自证 + 变异用例。

**真注入回归（关键一步，不然等于没加）**：把出口那句 `max(0.25, e[2])` 改回
`min(e[2], SPB - e[1])` 复现坑 114 → 重新生成 → **守卫实测 FAIL**
（"落点 53%/时值 66% —— 先查 melody_gen 的出口裁剪/落点过滤"）。恢复后重新生成 + 重渲染，
自检回到 85/85。

**没做的**：11–30 那十首重写过的旋律是**旧参数**产物（同样有"长音被砍"），它们没有
`melody_gen` 元数据、不在新守卫覆盖内 —— 要纳入就得按新口径重跑那批旋律并重渲染（15–25 分钟）。

## 2026-09-13 模板库 2 号：网络多风格 MIDI

**需求**：用户要求"不要参考库的音乐 mid，在网络上找很多不同风格的 mid 做模板，重新生成模板库 2 号"。

**做法**：新增 `scripts/fetch_midi_lib.py`，三个公开来源（均无需认证）：
**BitMidi** 搜索 API（按风格关键词，JSON）/ **VGMusic** 控制台厂商-平台目录页（游戏音乐跨年代）/
**Mutopia** 查询页（公有领域古典）。功能：18 风格抓取、断点续传、md5 去重、
`MThd` 头校验、单文件 900KB 上限、**解析失败的隔离到 `_broken/`**
（实测抓到一个"头部声明 9 轨、后面没有 MTrk"的残缺下载 —— 留在库里会让索引与磁盘对不上）。

**产物** `refs/midi2/`：**223 首 / 6.9MB / 18 风格**；BPM 跨 **10–291**、
拍号 4/4(165)·3/4(26)·2/4(8)·6/8(6)·7/4(4)·其它、音轨 1–8、总音符 **80 万**；
`_index.json`（与 1 号库同格式，加 style/md5/bytes）、`_sources.json`（逐首来源 URL，可溯源重建）。

**版权处理**：`.mid` **不进仓库**（新增 `.gitignore` 规则 `refs/midi2/**/*.mid`，与 1 号库
`refs/midi/**/*.mid` 同规矩）—— BitMidi/VGMusic 上是粉丝制作的游戏/流行曲，公开再分发不合适；
仓库里只放**索引 + 来源表 + 重建脚本**，clone 后跑一次 `fetch_midi_lib.py` 即可复原整套。

**守卫**：自检新增 `midi_lib_index_sync`（86 项）：① 索引里的文件都真实存在
② 磁盘上的 .mid 都在索引里 ③ bpm/小节/音域数值合理（`_` 开头目录按内部目录跳过）；
配变异用例"模板库目录被清空 → 必须报空转"。

## 2026-09-14 主题模板包：一次生成依据"很多同主题模板"（用户口径）

**需求**（用户原话）：*"一个音乐生成要依据很多不同的相同主题的模板，模板只能是
`refs\midi2\` 里的或网络上的权威数据"*。澄清后的三条：① 主题口径 = **主题词 → 风格集合 → 模板**
② 多模板用法 = **聚合成一个"主题模板包"画像，生成时整体依据它** ③ 来源 = **只用 refs/midi2 现有
225 首，同主题 ≥8 首，不足才联网抓**。

**新增 `scripts/theme_pack.py`**：主题词表 15 个（日常/欢快/温柔/夜晚/海边/悲伤/战斗/神秘/华丽/
霓虹/复古/酒馆/民谣/古典/三拍圆舞），每个映射 3~5 个 midi2 风格目录 + 一个引擎预设。
选模板 = 风格配额（每风格至少 1 首）+ 风格内**均匀间隔**取（首尾都取到，覆盖速度带），确定性可复现。
逐首分析（音符层，精确）：调式（Krumhansl + **低音线加权 3 倍**，否则关系大小调互串）、
和声（`midi_ref` 逐小节和弦 → 罗马级数）、节奏（每小节 16 分格占用率 + 按轨角色的密度）、
配器（GM 音色族 → 轨角色 + 音域）、旋律语言（`melody_profile.stats` 同口径 + **最高声部单音化**）。
聚合产物：`refs/themes/<主题>.json`（含**模板清单 + 逐首来源 URL**）+ `<主题>_melody.json`（喂 melody_gen）。

**生成路径**（`new_song.py --theme <主题>`）：模板包 → song.json 的 bpm/meter/style/段落/和弦/
编制（`arr_on`/`arr_off`/`arr_maybe` 三档；**perc 永远 =1**，按技能"perc=none 会让 5–18kHz 塌掉"）
→ 跑 melody_gen（画像 = 主题旋律画像，密度取 MIDI 精确值、不再乘 F0 补偿系数）→
把"依据了哪几首模板"写进 song.json 的 `theme` 字段。老 `--from <现成曲目>` 仍可用，但会写
`basis.kind=copied_song` 留痕，`check_song` 判为"依据不合规"（用户口径：不许拿自己做的曲子当模板）。

**守卫**（自检 86 → 89 项，判据与生成共用 `theme_pack.validate_pack`）：
`theme_pack_valid`（模板数 ≥8 / 不重复 / 在 `refs/midi2/_index.json` 里且 md5 对得上 /
风格属于该主题 / 来源 URL 在权威白名单 / 画像字段齐全）、`theme_basis_whitelist`（逐首核对
song.json 声明的模板名单与包一致、数量一致）。变异用例 5 条（白名单放宽 / 索引清空 / 主题表清空 /
包路径改坏 / 歌曲声明的主题认不出）全部实测被抓。

**踩到的坑（写进 PITFALLS 118）**：拍号混进 4/4 统计（共识节奏型算成 0.1 音/小节）、
密度拿 `rhythm_low/high`（全曲合并）当分子（密度 0.1、共识型全 ★）、和弦窗口被 `I6→Imaj7`
占满、`validate_pack` 先查索引把来源检查 `continue` 掉（判据自证当场抓到）、
主题旋律画像写在 `refs/themes/` 而 `probe_melody_health`/`melody_matches_profile` 硬拼
`refs/melody/` → **两条守卫静默跳过**（已统一走 `melody_profile.find_profile`）。

**端到端实测**：15 个主题包全部建成（各 10 首模板、25 万字节、来源可溯）；
`songs/35_seaside_probe`（seaside）：`check_song` 数据契约通过、melody_gen 承接度
落点 90%/时值 85%、`melody_health` 0 问题、渲染+自动调参出成品（响度 −14.7 / 宽度 0.316 与画像一致）。
**未做**：主题包的"频谱目标"仍借用 `refs/*.json` 音频画像（MIDI 渲染不能当混音目标）——
`new_song` 会打印"速度最接近本主题的画像"供选，但不自动改。

## 2026-09-14 三件收尾：主题包的「混音目标」层 + 响度口径 + 变异夹具

**① 主题模板包补上「混音目标」层**（用户要求：解决"包里没有混音目标"）。
约束：**不许拿 MIDI 渲染当频谱目标**（技能定死：MIDI 渲染 ≠ 真实录音）。做法：从项目**真实音频
画像** `refs/*.json` 里按主题特征挑一首，连同**判据与候选**一起写进包（`mix_target`）：
速度 0.55（差 30BPM 得 0）/ 打击感 0.30（主题 `perc_style` ↔ 画像"高频段电平 + 高频起音占比"）/
调式 0.15；**人声主导画像直接排除**（`character != instrumental`，人声母带特征不是我们的目标）。
15 个主题各自选到目标（dance→BGM15c、orchestral→BGM04、light→bgm01c/BGM16b、waltz→amakute），
`new_song --theme` 默认用它填 `render.json` 的 ref（`--ref` 可覆盖），并把 ref 写进 song.json 的
`theme.mix_target` 留痕；`validate_pack` 校验它仍在 refs/ 里、非人声、速度不差 30BPM。
⚠ 明确写进文档与包：**混音目标不是模板依据**（模板只来自 refs/midi2/权威网络数据），二者分工不同。

**② 响度"量错了量"（真 bug）**：`render_midi.render` 的响度归一化用**双声道** RMS，而
metrics/scorecard/自检量的是**单声道(mid)** RMS（`metrics.load` 取 `x.mean(axis=1)`）——
加宽把 side 放大 → 双声道虚高 → 成品单声道响度差 **−2.09dB（宽度 2.2）/ −0.25dB（宽度 1.0）**，
于是自动调参每轮重设同一个 rms、永远追不上，**6 轮渲染白烧 ≈150 秒**（日志里"调 响度差 +1.3"
重复 5 次）。修：归一化改用 mono RMS（与验收同量）→ 宽度 1.0 直接命中 −16.90。
另外给 `render_midi.LAST`（侧信道，不改返回值）记「峰值是否受限 + 短多少」，
自动调参照此**冻结 rms 并报"响度到顶"**；其他参数加「**没进展也冻结**」（误差 <0.1dB 停手）。

**③ `autotune_idempotent` 判据改口径**：夹具的 1.2–5kHz 比参考薄 9.7dB，而 `mid_db` 上限 10 是
**故意保守**的（顶到 9.84 再推反而更差 → 机制冻结它）。原来的"残差一律 ≤2.5dB"等于逼实现假装收敛。
现在：**收敛，或"到顶/已冻结"里点名**（并断言"第一轮必须真调过"，防"一律冻结"的假通过）。该项转绿。

**④ `mutation_check` 4 条"漏了"→ 96/96**：不是检查坏了，而是仓库"只留 MIDI"后
`songs/` 里没有音频 → `audio_health`/`audio_semantics`/`alignment_vs_refs` 遍历空集合、
**整条静默跳过**。修法：`audio_fixture()` 现场造 16 小节小曲 + 真渲染 `_sf.wav` + render.json，
变异用例叠上故障注入。夹具长度是坑：1 小节（2s）配"+8s 混响尾"窗口漏判 → 16 小节（32s）才咬得住。
另加一条"主题包混音目标指向不存在的画像"（临时改写包文件注入）—— 守卫实测抓到。

**验证**：`selftest` 89/89（含新增/改口径的检查）、`mutation_check` **96/96**、
`theme_pack --selftest` 33/33、`check_song --all` 27 首数据契约全过、`rehearsal` 全部通过；
`35_seaside_probe` 重出（模板依据 seaside + 混音目标 bgm01c）端到端通过，
**响度 −14.3 = 参考 −14.3（差 0.0）**。

## 2026-09-14 第二批：混音目标补齐（段间曲线）+ 生成期自检 + 文档重排

**① 混音目标补上"段间能量曲线"**（承接上批的混音目标层）。依据：技能里定死
**"像不像"主要来自段间对比**，全曲一条直线 = 亮却闷。数据源 = 目标画像的 `structure`
（`metrics.structure`：**每 8 小节一块**的响度）→ 折成"相对均值"的 dB 起伏（均值为 0 →
只改段间对比、不改整体响度）→ 生成时按相对位置映射到我们的段（段长默认也是 8 小节），
换算成 CC7 用**线性幅度**关系（`new = cur × 10^(ΔdB/20)`，不是"加几个数"），夹 ±4dB、
|Δ|<0.5dB 不写、目标本身平坦就整段不写。15 个主题全部拿到曲线（如 bgm01c
`[-2.6, 0.4 ×7, -0.6, 0.4]`、BGM15c `[-2.4, 0.6, 2.6, -0.4, -3.4, 1.6, 0.6, 0.6]`），
实际用的曲线记进 `song.json` 的 `theme.energy_curve_db`。

**A/B 实测**（同 seed，只差曲线，同一个 `metrics.structure` 口径）：
`battle`/BGM15c：目标 6.0dB → **+曲线 8.0dB vs −曲线 1.0dB**，最差频段偏差 3.1 vs 3.2dB（没变差）、
响度 −19.3 = 参考；`seaside`/bgm01c：目标 3.0dB → 本曲 2.0dB、响度 −14.3 = 参考。

**② 生成期干跑（踩了坑 92 的第二次）**：段间曲线第一版把 `arr.mix` 写成 `[pan, level]`，
数据检查全绿、`make_song` 到 `write_midi` 才 `int(list)` 崩。根因是**生成路径没有干跑**
（`check_song` 的 `compose_dry_run` 只在渲染前跑）。修：`new_song --theme` 生成后立刻
`dry_compose()`（写 MIDI 到临时目录，<1 秒），跑不通就非零退出。

**③ 旋律生成后当场体检 + 退密度**：`battle` 的模板是十六分跑句，主题画像 2.58 音/小节 →
生成出来**碎音 37%**（连画像宽容档 30% 都过不去）。修：按 1.0→0.75→0.6→0.5 密度阶梯重试，
取第一个形态无问题的（实测 2.58→1.94 通过）；全不行就如实报剩余问题、**不许当"已修好"**。

**④ 响度口径标记**：`render_midi.NORM = 'mono'` 写进 render.json（`norm` 字段）。
旧配置（无标记 = 双声道时代）重渲染会变响（更贴近参考，最多 ~2.1dB）→ `make_song` 会提示
"别用 `--no-tune` 做 A/B，让自动调参重新收敛"。**实测老曲 34 号副本**：告警如期出现，
自动调参 1 轮收敛，**响度 −18.6 = 参考 −18.6（差 0.0）**、宽度精确对齐、rc=0。
自检 `render_json_schema` 校验 `norm` 值（写错 = 提示失灵）。

**⑤ 文档重排 + 预算上调**（用户明确授权"预算实在不够可以加"）：
新增 **`docs/THEME-PACK.md`**（主题→模板→包→混音目标→段间曲线的完整口径、判据表、守卫表、
A/B 实测、边界），SKILL 路由表加一行指向它、README 地图把"写新歌"指向它、
`token_audit` 的 DOCS/LIMITS 加一项、`t_docs_paths` 两处列表加上它；
预算按条上调并**逐行写明理由**：SKILL 1700→1800、README 6440→6600、SONG-FORMAT 2000→2100、
CHEATSHEET 1600→1700、THEME-PACK 新增 3000。PITFALLS 超预算仍按约定**拆归档**（81–88 进 ARCHIVE）。

**验证**：`selftest` 89/89、`mutation_check` 96/96、`theme_pack --selftest` 33/33、
`check_song --all` 数据契约全过、`rehearsal` 全通过、token_audit 无超预算项；
`35_seaside_probe` 用全部新层重出：3 轮收敛 ✓ 达标、响度 −14.3 = 参考、宽度 0.316 = 参考。

## 2026-09-14 第三批：段间曲线的**系数标定**（自选 6 个主题实测）

**用户要求**："你自选测主题加系数"。选了 6 个主题（battle/seaside/night/gorgeous/neon/daily，
覆盖 dance/light/orchestral 三类 + 大/中/小曲线），每个主题同一 seed 出两版（k=0 不写曲线、
k=1 照抄目标起伏），用**同一口径**（`metrics.structure`，每 8 小节一块）量成品起伏。

**标定过程里挖出两个真 bug（都不是"系数"问题）**：
1. **CC7 不是线性幅度**：第一版按 `cur × 10^(Δ/20)` 换算 → battle 目标 6dB、成品 **12dB**（整一倍）。
   实测 FluidSynth + GeneralUser GS 的 CC7 是 **GM 凹曲线**：`dB(cc, cc₀) ≈ 38·log₁₀(cc/cc₀)`
   （幅度 ∝ cc^1.9）。测法本身也踩了坑：**必须把 CC7=100 的参考段放进同一个渲染**
   （渲染器把整首归一到同一响度，跨渲染比绝对电平会测出 `74→+1.16dB` 这种鬼值）。
   修：`new_song.cc7_at()` 走实测曲线（表见常量注释）。
2. **CC7 是持续状态，漏写的段落会继承上一段**：我为了省字节把 `|Δ|<0.5dB` 的段跳过不写 mix →
   只有第 1 段有偏移的主题（seaside/daily）让**第 2~8 段全继承了那个偏移**，整首被压低、
   段间对比**归零**（表现为"目标 3dB、成品 2dB、形状相关 −0.25"，看着像映射不准，其实压根没生效）。
   修：**只要决定写曲线，就每段都写**（偏移 0 的段写回预设电平）。
3. 顺带修：平坦目标的 `structure` 被门槛连坐丢掉（诊断里全变 0）→ 现在**原始 structure 一律保留**。

**标定结果（修完上面两条之后）**：

| 主题 | 目标起伏 | k=0 | k=1 | k* | 形状相关 |
|---|---|---|---|---|---|
| battle | 6.0dB | 0.0 | 6.0 | **1.00** | **1.00** |
| neon | 6.0dB | 0.0 | 6.0 | **1.00** | **1.00** |
| seaside | 3.0dB | 0.0 | 3.0 | **1.00** | 0.77 |
| daily | 3.0dB | 0.0 | 4.0 | 0.75 | 0.94 |
| night | 3.0dB | 2.0 | 2.0 | —（编配自带已到量级） | 0.32 |
| gorgeous | 1.0dB | 1.0 | 2.0 | 0.0（目标太平） | 0.71 |

→ **中位 k\* = 1.0：系数最终就是 1.0**（先前那版 0.6 是在补偿 CC7 口径错 —— 口径修对后不该留假阻尼）。
另加一条判据：**目标起伏 <1.5dB 的主题，包里直接不给曲线**（gorgeous/BGM04 这类：
我们编配自带走 1dB 起伏，再叠加就过冲到 2dB —— "目标本来就没对比"就不该硬造）。

**开满自动调参的复核**（生产设置，非 --no-tune）：
`battle`/BGM15c 目标 6.0 → 成品 **6.0dB**，blocks `[-22,-19,-17,-20,-23,-18,-19,-19]`
**与目标画像 structure 逐块相同**；`seaside`/bgm01c 目标 3.0 → 成品 4.0dB（形状吻合，只有一块差 1dB）。
两首响度都 = 参考（−19.3 / −14.3），最差频段偏差未变差（4.2 / 6.8dB，后者是既有的编配差距）。

**验证**：selftest 89/89、mutation_check 96/96、rehearsal 全通过、theme_pack --selftest 33/33、
check_song --all 全过、token_audit 无超预算。

## 2026-09-14 第四批：标定做扎实（15 主题 + 多 seed）——**结论是"不需要逐主题系数"**

**做法**：把上一轮的 6 主题 / 单 seed 标定扩到 **14 个主题**（waltz 因 3/4 需手写旋律不参与），
并把测量口径从 `metrics.structure`（**整数** dB）换成自算的 `win_db`（每 8 小节**浮点** dB，
与画像 structure 同口径但不取整）—— 整数化会把 1dB 的差异吃掉 20% 分辨率。

**第一轮（14 主题 × 单 seed=5）**：k=0（编配自带起伏）0.30~1.68dB、k=1 后 0.52~6.25dB，
`k* = (目标 − r0)/(r1 − r0)` 落在 **0.61~1.07**，形状相关 0.66~1.00，最差频段变化 ≤0.5dB
（**曲线没弄坏整体对齐**）。看着像"该按主题给不同系数" → 于是把逐主题 `energy_gain` 写进包。

**第二轮（多 seed 决策 A/B，把上一轮推翻了）**：4 主题 × 2 seed × 两种系数，
比 |成品起伏 − 目标起伏|：

| 主题 | seed 5 / 11（逐主题 vs 全局 1.0） |
|---|---|
| mystery | 0.84 vs **0.14** ／ **0.13** vs 0.85 ← **好坏完全翻转** |
| daily | 0.69 vs 0.08 ／ 0.83 vs 0.22 |
| lounge | 0.25 vs 0.35 ／ 0.33 vs 0.28 |
| battle | 0.32 vs 0.03 ／ 0.57 vs 0.28 |
| **平均** | 逐主题 **0.50dB** ／ 全局 **0.28dB** |

→ **逐主题系数是在拟合单次生成的随机性**（生成带 `--avoid songs` 去重、旋律候选会随曲库微变，
段间起伏本身有 ±1dB 波动）。决定：**包里不写 `energy_gain`，回到全局 `ENERGY_GAIN = 1.0`**。
`--calibrate` 保留但改成**多 seed 测量 + 默认只写证据**（各 seed 的 r0/r1/k*/相关/对齐 + verdict），
要写系数得显式 `--set-gain`。

**顺带修掉一个真 bug（声音塌了但检查全绿）**：`classic` 主题的模板是钢琴/古钢琴，**没有独立贝斯轨**
→ 我的配器规则"角色占比 0% → 关"把 `bass` 关了 → 成品 **40–80Hz −33dB**（参考 0dB）、只剩中频。
"没有贝斯轨"≠"没有低音"（低音区有 8.8 起音/小节，在左手）。修：`FOUNDATION=('bass','piano')`
不按角色缺失关 + `arr['bass']` 恒为 True + `validate_pack` 守这条（配变异用例）。
修完：40–80Hz **−33 → −2.6dB**，最差频段 33 → 8.6dB。

**另一处小事故**：新写的 `_corr(a,b)` 与 `_key_of` 用的 `_corr(v,prof,tonic)` 同名 → 静默覆盖，
`theme_pack --selftest` 当场 TypeError（改名 `_corr_seq`）。教训写进 PITFALLS 124 尾部。

**验证**：selftest 89/89、mutation_check 97/97（新增"bass 被按角色缺失关掉"用例）、
rehearsal 全通过、theme_pack --selftest 33/33、check_song --all 全过、token_audit 无超预算。
演示曲 35_seaside_probe 用最终设置重出：3 轮 ✓ 达标、响度 −14.3=参考、宽度 0.316=参考、
段间起伏 4.0dB（目标 bgm01c 3.0dB，同口径）。

## 2026-09-14 第五批：把"C 盘变小"的漏点堵上（工具链在漏临时文件）

**用户报**："怎么我的 C 盘空间变这么少了"。量出来：C 盘 300GB 只剩 38GB，其中
`%TEMP%` **41.3GB / 15.6 万个文件**。逐项归因：

- **本工具链的锅（~7.8GB，已清 + 已修根因）**：`%TEMP%` 里有 **1601 个 `selftest_*` 目录 / 7.53GB**
  （装着自检/校验收渲染出的 WAV/OGG）+ 618 个 `mutation_*` + 485 个 `rehearsal_*`。
  根因：`selftest.py` 的 `TMP = tempfile.mkdtemp(prefix='selftest_')` 是**模块级**创建的，
  而 `selftest` 被 `check_song` / `build_song` / `midi_ref` / `theme_pack` / `mutation_check`
  到处 import → **每次跑这些工具都会留一个目录，且从不清理**（`rehearsal`/`check_song` 自己的
  倒是清了）。修：两处注册 `atexit` 清理（`DSH_KEEP_TMP=1` 可保留）+ 新守卫 `tmp_hygiene`
  （**>24 小时的残留当场自愈清掉**（进程被杀时 atexit 跑不到）、累计 >512MB 则 FAIL、
  判据自证 + 子进程验证"退出后目录真消失"），配变异用例"总量预算改坏"。
- **别人的锅（未动，需用户决定）**：`.dll` 13.7GB（.NET/VS 安装器残留，如 `Emsdk.*`、
  `Microsoft.AspNetCore.SharedFramework.*`）、`.tmp` 8.1GB、`.psd` 5.4GB（17 个
  `TFR*.tmp.psd`，2025 年 10~12 月）、`.vsix` 2GB（VS Code 扩展安装包）、
  `.exe/.msi/.cab` ~2GB、面板音频缓存 `bgm-studio-audio` 1.0GB。

**验证**：selftest **90/90**（新增 tmp_hygiene）、mutation_check **98/98**（新增 2 条用例）、
清理后 C 盘可用 38.4 → **46.2GB**；再跑 selftest/check_song/theme_pack/new_song 各一遍，
`selftest_*` / `mutation_*` 残留数**保持 0**（漏点确实堵住）。

**补记（同一轮）**：面板音频缓存也没人清 —— `%TEMP%\bgm-studio-audio`（试听/搜索/配平/分轨
每次丢文件）实测 0.93GB / 12 个 job 目录。加 `studio.prune_tmp_audio()`：启动时 >7 天删、
总量压到 512MB（最旧先删），`--keep-tmp-audio` 可跳过；配守卫 `studio_cache_prune`
（功能测试 4 条判据）+ 变异用例"清理变成空操作"。踩到一个隐蔽坑：检查项与变异用例**各自
import 了一次 `studio/server.py`**（模块名不同）→ patch 打在另一个副本上，检查照过、变异报"漏了"；
改成两边共用 `st.load_studio_server()`（缓存进 `sys.modules`）。

**最终验证**：selftest **91/91**（+tmp_hygiene、+studio_cache_prune）、mutation_check **99/99**
（+2 条用例）、`studio/server.py --help` 正常、token_audit 无超预算；
清理后 C 盘可用 **38.4 → 46.1GB**，且再跑一遍自检/校验/生成后临时目录残留数保持 0。

## 2026-09-14 第六批：让旋律"像人写的" —— 动机 / 期待 / 终止式（用户：这个不是很好听）

**起点**：36 号（欢快主题）**所有守卫全绿**（响度差 0.0、宽度 0.01、段间起伏 5.8 vs 目标 6.0、
旋律形态 0 问题），用户听完说"不好听"。诊断：守卫量的全是**分布/频谱**，而听感上"像人写的"
靠 **结构三层** —— 而这层我们**一层都没有**：和声是模板进行原样循环 16 遍（无终止式）、
旋律逐音从直方图抽样（无动机）、8 段只有 2 种编配且按 `i%3` 机械轮换（无张力曲线）。

**理论（上网查的，链接进 PITFALLS 126）**：Narmour 旋律期待模型（大跳后反向 + 回填）、
Huron 期待-情绪理论、Ithaca 的动机发展手法表（模进/倒影/扩缩/分裂）、
"乐句 = 带终止式的完整乐思"（Navy 手册）。→ 落成三条可实现的规则 + 一条句法规则。

**实现**（`melody_gen.py`，v2 动机层，**默认开**，`--motif off` 可退回旧版做 A/B）：
① **动机**：一小节固定节奏 figure（落点+时值）+ 固定音程 cell；落点抽 8 个候选、挑**最贴画像
   方言**的那个（`_cell_fit`）；② **发展**：figure 逐小节重复，每小节**整体移调**到该小节和弦
   （`_bar_shift` = 模进）；③ **期待**：音程过 `_ir_intervals`（大跳≥5 半音后反向、
   55% 回填、小音程 50% 同向）；④ **句法**：动机模式乐句固定 4 小节（偶尔 2），
   句末小节**只铺动机前半**（fragmentation）+ 末音延长 + 落和弦根音/主音（终止式）。

**踩的四个坑**（每个都"指标先看起来对了"再翻车，详见 PITFALLS 126）：
① 句末休止把终止音截成 0.5 拍 → 收束率 **0%（38%）**；② 小节**接缝**不受 cell 约束 →
   跳后反向率被拉到 **52%**（把期待规则也用在小节根音选择上 → 80%）；
③ 动机落点若不"可循环"（间距 <0.5 拍 / 太靠小节末）会被出口的落点间距规则在小节间删掉 →
   动机重复率 **20%**（加约束 + 挑最贴方言的 cell → 70%）；
④ **指标自己骗人**：节奏动机重复若跨段统计（8 段各一个动机）只剩 16% 假低 → 改**逐段算再平均**；
   结构判据单样本粒度太粗（16 小节只有 4~6 个大跳）→ 判据用**多 seed 平均**并断言样本量。

**A/B（同一骨架、只换旋律；36 = 旧，37 = 动机版）**：
节奏动机重复 **19% → 70%** · 句末收束 **0% → 81%**（落根音/主音 69%）· 跳后反向 **71% → 80%** ·
回填 **75% → 92%**；**混音层持平**（段间起伏 5.7 vs 5.8dB、最大频段差 4.2 vs 3.9dB）——
结构层改动没有破坏已对齐的频谱。两首都留了 OGG 可试听（`songs/36_d132_cheerful/`、`songs/37_d132_motif/`）。

**守卫**：新增自检 `melody_motif_rules`（4 条下限 + 判据自证"关掉动机层必须掉到门以下"，
实测旧版 12%/0% vs 新版 69%/69%）+ 2 条变异用例（大跳阈值 99 / 0）；落盘的动机模式曲目也逐首核对。

**顺带**：`rehearsal` 的"文档开关真实存在"原来只扫 `scripts/*.py` → 面板的 `--keep-tmp-audio`
被误报缺失，改成连 `studio/**.py` 一起扫；CHEATSHEET 补 `--motif off`；
PITFALLS 超预算按约定**拆归档**（93–100 进 ARCHIVE，现 101–126）。

**验证**：selftest **92/92**、mutation_check **101/101**、rehearsal 全通过、check_song --all 全过、
token_audit 无超预算。

## 2026-09-14 第七批：让旋律"好听"· 第 2 阶段 —— **先量真值，再定判据**（用户：还是不如普通的曲子）

**起点**：用户对 37 号（上一批的动机版）的评价是"好了一点，但还是不如普通的曲子"，而它
**所有守卫全绿**。第一步不是改代码，是**把真值量出来**：写一次性探针
（`theme_pack._melody_notes` 从 `refs/midi2/` 的 150 首模板提旋律，与生成端同一套定义）量五个指标：

| 指标 | 真实（中位 / cheerful） | 旧版 37 号 | 新 38 号 |
|---|---|---|---|
| 末落点 ≥8 格（跨过第 2 拍）的小节占比 | 90% / 79% | 61% | 77% |
| 小节内最大空档中位 | 1.03 / 1.40 拍 | 2.00 | 1.50 |
| 格 0（小节第 1 拍）落点占比 | 12.9% / 14.8% | 25.7% | 5~9% |
| **小节节奏签名重复率** | **23% / 33%** | 66% | 30% |
| 密度（音/小节） | — / 2.63 | 3.41 | 2.6 |

**最重要的发现**：真实旋律的小节节奏签名重复率**中位只有 23%** —— 而上一批（坑 126）定的门是
"动机重复率 **≥55%**（下限）"，方向**反了**：旧版的 66%（每小节复刻同一 figure）才是"呆板"的
量化真身。→ `MOTIF_MIN_REPEAT` 改成上限 `MOTIF_MAX_REPEAT = 0.45`，并新增 `melody_form_rules`。

**七个实现坑（每个都"指标先看起来对了"再翻车，详见 PITFALLS 127）**：
① **判据方向错**（上面那条）；② **落点铺满 ≠ 声音铺满**：只铺满落点后断句**反而更多**
（74→95 处）—— 时值仍照画像直方图抽（中位 0.5 拍，那是 F0 量的"发声时长"），间隔 1.5 拍就空
1.0 拍；真实模板的"无声空档"中位只有 **0.12 拍**；③ **变体层**（移位/压缩/扩展/稀疏长音 +
不许连续两小节同一个）；④ **接缝候选太窄废掉期待规则**（`Am6 = A C E F#`，可用方向只有一两个）
→ 反向率 43% → **82%**；⑤ **拱形在动机模式里从未生效**（⑤ 只写在非动机分支），且要落在
**骨架音**上而不是每个音上；⑥ **终止音被 ⑧⑨ 打回**（收束率 50~69% 抖 → 81%）；
⑦ **和声从不收束**（A 段永远停在 B7 属和弦）→ `cadence_pair` 段末 2 小节 V→I。

**被数据推翻的尝试**（都记在注释里，别重犯）：
· 时值填充系数扫描（0.0~1.0）：fill=1.0 时断句大降但**时值方言承接度掉到 33%**（门 55%）
  → 定 0.45（承接度 68%，断句从 95 降到 ~75）；
· **拱形加在每个音上**：句高点中位仍只有 0.13（随机游走把它淹了）→ 改成只作用在骨架音
  （代价 1.0 vs 平滑 0.45）；
· **拱形方向对半随机**：一半句子"倒拱"，高点必然在句首 → 85% 上拱 + 段末句下拱；
· **对称地加"首落点偏好靠前"**：末落点 ≥8 的小节占比反而 77%→**61%**、密度掉到 2.2 → 回退；
· 拱形度量：**单音尖峰 0.40 vs 三音滑动平均 0.67**（听感的"轮廓"是平滑后的形状）。

**和声收束的材料来源**（用户硬口径：不许自己造和弦）：`cadence_pair` 三层退让 —— ① 模板进行里
真实的"属→主"相邻对 ② 同一进行里同时出现的属与主 ③ `chord_pool` 的 degree 7/0（带自己的
suffix，`_stable` 挡掉 sus/dim）。15 个主题全部拿到收束对，cheerful 得 **`G7→Cmaj7`**。

**力度**：旋律原来只有硬编码两档（96 / 低八度 62）→ `song_engine.mel_dyn_env` +
`patterns.melody_dyn`（**opt-in**：缺省时引擎一行都不改 → 老曲 MIDI 字节不变、不用重渲）。
主题路径的新歌默认开。

**A/B（同一主题、同一 seed=21；36 = 逐音直方图，37 = 动机 v2，38 = 本批形态层）**：
| | 36 | 37 | 38 |
|---|---|---|---|
| 密度 | 2.36 | 3.41 | 2.60 |
| 节奏动机重复 | — | 66% | **30%** |
| 末落点≥8 的小节 | — | 61% | **77%** |
| 小节内空档中位 | — | 2.00 拍 | **1.50 拍** |
| 音域 | 13 半音 | 13 | **17** |
| 句末收束 | — | 94% | 69% |
| 跳后反向 | — | 59% | **75%** |
| 和声段末 | B7（悬） | B7（悬） | **G7→Cmaj7** |
三首 OGG 都在：`songs/36_d132_cheerful/`、`songs/37_d132_motif/`、`songs/38_d132_full/`。

**守卫（每一层改动都配判据 + 注入用例）**：新增 `melody_form_rules`（末落点 / 空档 / 格 0 /
密度 / 句内高点位置 / **音域** 六条，全部带真实对照值 + 自证"旧形态至少破两条"）、
`theme_cadence`（15 个主题包 120 个段落段末必须"属→主"；自证 = 旧行为"进行原样循环"必须判失败）、
`melody_dyn_optin`（缺省 2 档 = 老行为 / 打开 ≥6 档 / 显式 `false` 与缺省逐字节相同；自证 = 把
`mel_dyn_env` 换恒等函数必须掉回 2 档）；`melody_motif_rules` 的重复率改上限。
**变异用例 101 → 108**（关变体层 / 关铺满机制 / 关间隔上限 / 关拱形 / 收窄音域 / 关
`cadence_pair` / 关包络函数）。
**变异用例的教训**：四个注入里"只把 `CELL_LAST_MIN` 设 0"**抓不到** —— `_cell_fit` 里"末落点越
靠后越好"那一档仍会把落点挑到小节末。**注入必须打在真机制上**（改成"覆盖下限 + 打分偏好一起关"，
并加 `VARIANTS_ON` / `ARCH_W` / `SPAN_TRIM` 三个消融开关）。
**两条判据的口径**（都是量出来的）：① **音域**要**跨 seed 合并**算 —— 16 小节夹具单样本撑不开
（实测单 seed 8~19、4 seed 合并 19 = 画像 24 的 79%），而 64 小节的真实曲目才用满（38 号 100%）；
② **句内高点位置**要用**三音滑动平均的轮廓**（单音尖峰会被装饰性跳进带偏：同一批样本 0.40 vs 0.67）。

**验证**：selftest **95/95**（+`melody_form_rules`、`theme_cadence`、`melody_dyn_optin`）、
mutation_check **108/108**（7 条旋律/和声/力度用例）、rehearsal 全部通过、check_song --all 30 首
全过；38 号渲染 2 轮达标（响度 −19.3 = 参考、**宽度 0.660 = 参考 0.660（差 0.000）**、
质心 +229Hz、null test −48.6dBFS）。PITFALLS 超预算 → 按 LIMITS 注释**拆归档 101–104**（现 101–127）。

## 2026-09-14 第八批：**"旋律和伴奏配合不好" = 伴奏整轨在和弦外**（用户听感反馈）

**起点**：用户听完 38 号说"不好听，主旋律和伴奏没有很好配合"。这是**纵向**问题（旋律与同时
发声的伴奏的关系），与第七批做的横向形态（旋律自身的节奏）不是一回事。

**先量，不猜**。写探针把"旋律 vs 伴奏"的五个维度在同口径下量出来（真实侧 = `refs/midi2/` 里
各主题包所列的 80 首模板，旋律轨由 `theme_pack._melody_notes` 挑、其余轨当伴奏）：

| 指标 | 真实模板（中位） | 38 号（修前） | 38 号（修后） |
|---|---|---|---|
| 咬合（旋律落点处伴奏也在起音） | 68% | 82% | 82% |
| 咬合（鼓） | 0% | 70% | — |
| **音区分离**（旋律 − 同拍伴奏最高音） | **+12 半音** | **−2** ✗ | **+16** ✓ |
| **旋律在下** | 1% | **59%** ✗ | **0%** ✓ |
| **半音冲突**（同拍小二度/大七度） | **6%** | **45%** ✗ | **8%** ✓ |
| 同度撞车 | 8% | 10% | 2% |

**根因（真 bug）**：`song_engine.TR_SHIFT` 的音区分工表是 `Pad −5 / Hook −5 / Piano +4 /
Strings −3 / Arp +3 / Melody +7`，注释写着"**只改 MIDI 音高、不动和声：整轨同移不改变和弦内的
音程关系**" —— **那句话是错的推理**：轨内音程关系确实不变，但**与和弦的关系全变了**（+3 把
B 变成 D、+4 把 G 变成 B…）。实测各轨"音的 pc 落在当小节和弦音集里"的比例：
Bass **98.6%**（无移调，作对照）· Hook **15.1%** · Arp 31.6% · Piano 37.5% · Strings 43.2%
—— **伴奏有 60~85% 的音是和弦外音**。旋律有"强拍贴和弦"纪律、伴奏却弹另一组音，就是用户听到的
"配合不好"。

**修**：① `TR_SHIFT` **只允许纯八度**（伴奏整体 −12 让位、旋律 +12 独占最高，各轨仍分居不同
八度）；② **边界保护**：整轨移调后若越出乐器音域就整轨不移（不许轨内八度跳变）—— 实测
17/28 号旋律升八度后到 104/105（Melody 上界 103）、12/13/14 号 Hook 降八度后到 29（吉他下界 32），
由 `track_ranges_musical` 抓到；③ 乐器合理音域表**从自检移进引擎** `TR_RANGE`（单一出处：
引擎的保护与自检的守卫必须用同一张表）；④ Bass 的硬编码 `+7` 五度改成 `fifth_tone()`
（m7b5/dim 和弦的五度是减五度，硬写 +7 会落在和弦外）→ Bass 贴合 98.6% → **99.4%**。

**顺带查出两处守卫的假象**：① `harmony_layer` 写死"音区 65~76 才算副旋律"——`TR_SHIFT` 一改那些
音就掉出区间、**检查当场空转**（变异用例"配成半音三度"漏抓）；而且它原来的夹具旋律（76/79）比
和弦最高音高一个八度，`harmony_below` **根本不产音**，`assert harm` 是**假通过**（Hook 的普通
伴奏音恰好撞进断言的集合）。改成"机制级单元判据 + 端到端必须真写进 Strings 轨"。
② 追查 8 条"来源不明的 Bass 音"追到 `bass_part` 末尾的**句尾半音引导音**（`i%4==3` 时朝下一和弦
根音走半音，力度 78 / 0.3 拍）—— **不是 bug**，是有意的经过音，落在和弦外正确；判据据此不把
Bass 算进"伴奏和弦贴合"。

**代价**：改引擎行为 = **全库 MIDI 过期**。`determinism_and_bytes` 逐首比对已交付 MIDI，TR_SHIFT
一改 30 首全部要重跑 `make_song`（这次真的跑了全库，不是"跑一首就行"）。

**守卫**：新增 `accompaniment_harmony`（`TR_SHIFT` 必须纯八度 / 伴奏贴合 ≥95% / 音区分离中位 ≥6 /
旋律在下 ≤15%）+ 变异用例"换回非八度表"（109 → **110**）。
PITFALLS 超预算 → 按 LIMITS 注释**拆归档 114–120**（现 101–128）。

## 2026-09-14 第九批：**给旋律留空间**（第 1 条手法：伴奏减薄 + 运行时让位）

**背景**：用户说"一条一条做"，并按建议顺序开工。第 1 条 = 网上编曲手法的第一条
"creating space for a melody"（[Oak 教学单元](https://www.thenational.academy/teachers/programmes/music-secondary-ks4-edexcel/units/composition-exemplars-chamber-groups-and-ensembles/lessons/creating-space-for-a-melody)、
[Leave Space](https://cursa.app/en/page/music-production-fundamentals-writing-musical-parts-that-leave-space)）。

**先量真值**（新探针 `_space_probe` / `_dens_probe`，真实侧 = `refs/midi2/` 的 80 首模板）：

| 指标 | 真实模板 | 我们（38 号，修前） |
|---|---|---|
| **伴奏起音密度** | **19.8 音/小节** | **45.0**（2.3 倍） |
| 真模板非鼓轨**每轨**中位 | **3.6 音/小节** | Hook 14.7 / Bass 12.1 / Arp 8.0 / Piano 7.2 |
| **旋律"独唱率"** | **43%** | **15%** |
| 旋律起音处的伴奏音数 | 3.0 | **6.5** |
| 踩鼓点比例 | 33%（中位 0%） | 79%（鼓本身也偏密：10.4 vs 真实 6.0） |

**做法（`patterns.space`，opt-in）**：
① **减薄**（有鼓的段落）：Arp 每半拍→每拍、时值 0.28→0.9 拍（**靠"持续"而不是"音数"占高频**，
与 `shimmer` 层同一课）；EP 每拍 3→2 音；Piano 反拍 3→2 音；Bass 十六分三连→两条。
**无鼓段落不减** —— 那里本来就稀，再减撑不住织体。
② **运行时让位**：旋律起音的同一刻，**伴奏最多 3 条轨发声**、每轨该刻最多留 1 个音。

**①不够、②才是关键**：减薄只降"平均密度"，"旋律响的那一刻仍挤着 6 条轨"只能靠让位 ——
实测减薄后"旋律起音处的伴奏音数"**一点没降（6.5 → 6.5）**；加让位后 **6.50 → 2.50**
（真实模板 3.0）。38 号最终：密度 **45 → 26 音/小节**。

**两条纪律（都踩过）**：① **改引擎行为一律 opt-in** —— 第一版直接改，30 首 MIDI 全部过期、
rehearsal 的边界夹具（"无打击乐段落"）调参误差卡在 3.5；连试两次补偿（提亮 Arp / 加长时值）
都没解决，改成 `patterns.space` 之后老曲字节不变、rehearsal **立刻全绿**。
② **"独唱率"不能当判据** —— 它要求伴奏放弃整个八分网格（真实模板靠"旋律节奏自由"实现），
在"16 分格全覆盖"的编配里**恒为 0**；改用"**旋律起音处的伴奏音数**"。

**守卫**：`melody_space`（开关两版就地对照：密度 ≤85% / 起音处伴奏音数必须下降 / 无鼓不减 /
自证"`space_on` 失效时两版必须完全一致"）+ 变异用例（110 → **111**）。

## 2026-09-14 第十批：**第 2、3 条手法被真值否掉 / 纠正**（先量真值的价值）

**第 2 条「微时序人性化」—— 经真值检验，不做**。新探针 `_human_probe` 量真实模板每个音相对
最近 16 分格的偏差：**中位 0.0ms、88% 的轨是量化的（<1ms）**。也就是说 MIDI 模板的"味道"
不来自微时序 —— 我们本来就是严格量化，加上去反而**偏离参照系**。
（网上讲 humanization 的文章讲的是**人演奏**的录音，与"MIDI 模板"不是一类对象。）

**第 3 条「配器张力曲线」—— 参照系换对了才成立**。先按原计划拿 **MIDI 模板的编配密度曲线**
当真值（新探针 `_arr_probe`）：真实模板"每 8 小节密度"的**相对起伏中位 0.00**
（四分位 0.00~0.57，一半以上完全平）—— MIDI 模板库在编配层是**扁平的**，回答不了
"副歌该比主歌厚多少"。换成**混音目标画像的段间能量块**（来自**真实音频**的每 8 小节响度）后成立：
`new_song.arr_level` 让能量高于均值的段开第二梯队，**替换旧的 `level = i % 3`**。
实测高/低能量段的编配厚度**排序相关 0.87**（机械轮换 0.64）。
38 号的段编配因此从 `[0,1,2,0,1,2,0,1]` 变成"能量高的 A5/B2 厚、能量最低的 C 段薄"。

**判据本身也踩了一次（与坑 128 的教训同源）**：第一版判据用"排序相关 >0.5"，结果
`i % 3` 与**周期性的能量曲线**（8 段循环）撞出**伪相关 0.64**、区分不开；改成**机制级判据**
（`arr_level` 必须是能量的**单调不减**函数 + 平坦曲线必须全 0）后，机械轮换**立刻失败**
（在 `[-3,-1,0,1,3]` 上给出 `[0,1,2,0,1]`）。
**通则：判据尽量直接测机制（函数性质），少用统计量 —— 相关系数会被周期性骗。**

**守卫**：`theme_arrangement_dynamic`（机制级单调性 + 端到端排序相关 + "平坦曲线不许造差异"）+
变异用例（111 → **112**）。**验收**：selftest --fast **98/98**。

**四条手法的处置**：① 给旋律留空间 —— 做（坑 129）；② 微时序人性化 —— **真值否掉，不做**；
③ 配器张力曲线 —— **换参照系后做**（本条）；④ 平行五/八度 —— **量了真值：我们本来就在范围内**
（真实模板平行五中位 0.000 / 平行八中位 0.002，但**最大 0.829** —— 古典禁忌在舞曲里很常见），
所以**不加约束、只加防退化守卫** `melody_voice_leading`（门 0.15/0.20，抓"整条旋律跟着低音走八度"
那种极端退化；变异用例实测抓到 42~50%）。

**最终验收**：selftest **99/99** · mutation_check **112/112** · rehearsal 全通过 ·
check_song --all 30 首全过 · token_audit 无超预算（PITFALLS 拆归档 114–120 腾的位）。

## 2026-09-14 第十一批：**混音参考改"多份聚合 + 逐份可溯源"**（用户："要参考权威音源，也要多方参考"）

**起点**：做新测试曲 39（`tender` 抒情主题）时暴露 —— 成品**中频比参考厚 9.6dB**，
成绩单直接报"先改 BPM 再谈其它"。查下去是两个结构性问题：

| 问题 | 现状 | 后果 |
|---|---|---|
| **只挑一份参考** | `mix_target` 从 46 份画像里挑分数最高的一份 | 单份的个性（偏亮/偏薄）整体带进成品：`tender`（ballad 编配，中频天生厚）挑到 **BGM04**（315–1250Hz 在 −10~−14dB 的亮薄参考） |
| **画像是"挑选维度"缺亮度** | 评分只有 速度 0.55 / 打击感 0.30 / 调式 0.15 | 挑不出"中频厚薄"不匹配的参考 |
| **画像没有出处** | 46 份 `refs/*.json` 都无 `source` | 参考来自项目素材包 `Bgm`（galgame 提取的 50 首 BGM）—— **能溯源到文件、但无从谈"权威"** |

**做**：
① `theme_pack.tag_ref_sources()`（幂等）给 46 份画像补 `source` —— **如实**写"项目素材包 `Bgm`
（提取自 galgame 素材的 50 首 BGM，真实商业混音），**厂商/作品名未标注**"，**不假装权威**；
以后引入带 URL 的来源写 `source.kind='web'` + `url` 即可（守卫要求每份成员都有 `source`）。
② `mix_target` 改成**合格的前 N 份（≥3，实测每主题 6 份）逐维度取中位数**（`aggregate_refs`），
落成 `refs/mix_targets/<主题>_mix.json`；`new_song` 的 `render.json` 指向它；
`scorecard`/`selftest` 的画像查找统一到 `scorecard.ref_path`（单份在 `refs/`、聚合在
`refs/mix_targets/`）。
③ 新增守卫 `mix_target_aggregate`：**必须是聚合 / 成员 ≥3 / 逐份带 source / 逐频段核对
"聚合值 = 成员中位数"**（机制级）+ 2 条变异用例（砍成单份 / 抹掉 source）。

**效果（tender）**：质心 2281 → **3284**、1250–2500Hz −20.9 → **−9.4** ——
单份的极端个性被削掉；39 号的成品中频差 **9.6 → 5.2dB**（剩下的 5dB 是 ballad 编配本身的
中频厚度，属编配层差距，如实留档）。

**三个坑**（见 PITFALLS 132）：聚合画像文件名必须与 `ref` 同名；离散字段（节奏型/音级分布）
不能取中位；聚合后目标曲线更平 → 依赖"有曲线的主题数"的判据门要按实际校准。

**验收**：selftest --fast **99/99**、mutation **114/114**、rehearsal 全通过、
check_song --all 全过、token_audit 无超预算。

## 2026-09-14 第十二批：**"怎么感觉你写的好多部分都是一样的"** —— 量完发现两头都反了

**用户反馈** → **先量**（临时探针把"一样"拆成旋律 / 编配 / CC7 三层，对比 36/38/39）：

| 层 | 38 / 39 号（修前） | 应该是 |
|---|---|---|
| 旋律 | **8 段 8 支完全不同的旋律**（A/A2/A3/A4/A5 各一支） | A 段复现**共用**主题旋律（记忆点） |
| 编配 | 39 号**只有 1 种组合**、CC7 全曲同一套 | 主歌薄、副歌/桥段厚（张力） |

**两头都反了** —— 该重复的没重复（旋律没有"主题"），该变的没变（8 段编配一模一样）。

**修**：
① **旋律复用**：根因是 `build_from_theme` 里一行命名 —— `mname = 'm%d' % (i + 1)` 给每段
   一个新名字，而 `melody_gen` 本来就是**按旋律名分组、同名共用**的（等于把复用机制掐掉了）。
   改成 `role_melody_name`（去尾部数字：A2/A3/A4/A5 → A）后，8 段只用 **3 支旋律**
   （A×5 / B×2 / C×1）—— 这才是曲式（AABA）的样子。
② **编配跟曲式**：`arr_level` 原来在能量曲线平坦时返回全 0（"不造假变化"），而 tender 的目标
   画像**本身平坦** → 8 段编配全同。改成**曲式角色兜底**（非 A 段开第二梯队、A 段关）——
   用已有信息（`form.plan` 的段落名来自模板分析），不编造数据。
   39 号的编配组合从 **1 种 → 2 种**。

**顺带修一处判据口径错**：`melody_form_rules` 原来要求"单曲音域 ≥ 画像 range × 0.85"，
而画像是**同主题多首模板的并集**（tender 34 半音）—— 复用后 39 号只剩 18 半音（53%）
被误判"不达标"。改成"落在合理区间"（`FORM_SPAN_MIN` = 12 ≤ span ≤ 画像 range）。
**通则：拿"多首聚合的画像"当"单曲的下限"是口径错**（坑 105 的同一族）。

**守卫**：新增 `theme_melody_reuse`（同名段落必须共用旋律；且不许全曲只有一支 ——
那又成"整首一个样"）+ `theme.melody_reuse` 标记（旧曲目不追溯，38 号仍留作 A/B 对照）；
`theme_arrangement_dynamic` 的"曲线平坦"分支改成"编配不许全平"；变异用例 114 → **115**。

**验收**：selftest **102/102**、mutation **115/115**、rehearsal 全通过、check_song 全过；
39 号用新引擎重出（`39_tender_probe`）：响度 −16.9 = 参考、宽度 0.369 = 参考、最大频段差 4.2dB。

### 补记（同一轮，来源 URL 落地）

用户要求"参考**权威**音源"。查画像的 `file` 字段发现 **5 份带完整原始路径**，指向**两部 galgame**：
`<参考曲目录/仰望夜空星辰 FINE DAYS>`（4 份）与 `<参考曲目录/ピュアソングガーデン>`（1 份）。
联网查证到作品的**权威页面**（中文维基 / VNDB / 萌娘百科）并写进 `source.url` + `url_alt`。
⚠ **厂牌与发行日期不写死** —— 搜索摘要里没有逐项核实的年份/厂牌，写上去就是把没核实的当事实
（"写了权威却不给依据"比不写更糟），所以字段里只留"以 `url` 页面为准（本次未逐项核对）"。
其余 41 份画像只记了文件名、**无法定位作品** → 如实写"作品级出处未记录"（同批其它画像指向
galgame 商业 BGM，可交叉印证，但不替它下结论）。守卫加强：成员 `source` **至少要 url 或 file**，
声明 `kind='web'` 的**必须有 url**。
（原始路径现已不存在（`Test-Path` = False）—— 素材已不在原处，但**出处记录留在了画像里**。）

## 2026-09-14 第十三批：**"都有" → 编配与吉他各一层**（同一条听感反馈的第三轮量测）

用户先答"都有"（哪些部分一样：曲内 + 跨曲），再直接点出"**怎么每个曲子的刚弦吉他都是这个
节奏音调**"。这一轮把"一样"拆成四个独立自由度逐个量：乐器组合 / 落点 / 音高序列 / 音量曲线。

### 1. 编配：段落间"同一套乐器从头铺到尾"（坑 134）
- **真值（修前）**：231 个段落 bass **100%** / piano 98% / perc 97% 在场；段间乐器组合 Jaccard
  **中位 0.86**（36~39 全 0.86，18/20/24/25/27 **1.00**）。对照 13 首真实商业 BGM 的分段画像
  （`refs/sections/*.json`，以 160–315Hz 为基准的相对谱重建）：高频 5–10k 段间起伏中位 **7.8dB**、
  10–18k **9.1dB**，我们 **0.5 / 0.5dB**。
- **改**：`song_engine.arr_by_role`（opt-in `patterns.arr_by_role`）—— 引子/尾声=钢琴+贝斯+垫子、
  主歌=+吉他分解、副歌=+钟琴/琶音/打击/弦乐、桥段=换电钢（另一条频段），副歌按出现次序升级；
  `perc` 由开关变成 **0/1/2/3 层次**；同角色段落共用一套（曲式可预期性）；兜底防"全曲无打击"。
- **验收**（41 号 vs 旧 39 号，同主题同速 120BPM）：Jaccard 0.86 → **0.62**（最低 0.40）、
  strings 在场 100% → **25%**、高频段间起伏 1.1/1.0 → **4.6/5.0dB**。
  守卫 `arr_role_variety` + 变异"段落编制不随角色变"。

### 2. 吉他：跨曲硬编码 + 曲内逐小节复读（坑 135）
- **根因两条**：① `patterns.arpeggio` 是引擎硬编码 `[0,2,3,4,3,2,4]`、15 个主题包全都没有它
  → 每首歌同一组落点 + 同一组和弦音序；② `arp[k % len(arp)]` 每小节相同 → 和弦相同的小节
  音高序列**逐音重复**（39 号 **94%** 的小节重复）。
- **改**：音型按主题包真实吉他音域跨度分三档（`retro` 跨度 46→宽琶音、`night` 17→窄音型）；
  落点按真实高音区占用率 `rhythm.high_slot_share`；曲内加相位轮换 + 同和弦换把位。
- **验收**：跨主题组合 1 → **12 种**；同和弦 6 小节音高序列 1 → **6 种**；42 号重复小节 94% → **75%**。
  守卫 `guitar_variation` + 2 条变异用例；**老曲字节不变**有断言守着（`vary=False` 逐音一致）。

### 3. 顺带修的
- `role_melody_name` 改用 `song_engine.role_of_section`：`Final (A minor)` 不再另起一支旋律
  （17 号 `b`~`b_bridge` 93%、28 号 `a`~`a2` 87% 这类"曲内孪生"的根因）。
- 新工具 `probe_sameness.py`（曲内/曲间/主题三层体检 + 判据对照，含段间编配 Jaccard）。
- 文档：坑 134~136 追加；**归档 105–108 与 127–130**（加入新坑后 PITFALLS 超 9600 预算 →
  按 LIMITS 注释拆，不许抬上限）。

### 4. 教训（坑 136）
同一条听感反馈量了三轮才拆完 —— 旋律（133）→ 编配（134）→ 吉他音型（135）。
**"像/一样"必须拆成"哪个乐器、哪一层"逐个量**，量到一个维度就宣布"修好了"会连续返工。
（数字全在 `songs/41_role_tender/notes.md` 与 `songs/42_gtr_tender/notes.md`。）

## 2026-09-14 第十四批：**可视化面板 → 对标 miditoolbox 的完整 MIDI 编辑器**

用户要求："把可视化界面做成 miditoolbox.com/zh/editor 这样，功能也要齐全"。先量清对方功能面
（抓页面正文 + 技术规格），再列差距表，按用户选择的三条口径实施：**B 先做完整编辑器再桥接引擎** ·
**任意 .mid 可导入** · **力度可落盘**。

### 1. 先量差距（对方有什么 / 我们缺什么）
对方（纯浏览器编辑器，无声音渲染）：全轨钢琴卷帘 · 网格吸附 1/4~1/32 · 智能量化 ·
力度与曲线 · 半音移调 + 覆盖 BPM · 多轨（独奏/静音/隐藏/换乐器/通道）· Type 0&1 导入导出 ·
**和弦检测** · 100% 本地。
我们原有：真实渲染 · 分轨混音台 · 自动配平 · 候选搜索 · 参考曲 A/B · 频谱体检（**对方完全没有**），
缺的正是"标准 MIDI 文件编辑"这一整块。

### 2. 新增（三层，口径全在 Python）
- `scripts/midi_probe.parse_full` —— **事件级** SMF 解析（力度/CC/拍号/速度/marker 全留）。
  与 `parse()` 的分工：那个是统计口径（theme_pack 用），这个是**编辑口径**（要能原样导出）。
  修掉两个真 bug：① 松键事件的 vel 被当成按键力度（外部 MIDI 会被整首压成 95~96）；
  ② 未松键的音按轨尾收时丢了力度。
- `scripts/midi_file.py` —— **导入/导出**：任意 Type 0/1 + 拍号分母 2 的幂（2/2、3/4、6/8…）；
  导出支持 Type 1 多轨与 Type 0 单轨（**逐轨分配通道**，鼓轨固定 9）。
  `roundtrip_report` 是往返判据：无重叠轨**逐音一致**，有重叠轨**发声时刻集合一致**
  （MIDI 对同音重叠无法唯一还原配对 = 格式固有歧义，不是我们的 bug）。
- `scripts/midi_ops.py` —— **编辑语义唯一入口**：量化（含强度/摇摆/时值）、移调、力度
  （缩放/偏移/设定/夹取/人性化/**渐变**）、增删改、复制粘贴、轨道管理、去重、修剪重叠、吸附。
- `scripts/midi_chords.py` —— **和弦识别** + 生成和弦轨。
- `studio/web/ed.html|ed.js|ed.css`（`/editor`）+ `studio/server.py` 的 `/api/ed/*`；
  `studio/tools/smoke_ed.js`（无浏览器冒烟，走真实 HTTP）。

### 3. 踩过的坑（都写进代码注释了）
- **base64 必须分块**：`String.fromCharCode(...u8)` 在 29KB 就撑爆调用栈（真实 MIDI 常几 MB）。
- **Node 转发必须显式给 Content-Length**：Python 的 http.server 只按它读 body，否则收到空 body。
- **`bind()` 漏调 `bindRoll()`**：卷帘在浏览器里完全不响应鼠标 —— 冒烟测试抓到的第一个真 bug。
- **和弦取样的"借窗"只能向前**：借下一小节的音会让相邻小节互相污染（`B7`+`Gmaj7` 糊成 `Gmaj9`，
  64 格并成 43 段）；而完全不借又会漏掉"起音在窗口之前的长音"（`Am6` → `A6`）。
- **和弦识别的低音必须单独保留**：低音是转位/根音的唯一依据，按权重阈值筛会把它筛掉。
- **变异函数要捕获原函数对象**：`lambda: mod.fn(...)` 会被后来的替换套娃 → RecursionError
  （那是"变异写错"，不是"检查抓到"）。

### 4. 验收
自检 **107/107**（新增 `midi_file_editor_roundtrip`、`midi_ops_semantics`、`midi_chords_detect`）·
变异 **120/120**（新增吉他 2 条、和弦 2 条）· 编辑器冒烟 **31/31** · 引擎面板冒烟仍全过 ·
随机 20 首真实 MIDI 往返 20/20 一致。
和弦识别：**95 个模板 × 5 个根音全部正确**，转位/漏音/外音/边界/时间轴全过；
41 号前 8 小节 `D7 Gsus4 Cm7 Cm` **8/8 全对**（38 号受编配省略音影响 53%）。

## 2026-09-14 第十五批：**编辑器改用真音源渲染**（用户："还是比不上主界面"）

用户把 `/editor`（WebAudio 合成音）和 `/`（引擎面板放真音源渲染的 ogg）一比 —— 合成音怎么调
都比不过 GeneralUser GS 采样音源。所以正确的解法不是继续堆合成算法，而是**让编辑器也用同一条渲染管线**。

做法：`/api/ed/render-audio`（**后台任务**，见下）→ 授权用 `scripts/render_midi.py` 把当前编辑模型
导出成 MIDI 再渲染成 OGG → 前端 `<audio>` 直接播放它；合成音退为**兜底**（未渲染/渲染失败时）。

关键设计：
- **播放时钟抽成"锚点对"** `(anchorBeat, anchorT)`：拍位 = anchorBeat + (t − anchorT) / 秒每拍。
  `synth` 用 `ctx.currentTime`、`media` 用 `player.currentTime`；换音源 / seek / 滚屏只改锚点，
  位置天然连续。前面三批踩的坑（基准不同源、绝对时刻算错、滚屏补偿成恒等式）在这一层统一解决。
- **渲染必须异步**：整曲实测约 40~60 秒（FluidSynth + OGG 编码），同步会把面板卡死并让请求超时。
  所以 `render-audio` 先返回 `task`，前端轮询 `render-status`；期间继续用合成音播放，好了再切过去。
- **缓存键 = 音符指纹**（音数/末拍/BPM/轨数/CC 数）：改了音符才会重渲，否则秒回。
- 顶栏显示当前音源（🎹 合成音 / 🎧 真音源 / ⚡ 试听毛片）。

**两个真坑（都是"手工能跑、走面板必失败"那一类）**：
① `render_midi.py` 会在输出基名旁写 `<base>.raw.wav`，而面板会话路径
   `%TEMP%\bgm-studio-edits\<eid>\render_<fp>.raw.wav` 太长 → FluidSynth 写不出来 →
   `os.remove(raw)` 抛 `FileNotFoundError`。改为在 `%TEMP%` 下用**短名**渲染，产物再搬回会话目录。
② 短名**必须唯一**（加 uuid 后缀）：两个渲染任务撞同一个短名时会互相删对方的 `raw.wav`
   —— 症状同样是"手工渲染成功、面板失败"。
判据（`smoke_ed.js` 第 7 节）：渲染出音频且字节数合理、音源切成 media、状态栏显示真音源、
媒体时钟推进后播放头前进、**播放头与音频时间对齐（2.00s vs 2.00s）**、停止时媒体也暂停。
实测：123 秒音频渲染耗时约 7 秒（含 OGG 编码），前端播放与卷帘/进度条严格对齐。

## 2026-09-14 第十六批：**主界面卷帘"空白"的排查 → 挖出"引擎修了但全库没重渲"**

用户报"主界面的钢琴卷帘是空的"。查下来是三件事叠在一起：

### 1. 直接原因：`renderRoll()` 在没数据时抛异常，把整个重绘吃掉
`renderRoll()` 开头直接 `S.song.sections` —— 曲目还没加载（或加载失败）时抛 TypeError，
`renderAll()` 里后面的 `renderStrips/drawWave/renderSearchSec` 全不执行，画布保持空白、**也没有任何提示**。
修：① 没数据就**在画布上写明原因**（"还没加载曲目"/"加载失败：<原因>"）；② `loadSong()` 失败时
记 `S.loadErr` 并重绘（原来只有一句 `alert`）；③ 卷帘左上角常驻一行自证
**"<曲名> · 音符 N（可编辑 M）"** —— 以后"空"这件事一眼可辨，不用再来回猜。
另：`index.html` 的静态资源加版本戳（`?v=3`），避免浏览器吃旧 app.js。

### 2. 真正的大问题：34 首交付物全是**坏引擎**的产物
顺手扫全库 determinism，发现 **34 首的音符内容全都变了**（同数量、内容不同）。
根因：`TR_SHIFT` 的非八度 bug（坑 128：伴奏 60~85% 和弦外音）**早已修成纯八度**，
但**既没重渲全库、也没提交** → `songs/*.mid` 一直停留在坏引擎版本，
伴奏相对旋律系统性偏低 7 个半音。修：全库 `make_song --no-tune` 重渲
**34 首 / 5.2 分钟 / 0 失败**，`selftest` 由 106/107 回到 **107/107**。

### 3. 途中我踩的坑（已写进 PITFALLS 141）
- 用 `song_engine.py <song.json> <out.mid>` 想"只生成到临时目录"——**命令行忽略第二个参数**，
  MIDI 直接写进 `songs/`，**覆盖了 67 个交付文件**（已 `git checkout -- songs/` 恢复）。
  要注意：覆盖后的产物其实与当前引擎自洽，真正丢的是"交付版本与引擎对不上"这一信息。
  正确做法是调 API：`song_engine.compose(song.json, out_mid)`。
- 全库一致性**可以秒扫**：临时目录里逐首 compose 再与交付 MIDI 比音符（比字节宽松），
  几分钟就能拿到完整清单；靠 `selftest` 一次只报第一首，会一轮一轮耗。

## 2026-09-14 第十七批：**面板"只有产物和大小" + 拖进度条满屏红线**（两个都是取数/绘制层）

用户报两句：① 左侧【音轨】面板"只有产物和大小"；② "拖动进度条之后还会有显示bug"（截图里卷帘上是
几十条红色竖线）。两件事各自量到真值后才动手，**反证由 smoke 的旧版对照给出**。

### 1. 音轨面板空：UI 读的是 song.json 里**不存在**的字段
`/api/song` 返回的 `song` 键里**没有** `programs`/`mix`（42_gtr_tender 实测就是这种形状）——
因为引擎侧的真值是 `DEFAULT + 风格预设(style) + song.json 覆盖`（`song_engine.py` 345-351），
曲目省写这两个字段是**合法**的。而 `renderTracks()` 用 `Object.keys(S.song.programs||{})` 枚举音轨
→ `[]` → **一张卡都不渲染**，面板里只剩下面的"产物/大小"表。
同一根因的还有两处：混音台 `strips` 空；`applyMixToSong()` 的
`if(!(t in (S.song.mix||{}))) continue;` → 拖完混音台点"应用到曲目"**静默什么都不写**。
修：新增统一取数 `trackRows()`（**events 实测值** > song.json 覆盖）、`setTrackProg/setTrackMix`
（**惰性建字段 + 只改被编辑的那一维**，通道号沿用 events 的真实值，避免撞车）、
`applyMixToSong` 改以 `trackRows()` 为准；面板上把"跟随预设"标出来。
**为什么一直没被发现**：`fixtures/song.json` 自带 programs/mix —— smoke 的假数据比真数据"完整"，
恰好绕过这个形状。所以断言里先 `delete S.song.programs/mix` 再渲染。

### 2. 拖进度条 → 满屏红线：常驻 rAF 往画布上叠画、从不擦除
`drawPlayhead()` 每帧在**卷帘画布**上补一条 2px 红线（`fillRect(x-1,0,2,H)`），**不擦除上一帧的**：
播放时位置连续、旧线叠成一片红带；**拖进度条时位置大跨度跳跃，旧线全部留下**。
修：播放头改成**独立 DOM 叠加层**（`#rollWrap > #rollPh`，只改 `transform`，不重绘画布），
位置用 `paintRoll()` 记下的 `S.rollG` 几何算（与本次绘制严格同一套坐标）。

### 3. 验证（都做了反证，不是自己说自己好）
`studio/tools/smoke_ui.js` 的 canvas 替身从"空壳"升级为**记录绘制**（并修正 `innerHTML=''`
不清 children 的假象），新增断言 + `SMOKE_APP_JS` 换旧版对照：

| 断言 | 旧版（git HEAD） | 新版 |
|---|---|---|
| 缺 programs/mix 时音轨卡 | **0/9** | 9/9 |
| 混音台 strips | **0/9** | 9/9 |
| 拖进度条后卷帘红色满高残影 | **5 条** | **0 条** |
| 手工画 2 条红线能否被数出（探针自证） | 7（=5+2，探针有效） | 2 |
| 播放头叠加层被驱动 | 否 | 是 |

`selftest --fast` **107/107**、`smoke_ui` 全绿。文档：PITFALLS 新增 142/143；
加入后**超预算 54 字符** → 按 LIMITS 约定把 133–134（已有 selftest 守卫固化）归档进
`PITFALLS-ARCHIVE.md`，PITFALLS 7572 字符 / 余量 2028（**没有抬上限**）。

### 4. 新增真浏览器验收 `tools/browser_check.js`（补上一直缺的那一环）
替身环境只能证明"绘制调用是对的"，证明不了"浏览器里真看得见"。这个脚本零依赖
（Node 24 自带 `WebSocket` + Edge 的 CDP）：开 headless Edge → 切到目标曲目 →
**用 `Input.dispatchMouseEvent` 派发真实鼠标拖动波形** → 直接读 canvas 像素与 `#rollPh` 的 rect。

实测（42_gtr_tender）：音轨卡 **9/9**、混音台 9、卷帘非背景像素 360720、
红色像素「初始 0 · 手工画 1 条 → 720（探针自证有效）· 重画后 0 · 拖动后 0」；
拖到 80% → 播放头 **82.9%**（几何算 83.0%、引擎位置 80.0%），再拖到 20% → **20.7%**（跟着走）。

途中两个**必须记住**的量法教训（都写进 PITFALLS 143）：
① **headless 里 rAF 不会自己跑**（`posInfo` 恒停 `0.0 / 0.0s`）→ 播放头停在拖动前那一帧，
   一度被误判成"定位没跟上"；要靠 `Page.captureScreenshot` 逼合成一帧或直接驱动一次绘制。
② 80% 音频 ↔ 83% 卷帘宽度**是对的**：音频 132.9s 比音乐 128s 长（尾部混响），
   判据要拿"几何算出的比例"当期望值，别拿拖动比例。
另外两处我自己的口径错也当场纠了：`window.ENG` 读不到（app.js 是普通脚本，顶层 `const`
进的是**全局词法环境**，直接写 `ENG` 才对）；截图里 "120BPM" 被我看成 128 —— 数字一律查
`/api/song`，不靠肉眼。

### 5. 用户接着报"音轨的试听按钮没有用"（三层根因，同一批修完）
第一层就是**前任的死代码**：`pollJob()` 完成后对**所有**任务一律
`S.player.src = '…kind=mix…'`，而 `S.player` **从未被赋值**（只有这一处写属性）→
`TypeError: Cannot set properties of undefined`，且它发生在 `await loadSong()` **之前** ——
所以"试听没声音"和"任务完成后界面不刷新"是同一个 bug。
（"⚡ 试听本段"一直好用，只因它的分支写在这一行之前就 return 了。）
第二层：服务端 `/api/audio?kind=solo&track=` 早就就绪、产物就落在 `jobs_dir`（= `TMP_AUDIO`），
前端**从没请求过**。修：`playSolo(tr)` 把该轨音频接进 ENG 的完整播放通道
（进度条/波形/播放头/⏹ 都能用），▶/「↩ 回整曲」换回整曲；删掉谁也读不到的 `<audio id="player">`。
第三层：`loadMaster()` 没有返回值，`if(!ok)` 会永远早退（我第一版就踩了）—— 现在如实返回布尔。

验证走**真浏览器端到端**（`browser_check.js`，真实鼠标点击音轨卡上的 🎧）：
`音源 Melody · 播放中 true · 时长 128.0s · kind=solo 请求 1 次 · solo:Melody done rc=0`。
反证（旧版 app.js）：`没有请求 /api/audio?kind=solo…` · `试听没有接管音源` ·
**`未捕获的异步异常：Cannot set properties of undefined (setting 'src')`** —— 一行就是根因。

途中我自己的验收脚本也踩了一个坑（已写进 PITFALLS 144）：等待逻辑写成"job 为空就退出"，
于是**明明点成功了却判成没点到** → 又点一次 → **两个任务并发渲染同一轨、互相删 `.raw.wav`**
（FileNotFoundError，坑 140 ② 同一族）。修法：点击后先等固定时间，且只有"从头到尾没建过任务"
才回退 DOM `click()`。另外确认了后端 `bridge solo` 本身没问题（手动跑 `rc=0`、128.04s / 3.52MB）。

### 6. 收尾时 selftest 报了 106/107 —— 查清后**不是 bug，是守卫在正常工作**
`determinism_and_bytes`：`42_gtr_tender: 已交付的 gtr_tender.mid 与当前引擎输出不一致`。
查 diff：`songs/42_gtr_tender/song.json` 在 23:32 被写入了 `"mix": {"Melody": [76, 22]}`
（pan=76 正是 events 里的真实值 → 说明是**在面板上拖了 Melody 的音量滑杆**、任务启动时
`saveSong()` 顺手存盘），而交付 MIDI 是 22:20 生成的（音量仍是 100）→ **两者当然不一致**。
这正是 `determinism_and_bytes` 该报的东西（"改了 mix 就要重渲"）。
顺带修掉我自己的一个副作用：`songMixSlot()` 原来**无条件**把 `programs`/`mix` 都建成 `{}`，
于是"只拖了下音量"也往交付物里塞了 `"programs": {}`（已清理，并加了 smoke 断言
"改音量时不许建 programs"）。**处理方式**：`mix.Melody` 是人的改动 → 保留并交给用户定夺
（重渲认下 / 撤回），**不动检查**。（结论：用户选择撤掉那处混音改动 → 已还原，
`song.json` 与暂存区一致、`check_song` 通过、`determinism_and_bytes` 随之恢复。）

## 2026-09-14 第十八批：**推送前把本机绝对路径全部换成占位符**

用户要求"更新到 `https://github.com/wszzssb/music-gen`"（`origin` 本来就是它）。
推完前两批之后做了第二件事：**仓库是公开的，而文档与数据里写着本机绝对路径**。

量出来 **87 个文件 / 169 处**（refs 41 · songs 35 · scripts 5 · docs 2 · studio 1 · 根文档 3），
换成 `<工具链根>` / `<参考曲目录>` / `<仓库根>` / `<临时工作目录>`；
浏览器安装路径（`C:\Program Files\…\msedge.exe`）**保留** —— 那是通用路径，也正是
`tools/browser_check.js` 的探测逻辑本身。`scripts/new_song.py` 里**生成 notes.md 的模板**
也一并改 → 以后新曲目的成绩单不再带本机路径。

**踩的坑（值得记）**：第一版实现是"前缀 replace + 带**单**反斜杠的替换值"，而文本里同时存在
两种写法（命令行/markdown 用单反斜杠，JSON / Python 源码里是转义后的双反斜杠）→ 结果把
`refs/fine_BGM09.json` 写成 `"<参考曲目录>\仰望…\\BGM09.ogg"` → **Invalid escape、JSON 当场损坏**
（`git checkout -- .` 恢复后重做）。第二版改成"**认出根前缀 → 吃掉整条路径 → `<占位符>` + 逐级 `/名字`**"，
占位符与尾部一律正斜杠：JSON / Python / markdown 都合法，而且让
`theme_pack.REF_PROV` 的 `match in file` 子串判断**继续成立**（两边同时被规范化）。

**我的验证脚本自己连踩三次"数据形状"**（与坑 142 同一族）：以为 `REF_PROV` 是"元组的元组"
（其实是 dict 元组）、以为 `refs/**/*.json` 顶层都是 dict（有 list）、以为 `source` 一定是 dict
（有字符串）—— 三次都是"先假设形状、再被数据打脸"。

验证：JSON **219 个全部可解析（0 损坏）** · 参考曲溯源 **命中 5 / 丢失 0** · theme_pack 自测 33/33 ·
selftest **107/107** · smoke_ui · smoke_ed 74/74；远端用 **GitHub raw 读回**确认占位符真的在服务器上，
仓库内 `D:\software` / `D:\test` / `D:\game` / `C:\Users` 命中 **0 行**。
替换后 CHEATSHEET 在估算里贵了 1 tok → 压缩纯格式与一个词（1701 → 1696，**没有抬上限**）。
去路径的脚本留在**仓库外**（不进 git），将来新曲目若又带路径可以再跑一次。

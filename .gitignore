# ============================================================
#  music-gen —— 什么进仓库、什么自己生成
# ============================================================

# ---- ① 环境与依赖：各人自建，绝不入库 ----
.venv/
.venv-ml/

# ---- ② 音源与二进制：有各自许可，不下发 ----
# 跑 `python scripts\setup_soundfont.py` 自动获取（GeneralUser GS v1.471 + fluidsynth.exe）
vendor/

# ---- ③ 大体积产物：随时能用 make_song.py 重新生成 ----
# 音视频体积大、可一条命令重生成，故默认只留谱面（song.json/spec.json/MIDI/notes.md）；
# 仅「很好」的曲目带全套音频，其余跑一次 make_song.py 即出。
songs/**/*.wav
songs/**/*.ogg
# **只带一首开箱可听的示例**：新人 clone 下来第一件事就是"听一耳朵"，仓库一个 ogg 都
# 没有会直接卡住这一步（INSTALL.md 末尾的示例指向它）。
# ⚠ 这里原来有 5 条 `!songs/<旧曲目>/*.ogg` 豁免，2026-09-19 删掉：那些曲目
# （12_d75_warm / 13_d75_rising / 19_d129_tender / 23_d150_skip_along / 25_d150_joy）
# 在"19 首用当前能力从 0 重写"时已被替换 → 豁免**指向不存在的目录**、一条都没生效，
# 于是"仓库带几首成品"变成"一首都没有"，而 INSTALL.md 还在让人去听其中一个（实测：新手照做必扑空）。
!songs/08_smoke_jazz/*.ogg

stems/
export/
# 扒谱/转录中间产物（转录 MIDI + 分轨 + **原曲 wav**）：版权敏感，公开仓库绝不收
# （2026-09-20 加：当时 `_transcribe/` 未排除，`git add -A` 会把原曲 wav 一起推上去）
_transcribe/

# MIDI 参考素材：游戏/动漫 MIDI 多为粉丝转录（版权灰色），只作内部学习，不进仓库。
# 分析结果 refs/midi/_index.json 是统计事实（速度/和声/曲式），可以进。
refs/midi/**/*.mid
refs/midi/**/*.midi

*.preshelf
*.bak_mixfit
*.bak

# ---- ④ 运行时临时物 ----
__pycache__/
*.pyc
*.pyo
_lint_sandbox/
_lint_*/
songs/_*/

# 验收产物（每次跑 studio\tools\browser_check.js 都会覆盖，不进仓库）
studio/tools/_browser_check.png

# 面板里改过的**曲库根**（每台机器的目录不同，只是本机偏好，不入库）
studio/.libpath
# 参考素材目录（`scorecard.ref_dir()` 读它，等价于 `BGM_REF_DIR`）——
# 素材是自备的、且带版权，**绝不入库**（同 `.libpath` 的道理）。
studio/.refdir

# ---- ⑤ 编辑器 / 系统 ----
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store
# 同 §③：`31_f123_night_drift` 也是被 19 首重写替掉的旧曲目 → 这条豁免同样指向不存在的目录
refs/midi2/**/*.mid
refs/midi2/**/*.midi
refs/midi2/_broken/
!refs/midi/mid-*/*.mid

# 面板曲库的本地镜像（junction 目标也在别处）：里面可能放**参考曲原音频**，
# 公开仓库不能收（用户 2026-09-19 要求提交时当场发现 studio_lib/songs/60_b35_imitate/
# 下有 00_原曲_BGM35.ogg）
studio_lib/

# **公有领域古典 MIDI**（2026-09-21 加）：与 refs/midi2 分开存放 ——
#   这一支**只收许可明确的**：Mutopia（PD/CC）与 HuggingFace
#   `TiMauzi/imslp-midi-cc0-1.0` 里**逐行 license=cc0-1.0** 的条目。
#   用户口径："可以抓的都抓，转录许可不明不用，按照风格分类"。
#   来源与许可逐首记在 `refs/classical_pd/_index_*.json`。
!refs/classical_pd/**/*.mid

# **生成的曲目一律不进仓库**（用户 2026-09-21："你生成的先不上传，除了我认为特别好的"）
#   已提交的 29 首（01–19 / 40–43 / 90_new_* / 20_piano_rain）已从索引撤下，磁盘保留。
#   要上传某一首时**显式例外**：加一行 `!songs/<曲名>/`，别放开整个目录。
songs/
!songs/.gitkeep
# 2026-09-24：曲库**拆成两个独立根**（用户："把直接写的歌和模仿写的歌放不同文件夹"）——
#   `songs_direct/` = 直接作曲（主题模板包路径）· `songs_imitate/` = 模仿写歌；
#   而 `songs/` 现在是**指向 `songs_direct/` 的 junction**（兼容层：面板/守卫/脚本的
#   硬编码 `songs/` 路径全部照常工作）。口径完全不变：**生成的曲目一律不进仓库**。
#   ⚠ 不加这两行的话 `git add -A` 会把 31 首曲目（含 ogg/wav）一起推上去。
songs_direct/
songs_imitate/

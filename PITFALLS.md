# 坑台账（当前：161–246）

> 归档的坑见 `PITFALLS-ARCHIVE.md`（1–40、81–88、89–92、93–104、105–108、109–113、
> 114–120、121–126、127–130、131–132、133–143、**144–160**，**编号不变**）。
> 本文件只留"新近 + 高频"的坑 —— 台账会一直长，按 token 预算定期归档。
> 最近一次归档：**144–160**（2026-09-17 —— 台账涨到 15149 tok、超上限 9600 的 58%，
> 自检 `docs_budget_and_skill_intact` 一直 FAIL 时并入；归档脚本 `archive_pitfalls.py`）。
> **2026-09-17 起按「先抬上限、后压缩」处理**（用户定），抬上限的记录与**压缩目标**见
> `scripts/token_audit.py` 的 `LIMITS` 行尾。

## 主题索引 —— **入账新坑前先查这里**

> 同一个坑会以不同面貌反复出现：实测"静默失效"一族散在 **9 条**里，
> 只翻到其中一条 = 看不到全集 = 下次照犯。所以先按主题拿**全部**编号，再逐条读。
> ⚠ 本索引按**标题词频 + 同共享代码标识符**自动聚合（脚本 `pitfall_dup.py`），
> 编号可靠；"一句话"是摘要，**细节以条目正文为准**。

| 主题 | 坑号 |
|---|---|
| **静默失效**（写了却不生效 / 不报错） | 31 62 97 104 108 118 153 162 **182 195 200 206 207 208 213 216** **221 222 223 243** |
| **引擎改了却没进成品** | 141 151 **183 185** |
| **MIDI 的 tick / 单位语义** | 161 **184** |
| **音域与八度** | 10 14 128 131 **182 192** |
| **读数与归因的方法错** | 165 166 167 **186 187 193 194 201 204 209 224 246** |
| **有序规则集**（关键词表 / 映射表：顺序即语义） | **191** |
| **锚点与文本编辑** | **189** |
| **面板与渲染链** | 140 142 144 176 177 178 **214 215 222 223** |
| **交付物与数据不同步**（音频比 MIDI 旧 / 规范化后未重渲） | **216** |
| **编码 / 路径 / 环境** | 12 58 156 170 **184 202** |
| **每份清单都会漂移**（别抄第二份） | 91 118 **188 245** |
| **手写 `song.json` 的结构与写盘口径**（扩段 / 改段长一族 · 报错离真因远） | **242 243 244** |
| **曲风语境**（规则不是普适铁律） | 175 **190** |
| **硬编码判据 / 参数覆盖**（工具的"默认"盖掉用户给的值） | **217** |
| **判据 vs 听感（谁让谁）** | **203** |
| **破坏性操作 / 数据恢复**（移走 · 删除 · 覆盖） | **210 218 222** |
| **两条路径混用 / 依据分层**（直接作曲 · 模仿写歌 · 还原扒带） | **205 243** |


161. **导出 MIDI 时同一 tick 上"按键"必须排在"松键"之后写（off 先于 on）**（2026-09-16）。
     现场：复刻参考曲 `bgm01e`（极简 drone：C/Bb 空五度交替、**每 2 小节一个同音高长音**、
     无鼓无旋律）时，走 **studio 面板**（导入 → 面板里导出 → 渲染）得到的音频**逐段衰减**：
     逐小节中位 RMS 从 −17dB（段首两小节）一路掉到 **−80dB**，整曲动态 **67.8dB**
     （参考曲只有 14.6dB）；而**同一份 MIDI 用 CLI `render_midi.py` 直接渲染完全正常**。
     **根因不在曲子和音源，在 `midi_file.export_midi` 的排序权重**：`track_events` 给
     note-on 权重 3、note-off 权重 4（升序输出 → **on 先于 off**）。同音高**首尾相接**的音
     会让 off 与 on 落在**同一个 tick**（前音终点 == 后音起点），音源的处理是"先起音、
     紧接着被这个 tick 的 off 关掉" —— MIDI 的 note-off **只带音高、不带 id**，
     关的是该音高上**所有**正在响的 voice，于是**新音被吞**。段界处和弦换音高（C↔Bb）
     不冲突，所以听感是"每段头两小节有声、后面没了"，极易误判成"编配/音色/录音问题"。
     **为什么判据全都没拦住**：导出的事件集合、tick、音符表**全部正确**，
     而且导入端按先入先出配对 —— on-before-off 照样配出同样的音符表，
     `midi_file.roundtrip_report` 与自检 `midi_file_editor_roundtrip` 都报"一致"。
     **改法与守卫**：权重改成 `W_OFF=3 < W_ON=4`（提成模块级常量，供变异注入），
     新增自检 `midi_export_noteoff_first`（合成"同音高首尾相接"夹具 fmt 1/0 +
     `refs/midi2` 抽样与 `songs/` 最大几首往返共 3.2 万音，**零违规**才算过），
     并配变异用例"把 W_ON 压到 2"（必须报警）。**验证口径**：修复后"导入→导出"的 MIDI
     渲染指标与源 MIDI **完全相同**（85.6s / 峰值 0.481 / RMS −22.5dBFS / 质心 685Hz）。
     **教训**：这类"顺序敏感"的缺陷**只能靠文件层顺序判据**抓 —— 统计口径（音符数/
     力度/发声时刻集合）对它天然免疫；面板与 CLI 双路径渲染也必须能互相复核。
     ⚠ 面板的渲染缓存键只由"音符数/末拍/BPM/轨数/CC 数"组成，**修好代码后旧缓存仍会命中**
     （指纹没变）→ 必须 `force` 重渲才会看到修复后的音频。

162. **同一首曲子会有「多份 `song.json`」，拿错那份重跑引擎 = 静默覆盖成果**（2026-09-17，
     用户评语"你怎么更差了"）。BGM35 实测两份：**活跃工作区**是
     `b35_studio\songs\99_b35_remake\song.json`（`b35_group_trim.py` / `b35_instrument.py` /
     `b35_melprog.py` 等**全部硬编码写它**），而 `b35_remake\song.json` 是**过时副本**。
     症状极隐蔽：引擎不报错、产物大小甚至相同，只是内容退回旧状态 ——
     实测 **11916 音**（Hook 2070 / Bass 299 / Perc 3849）被换成 **11291 音**（Hook 1350 / Bass 579）。
     **三条纪律**：
     ① **改前先确认基准** —— `b35_midstat.py` 核对**逐轨**音符数（**两份都看**），
        `b35_prog.py` 看 style / programs / melody_prog；
     ② **覆盖前记 SHA256** —— 只有它可靠：本次两版**同为 11916 音、连字节数都是 102814**，
        靠大小完全分辨不出；
     ③ **恢复按指纹**逐个备份跑引擎比对（`b35_hashfind.py`），**绝不叠加任何后处理** ——
        第一次"恢复"在 `pre_r2.bak` 之上多跑了 `instrument` + `melprog`，字节变 `F31CD02…`，
        音色与 CC7 被改，用户仍说"比之前差"。
     ⚠ **v17 真身根本没写 `programs`**（音色走 `style: dance` 预设 + `melody_prog` 逐段轮换），
     正对应用户那句「**我就听 midi 不关音源的事**」→ 换音色（`b35_instrument.py`）是弯路。
     **只改一处的正确姿势**：引擎里 `progs.update()` 是**合并**，写 `{"Glock": [12, 7]}`
     就只改 Glock，其余轨与 `melody_prog` 一个字节不动。
     → 详见 原案例实测台账（已按用户要求删除 2026-09-24） 第 26 条。

163. **改音乐必须分段，一刀切必被否**（2026-09-17）。引擎**每一层都是分段**的：     `arr` 每段一套编配开关 · `notes_extra[轨].target` 是**逐段数组**（26）·
     `melody_prog` / `perc_target` 逐段 · `drum_grid.per_bar` 还能**逐小节**（208 条）。
     而用户的听感反馈**多数指向局部**（"开头乱" / "110s 左右像" / "180s 之后也有一点像"）——
     ⚠ 但**整体判断也常有**（"整首太平" / "太电音" / "不够宽"），别把"局部"当铁律：
     先问清是**哪一段**还是**整首**，整首的反馈要走"全库扫描 + 逐段对照"。
     **反例（都被否掉）**：`glock_oct` 一把设到**全部 22 段** ✗、
     `b35_instrument.py` 一次换掉**全部轨**音色 ✗。
     **正例**：`introThin` 只改前 3 段的 `notes_extra.target`，前 8 小节音符数
     324 → **207**（原曲 202，几乎持平）。
     ⚠ **定位手法 = 出「逐轨 × 逐小节」的表**（范例 `b35_intro_who.py`）——
     BGM35 开头"乱"就是这样定出来的：非鼓部分 190 音 vs 参考 MIDI 201 音（**已对齐**），
     而**鼓 182 音**、第 9 小节从 6 格突变到 24 格（kick 出现 `5,6,7` 与 `11–15` **连续连击**）
     → 所以是**鼓的落点**问题，不是密度、更不是音色。

164. **「吃过的苦」清单：BGM35 这一轮的 6 条，每条都付了代价**（2026-09-17）。

     ① **加层与压音量不能同时做**。给 18 个安静段补 Pad 时，为防"变厚"顺手把
        `mix.Pad` 从 60–68 压到 **45** —— 结果 Pad 占 **20.9% 的发声量**（4.1 拍长音 ×370 个）
        却**几乎听不见**，等于把刚补的垫底又按回去，用户直接回"还点"。
        **一次只动一个变量**；"加了什么"与"调多响"要分两轮验证。

     ② **用户指的方向如果数据不支持，要如实报告，不能照着做**。用户说
        "段23 的 Piano 只有 2 音…重跑转录"，但实测原曲该段 piano stem 只有
        **−77.7 dB（静音）**（邻段 −34/−39、中位 −29）—— 转录是**对的**，补钢琴才是造假。
        **证伪也是成果**：给出证据 + 通用判据（`b35_realgap.py`：只有"原曲能量 >
        中位+1dB 而我方缺席"才算真缺口），比照做一次无用功有价值。

     ③ **对照物口径必须对等，否则是坏尺子**。本次踩了三次：
        Demucs 转录总数**不完整**（段 12 算出 −13 音）；参考 MIDI `BGM35 (1).mid`
        是**钢琴缩编、不含鼓**（拿它比我方"含鼓总数"无意义）；MIDI 音高
        **84 是 C6 不是 C7**（据此算出的"超标 19 倍"实为 1.7 倍）。

     ④ **要测「用户感知的那个量」，别停在代理指标**。用户说"点状"，我先测了
        空洞格（0.5%，正常）、时值中位（0.333 vs 参考 0.302，正常）—— 全是"正常"，
        但听感就是点。换成**按包络类型算发声量**（`Σ(时值 × note_on 力度)`）才看见真因：
        percussion + pluck 占 **54.3%**，Arp 单轨 21.2%。
        ⚠ 算发声量时**别用 note_off 的 velocity**（恒为 0，会让结果全 0）。

     ⑤ **改完必须复算同一个指标来验收**。`b35_deoverlap` 改前后音符数应完全一致
        （12223 → 12223）且逐轨一致 —— 这类"只动时间不改内容"的改动，
        **音符表不变才算对**，变了就是改坏了。

     ⑥ **能固化的立刻固化，别留成一次性后处理**。`b35_deoverlap.py` 作为独立脚本
        已验证有效，但**不在流水线里** → 下次重跑引擎就丢。已改成引擎 opt-in
        `patterns.legato_trim`（默认关 = 全库逐字节不变），并验证两种实现结果等效。

165. **别用频谱指标判断「像不像」—— 用户认可的模板自己就是"频谱不像"的**（2026-09-17，最贵的一课）。
     把用户认可的 `BGM35 (1).mid` **自己渲染一遍**：

     | | 参考原曲 | **模板 BGM35 (1).mid（用户认可）** | 我的 9 轨版 | 我的纯钢琴版 |
     |---|---|---|---|---|
     | 质心 | 3522 | **1035** | 3973 | 1298 |
     | 10000–18000Hz 差 | — | **−47.9 dB** | +0.1 | −33.8 |
     | 平均带差 | — | **14.11 dB** | 1.04 | 12.93 |

     **模板的频谱比我的版本差一个数量级，但用户认可的正是模板。**
     → 「像不像」**只在音符层**（音高集合 / 密度 / 织体 / 起音 / 力度）；
     `band_match` / 质心 / 带差是**成品音频的混音工序**，**不是还原目标**。
     本轮因为搞反这一点，我把频谱"更对齐"的版本当成进步，把用户认可的方向改坏两次。

     **推论（同样重要）**：**删掉高音色层会让 5–18kHz 塌掉**（塌多少取决于编制与音源）——
     去掉鼓（3854 音）+ 木琴后，10000–18000 掉到 −33.8 dB；
     **这是编制的物理结果，不是缺陷**，别拿它否决"纯钢琴/纯弦乐"这类编制方案。


166. **`dt` 宽窗搜索必须显式传原点 —— 一个 bug 让我把"内容完全相同"判成"内容不同"**（2026-09-17）。
     症状：把用户认可的模板 **1:1** 转成新 MIDI（逐对时间差 0.0000s、音高全同，`b35_cmp2.py` 已核对），
     再用 `b35_segalign.py` 互比：`F1(0,0)` 只有 0.02–0.2、`最佳 dt` 恒为 **+3.2s**、`F1(最佳)` 0.80。
     **内容相同的两个文件不可能 F1=0.2** —— 这本身就足以判定"工具坏了"，
     但我先信了数字，还据此写下"我的版本与模板音高随机无关"的结论（差一步就推翻整个方向）。

     根因：为搜 ±3.2s 偏移，把被评窗口放宽成 `[a-3.2, b+3.2]`，却仍以 `a-3.2` 当格号原点
     → 被评格号整体 **+32 格**（3.2s / 0.1s）；"最佳 dt"只是把这个偏移补回来，
     于是**每一组对比都被系统性地抬高 +3.2s**。
     修法：`seg_arrays(..., origin=a)`（宽窗 + 显式原点）；格号用 `np.round(t/0.1)` 而非 `floor(t/0.1+eps)`
     （后者对格边界抖动敏感）。

     **三条纪律**：
       ① **新指标先用已知答案自检**：拿"内容相同的两份文件"跑一遍，F1 必须接近上限才允许用来判断别的；
       ② **记住尺子的天花板**：本尺子对恒等输入是 **0.75–0.94**（16 分格边界抖动），**不是 1.0**；
          报数必须说明"相对天花板"，否则 0.30 会被误读成"很差"；
       ③ **两份独立实现的换算要互检**：`b35_cmp2.py` 打印"逐对时间差中位 0.0000s"就是互检动作。

     附带坑：`pwsh` 的 `>` 重定向可能写出 **UTF-16**（文件含 NUL），`read` 工具会判成 binary，
     解析脚本要按 `utf-8-sig → utf-16` 兜底解码。


167. **`F, P, R = f1_of(...)` 会把路径常量 `R` 覆盖成 float**（2026-09-17，一次会话踩两次）。
     写评估脚本时的常规写法：
     ```python
     R = r"D:\test\llm_direct\b35_remake"     # 路径常量
     ...
     F, P, R = f1_of(ct, keymap(ns))          # ← 把 R 覆盖成 float
     ...
     ns = notes_sec(os.path.join(R, f))       # TypeError: expected str … not float
     ```
     **规则**：路径常量别用 `R` 这种单字母大写（用 `RB`/`ROOT`），或把指标变量写成
     `f1v, prec, rec`。排查"上一轮还跑得好好的、这一轮 TypeError"时，
     先 grep 有没有把单字母常量当结果变量用。


168. **"全频段逐样本相减"不能用来判断"内容是否相同" —— 能量大的成分会主导残差**（2026-09-17）。
     场景：用户说 `BGM34.ogg` = `BGM35.ogg` + 人声，让我对比。我做了时间对齐、最小二乘增益、
     全曲逐样本相减，得到 **相关 0.716、残差 ≈ 原曲能量**，于是写下"两曲器乐不同 / BGM35 是另一版混音"。
     **用户直接质疑"背景音乐肯定是一样的" —— 用户是对的。**

     错因：**残差被能量占比最大的成分主导**，这里就是人声。两条廉价而正确的判据：
       · **分频段看**：低频几乎不受人声影响 → 实测 20–200Hz **相关 0.9978、残差 6.7%**（骨架相同）；
         差异全部集中在 200–2000Hz（人声带）。
       · **先分离出差异成分再比**：用 htdemucs 分出 BGM34 的人声 → "去人声 vs BGM35" 相关
         **0.716 → 0.9492**、残差 97.6% → 33.2%；而"人声 vs BGM35" 相关 0.075（确实无关）。
     辅助判据：**逐秒动态对齐**（每 1s 各自拟合增益）—— 这里 P90 相关 **0.998**，
     说明"纯器乐处几乎逐样本相同"，全域 0.716 是被有人声的那些秒拉低的。

     **纪律**：判断"两段音频是不是同一内容"，先问"差异成分占多少能量"；
     用**分频段**或**先成分离再比**，永远不要只看全域一个相关数就下"内容不同"的结论。


169. **只优化"间隙干净度"会悄悄削掉弱人声 —— 降噪类改动必须同时看"保护区"指标**（2026-09-17）。
     场景：用户说差分人声"两个人声中间有一点噪音"。我一路只盯"间隙残留"一个指标做优化：
     逐频点增益补偿 → AI 清理 → **Wiener 掩码融合**（拿 htdemucs_ft 的掩码去筛差分信号），
     三版下来间隙数值看着在降，结果**用户一听就否**："v2/ft 每段开头人声很小，都不如 final"。

     **补上第二个指标立刻现形**（同一文件、同一 VAD 基准）：

     | 版本 | 强人声段 | 弱人声段 | 间隙 |
     |---|---|---|---|
     | **final（用户认可）** | 0.18651 | 0.06943 | **0.00156** |
     | ft（fine-tune 模型） | 0.15432 | 0.05850 | 0.02067 |
     | v2（掩码融合） | 0.14210（**−2.4 dB**） | 0.05211（**−2.5 dB**） | 0.01416 |

     根因：**掩码/Wiener 类处理在所有时刻都生效**，把气声、弱起唱这些"不太像人声"的成分
     一并压掉；而**门控只在间隙开合、人声段原样通过** —— 所以 final 三项全胜，
     "换更强模型 + 融合"反而全输。

     **纪律**：
       ① 任何降噪/净化类改动，必须**同时**报告"目标区（要压的）"与"保护区（不能伤的）"两个指标；
       ② **单一指标一路变好是危险信号** —— 先怀疑指标漏了什么，而不是庆祝进步；
       ③ 分区用 VAD 时，**弱人声段**（活跃段里能量最低的 30%）是最容易被误伤、最该盯的区间。





170. **`<开关> name=path:opt` 这类参数别用 `:` 当字段分隔符 —— Windows 路径自带 `D:`**（2026-09-17，本仓库犯两次）。
     第一次在 `ensemble_transcribe.py`：`--source 名字=文件:低:高:类别`，
     实测报 `invalid literal for int() with base 10: '\\test\\llm_direct\\...'` —— 盘符被切开了；
     改成 `|` 后正常。
     第二次在 `mix_stems.py`：`--track 名字:文件:份额`，报
     `could not convert string to float: '/test/.../Acoustic Piano.wav'`，同一个原因。
     **规则**：CLI 里凡是"名字 + 路径 + 若干参数"打包成一个字符串的，分隔符一律避开 `:`（用 `|` 或 `,`）。
     顺带：路径里可能含**空格**（如 `Acoustic Piano.wav`），shell 侧要加引号。


171. **OGG 不能当"逐字节相同"的判据 —— ffmpeg 的 ogg 封装每次写不同的随机 serial**（2026-09-17）。
     场景：把渲染链里四个频域滤波合并成一次 FFT 后验证等价性，看到
     `old.ogg` 与 `new.ogg` 的 SHA256 不同（B2F29EA1… vs 50CA861A…），
     差点当成"改动引入了差异"去回滚。
     实测证伪：**同一个 WAV 连编两次**，两次 ogg 的 SHA256 也不同
     （58b230C4… vs 9BE10111…），但**解码回来的 PCM 逐位相同**。
     根因：ogg 页头带随机 bitstream serial number，与音频内容无关。
     **规则**：① 等价性/可复现性验收一律比 **WAV 或解码后的 PCM**，不比 ogg 字节；
     ② 本次最终判据 = 合并前后 WAV **逐字节相同**（SHA256 同为 E1BE6E3C…，逐样本最大差 0.000e+00）。


172. **"原地改数组"的函数 + "跑两遍对比/计时"写在同一段里 → 自检假 PASS、差异假 FAIL**（2026-09-17）。
     场景：新写的 `apply_chain_np()` 沿用旧滤波函数**原地写回**的语义（返回值就是入参），
     而验证脚本写成：
     ```python
     a = old_path(x, **case)              # a 是副本
     b = R.apply_chain_np(x, **case)      # b is x —— x 已被原地改过
     b2 = R.apply_chain_np(x, **case)     # 同一个对象再滤一遍
     det = np.array_equal(b, b2)          # 恒 True（b、b2 是同一个数组）
     d = np.max(np.abs(a - b))            # 此时 b 已是"滤了两遍"的值！
     ```
     结果：自检报"两遍一致=True"（假），差异报 **0.447**（假 —— 差了两级滤波的量级），
     而单级/两级分开测的真实差异只有 **5e-16**。
     **规则**：① 比对前先 `.copy()`；② "同函数跑两遍一致性"必须用**两个独立副本**；
     ③ 看到"等价性突然差 0.4"这种大数，先怀疑**验证脚本**，再怀疑实现。


173. **工具链脚本顶层 import 重依赖（torch/demucs）→ 主 venv 的自检 `import_all` 直接 FAIL**（2026-09-17）。
     场景：新增 `stem_split.py` 顶部 `import torch`，而自检跑在工具链主 venv（**没有 torch**，
     torch 只装在 `.venv-ml`）→ `FAIL import_all: stem_split: No module named 'torch'`。
     同一条自检还挂着 `console_encoding_safe`：**每个带 `if __name__` 的脚本都必须调 `cli_utf8.setup()`**
     —— 新增的 `mix_stems.py / stem_split.py / timbre_candidates.py / transcribe_ymt3.py` 都没做。
     **规则**：① 只在某个 venv 里装得上的依赖（torch/lightning/demucs/librosa…）一律**函数内惰性 import**；
     ② 新脚本照抄这个收尾：
     ```python
     if __name__ == "__main__":
         try:
             import cli_utf8 as _cu; _cu.setup()
         except Exception:
             pass
         main()
     ```
     ③ 惰性 import 不能只是"把 FAIL 藏起来"—— 还要在**正确的 venv 里真调一次**求证
     （本次实测 `.venv-ml` 下 `torch 2.11.0+cu128`、`apply_model`、`get_model` 都取得到）。


174. **逐带比较必须先统一采样率；"绝对带差"里混着整体电平差**（2026-09-17）。
     场景：给 BGM29 补完低音，`band_match` 报"补偿后 ≈ 0.33 dB"，我的体检脚本却报
     "20-40Hz 仍差 −7.4 dB" —— 两个数字不可能同时对，于是查口径，查出**两个错**：

     ① **`metrics._avg_spec` 用固定 8192 点窗**（不按采样率折算）：44.1kHz 下 186ms、
        96kHz 下只有 85ms（分辨率 5.4 vs 11.7Hz），而 20-40 带只含 ~4 / ~2 个 bin。
        同一段音频实测：BGM29 参考的 20-40 带 @44.1k **−9.1** vs @96k **−7.8**（差 1.3dB）、
        BGM35 参考差 **2.6dB**。两首参考都是 **96kHz**，我方渲染一律 44.1kHz
        → 此前所有"低频差 7~9 dB"的结论都掺了 1~2.6 dB 的纯口径假象。
     ② **`band_energy_db` 给的是绝对 dB**：把成品 RMS 从 −15.0 归一到 −16.1，
        十条带会**一起**掉 1.1 dB —— 看起来"频谱全歪"，其实平衡没变。
        实测同一平衡的两个文件（宽度/RMS 不同的交付版）：
        绝对带差 0.76 → 1.40 dB，而**相对带差恒为 0.95 dB**。

     **规则**：① 跨采样率比较一律**先统一到 44.1kHz**（`band_match` 早就这么做，别绕开它）；
     ② 报数要**同时**给"绝对带差（含电平）"与"**相对带差**（每带先减本曲 80-160Hz，看平衡）"，
     ③ 工具：`scripts/band_grade.py`（本次新增，已内置这两条）。


175. **"固定参数套所有曲子"必翻车 —— 低频策略要**从参考曲自己推**（2026-09-17）。
     场景：给 BGM29 调出"不切低频(hp 0) + 加低八度 sub 层"，平均相对带差 **2.61 → 0.82 dB**，
     效果很好；于是把同一套固定参数直接写进通用流水线去跑 BGM35 →
     **平均相对带差 6.46 dB**（比它自己的手工交付版 1.57 差 4 倍），
     最差带 20-40 相对 **+10.95 dB**（低频直接爆表）。

     根因：**两首参考曲的 sub 含量差了 18 dB**（同一个"20-40 相对本曲 80-160"口径）：

     | 参考曲 | 20-40 相对 80-160 | 含义 |
     |---|---|---|
     | BGM29 | **−7.9 dB** | 有 sub，"低音是面不是点" → 要 hp 0 + 加 sub |
     | BGM35（**仅那一首**） | **−18.2 dB** | 母带基本把 sub 切了 → 该曲要高通、且**别给它加 sub**；别的曲子别照搬这条 |

     加错方向的后果是链式的：sub 把 40-160 抬高 → `band_match` 为对齐参考又把它整体压 4.5 dB
     → 于是 160Hz 以上相对 80-160 全线虚高（+4~+7.5 dB），看着像"高频太多"。

     **修法**：`imitate_ref.py` 先量参考曲的低频含量再决定参数
     （`rel < −15 → hp36/不加 · < −10 → hp28 · < −6 → hp18 · 否则 hp0+加 sub`），
     并允许 `--hp/--sub/--no-sub` 覆盖。改完 BGM35 回到 **1.60 dB**（手工版 1.57）。

     **纪律**：凡是"在某首曲子上调出来的参数"，进通用流程前必须**问它依赖参考曲的哪个可观测量**；
     推不出依赖关系的参数，一律先跑第二首曲子验证，再决定要不要固化。


176. **往 studio 面板里放"转录取的成品"，要过三道门；缺任何一道的表现都是"卷帘空白 + 一行 TypeError"**
     （2026-09-17，用户报"`99_bgm29_remake` 里的四个版本怎么没找到"，随后又报"怎么回事"。
     **我第一次归因错了**——以为是"缺 song.json"，其实那只是第一道门；真正的崩因在第三道。）

     ① **曲目 = 有 `song.json` 的子目录**：`songs_list()` 只认这个
        （`probe_lib` 的 nested/flat/single 三种布局都以此为准据）。
        转录/还原类产物默认只有 .ogg/.mid → 面板里**根本不出现**。
     ② **面板进程得真的换掉**：`--lib` 指成了单个曲目目录（`...\songs\99_b35_remake`）时下拉里只有一首；
        而且**端口被旧进程占着 → 新进程 `exit 1` 静默失败，旧进程继续服务**
        （你以为重启了，其实页面还是旧的）。→ 先 `netstat` 看 8765 上是谁，必要时 `Stop-Process`；
        旧 `--lib` 还会持久化在 `studio/.libpath`。
     ③ **`song.json` 必须能被 `scripts/song_events.py` 跑通**，否则 `/api/song` 的 `events = None`
        → 前端读 `undefined.ref` → `TypeError: Cannot read properties of undefined (reading 'ref')`
        → 卷帘只剩一行"绘制错误"。**实测的真正根因**：
        `song_engine.build_events` 是**按小节索引取和弦**的（`song_engine.py:1068`
        `nn = sec['chords'][i + 1]`），**`len(sections[i].chords)` 必须 ≥ `bars`**。
        我拿引擎曲目的 song.json 当模板，把"8 小节 / 8 个和弦"的段落直接改成"133 小节" →
        `IndexError: list index out of range` → events=None。修法：把和弦列表**循环铺满 bars 个**。
        另外 `arr`/`melody` 可以为空（实测 `fill=empty` 也能生成 events、卷帘显示 0 音符，
        这恰恰正确 —— 转录曲目本来就没有引擎编配内容）。
     ④ 附带：`/api/audio?kind=mix` 只认 `<曲目目录>/<render.json.out>.ogg`（再退 .wav），
        所以**一个目录只能代表一个成品**；多版本必须**各自建目录**
        （song.json + render.json(out=version) + version.ogg + render.json.mid 指向的 .mid）。

     **诊断顺序（别猜前端）**：`curl '/api/song?id=<曲目>'` 看 `events` 是否为 null →
     是就直接 `python scripts/song_events.py <曲目>/song.json --json` 看真实异常栈。

     **成品**：`studio_lib/songs/99_bgm29_remake`（定版）+ `99_bgm29_old` / `99_bgm29_leadEP` /
     `99_bgm29_leadVib` + `99_b35_v5` / `99_b35_pipeline` —— 六个都能在面板里直接选、直接播
     （各自 events 正常：BGM29 四轨 / BGM35 九轨）。


177. **canvas 尺寸**永远不要写内联 `style.width`** —— 它会**自我锁定**，表现为"两个元素长度不一样"**
     （2026-09-17，用户截图问"长度怎么不一样"）。
     现场：`#wave`（波形）铺满整宽，`#roll`（钢琴卷帘）只到 ~64% 就断了，
     而底部小节标尺又是铺满的（133 小节）—— 看着像"曲目长度不对"。

     **根因**（三处都在干同一件错事）：
     ```js
     const cssW = Math.max(320, c.clientWidth || …);   // ← 读**自己**
     if (c.width !== wantW) { c.width = wantW; c.style.width = cssW + 'px'; }  // ← 写**内联**
     ```
     写了内联之后，**下一帧 `c.clientWidth` 就等于那个内联值** —— 于是：
     ① 宽度再也不会随窗口变化（`#wave` 用纯 CSS `width:100%`，会跟着变 → 两者不等宽）；
     ② 连"卷帘自维持"循环（`rAF` 里每帧查尺寸那条）也一起被锁死，因为它读的也是 `c.clientWidth`。

     **修法**：宽度来源一律取**父容器**（`c.parentElement.clientWidth`），并且
     **只设像素缓冲 `c.width`，不写 `c.style.width`** —— 显示宽度交给 CSS 的 `#roll{width:100%}`。
     三处都要改：`rollGeom()`、`renderRoll()` 的空画布兜底、`rAF()` 的自维持。
     改完把 `index.html` 的 `app.js?v=18` 提到 `v=19`（配合 server 已有的
     `no-store` 双保险）。

     **旁证**：`ed.js` 的卷帘一直是好的 —— 它本来就写 `cv.parentElement.clientWidth - 4`
     （父容器），所以从没锁死过。**同一个仓库里两种写法，一种有病一种没有，对照就是证据。**


178. **"每帧都发一次滑动窗口调度" → 同一个音被排 90 次**（编辑器播放糊/爆音的根因）（2026-09-17）。
     用户报："编辑器播放 `bgm29_v3_source.mid`，**9 秒**有问题"（并明确"不是歌的问题，是编辑器的问题"）。

     **根因**：`tickPlay()` **每帧**调
     ```js
     scheduleWindow(S.posBeat, S.posBeat + 1.5, w.currentTime - (S.posBeat - S.x0) * SPB());
     ```
     —— 提前量 1.5 秒的**滑动窗口**；而 `scheduleWindow` 对**窗口里每个音**都 `playTone` 一次，
     `S.live` 的 key 又是自增序号（`'k' + S.seq++`，天然不去重）。
     于是一个音从"进入窗口"到"真正响起"会被调度 **1.5s / 16.7ms ≈ 90 次**，
     90 组振荡器压在同一时刻的同一个音上 → 密集段直接糊成一团/爆音。
     BGM29 第 9 秒附近 3.5 秒内恰好有 **107 个音**（Drums 54 · Synth Lead 18 ·
     Chromatic Percussion 14 · Bass 15 · Pad 3 · Sub 3）→ 最严重的地方正好是那里。

     **修法**：加**去重游标** `S.schedUpTo` —— 只调度"上次之后新进入窗口"的那一段；
     `fromBeat < schedUpTo - 0.05` 判定为 seek / 循环回跳并自动重置。

     同一轮顺手修的两处同类问题：
     ① **限流杀振荡器用瞬断**（`victim.stop()`）—— 每次 kill 一次咔哒；改成 6ms 淡出
        （新增 `fadeStop(w, g, parts, fade)`）。
     ② **`stopPlay()` 不清理已排进时间轴的音** —— 按下停止后还会继续响 ~1.2 秒，
        而且那些 entry 一直占着振荡器预算 → 再按播放时**开头一串音被限流杀掉**（"开头没声"）。

     **教训**（比这个 bug 本身更值钱）：
     这个模块的注释里早写着"用户报卡卡的""只有开头一秒有声音"，而当时的修法是**加限流**
     （按振荡器预算 160/40）—— 那只是把爆音压成"爆音 + 咔哒"，**根因一次都没碰**。
     **遇到"声音糊/卡/某个位置不对"，先数"同一个音被排了几次"**（`S.pt.n` vs 音符数），
     而不是先去调预算、换音色、改方程。


179. **`| grep 关键字` 会把失败连同 traceback 一起吞掉，还让管道返回 0**（2026-09-17，我自己踩）。
     场景：给低音集成加"八度校正"后重跑流水线，脚本写成
     ```bash
     python imitate_ref.py … 2>&1 | grep -E '低频策略|阈值|八度校正|替换轨|成品'
     ```
     屏幕上只看到前几行，**没有"八度校正"那行**，我以为"跑过了、只是没输出"，
     于是拿**没被改动的旧 song.mid** 复测，得到与校正前**逐位相同**的五个数
     （4635/2419/1721/785/1261）—— 还照着这组数字往下写了结论。
     真相：`bass_ensemble.py` 里新加的校正用了 `np` 但**模块没 import numpy** →
     `NameError` → stage 5 直接崩；而 `| grep` 把 traceback 滤掉、退出码是 grep 的 0。

     **两次同一族的坑**（另一次见"`*>` 重定向吞掉 OGG 编码失败"）：**过滤输出 ≠ 检查结果**。
     **规则**：① 长流程要么**不过滤**（写日志文件再 tail），要么**先看退出码**再过滤
     （`cmd > log 2>&1; rc=$?; [ $rc != 0 ] && tail log`）；
     ② 复测数字**与上一轮逐位相同**时，第一反应是"改动没生效"，不是"改动无效"。


180. **还原任务的第一道工序是"量识别精度"，不是调混音**（用户 2026-09-17 定的规矩）。
     用户先问"**先看看你是不是识别不准吗**"，随后明确："**以后要先确定识别问题**"。

     **为什么必须排在前面**：混音/音色/EQ/宽度全是**在转录结果之上**的加工。
     识别错一个八度时，这些指标**照样会变好** —— 于是会一路认真地加工一个错东西，
     而且每改一轮都"有进步"。BGM29 实测：
       · 低音补到 1416 音 → 平均相对带差 **2.61 → 0.83 dB**（看起来大好）；
       · 同一批音，`transcribe_audit.py` 逐帧比参考分轨：**一致率只有 48.5%**，
         **差整整一个八度占 25.3%** —— 也就是"最好的那一版"里，低音有 1/4 时间是错的。

     **怎么量**（`scripts/transcribe_audit.py`，新增）：拿 **demucs 分出来的参考分轨**当基准
     （内容就是原曲本身），用 **pyin** 提基频 —— 它与 YourMT3/Basic Pitch **完全独立**，
     三方都错才会同时错。出五个数：一致 / 差八度 / 音高不同 / 漏检 / 假音。
     **判据：一致率 < 70% 就先修识别**，别去调混音、音色、EQ。

     **怎么修**（`scripts/bass_ensemble.py --octave-ref <参考bass分轨>`）：
     只改**正好 ±12/±24** 的音（别的音程一律不动，可能是和弦内音、或 pyin 自己错）。
     实测一致率 **48.5% → 69.9%**、八度错误 **25.3% → 2.3%**
     （`--min-prob 扫描`：0.35→64.9% · **0.20→69.9%** · 0.12→70.1%，拐点在 0.20）。

     **三条必须一起记住的**：
     ① ⚠ **多模型集成本来就修不掉系统性错误** —— YMT3/BP4/BP6 犯的是同一个错
        （把泛音当基频），交叉验证只会让错误互相印证。**必须引入独立方法当尺子**。
     ② ⚠ **复音层不能用 pyin 量**：钢琴+吉他叠在一起时它只给一条最强基频，
        实测 `other` 层报"假音 66.5%"—— 那是**方法本身**的假数，不是我们的错。
        只对 bass 这类单音层有效。
     ③ **代价要认**：校正后 sub 低音层 705→252 音、带差 0.83→1.26 dB ——
        旧版那个漂亮数字**有一部分是错音撑起来的**，不能拿它当"改进"。


181. **写进文档的"实测"也要当场跑一遍 —— 转述会失真**（2026-09-17）。
     展开 `~/.dsh/docs/SHELL-NOTES.md` 时，我照抄了旧记录"`Set-Content -Encoding UTF8`
     会把**中文变成乱码**"。用户说"验证一下"，实测**那条是错的** —— 写出的是
     **带 BOM 的正确 UTF-8**（首字节 `EF BB BF`，内容一个字节没坏），真正会坏事的是 **BOM**
     （`json.load(utf-8)` 报 `Unexpected UTF-8 BOM`，`utf-8-sig` 可解）；
     "整文件变乱码"更像**另一条坑**（`>` 重定向写 UTF-16LE、首字节 `FF FE`）被记串了。
     **纪律**：凡标"实测"，落笔前当场跑一次；转述别人的记录必须标"未复验"。
     （同轮其余各条实测属实，清单见 `~/.dsh/docs/SHELL-NOTES.md`）

182. **引擎的四处"静默失败"：成品走样，一声不吭**（2026-09-18，BGM35 从零还原时全部踩中）。
     四条已全部改成**作曲时当场拦住**（`song_engine.load` / `build_events`）：
     ① **`melody` 没人引用** —— `melody` 里 A/B/C/D 四支共 224 个音写得好好的，但 `sections`
        漏了 `"melody": "A"` → composer 只打印一句 `(跳过空轨: Melody)`，**整轨没有旋律**，
        而所有校验全绿（守卫量的是 melody 键，键本身没问题）。现在 `SystemExit`。
     ② **旋律小节号写成了全局号** —— 小节号必须是**段内**的。把 56 小节的旋律挂到 3 小节的
        `Ending` 上，53 个音静默消失。现在按段长校验并报错。
     ③ **`notes_extra` 原样接受越界音高** —— 分轨转录会给出不属于该乐器的音：
        实测 `ymt3_6s_bass` 输出 **28–93**（真贝斯 16–71），写进去就是"次声波贝斯 +
        中音区幽灵"，由守卫 `track_ranges_musical` 抓到（`Bass 实际 29-93（合理 16-71）`）。
        现在按 `TR_RANGE` 修正：整轨移八度能进就移，移不进就丢掉越界音（宁缺勿错）——
        实测一次丢掉 677 个越界音，`track_ranges_musical` 从 4 轨越界降到 1 轨。
     ④ **`patterns` 连白名单都没有** —— 键名拼错 = 那一项完全不生效且不打印
        （`arr` 至少还有警告）。现在引擎自声明 `PAT_KEYS`（28 项），`load()` 校验未知键。
     **教训**：这四条都属于"写了却不生效"——它比报错危险得多，因为**所有校验都能是绿的**。

183. **`make_song` 的两个陷阱，各耗掉我好几轮**（2026-09-18）。
     ① **首次跑必然卡死**：`composer` 从 `render.json` 读，而 `render.json` 由**本次渲染写出**
        → 首次恒为空 → 打印「[1/3] 跳过作曲」→ 紧接着「找不到 MIDI」。而那句提示
        「第一次跑不要加 `--no-compose`（要先生成 MIDI）」是**误导**（根本没加那个开关）。
        已修：没有 `render.json` 时，目录里有 `compose.py` 就默认用它。
     ② **见到已有 MIDI 就跳过作曲** —— 改了 `song.json` 但没删旧 MIDI，改动**一点不进成品**。
        症状是"逐带偏差一位数字都没变"，极易误判成"参数没生效/被覆盖"。
        **纪律**：改数据后先 `rm -f <曲目>/*.mid`，再看结果 —— 或者直接跑
        `make_song.py <曲目>`（它默认就会先作曲，见上面 ①）。
        ⚠ 我为此把责任错推给 `arr_by_role`（见 187）。

184. **转录工具的四个数据陷阱**（2026-09-18，全部实测）。
     ① **`mido` 的 `msg.time` 是 ticks 不是秒** —— 当秒用会让全曲音**挤进第 0 格**
        （实测"转录只覆盖 1 个小节"）。正解：`MidiFile.length`（mido 算好的秒数）除以总 tick
        得换算系数，不必假设文件 tempo。我因此给出过**错的八度错误率**。
     ② **主 venv 没有 `mido`**，`.venv-ml` 才有 → 读 MIDI 的脚本要用 `.venv-ml` 跑。
     ③ **`midi_file.import_midi` 的返回不是"每音一个元组"** —— `n[0]` 是字符串 `'f'`，
        直接索引报 `could not convert string to float`。别猜结构，先打印一个。
     ④ **MSYS 的 `/tmp` ≠ Windows python 眼里的 `\tmp`** —— 日志写 `/tmp/x.log` 再用
        Windows python 读，`FileNotFoundError`。要么落工作区，要么落仓库内路径。

185. **还原/扒带要"写进 `song.json` 让引擎编配"，不是"手工拼 MIDI"**（2026-09-18，最大的一条）。
     我一开始绕开引擎：转录出 MIDI → 手动 `gain` 拼轨 → 直接 `render_midi`。
     结果**自己重造了引擎一半的功能还做不全**：音色靠猜、平衡调 4 轮没调对、
     60Hz 段差 5dB 以上。正确入口是 **`notes_extra`**（"从 Demucs 分轨各自扒出的音符按轨写进来"），
     引擎会自己编配/分配音色/按 `density` 抽样服从段落结构/混音/调参。
     **对照证据**：同一批转录音符，走引擎（`notes_extra` 23033 音）后 5000–10000Hz 的
     缺口从 **−12.6dB 收到 −3.8dB**，而我只手工拼时这一段一直是硬伤。
     ⚠ 还原场景记得开 `patterns.notes_extra_full`（默认会按 density 抽样，那是为"服从段落结构"
     设计的，还原要的是忠实；这个取舍该由人做）。

186. **用户说"不能参照现成模板内容"≠"不能用工具"**（2026-09-18，我自己理解反了，白烧十几轮）。
     用户的原话是"**不能用已经做好的关于它的模板内容**"（指 `BGM35 (1).mid` 这类成品、
     以及配方文档），**工具随便用**。我却把它听成"不能用工具"，于是：
     拿那份模板 MIDI 当基准刷了一整轮音符 F1（0.333→0.771），而真正该用的
     `band_match` / `make_song` / 引擎编配层**一个都没碰**。
     **更要紧的是**：那份模板其实是**单轨钢琴改编谱**（GM0 钢琴、120BPM、5730 音），
     把它当"原曲"去优化，只会做出一首**钢琴独奏**（质心 308Hz，原曲 4027Hz）。
     **纪律**：口径不清时先问，别按自己的理解闷头做一轮。

187. **读数与归因的三连错**（2026-09-18，同一轮里犯的，都必须当场自检）。
     ① **把偏差表读反**：`b35_bands.py` 第一列是原曲的绝对形状、后面全是**相对偏差**，
        我却把偏差当绝对值，得出"20–40Hz 多了 18dB"，真相是**少了 5.4dB** —— 方向反了。
     ② **口径未自检就报数**：八度错误率按"占未匹配"算，小分母把 63% 放大得吓人，
        实际只占全曲 1.7%；换正确口径后是 **2.3%**。**报数前先跑恒等输入自检**
        （同 `eval_transcription --self-test` 的做法）。
     ③ **归因错**：把"改了 `arr` 毫无变化"归给 `arr_by_role` 覆盖 —— 实际那个函数全仓库
        只有 `new_song.py` 调，作曲路径根本不走；真因是 183② 的"跳过作曲"。
     **纪律**：**先证明"是它"，再改它**；改完数字没动，第一件事是查"改动有没有真的进到产物里"。

188. **人工抄一份清单 = 埋一处漂移（现场复现）**（2026-09-18）。
     给 `song.json` 做语义体检时，我在体检脚本里**人工列**了一张 `patterns` 白名单，
     结果把引擎**合法使用**的 `voicing_shift` / `sub_dur` / `sub_gain` 报成"无效键"，
     **18/18 首全部误报**。查证：`song_engine.py:117` 有默认值、`:745`/`:1088` 在用、
     `selftest.py:301` 还有专门的 `t_voicing_shift`。
     **修法**：白名单只在**引擎里声明一份**（`PAT_KEYS`），别处一律引用。
     这正是 `docs/CONVENTION.md` §1「能由代码回答的别抄进文档」的现场版 —— 那条不只是文档纪律，
     **脚本里抄清单同样会漂移**。

189. **`edit` 的锚点：尾换行会吃掉下一行，短锚点会命中上千次**（2026-09-18）。
     ① 我拿 `"ARR_KEYS = (...)  'arp',\n"`（**带尾换行**）作锚点、新串**不带**换行 →
        `ARR_KEYS` 与下一行**被合并成一行**，差点改坏引擎（靠 read 现场 + 立刻修回）。
     ② 用 `)` 作锚点 → **匹配 1342 次**直接失败（`grep` 的 `^)$` 是行锚定，`edit` 是子串匹配，
        两者不是一回事）。`docs/CONVENTION.md` §4-D 早写了"锚点要够长（≥6 连续字符）或整行"，
        我仍两次违反。**纪律**：多行锚点的首尾都要**避开行边界**，改完立刻 read 复核。

190. **"段落等长"要看曲风，不能一刀切**（2026-09-18，用户当场纠正）。
     我扫出"10 首的所有主体段落都是 8 小节"就判为结构同质化 —— 用户指出：
     **古典的段落本来就是规整的，现代大多数才不等长，要看情况**。
     `docs/CASE-BGM36.md` 记的"段落不能等长"是**针对那首抒情向案例**的观察，不是普适铁律。
     **纪律**：把个案观察写成规则前，先问"这条适用于哪些曲风"；报告问题时带上曲风语境。

191. **轨名关键词规则会互相打架，顺序就是语义**（2026-09-18，从 300 首模板抽音色时）。
     按轨名归类踩了两下：① `bass` 规则排在 `drum` **之前** → `BassDrum` 被判成**贝斯**；
     ② `Bassoon`（巴松 70）/`Bass Clarinet` 都含 "bass" → classic 主题的**贝斯众数
     一度是"巴松"**，整份音色依据就错了（修法：给 `bass` 规则看"摘掉巴松"的串）。
     ⚠ 但**别把清理后的串给所有规则用** —— 我第一版这么做，`Woodwind` 规则里的 `bassoon`
     也一起失效，巴松从"贝斯"掉进 `Piano` 池（错得更隐蔽）。
     **纪律**：关键词表是**有序规则集**，写时同时确认①顺序②清理串的作用域。

192. **音域兜底会凭空造出模板里根本没有的声部**（2026-09-18，同上）。
     "最高音轨判旋律、最低音轨判贝斯"看着稳妥 —— 实测 `classic` 的 10 首里 **5 首是
     纯钢琴曲**（只 1~2 轨），左手低音区被兜底判成"贝斯声部"，于是 `Bass` 池里全是
     巴松/长号/大键琴，**没一个是贝斯**。
     **纪律**：**轨名优先；音域兜底只在"整首都没有可识别轨名"时启用**（两遍扫描：先看
     有没有名字，再决定要不要兜底）。**"模板里没有这个声部"是真信息**，不是待补的空。

193. **重跑生成器会冲掉人工标定值 → 改走"只注入一个字段"**（2026-09-18，同上）。
     给主题包加字段的第一反应是改 `theme_pack.py` 再重跑 —— 但 `mix_target.energy_gain`
     / `calibration` 是**标定流程**逐主题测出来写进 pack 的（`theme_pack.py:1437` 明写
     "不自动写"），重跑会**整包重建**把它们冲掉。改成独立注入：读 pack → 只更新目标字段
     → 原样写回，并**逐文件核对"除该字段外完全一致"**（`newline='\n'` 必写，否则 Windows
     默认 CRLF → 整文件 diff）。
     **纪律**：**凡起"重跑生成器"的念头，先查那份产物里有没有人工/标定写入的字段**。

194. **守卫的判据别对着"事实"收紧**（2026-09-18，同上）。
     新守卫要求"音色池里每个值都是 0-95 的配器音色"，第一轮就误报两条：`cheerful` 的
     **114**（钢鼓）、`daily` 的 **111**（唢呐）—— 它们是 GM 112-119 打击组与 104-111
     ethnic 族，**模板里真有这些轨**，池记录的正是事实。
     **正确切分**：**池判"是不是合法 GM（0-127）"，"能不能用"判在取用层**（在
     `theme_programs` 的**输出**上判慢起音/非拨弦）。守卫对着事实报错 = 逼人去改数据或
     删检查，两个结果都比误报更糟。

199. **"起伏"要做在编配层，不是音量层**（2026-09-18，用户："**按照改写的经验来会好听
     很多**"）。症状是"段界硬切"和"整首太平"**同时**存在 —— 看着像两条互相冲突的需求，
     其实是同一个错：**只拿音量做段间对比**。
     实测仿写成品 `99_b35_remake`（26 段）：段间**响度差中位只有 0.6dB**、`fade` 值大多
     在 ±3dB 内（**它根本没有留白**）—— **它的起伏全在编配上**（9 条轨的加减）。
     而生成路径用 `energy_mix` 写 CC7 能量曲线做对比 → `jump` 拉到 **10~22dB** →
     听感"硬邦邦切过去"；同时因为**编配没变**，又觉得"太平"。
     **修法**：厚度差**保留**（副歌还是更厚），但让它在**一个小节内逐条进场/退场**
     （`song_engine.build_events` 的 `_RAMP` + `_ORDER` 骨架顺序）—— 02_wave_walk 的
     `fade_out` 从 1.9 → **4.1~6.9dB**（6/6 过门），`jump` 仍是 10dB（起伏没被削）。
     **门限一个字没改**（改的是编配，不是判据）。
     ⚠ **只做段末退场，别做段首减配**：头尾都减配会让每段开头空近 2 秒、全曲反复 7 次，
     段内听着发乱（用户当场听出来的："内部有一点乱，之前没有说"）。
     ⚠ **这类问题必须直接量音频包络**（每 0.05s RMS）再动手：我先后猜了 4 个方向
     （加留白 / 压电平 / 调 `section_gap` / 段首稀疏）**全部无效**，量了包络才看清
     "边界处其实有 −38dB 的清晰留白，是判据的参照窗口自己掉了进去"。
     ⚠ 另一条副产品：**判据本身也可能错**。`t_section_transition` 固定拿"边界前
     0.3~0.9s"当主体参照，与它自己要的"留白"自相矛盾（留白越充分越判硬切）。用户口径
     是"**这两个不用固定，每个段落各选其一；要留白时把参照时间往前挪**" —— 取样改到
     "段中部 30%~70%"后 FAIL 从 4 首降到 3 首，而**门限没动**。

195. **"全局常量 + 多处引用"里的拍号假设：只改守卫不够，常量本身要按曲设**（2026-09-18，
     用户要求修 3/4）。`melody_gen` 引用 `SPB`（一小节几拍）**33 处**，4/4 时是个常量 4.0，
     非 4/4 直接拒绝。放开 3/4 时**只删那句拒绝远远不够** —— 落点/句长/强拍全按 `SPB` 算，
     不按拍号设它就会**静默**把第 4 拍撒到 3/4 的第 1 拍上（节奏整体错位，且不报错）。
     两个必须一起处理的细节：
     ① **默认参数在函数定义时就绑死了**：`def form_stats(..., bar_beats=SPB)` **不会**跟着
        模块全局变 → 统计函数仍按 4 拍切小节、守卫拿错基准。要么改成 `None` + 函数内取，
        要么在 `set_meter()` 里重绑 `fn.__defaults__`（本项目选了后者，33 处引用不动）。
     ② **守卫跑完必须还原全局**：`SPB` 是模块级的，检查之间会互相污染 —— `t_meter_spb_fits`
        用 `try/finally` 还原成 4/4，末尾再断言一次。
     **判据要选"变了就一定变形"的**：MIDI `timesig=[3,4]` + `end_beat=144.0`（48 小节×3 拍）
     + 旋律 beat 最大值 2.75 < 3.0 —— 若 `SPB` 还是 4.0，落点会大量 ≥3.0。

196. **改了引擎重跑，曲子却没变 —— 因为同名目录被"静默拒绝"**（2026-09-18，用户当场指出：
     "每次优化都要更新 song.json make_song new_song"）。`new_song.py` 遇到已存在的目录只打印
     一句 `已存在: <path>` 就 `return 1`，**照字面读不出"什么都没做"**。我加了音色平衡补偿后
     重跑，看输出以为生效了 —— 实际 `song.json` 一字未动，`make_song` 渲染的还是旧 json，
     于是 `track_balance` 的数值**一字不变**（19.2 / 21.0dB），我据此又白调了两轮参数。
     **修法（两件一起）**：① 加 `--force`（删旧重建）；② 无 `--force` 时把话说明白 ——
     "**未做任何修改** —— 引擎/参数改了也不会进这首曲子"，并提示 `--force` + "改完还必须跑
     `make_song` 重渲染"。
     **纪律**：**改了引擎/参数 ⇒ `new_song --force` 重生成 + `make_song` 重渲染**，两步缺一不可；
     看到"没报错"就当优化生效，是这套工具里最容易犯的错。

197. **`arr.mix` 的 CC7 与"频段实测 dB"不是同一尺度**（2026-09-18，修 `track_balance`）。
     `cc7_at` 走 GM 凹曲线，与"该轨 2.5–5kHz 实测电平"差约 **0.49 倍**（写 −12dB 的 CC7 平移，
     实测只降 5.8dB）；而且**低端还会衰减** —— 压到 CC7=1（几乎静音）也只降约 **12dB**。
     所以"模板音色太响"这个问题**压电平压不平**：模板把 Hook 给了钢弦吉他(48.8)、主奏是
     长笛(29.7)，差 19dB；实测 01/04 正是 19.2 / 21.0dB。
     **两条出路**：① 换音色（用户 2026-09-18 选的：Hook 避开 2.5–5kHz > 41dB 的吉他族，
     池被筛空时往**同主题其它声部池**找 —— 仍是模板里真实出现过的音色）；② 压电平，
     但要接受"伴奏被压到听不见"才勉强达标。
     **纪律**：**音色一换，电平必须跟着标定** —— 音色之间的 2.5–5kHz 跨度有 **34dB**
     （电钢 15.5 → 失真吉他 49.8），而 `mix` 是按**轨名**写死的。

198. **bash 多行 heredoc 喂 `while read` 会吃掉行首字符**（2026-09-18，批量重写 18 首时）。
     `done <<< "$PLAN"` 这种多行字符串 + `IFS='|' read -r name ...` 的写法，实测把目录名前缀
     吃掉了：`02_wave_walk` → `2_wave_walk`、`04_pulse_city` → `_pulse_city`、
     `13_midnight_drift` → `_midnight_drift`（18 首里 17 首名字都错了，曲子本身没错）。
     **修法**：用 **bash 数组 + 单行 `<<<`**（`for spec in "${PLAN[@]}"; do IFS='|' read -r ... <<< "$spec"`），
     或把清单写成文件逐行读。
     **纪律**：批量脚本**先跑 1 条验证命名**再全量；跑完用 `song.json` 的 `name` 字段核对目录名
     （那是生成时算好的，不受 shell 影响）。

200. **`avoid_lead` 第一版"改完读数一字未变" —— `ch_all` 的键是**和弦标识**、不是小节号**（2026-09-18，修"伴奏撞主奏"）。
     现场：给 `04_pulse_city` 开了 `patterns.avoid_lead` 并重渲染，撞音读数 **17 处 / 15 处**，
     与改动前**一模一样**（不是"没效果"，是**根本没跑**）。
     根因：和弦**不按全局小节存** —— 每段用 `sec['chords'][i]` 逐小节指定和弦标识 `cn`，
     `ch_all` 的键就是 `cn`。我写的查表 `ch_all.get(int(t // B))` 于是**永远返回 None**，
     整段静默走 `continue`（一个音没动、也不报错）。
     **修法**：段循环里建 `bar_chord[bar0 + i] = cn`，查表和弦一律先过它。
     **纪律**：**"读数一字未变"要当"没生效"查，别当"改动无效"** —— 两者排查方向正好相反
     （前者查键/路径/是否真执行，后者查参数/阈值）。这条与 182/195 同族。

201. **段界"硬切"：段末压力度（在我试的那组参数下）没动读数，段首有效**（2026-09-18，修 17/19 的 `section_transition`）。
     现场：`17_blade_two` 的 B2 边界 `fade_out` 只有 **2.7dB**（门 4）、`19_spec_echo` 的 B 段 **3.8dB**（差 0.2）。
     第一次照"段末"改（边界前 0.15 小节给保留声部加 −7dB 下坡）→ **读数一字未变**。
     根因：`p_edge` 那 0.15s 的 RMS **由更早起音的长音撑着**（Pad/Strings 的长音半小节前就起音了），
     压"新起音的力度"压不到它 —— **引擎里早有这条注释，我还是踩了**。
     而**段首的音全是新起的** → 同样的压法在段首**立刻有效**（`patterns.seg_in = 0.75` 拍）。
     **纪律**：改包络类判据之前先问一句"**这个窗口里的能量是谁发出来的**"（新起音 / 持续音 / 混响尾巴），
     答案决定该动起音还是动时值。

202. **临时脚本别落在 `scripts/`**（2026-09-18）。
     现象：自检凭空多两条 FAIL —— `import_all`（`No module named 'mido'`，因为 `.venv` 没装 mido、
     只有 `.venv-ml` 有）与 `console_encoding_safe`（入口脚本没做编码兜底）。
     根因：`selftest` 会把 `scripts/*.py` **全部当入口脚本**扫（import 一遍 + 查编码兜底），
     临时探针放进去就等于给自己造了两条 FAIL。
     **纪律**：临时脚本落**仓库外**（如 `D:\test\`）；要常驻就走 `docs/CONVENTION.md` §4A
     （`__doc__` 写清用法 + README 清单 + 自检 + 变异用例）。

203. **为迁就守卫口径去削听感 = 双输**（2026-09-19，用户："**从 0 重新生成的质量没有之前高**"）。
     现象：从零生成的 5 首新曲，**客观分与 19 首旧曲同档甚至更好**（鼓 16/16 格、力度档更多、
     撞主奏更少、旋律交叠持平），用户一听就说质量不如之前 —— 两把尺子**指向相反**。
     查法：把**同主题的新旧曲逐键 diff**（`01_morning_light` vs `90_new_daily` 等三对）——
     `programs` / `arr` / 段落结构 / 音色**全同**，**唯一实质差异**是
     `patterns.section_gap`（旧曲 **3.0** 拍 / 新曲 **0.5** 拍）= 段末留白 ≈1.29s vs 0.22s，
     听感差别就是"**段落之间有没有呼吸**"。
     根因：0.5 是 2026-09-18 为了迁就 `section_transition` 的 `pre` 参考窗口选的
     （留白长过 0.3s 时 `pre` 自己也落进留白里、渐变量不出来）—— **为判据改了音乐**，
     而且**双输**：新曲段界照样 FAIL 4 个、听感还掉了（同一份注释里其实记着"G=2.0→6/6 达标"）。
     **改回 3.0 后：段界 0 FAIL + 听感恢复**（用户方针："怎么样好听就怎么样，重要的是听感好"）。
     **纪律**：① 判据与听感冲突时**先怀疑判据** —— `pre` 取"段中部 30%~70%"本身就盲于长留白；
     ② **别把"过守卫"当成改音乐的理由**，那会同时输掉两边；
     ③ **同主题新旧曲 diff `song.json`** 是最快的归因手段（唯一差异 = 唯一变量）；
     ④ 判据确实太绝对时，走 `patterns.melody_exempt` 那种"**写清理由即可放行**"的路
     （空理由 = 没写，放行时打印标注），**不要**去改全局阈值。

204. **"旋律不自然"无法用统计量预报 —— 四层指标全部重叠**（2026-09-19，用户："**检查一下
     其它不自然的原因是什么**"）。
     背景：从 0 生成的 5 首新曲，用户听出"旋律不是很自然"。逐个排查的结果：
     ① **已排除** `--dur-bias`：同 seed 带它 / 不带它各生成一次，`dens`、碎音、同音、落点格、
        强拍、时值桶分布**逐项相同**（单变量 A/B，`_k_durbias_ab.py`）；
     ② `section_gap` 是**另一件事**（段落呼吸）—— 它解释"质量没有之前高"，**不解释旋律**；
     ③ 量了四层，**全部无法区分自然 / 不自然**：
        · 节奏密度层：碎音（自然 2~13% / 不自然 9~21%）· 同音串 · 落点格 · 强拍 · 空档 · 末落点
        · 线条层：同音 / 级进 / 小跳 / 中跳 / 大跳占比 · 跳后反向 · 音与音间隙 · 句长 · 拱形 · 音域
        · 结构层：节奏动机重复（25~38% 两组都有）· 跳后反向率 · 回填率 · 句末终止式
        · 与画像的五维交叠（落点 / 时值 / 音程 / 句长 / 拱形）
     唯一有信号的是**大跳数**（自然组中位 14 / 不自然组中位 24），但**重叠区间很大**
     （自然组里也有 26 个的）→ 不足以当判据。
     **结论**：这类"自然 / 不自然"落在**具体的音**上，统计量预报不了 —— 与
     原案例实测台账（已按用户要求删除 2026-09-24） 第 28 条（8 轮造了 6 个坏尺子）同源。
     **可操作的做法**：**多生成几版（只换 seed）让人耳挑** —— 实测同主题只换 seed：
     碎音率 2%→17%、大跳数 6→33，**随机性远大于任何可自动判的差异**；
     用户一次就选中了其中一版（`91_var_daily_a`）。**别再为"自然度"造指标。**

205. **两条路径混用：拿"直接作曲"的口径去干"模仿写歌"，`song.json` 上看不出来**（2026-09-19，
     用户："**把模仿写歌和直接作曲的功能和文档分开，防止错用**"）。
     现场：任务是"照着这几首最长的参考曲写歌"，我用 `new_song.py --theme` 出骨架 —— 那走的是
     **直接作曲**的路径（段落来自主题包 `form.plan`）—— 再用**仓库外的手写脚本**（`_build40.py`）
     把 26 段结构塞回 `song.json`。命令全 ok、渲染全 ok，但**成品数据里看不出它已经不是主题包的
     结构**：check_song 与后续接手的人都会按"直接作曲"的口径理解它，那份手写脚本也留不下来复用。
     **同一条路上还埋着一个静默错**：段名写成 `Rise`/`Peak`/`Quiet`/`Surge`/`Wave`，而
     `song_engine.role_of_section` 的规则是"**取段名里第一个 a–e 字母**当角色"（intro/outro 特判）——
     这些名字第一个 a–e 字母**都是 `e`** → 5 个不同段落全归成角色 `E` → 被要求共用一支旋律，
     守卫当场报 `同名段落 E 用了 5 支不同旋律 ['Lone','Peak','Quiet','Rise','Surge']`；
     落进 'A' 兜底的（`High`/`Hush`，一个 a–e 字母都没有）同样是错的，只是**一声不吭**。
     **修法（已固化）**：
     · `scripts/imitate_plan.py` —— 模仿写歌的**唯一段落层入口**：吃结构表（进行 / 每段
       `density` / `perc_target` / 编配 / 主奏音色），写盘前**硬校验**（段名只能用 A–E ·
       同角色 = 同进行 + 同旋律 · 段数 / 和弦数 / 参数范围），并写
       `basis.structure_source="imitate:<参考曲>"` 留痕；**和弦没变时保留旋律**（免得白重生成）。
     · `t_imitate_path_marked` —— 带 `theme.pack` 的曲目，**段数 ≠ 包 `form.plan` 段数又没有
       `imitate:` 留痕** → FAIL。配变异用例（注入"抹掉留痕"必须红）。
     · `docs/IMITATE-PATH.md` —— 三条路径（直接作曲 / 模仿写歌 / 还原扒带）的**依据分界表** +
       七步工序 + 段名纪律 + 命令。
     ⚠ 附带一条当场被自检抓到的：**`basis` 一旦写了就必须带 `kind='theme_pack'`** ——
       只写 `structure_source` 会让 `theme_basis_whitelist` 读到 `basis.kind=None`，
       判成"依据不是白名单模板"（模仿只换段落层，依据本来就是主题包）。

206. **"杂乱 / 不流畅"能量出来：旋律轨时值太短 + 多余轨没并**（2026-09-19）。
     用户判 BGM16/23/29"太杂乱，而且不流畅"。逐轨量（**时值中位秒 · 碎音率**＝时值 ≤0.25 拍占比），
     BGM16 修前 → 修后 → 认可版 `v5_source.mid`：
     · Piano 0.190 → **0.275** → 0.264 ｜ 碎音 30.4% → **13.6%** → 2.7%
     · Bass 0.175 → **0.275** → 0.232 ｜ 22.1% → **0%** → 2.7%
     · Guitar 0.100 → **0.275** → 0.344 ｜ 92.5% → **0%** → 3.9%
     · 轨数 **9 → 5**（v5 也是 5）｜鼓轨不动（v5 的 Perc 同为 0.010s / 碎音 100%）
     修法：① 显式时值下限（`bass_ensemble --min-beats` / `merge_tracks --min-beats`；
     流水线 `--dur-floor` 默认 0.55 拍）② 新增 `scripts/merge_tracks.py` 并轨
     （流水线 `--absorb` / `--absorb-into`）。
     · ⚠ **一律换秒再比**：本链 `song.mid` 是 120BPM 语义、参考曲 150BPM，比"拍"会得出错的倍数。
     · ⚠ **时值下限是"延长"不是"合并"**（所有音起点不动）—— 与 `docs/RESTORE-METHOD.md` §2
       那条"碎片合并（≤40ms）→ F1 变差"**不冲突**。
     · ⚠ **顺序：先并轨、后拉长**（反了 Piano 碎音 46.6%；只拉不并 30.4%）。
     · ⚠ **并进哪条轨是听感问题**：4 条合成器通道是原曲 `other` 主体（BGM16 占能量 **44%**、
       钢琴只占 **7.9%**）→ 并进 Piano = 用钢琴音色弹它（用户"一点都不像"），并进 Strings 才对。
      旁证：用户对 other 占比最低（19.2%）的 BGM29 评价是"还可以"。
     完整判据与量法 → `docs/THEME-PACK.md` §4b（写歌）/ `docs/IMITATE-PATH.md` §4b（仿写）。

207. **`stale()` 名不副实：只判"文件在不在" → "改了 MIDI 却渲染旧音频"**（2026-09-19）。
     实测：`--force bass` 改完 `song.mid`，第 6/7/8 段照样打印"已存在，跳过" → 命令 exit=0、
     日志写"== 完成 =="，而**成品音频还是上一版**（三首一起跑 18 秒就"完成"）。
     根因：`def stale(path, tag): return tag in a.force or not have(path)` —— 名字叫 stale，从不看时间。
     修法：抽成**模块级** `needs_redo(path, force_tags, tag, up)`（任一上游比产物新 → 重做），
     各阶段补 `up=`：stems←ref · ymt3←ref · ymt3bass←bass.wav · bp←该分轨 wav ·
     render←song.mid · match←render.wav · finish←bm2。抽成模块级是为了能被**行为单测**
     （`t_dur_floor_wired` ⑤：建真文件 + 造 mtime 差）。
     ⚠ 上游是"三条转录产物"的集成段，**参数（`--dur-floor` / `--absorb` / `--merge-thr`）变了它看不出**
     → 改参数要用 `--force bass`。

208. **成品名与参考同名 → 收尾把参考原曲覆盖掉**（2026-09-19，与 207 同批挖出）。
     把原曲拷进输出目录当 ref（很自然的做法）+ `--name` 默认取参考名 → 成品路径**就是**参考路径，
     `master_finish` 直接覆盖它：原曲 5,274,147 B 被写成成品 9,360,725 B，**当时毫无异常**
     （exit=0、日志"== 完成 =="）。连带后果：下一次跑拿"上一版成品"当 ref，而分轨/转录
     **不会重跑**（产物都在）→ 这个错误一路留在项目里。
     修法：`imitate_ref.py` 收尾前 `if os.path.abspath(out_ogg) == ref: raise SystemExit(...)`。
     **纪律：参考音频不要放进输出目录**（本项目统一放 `D:\test\_ref\BGMxx.ogg`，ASCII 路径）。

209. **"坑已写在 docstring 里"拦不住误判 —— 判据必须印在运行时输出上**（2026-09-19）。
     `transcribe_audit.py` 的 docstring **第 14 行就写着**"只适用于单音性强的层（bass 最典型）。
     复音层不能用：pyin 只能给出一条最强基频"，而我照样拿它量 Piano / Guitar / Strings，
     并据"**Piano 假音 63%** → 要修 YMT3 的通道映射"往下判 ✗
     —— **不是文档没写，是输出没写**：脚本每次都照样打印
     `→ 一致率 8.9%：**先修识别**（<70%），别去调混音/音色/EQ`，一个绝对门槛 + 行动指令，谁看谁信。
     **校准**（拿用户认可的 `v5_source.mid` 当被测对象、同一把尺子，BGM35）：
     Bass 53.2% · Guitar 12.5% · Piano 9.1% · Strings 6.2% —— 连**认可版**都过不了 70%，
     而我的每一轨**都比它高**（76.9 / 27.9 / 17.3 / 8.5）→ "假音 63%"是**尺子的病**。
     修法：`transcribe_audit.py` 打印一致率后**总是**跟上四个校准值 + 指向
     `eval_transcription.py`（音符级 F1，**天花板 1.000**；实测我这版 **0.551** = P 0.423 / R 0.789，
     远超历史基线 0.123（引擎自生成）/ 0.298（直接转录））；docstring 判据行同步补限定。
     ⚠ 同批还修了一个静默缺陷：轨名没匹配上时它打印"一致率 **0.0%**"（看着像转录全错，
     其实是参数没接上）→ 现在报错退出并列出现有轨名。
     **元教训**：结论要落在**决策当场看得见的那一行输出**上 —— 写在文档里只有"已经怀疑它的人"
     才会读，而误判恰恰发生在还没怀疑的时候。同族但机制不同：31/162/200 是"工具静默不生效"，
     这条是"**判据被无条件信任**"。

210. **"临时移走再移回"的脚本：`trap` + 手动恢复双重调用 → `rm -rf` 删掉真数据**（2026-09-19）。

     现场：为回答"别人 clone 下来（没有 MIDI 模板库）能不能直接写歌"，我写了个模拟脚本 ——
     `mv refs/midi2 refs/midi2_OFF` → 建空目录放回两个 json → 跑校验 → 恢复。
     恢复函数**同时挂在 `trap ... EXIT` 上、又在脚本末尾手动调了一次**：
     第一次恢复成功（20 项 / 216 个 MIDI），**第二次先 `rm -rf refs/midi2` 把刚恢复的删了**，
     紧接着 `mv refs/midi2_OFF refs/midi2` 因源已不存在而失败 → **整个模板库没了**。
     `refs/midi2/*.mid` 是 `.gitignore` 的（版权 + 体积）→ **git 恢复不了**（只能恢复两个 json），
     `rm -rf` 也不进回收站。

     **三条教训**：
     ① **破坏性的临时操作不要写在"会执行两次"的地方** —— `trap ... EXIT` 已经够了，
        再手动调一次就是双重执行；恢复逻辑必须**幂等**（先判"源还在不在"再动），
        且**删除与恢复的顺序**要对（先 `mv` 回来，再清临时目录；本次顺序反了）。
     ② **移走任何东西之前，先把"重建路径"确认到可执行** —— 这次敢移是因为"MIDI 是抓来的"，
        但**没先确认还能抓回来**；事后才抽样验证 URL 仍可精确恢复（6/6 md5 一致），纯属运气。
     ③ **`.gitignore` 的资产 = 无备份资产**：动之前要么先**复制**（不是移动），要么确认重建通道。

     **恢复通道（已固化成命令）**：`fetch_midi_lib.py <库目录> --repair`
     —— 按 `_sources.json` 的**逐首原始 URL** 重下缺失文件并**逐首校验 md5**。
     ⚠ 不能用 `main()` 的默认抓取来恢复：它是**按搜索词**抓的，每次结果不同，
     抓到的是**另一批文件**（md5 对不上、画像会变）。

211. **编辑器默认放的是"实时合成音"，拿它跟采样音源播放器比 = 白调一轮**（2026-09-19）。

     现场：用户报"MIDI 播放很奇怪、卡卡的，还不如 PotPlayer"。我先去调合成音 —— 把
     "每个泛音一个 sine 振荡器 + 每振荡器一个 GainNode"改成 `createPeriodicWave` 波表
     （实测 3.42 → 2.00 个振荡器/音、同时发声上限 ~47 → ~160 音）。**"卡"确实修掉了一半**，
     但"不如播放器"这条**根本不是调参能解决的**，改完才想明白：

     `studio/web/ed.js` 的播放有**两种音源** ——
       · `synth`（默认）：现场用振荡器合成，定位只是"改一个音符立刻听个响"；
       · `media`：`render_midi.py` 渲出的 OGG（GeneralUser GS **采样**音源，与引擎面板同一条管线）。
     拿振荡器合成去比真乐器采样（PotPlayer、系统 GM 音源都是采样），差的是**一个数量级**。

     **正解**：`ed.js` 的 `autoRenderAudio()` —— 导入 MIDI 后自动取真音源（命中指纹缓存秒切，
     否则后台渲染几十秒，期间合成音照常能听、能继续编辑）。**顺带一条**：改 `ed.js` 必须同步抬
     `ed.html` 里的 `?v=` 版本号，否则浏览器继续用缓存的旧 JS，改动等于没发。

     **判据**：用户说"声音不如 XX 播放器" ⇒ **先确认他听的是哪种音源**（状态栏显示
     「🎹 合成音」还是「🎧 真音源」），别急着调合成音参数。

212. **`git push` 时通时不通 ⇒ 先看 `tools/git-push/`**（2026-09-19 实测）

     **症状**：`git push` 有时一推就过，有时连续十几轮 `Could not connect to github.com:443`
     （交接单里"push 常年不稳、先探 443、最多 8 轮"就是这么来的）。

     **别猜 DNS**：四个 DNS（本地运营商 + `8.8.8.8`/`223.5.5.5`/`119.29.29.29`）**一致返回
     `20.205.243.166`**、`ping` 它 **0% 丢包**，而同一时段 TCP 443 **50 次只成功 1 次**（SYN 被丢）；
     同段 `20.205.243.168`（`api.github.com`）**30/30 全 200**；**SNI 交叉**证明失败**随 IP 走、
     不随 SNI 走** ⇒ 被丢的是"**某个目标 IP 的 443**"。

     **怎么办**（实测表 / 脚本 / 验法 → **`tools/git-push/README.md`**）：① 零改动 —— 隔 15 秒重试
     （8 轮全 `000` 后慢重试第 3 轮就通）；② 一键绕行 —— `bash tools/git-push/push_via_tunnel.sh`；
     ③ 长期 —— SSH（`github.com:22` 可达，但公钥要先在 GitHub 登记；用本机 `gho_` token 走 API 会 404）。

213. **`midi_file.export_midi`：轨上有 `program_changes` 就不再写 `program`**（2026-09-20，扒
     DIMENSION TRAVERSE 时）。现场：把开头 163 个音拆到新轨、设 `program=81`（Saw Lead）想换音色，
     mido 读回却是 **`program_change=(8, 0)`** —— 变成 Acoustic Piano。根因在 `export_midi` 第 156 行
     `if prog is not None and not (t.get('program_changes') or [])`：新轨从钢琴轨复制，
     自带 `[[0, 0]]` → 走了"有 `program_changes` 就忽略 `program`"那条分支。
     **症状极隐蔽**：命令 ok、文件变大、渲染也 ok，只有渲染统计（质心/宽度/削波）
     与改动前**逐样本相同**才暴露"其实什么都没换"。**判据：改音色后读回验证 `program`**，
     别信"命令成功"（与 182/196 同族：引擎/工具层面的静默失败）。
     ⚠ 顺带一条同族发现：把新轨加到 `m['tracks']` 后**别复用已占用的 channel**（本例用了空的 8）。

     **加固（同日，附带一条"护栏也会误伤"的教训）**：先写成"两者冲突就**报错**"，
     被**真实 MIDI 当场证伪** —— 导入的 `ABBA.Name of the game K.mid` 里 `program=39` 而
     `program_changes[0]=84`（前者是**解析器推断**、后者是文件真值，两者不同是**常态**），
     一报错就让 `midi_file_editor_roundtrip` 从 PASS 变 FAIL（自检 133→132）。
     **工具猜不出该留哪个**（本例用户那条 `[[0,0]]` 本来就会覆盖），所以最终改成：
     **导出行为一字不改**（往返字节一致）+ **冲突时把"哪个才生效"打到 stderr**。
     变异用例 `conflict_test.py`：① 冲突**必须有警告** ② 一致 / 只有 program **不许报警**。

214. **整曲统一 EQ（`band_match.py`）对"分段音乐"会过头 —— 必须分段做**（2026-09-20）。
     现场：渲染后跑 `band_match` 对齐参考曲，**整曲平均带差 4.72 → 3.17dB**（看着很好），
     可**开头 0–10s 的 630–1250Hz 从 −0.5 掉到 −9.4dB**。根因：它按整曲平均算补偿
     （315–630Hz 补 −5.07dB），而**原曲开头与中段的频谱分布根本不同**（本例开头 80–160Hz 主导、
     中频低 7dB；中段 315–630Hz 主导）。
     **正解**：分段各算一套，两套各跑一遍**全程** `gain_curve`+`apply_eq`，再按时间权重线性混合
     （别拼接，避免边界不连续）。实测同一窗口 630–1250Hz 收到 **−4.9dB**，中段顺带改善
     （10–18kHz −4.4 → **−0.3**）。与 190（"段落等长要看曲风"）同族：**分段是默认，一刀切要举证**。

215. **粗调渲染 `--width` 会"相位抵消"掉中频 —— 宽度要用 `master_finish.py` 精确对齐**（2026-09-20）。
     现场：想让渲染更宽，把 `--width` 1.4 → 2.2：宽度确实 0.255 → 0.733（超过参考 0.612），
     但 **630–1250Hz 带差从 −0.5 恶化到 −7.3dB**、RMS 掉 1.2dB、峰值顶到 **−0.6dBFS**。
     原因：加宽走中侧（M/S）处理，侧信号拉猛会抵消**居中成分**（人声/主奏/贝斯都在中间）。
     **正解**：`master_finish.py <src> <dst> <参考音频>` —— 宽度**精确对齐参考实测值**
     （`set_width_exact`）+ 响度归一 + 峰值保护，一步到位（实测整曲宽度 0.601 vs 目标 0.611）。
     ⚠ 但它是**整曲**一个宽度值：本例整曲 0.601 达标，分窗看却开头 0.893 / 中段 0.481
     （参考 0.612 / 0.567）—— **宽度也随段变化**时，同 214 一样要分段。

216. **守卫抓"连续同音串"，抓不到"压平"—— 同音率高、串长却短**（2026-09-20，写钢琴独奏时）。
     现场三连：① 我用脚本把某段旋律 16 个音批量改成同一个音高（想收进钢琴高音区），它们
     散在 8 小节里 → **串长只有 2~3**，而**同音率 100%**；`probe_melody_health` 只判
     `maxrun ≤4`，20 个音**一个警报都没出**，只报了句"强拍 85%"，压平是我回看 JSON 才发现的。
     ② 同一轮为了"给旋律留空间"手推和弦的开放排列，把和弦音逐个下移 12/24 ——
     **B7 少了三音 D#、Am7 少了 A、F#m7 少了 A**（音级集合不完整），于是旋律里本来合法的
     和弦音在强拍上被判成"经过音"（`melody_health` 8 条 FAIL），而**根因在和弦不在旋律**。
     ③ 收尾用 `json_io.py` 规范化 `song.json` 之后没重渲染 —— **MIDI 比音频新 4 分钟**，
     "文件都在、时间戳也像对的"，是人工比对 mtime 才逮到。
     **共同点：我的动作做完了、工具全绿/全哑**（与 213 同族：引擎/工具层面的静默）。

     **加固（都已落地 + 配变异用例）**：
     · `probe_melody_health.MAX_SAME = 45%` —— 阈值有分布证据（`tools/same_rate_probe.py`
       量全库 29 首：中位 11.9% / P90 17.2% / 最大 18.5%）→ 抓真压平、不误伤现有曲目；
       变异用例：注入"同音率 100% 但串长 3"必须被抓、阈值改坏必须被抓。
     · 和弦音组**按音级完整性校验**（`tools/fix_chord_voicing.py`：每个 `%12` 至少要有一个音、
       不许同音高重复、以**原始生成定义**为基准 —— 拿磁盘当前值比对是循环论证，第一次跑就是这么被骗的）。
     · `make_song.py` 末尾加**音频/MIDI 同步检查**（音频比 MIDI 旧就报"交付前必须重渲染"）。

217. **`section_probe.py` 的段落地图是硬编码的 —— 换曲子直接"不适用"**（2026-09-20）。
     现场：写这首 72 小节的钢琴曲时想逐段量响度/质心，工具打印
     「!! 段落地图不适用：SECS 需要 10060.0s，文件只有 251.5s」——
     `SECS` 写死成某首 40 小节 × 1.6s 的曲子的段落表，**对任何别的曲子都不可用**，
     我只好另写一个脚本（这就是"工具在逼用户绕路"）。
     **正解**：`build_map()` 优先读同目录 `song.json` 的 `sections`（段名/段数/BPM 全来自数据）。
     同一份音频两种地图的读数**根本不同**（动态地图质心 1174Hz vs 硬编码 518Hz）。
     ⚠ 顺带修掉一个**参数覆盖**：显式给小节秒数时不许再被自动查到的 `song.json` 盖掉
     （第一次改完就是这样：`section_probe x.wav 3.478` 打出来的仍是 song.json 的地图）。
     变异用例：把 `build_map` 换回硬编码地图必须被抓。

218. **`melody_gen.py` 是写盘工具，不是诊断工具 —— 当"只读"跑会当场换掉整支旋律**（2026-09-20）。
     现场：`melody_step_bias` 这条自检报"`melody_gen` 非零退出（无详情）"，我想复现它的退出，
     就直接 `melody_gen.py songs\20_piano_rain\song.json refs\themes\tender_melody.json --avoid songs`
     —— 它是**写盘**工具（`json_io.save`），于是那首刚定稿的曲子 5 支旋律（含手写的引子/尾声、
     逐段微调、变奏加音）被 seed=7 的新旋律**整体替换**，而且全程**没有一句警告**。
     `git` 没追踪该曲（`?? songs/20_piano_rain/`）→ 没有版本副本，只能从**覆盖前渲染的 MIDI**
     抢救（`tools/restore_melody_from_midi.py`：起音吸 0.25 拍网格 → 拆 (小节,拍)，时值反推 `×0.96`；
     顺带学到一条：**不能拿"恢复总数 == MIDI 总数"当断言** —— 同一支旋律在多段复用，总数必然更多，
     真正的验收是"重渲后与旧 MIDI 逐音符一致"，本例 194/194 音一致、仅 1 个音差 4.7ms）。

     **加固**：`melody_gen.py --dry-run` —— 打印"新 n 音 / 盘上现有 m 音"的对照后**直接返回、不落盘**
     （对照实验：加 `--dry-run` 前后 `song.json` 的 SHA256 与 mtime 都不变）。

     **可迁移的判据（下次动手前问自己）**：① 这条命令**写不写盘**？（`--avoid`/`--candidates`
     这类参数的名字看着像"选择"，但不是只读信号）② 有没有 `--dry-run`？没有就先备份
     ③ `git status` 里是 `??` 的曲子 = **没有版本副本**，动手前务必先复制一份。

219. **纯钢琴（单乐器）编配的五条听感判据 —— 引擎默认是"小编制"，套钢琴会全踩**（2026-09-20）。
     现场：做一首 69BPM / 72 小节的纯钢琴抒情曲，用户连报四次听感问题
     （"不好听" → "音符延长得太奇怪，还有很多其它问题" → 逐条指认 1/2/3/4）。
     逐条量出来的根因**都不在混音层**（频谱/响度/宽度早已对齐），全在"把小编制写法套到钢琴上"：

     | # | 症状 | 根因（量值） | 判据 / 修法 |
     |---|---|---|---|
     | 1 | **"音符延长得太奇怪"** | `arr.pad`/`arr.strings` 在引擎里是**垫子写法**：时值写死一小节 = **3.565s（4.1 拍）**。钢琴音色靠**衰减**收尾，按住不放就成了"和弦糊到小节末" | 钢琴/拨弦/钟琴/贝斯族**不要开 pad/strings**；`check_song` 现在会提示（判据 = `arr` 开关 + `programs` 的 GM 族，**不是**扫 `melody`）。⚠ 第一版就是扫 `melody` 写的 —— 那两层是**引擎生成**的轨，`song.json` 里没有，对照实验当场证伪 |
     | 2 | **和弦空洞** | 我把和弦摊成"开放排列"（跨 2 个八度、省掉中间音级） | 正统钢琴左手 = **密集三度叠置**；改后音级完整性要校验（见 216） |
     | 3 | **低音太闷** | Bass 落在 **24–45（E1 起）** | 钢琴低音区取 **33–50**（A1 起）；低于 E1 的根音在 GM 钢琴上只有"嗡" |
     | 4 | **太慢** | 69 BPM × 每小节换和弦 | 抒情慢曲**别拖**：吟唱感靠**长音 + 留白**，不靠降速。提到 78 后时长 4:10 → 3:41 |

     **另一条同样重要的方法错**（用户原话"没有以前的生成经验吗"）：我从零调参，**没先读
     原案例文档（已删 2026-09-24） —— 那份写着用户指定的最佳案例（BGM35）的三条可迁移共性
     （**克制 + 交代** / 逐小节渐弱收尾 / 调式色彩明确），以及一句关键结论：
     **差距不在混音**（RMS 中位完全相同），**只能落在"音乐性"上**（旋律走向 / 和声选择 / 编配取舍）。
     **→ 接单乐器 / 抒情类新曲，第一步必须是"读案例 + 量公认模板"，不是打开 `song.json` 调参。**

     **第三条方法错（用户当天定成默认判据，`SKILL.md` §3 第 13 条）**：**"不好听"先查旋律**，
     不要先查响度 / EQ / 宽度 / 频段。我这一轮反着做了 —— 先量倍频程、推 `mix`、调 `shelf`、
     做段间音量曲线，**全对齐了用户仍说"不好听"**；直到量**时值**才发现真病根（垫子层 4.1 拍
     长音、音域跨 4 个八度、和弦开放排列空洞）。**外在因素只有在用户明说是混音问题、
     或旋律已达标而仍不好听时才动。**

     ⚠ 实测顺带证伪两个想当然的改法（别重走）：① 用 `melody_gen --dur-bias` 把音拉长以为
     "更抒情" —— 碎音 1% → **12%**，更差；② 用音量曲线做段间起伏 —— BGM35 全曲**只有 7dB**
     起伏，而我把段间做到 **18.4dB**，反而"硬邦邦"。

220. **面板「读不到曲目：纯音频目录」的四种真因 —— 全是曲库根布局问题，不是面板坏了**（2026-09-21）。
     现场：用户面板刷出一串 `!! 读不到曲目：曲目 piano_rain_交付 是**纯音频目录**` +
     `曲库已切换 → D:\test\piano_rain_交付`。逐层查下来四个独立真因：
     ① **交付目录被当曲库根**（我把它建在了曲库根里面，还建成了"有 song.json 的单曲目录"）；
     ② **曲库根里有散装音频文件** → `probe_lib`（`studio/server.py:225`）`has_audio(p)` 命中，
        返回 **single**（"整库就是一首曲"）→ **永远走不到容器探测那一支**，面板于是逐个文件报错；
     ③ **8 个悬空 junction**（`zz_bpm_*` 指向已删的 `songs\zz_bpm_*`）；
     ④ 曲目目录缺 `song.json`（面板 `need_json=False` **有意**放行纯音频目录做 A/B 试听）。
     **判据陷阱**（我写审计工具时连踩）：**`os.path.islink` 对 Windows 目录 junction 返回
     `False`**（悬空链接被误判成"散装文件"，清理脚本一个删不掉）；**悬空 junction 下
     `os.path.isdir` 也返回 `False`**（判定顺序必须「链接 → 目录 → 文件」）。
     **修法（用户当天追加要求："生成期间也要注意"）**：判据收成**一份**
     （`scripts/harmony_check.py`），两处调用 —— ① `make_song.py` 自动调参**每轮**跑
     （第 1 轮 + 末轮必打）② `check_song.py` 输出「和谐体检」段（`make_song` 每步都调它
     → 生成链必然跑到）。
     ⚠ 我第一版在 `check_song.py` 里**又写了一份**判据，结果同一条曲子上两个工具结论
     **不一致**（一个报四轨、一个报两轨）—— 正是 `CONVENTION.md` §1「抄一份 = 埋一处漂移」。
     **当场价值**：接上后立刻抓到我自己埋的数据错误 —— 批量降八度时写成 `max(40, b-12)`，
     把 8 个和弦的低音全压成 40(E)（`chord_names_match_notes` 报"低音与根音不符"）。

     **修法**：`scripts/studio_audit.py`（体检 + `--clean` 清悬空链接）+ 曲库根只放曲目目录 +
     交付目录放库外。症状→真因对照表与自检命令 → **`docs/STUDIO-WORKFLOW.md` §2.5**。

221. **读 JSON 索引时字段名写错 → 静默取到 0（"全 0%"没人报错）**（2026-09-21，接用户"网上下载的
     midi 模板库呢"时）。现场：用户要我别拿风格不符的 BGM35 比，改用 `refs/midi2/`（**网上下载的
     223 首 MIDI 模板库**，带风格目录 + 来源 URL）。我写 `pick_reference.py --midi` 给它按风格
     挑同编制模板，输出里 **"钢琴占比"一整列全是 0%** —— 包括
     `mozart-piano-concerto-21-2-...-**piano-solo**.mid`（那显然是 100% 钢琴）。
     根因：索引里音符数的键叫 **`notes`**，我读的是 `note_count`；`t.get('note_count') or 0`
     **静默返回 0**，于是"钢琴占比 = 0/0 → 0%"一路算下去，**没有任何异常**。
     改对字段名后立刻正常（KK Ballad 100%、Chopin 100%、Mozart PC21 100%）。

     **判据（可迁移）**：读外部 JSON 时，**"键不存在"与"值是 0"必须能区分** ——
     `d.get(k)` 返回 None 就该报错/告警，别用 `or 0` 把它抹平。本工具现在显式断言：
     一条轨都没读到音符数 = 索引格式变了，**当场 SystemExit**，而不是给一列 0%。
     ⚠ 同一天这类"读错/判错但不报错"已经第三次：`os.path.islink` 对 Windows junction
     返回 False（220）、悬空 junction 下 `isdir` 也是 False（220）、本次字段名（221）。
     **共同特征：默认值 / 兜底值把错误吃掉了。**

     **顺带回答"模板库有没有上传 GitHub"（用户当天问）**：`.gitignore` 明确排除
     `refs/midi2/**/*.mid`（版权灰色），仓库里**只有** `_index.json`（223 首索引 + 逐首 md5）
     与 `_sources.json`（**223/223 条下载 URL**）；实测 md5 与磁盘**223/223 一致**，
     故 clone 后 `python scripts\fetch_midi_lib.py` 可完整重建。

222. **把「曲库」指向一个"本身就是曲子"的目录 → 任何曲目 id 都解析成它自己，会写坏别的文件**
     （2026-09-21，**本轮唯一造成数据损坏的一条**）。现场：`D:\test\piano_rain_交付`
     这个**交付目录**被设成了面板曲库（`studio\.libpath`），而它自己带着 `song.json` +
     `piano_rain.mid` + `piano_rain_sf.ogg`（技能 §14 要求交付目录长这样）。
     `probe_lib` 于是判成 **`single`**（"目录本身就是一首曲子"——那是**有意支持**的合法布局），
     而 `song_dir(sid)` 在 single 下写的是 `d = base`（**忽略 sid**）：
     我起 `POST /api/job?id=44_skip_beat&kind=compose`，**实际跑的是该目录里的 piano_rain**，
     `piano_rain.mid` 被覆盖（11129 → 12114 字节）。曲目 id 在库里根本不存在，**却不报错**。
     **根因**：合法布局 + **静默兜底**（`sid` 无人校验）—— 与 220/221 同族：
     "默认值 / 兜底值把错误吃掉了"，而这一条的代价是**改别人的文件**。
     **修法**：`song_dir` 在 single 下校验 `sid == 库目录名`（= `songs_list` 给出的 id），
     对不上就 `FileNotFoundError` 并说清"这个 id 在本曲库下不存在、已拒绝"。
     **预防（可迁移）**：① 起面板/CLI 任务**前**先 `GET /api/songs` 看那个 id 在不在列表里
     （一次 1 秒，能挡住全部这类错配）；② 交付目录放**曲库外**、曲库根只放曲目目录、
     库根**不许有散装音频**（技能 §14 —— 库根一有音频，`probe_lib` 会把整库判成"一首曲"）。

223. **`check_song` 的沙箱会换掉 `selftest.ROOT` → 用 `ROOT` 拼脚本路径的检查"恒失败"，
     而且报错里看不到原因**（2026-09-21；这条让一项检查**哑了很久**）。
     现场：`check_song 44_skip_beat` 长期报 `melody_step_bias → melody_gen 非零退出：`
     —— **冒号后面是空的**，翻遍输出查不到任何线索；而单独手跑同一条 `melody_gen`
     **退出码 0**（两种 `--step-bias` 都成功）。
     **根因**：`check_song.run_checks_on` 把 `st.ROOT` 换成一个**沙箱目录**（那里没有
     `scripts/`），而 `t_melody_step_bias` 用 `os.path.join(ROOT, 'scripts', 'melody_gen.py')`
     拼路径 → 文件不存在 → 非零退出；**原因打的是 stderr，而断言只打印 `r.stdout`**。
     **修法**：脚本路径改用 `HERE`（本文件所在目录，物理上不随沙箱变）+ 报错信息并入 stderr。
     ⚠ **可迁移的判据**：同一文件里**同一种写法出现 6 次、只有 1 处不同** —— 那 1 处就是漏网
     （本文件其余 6 处子进程调用早就都用 `HERE` 了）。**"孤例"本身就是信号**，
     别因为它"只是 1 处"就跳过。另：断言里只打 stdout = 把 stderr 的根因扔掉。

224. **多方聚合的成员没按来源去重 → 同一首曲子重复投票，把中位数拉跑**（2026-09-21）。
     现场：`cheerful` 的混音目标 `cheerful_mix` 有 6 份成员，其中 `BGM16c` / `BGM16c_v2` /
     `bgm16c_new` **三份画像逐字段完全相同**（`source.file` 都是 `BGM16c.ogg`，
     bpm 150.0 / 质心 3250 / rms −16.9）—— 同一首曲子投了 3 票、**占一半权重**，
     于是聚合出的 bpm 中位 = **150.0**，而真实成员是 132.4 / 128.0 / 133.9 / 150.0（中位 ≈133）。
     症状不在聚合本身，而在**下游成绩单**：长期报"速度不一致：本曲 132.0 vs 参考 150.0"，
     让人以为"速度选错了"，其实是**参考画像错**。实测 4 个主题中招
     （battle / cheerful / neon / retro —— 都是快节奏主题，所以都挑中了 BGM16c）。
     **为什么必须去重**：`aggregate_refs` 的**全部意义**就是"多份取中位数以削掉单份个性"，
     重复投票恰好**放大**了个性 —— 与初衷相反；而且份数虚高让人误以为"参考很充分"。
     **修法**：按 `source.file`（退回 `file`/成员名）去重，跳过项记进 `dedup_skipped` 留痕。
     重建 4 个主题后 `cheerful_mix` 的 bpm 150.0 → **133.9**（与本曲 132 只差 1.9）。
     **守卫**：`t_mix_target_aggregate` 新增判据 ④（同一份音频不许重复投票）+ 变异用例
     "把同一个成员再塞一份"（`mutation_check` ⑭-b）。

225. **阈值型判据必须先审计触发率 —— 恒真 / 从不触发的判据等于没查**（2026-09-21）。
     现场：给用户做"把听到的问题报出来"的工具时，抽查 5 首发现**撞音判据 5/5 全报**，
     而且每一对的音域范围**完全相同**（Piano(40–59) vs Hook(40–59) 这种）—— 看
     `harmony_check._range_of()` 才明白：Piano/Hook/Arp 都走同一条公式
     （同一个和弦音组 + 各自 `TR_SHIFT`），**shift 相同时范围必然相同**，
     所以它检测的是**引擎的设计**、不是缺陷。全库审计（30 首）：
     **撞音 32/30 首（106%）· 音区 39/30（130%）· 长音层 0/30 · `probe_melody_health` 6 维 0/31**。
     **反证**：本仓库自己记着"仿写 `b35_remake` 有 498 处同起点同音高、**比我们多得多**"
     （`song_engine.py` 1739）—— **撞音多的反而是好曲子**，判据方向与听感相反。
     **消融验证**：关掉 Hook 层（MIDI 355→91 音，其余轨逐音符数不变、原曲 SHA256 未动）
     让用户 A/B → **"差不多"** → 该判据与听感无关。
     **判读口径**：触发率 **100% = 噪声**（恒真）· **0% = 空转**（从不触发）·
     **5%~30% = 有区分度** ✓。任何"加了阈值就该报问题"的判据，**上线前先在全库跑一遍触发率**。
     工具：`selfcheck.py`（按此口径只报**离群量**，见 SKILL §3 第 16 条）。

226. **HF 大模型走镜像下载时 Xet 后端 401**（2026-09-21，下 Qwen2-Audio-7B 15.65GB）。
     现场：下到 31% 抛
     `RuntimeError: CAS Client Error: HTTP status client error (401 Unauthorized),
     domain: https://cas-server.xethub.hf.co/v2/reconstructions/…`。
     根因：`.venv-ml` 装了 `hf_xet 1.6.0` → 走 HF 的 **Xet 存储后端**，
     而 **`HF_ENDPOINT=hf-mirror.com` 不代理 `cas-server.xethub.hf.co`**。
     ⚠ 报错里**没有"镜像"字样**，极易误判成 token / 权限问题去折腾登录。
     **修法**：`export HF_HUB_DISABLE_XET=1` → 回退传统 HTTP，**续传 2 分钟下完**。

227. **`transformers 5.17` 的 `audios=` 被静默忽略 → 多模态"盲评"**（2026-09-21）。
     现场：`Qwen2AudioProcessor.__call__` 的签名是
     `(self, images=None, text=None, videos=None, audio=None, **kwargs)` —— **`audio` 单数**。
     传 `audios=[wav]` 只发一句
     `Keyword argument 'audios' is not a valid argument for this processor and will be ignored.`
     **不报错**，`input_ids` 只有 **33 个 token**（纯文本），
     而模型**照样一本正经地评价音乐** —— 实测第一次问"好不好听"它给了 **9 分**。
     **危害**：不做冒烟测试就会拿到"像模像样、其实没听过音频"的结论。
     **修法**：**断言输入里有 `input_features`**，没有就直接抛错（`ask_qwen_music.py` 已内置）。
     **可迁移**：任何多模态推理，先验"输入真进去了"（看张量形状 / token 数），别信它不报错。

228. **`song.json` 会被面板写入 —— 核基准不只防"自己改错"，还要防"别人在改"**（2026-09-21）。
     现场：开工按纪律记下 `song.json` SHA256 = `f8ad9713…`，期间**我的全部命令都指向
     变体目录**（`make_song` 日志里 `finalize …\_ab_nohook_piano_rain\song.json` 可证）；
     收尾复核时原曲已变成 `92f6a297…`（9387→9285 字节，16:36:50）。
     真因：**用户在面板里调混音**（正常操作 —— 面板改的就是 `song.json`）。
     取证：与开工快照逐项比对，**段数 / 逐段 `arr` 开关 / `programs` / melody 键 / 顶层键
     全部一致** → 改的只是数值字段，和弦/编配/旋律/音色分配都没动。
     **补的这条**（坑 162 说"核基准"，此处加一句）：**基准会在他人在面板操作时改变** ——
     所以 ① 记 SHA256 ② **收尾再核一次** ③ **结构快照比 SHA256 更有用**
     （SHA256 只告诉你"变了"，快照能告诉你"变了什么"，且不需要旧副本）。

229. **`mix` / `sub_dur` 改了"没反应"，先查它们在引擎里有没有被读 —— 别怪渲染链**（2026-09-21）。
     现场：`20_piano_rain` 改了 `mix.Piano` 与 `sub_dur`（0.8→0.55）后重渲染，
     **解码后样本级差异 = -223dB（即 0）**，当时归因为"渲染链按目标响度归一化吸收了"。
     逐行读引擎后：**这两处改动本来就无效**，与渲染链无关 ——
     ① 顶层 `mix[轨] = [pan, vol]`：**第二个数才是音量**，写出来是 CC10=pan、CC7=vol，
        都在 **0.0 拍**；而每段 `arr.mix[轨] = 单整数` 是**段起点另写一条 CC7**。
        实测该曲 MIDI：Piano 轨 CC7 = `0.0→104`（顶层）**+** `0.0→72, 16→64, 48→76,
        80→88, 112→62, 144→82, 176→84, 208→94, 240→58, 272→48`（逐段，10 段）。
        同 tick 两条 CC7 → **后写的段级那条生效**，顶层那条从未生效。
        ⚠ 顺带纠正一个读数：交接文档里"MIDI 里 CC7 是 `[48,58,62,64,72,76]`、不是 1:1"
        是把 **CC10=58（pan）** 当成 CC7 读进去了。
     ② `patterns.sub_dur` 只在**部分** `bass_style` 下直传：`simple`/`waltz` 写
        `max(sub_dur, 1.2)`、`eighth` 写 `max(sub_dur, 0.8)`，只有 `offbeat`/`pump16` 是直传。
        对照实验（只 compose、不渲染，逐组从 MIDI 读回 sub 音时值）：
        `simple` 设 0.4 / 0.55 / 1.2 → 全都得 **1.2**、设 1.5 → 1.5；
        `offbeat` 设 0.4 → **0.4**；`eighth` 设 0.4 → **0.8**。
        该曲是 `simple` → `sub_dur` 0.8→0.55 **两个值都不生效**，sub 音永远是 1.2 拍。
     **判读**：改 CC7 / `sub_dur` 之前，先确认**这条轨的风格分支**与**段级是否覆盖**；
     "改了没反应"的第一嫌疑是"这个键在这条路径上有没有被读"，而不是后处理。

230. **验"改动进没进音频"只有一个可靠办法：解码算样本差 —— SHA256 不同 ≠ 生效**（2026-09-21）。
     现场：三个 ogg **字节数完全相同**、SHA256 却各不同（ogg 容器带编码序列号等随机元数据），
     于是"文件不同"被当成了"改动生效"的证据（据此报过"改后 `27bf866e` 已完成"，是错的）。
     `mtime` / 文件大小 / SHA256 **都不能**当证据。**查法**：`diff_audio_ab.py`
     （解码 → 逐样本相减 → 差异 RMS 与信号 RMS 之比）。
     判读：**≤ -60dB = 实质无效**（A/B 等于比两个一样的东西）· **-20~-40dB = 有可听差异**。
     实测三个消融版本：**-26.5dB**（旋律长音 2.25→1.75 拍）· **-14.1dB**（旋律两处音高）·
     **-20.1dB**（C 段换层）—— 而且**逐 20 秒块的差异能定位到段落**：v3 只有第 5–6 块变
     （100–140s = C 段 36–43 小节 @78BPM），与改动的段落位置对得上 ✓
     ——这条"差异位置 = 改动位置"的对照，才是"改动真的进了音频"的完整证据。

231. **量"和弦过渡生硬"要用音级，不是绝对音高**（2026-09-21）。
     现场：交接文档记"10 处零共同音 + 根音跳 ≥5"（`20_piano_rain`），据此准备去改和弦。
     逐处复核：**10 处里没有一处是音级层面的零共同音** —— `Am7`(`A3 C4 E4 G4`) → `Em7`
     (`E3 G3 B3 D4`) 的音级共同音是 **E、G**，只是低了一个八度；
     拿**绝对音高**求交集，低八度的同一个音级不算共同音 → 把 iv→i 这类常规进行报成病。
     真线索是**音区中心跳变**（和弦音集的平均音高差）：`D6→Esus4` **-9.0 半音**、
     `F#m7→D6` **+8.0**、`Am7→Em7` **-5.0**。
     **判读**：量"和声连接/功能"用**音级集合**（`{p % 12}`）；量"生硬、塌下去"用**音区中心差**。
     这条与坑 225 同类（判据口径错了，报出来的"问题"方向与听感相反），但错在**度量单位**
     而不是阈值 —— 换单位跑一遍就能证伪，成本极低，所以"改和弦"之前**必须先跑**。

234. **"贝斯突兀"往往是两件事：音色用错族 + 两条轨同八度齐奏**（2026-09-21，用户听感定案）。
     现场：用户报"**bass 前面突兀了，其实单听还好合起来就不行了**"。逐层量下来是两条独立的毛病：
     ① **`programs.Bass` 用了钢琴族**：`20_piano_rain` 是 `Bass=[0, 6]` = **GM 0
        Acoustic Grand Piano**（该曲设计就是"全轨钢琴族"）→ 引子里成了"用钢琴在 E2/A2 上
        弹 **1.4 拍长音、力度 96**"：低音区 + 长时值 + 钢琴的衰减 = 一坨混浊的低频块。
     ② **Piano 与 Bass 同八度弹同一个音高**：MIDI 实测 `Piano E2@0.12 A2@1.12 …` vs
        `Bass E2@0.00 A2@1.00 …` —— 音高**完全相同**（40/45）、只差 **0.12 拍（≈92ms）**，
        即两条轨齐奏同一个低音。⚠ 这正好**躲过了撞音判据**（它按"同起点 <0.05 拍"配对）——
        **判据的容差也会造成漏报**，本轮第一遍量"撞音"得到 0 处就是这么来的。
     **为什么"单听还好"**：单听任一条都是条正常低音线，只有合起来才顶出来。
     **定案与改法**：`programs.Bass` → **32**（Acoustic Bass）+ 引子 `arr.bass=false`
     （引子那 4 个钢琴音与 Bass 完全同音高，删掉不丢和声信息）。用户认可后已并回原曲。
     **守卫**：`t_bass_timbre_is_low`（`programs.Bass[0]` 必须在 GM 32–39 / 42–44）+
     变异用例"把允许集合改小"。全库实测：改前 39 首里 **31 首本就合规、唯一违规的就是这一首**
     —— 所以这条门**不会误伤**现有曲目。
     **可迁移**：听感问题定位到"某条轨"时，**先查它的音色族 + 它和谁在同音区齐奏**，再谈电平；
     消融版本（关 A / 关 B / 换音色各一版）比猜快得多 —— 本轮 `v5…v9` 五版里，用户否掉了
     "降电平"（v5）与"关钢琴"（v6），选中了"换音色 + 引子不用 bass"（v9）。

232. **音频大模型当尺子，先查 `do_sample`**（2026-09-21）。
     模型的 `generation_config.json` 是 `do_sample: true / temperature 0.7 / top_p 0.5` ——
     不显式覆盖就是**每次采样**：同一段音频连问 3 次，给出**三个不同位置**的答案
     （59–63s / 67–70s / 72–75s）。
     → 做对照实验必须 `do_sample=False`（实测三次逐字一致，换进程也一致）。
     **判据：任何"同输入应得同输出"的用途，先跑 3 次重复**；要多样性才采样，且那时取多数。
     现场与工具用法见 `docs/AUDIO-CRITIC.md`。

233. **主观模型的判定会"恒真"、且对微扰敏感 —— 它能定位大问题，不能判"微调后是否变好"**（2026-09-21）。
     实测：base **8 段全部**被判"有问题"（"和弦过渡生硬"铺满 5 版本 × 8 段的整张表 → 没有区分度）；
     只改了别处的版本，**没改动的段**也给出不同答案，而那两段音频只差 **0.2dB**（整曲归一化的
     副产物）；"改动段"与"判定变化"没有系统性对应。
     → 这类模型适合回答"**哪一段听起来乱**"，不适合"**这版比那版好吗**" ——
     后者只能靠**样本级音频差异 + 用户耳朵**。
     **加速**（同一轮实测）：CPU 上 decode 是**内存带宽**瓶颈（465ms/token ≈ 33GB/s；
     16→32 线程 **0.96×**、`max_new_tokens` 400→120 时间**完全不动**、fp32 加载失败）
     → **GPU NF4 4bit 快 17.5×**（52.7s → 3.0s/段），但量化会改变判定、A/B 必须同配置。
     全部数字与装法见 `docs/AUDIO-CRITIC.md`。

235. **"只响 0.几秒的音" = 主奏音色的衰减速度，不是时值 / 力度 / CC**（2026-09-22，两轮才定位）。
     现场：用户报 `20_piano_rain`"**有很多响了 0.几秒就没了的音**"，指认 1.7 / 2.2 / 4.6 / 5.0 / 9.3 秒。
     已排除（每条都有实测）：MIDI 时值不够（1.30 拍 = 0.997s；延到 0.45/0.55/0.75 拍 → 用户"**延长之后
     也很突兀**"）· 力度太低（58→88 音频几乎没变）· CC 阶跃（Intro 里 CC7 只落在段起点）·
     同音高重叠（0 处）· ogg 编码伪影（wav 里就有，差 0.2dB）· 采样循环点（6 个音色单音渲染
     都没有 0.40s 回升）。
     **真因**：那些时刻 MIDI 里**根本没有任何起音**（±0.35s 窗口内 0 个），只有一个 Melody 长音在响
     ——"哒"是**它自己衰减尾巴的起伏**。单音实测（GeneralUser GS · tempo 78 · 音高 78 · 力度 84）：
     钢琴 GM 0/1 从峰值掉 12 dB 只要 **0.19~0.25 秒**，而它写的时值是 **1.35 拍**；
     持续型（GM 4 电钢琴 1）**根本不掉**。
     ⚠ **量法纪律**：① 音频包络只能**量**一个音，**不能"数"音的个数** —— 我用 10ms 包络把一个音的
     衰减起伏数成"5 个独立的音"，于是把真凶安到了另一条轨上（`notes.md` ③ 那轮，白走一轮）；
     ② 只信**独奏窗口**：多轨齐奏时音头会被更响的那一轨"借走"（实测把 GM 4 的持续音误报成
     "0.04s 就掉 12dB"）；③ 包络里混着混响尾巴，别把它当音的时值。
     **修法与守卫**：`harmony_check` 第 ④ 项（数据层，`check_song` 与 `make_song` 每轮都跑到）——
     "主奏音色掉 12dB 时间 × 2 < 该段旋律时值中位"就报，并给候选（**GM 4** 钢琴族内 · 起音 1ms；
     弦乐 48 起音 **427ms** 会"慢半拍"，见 236）。⚠ 表里**只放实测过的音色**：第一版用"族兜底估值"
     判，把 6 首管乐误报成"只响 0.几秒"（2 首 → 8 首）→ **未实测一律返回"判不了"**。
     定位工具：`scripts/probe_sustain.py`；自检 `t_sustain_criteria`（含变异用例）。

236. **"镫一下"是力度问题，跟时值无关 —— 改错维度会得出"改了没用"的假结论**（2026-09-22）。
     现场：换成持续型主奏后用户又说"**只检查前面几秒，还是有镫一下的**" → 定位到 **0.385s** 的
     Piano 反拍短音（音高 40 · 时值 0.28 拍 · 力度 42）。它冒出来是因为主奏换音色后音头**轻了 6.5dB**
     （−26.3 → −32.8），原本被盖住的短音反而比旋律高 4.1dB —— **改一处要看三处**。
     **消融**（只动那一个音）：力度 42→26 → 跳变 **+10.3 → +6.7 dB**；**时值 0.28→0.6 拍 → 读数
     一字未变**（+10.3）；两者都改 = 只降力度的结果；删掉该音 = +4.9（下界）。⇒ **只跟力度有关**。
     ⚠ 同一位置**上一个对话试过改时值**（0.28→0.75 拍）并把力度**往上**调到 88，结论写成
     "实测几乎没变 ⇒ 不是这条轨的时值/力度问题"并回退 —— **维度错、方向反**；而且当时量的 4.64s
     是**旋律的衰减尾巴**，不是这个音（那条错记录本轮已在源码原地替换）。
     **修法与守卫**：`patterns.piano_stab_vel`（缺省 58 = 老行为**逐字节不变**）→ 本曲 36，
     全曲 392 个反拍音 58/64 → 36/42；`determinism_and_bytes` PASS（其他 29 首不受影响）。
     判据并入 `harmony_check` 第 ④ 项的附带提醒（**不单列** —— 按"衰减型短音轨 + 持续型主奏"判
     会 **27/30 首**命中 = 恒真噪声）；具体时刻用 `probe_sustain.py` 的 ② 榜
     （`harmony_check.stab_candidates`，按"比主奏响多少"排）。
     **可迁移**："改了没效果"先分清是**维度错**还是**没生效**，一次对照实验就能分开。

237. **本机无外网时 `imageio_ffmpeg.get_ffmpeg_exe()` 会卡死 —— 它不是"取路径"，是"联网检查"**（2026-09-22）。
     现场：全量自检 **>20 分钟**跑不完，其中**最慢的一项卡 >9 分钟**没返回；而同一台机器上
     `.venv\Lib\site-packages\imageio_ffmpeg\binaries\ffmpeg-win-x86_64-v7.1.exe`
     **本身秒回**（`-version` rc=0），直接拿它转码 rc=0。**关杀软、加内存都不解决**
     （我先后往这两个方向查过，都不对）。
     **两处都中过**：`scripts\to_ogg.py`（整个 wav→ogg 卡住）与 `scripts\metrics.py`
     （`t_read_audio_format_fallback` 卡死 ⇒ **全量自检最慢的一项就是它**）。
     **连带后果（最坑的一条）**：多进程并行时**每个 worker 各卡一次** ⇒ 实测 `--jobs 8`
     反而 **288× 退化**（4 路 1236 秒 vs 2 路 4.3 秒）。我因此误判成"FluidSynth 并发抢
     SoundFont/磁盘"，还上网查了一轮 —— 直到把**单项耗时**量出来才看清真凶。
     **修掉之后**：那一项 **0 秒**、**全量自检 117 秒（2 分钟）**、**并行彻底不需要了**。
     **修法与守卫**：`_ffmpeg_exe()` 一律**先取本地 binaries**、取不到才退回（并打印提示）；
     守卫 `t_ffmpeg_exe_is_local` 用 **AST 数真实调用**扫 `to_ogg.py`（≤1 次 = 兜底）与
     `metrics.py`（**0 次**）。⚠ **别用字符串计数**：第一版这么写，结果**注释里提到函数名
     也被算成一次裸调**（连续 FAIL 两次）。变异用例"metrics 改回直连"守着。
     **可迁移**：① **"慢"要先量单项耗时再猜**（`selftest.py --time` 打 top 20 / `--shard i/N`
     取样）—— 我前两次猜"并行""渲染"都被数据推翻；② 本机**没有外网**，任何"会联网的便捷
     函数"都要先怀疑（同类：`transformers` 加载要 `HF_HUB_OFFLINE=1`，见 `docs/AUDIO-CRITIC.md`）。

238. **一次改多处 = 之后无法归因，"以后不犯"也就落不到确定的一行上**（2026-09-22，本轮真踩）。
     现场：用户说"前面部分非常奇怪"，我**一次改了三处**（Intro 主奏音色 80→0 ·
     `patterns.arr_by_role` 改 false · Intro 关 `pad`/`perc`/`bass`）→ 用户答"现在好了"，
     可**我答不出"是哪一处起的作用"**。后果很实在："以后不犯"只能**三处一起固化**，
     而其中两条还**违背有真值依据的设计**（`arr_by_role` 给引子开 `perc=1` 是有 cheerful
     十首模板支撑的：7/10 首引子有鼓）。同一天我还在"只响 0.几秒"上犯过反向的错 ——
     那次倒是单变量消融（只动 0.385s 一个音），一跑就分清了"**只降力度有效、加长时值无效**"。
     **纪律**：听感问题**一次只改一个维度**；确实要一次改多处，就**当场记下"改了哪几处"**
     并**明说"归因不明"**，别事后把"用户说好了"当成"我知道为什么好了"。
     **可迁移**：与 `docs/CONVENTION.md` §1"抄一份 = 埋一处漂移"同一族 —— 都是**信息在传递中
     损失**；同族还有"改动没记 SHA / 没核基准"（坑 162）与"改完没看最后一个 program"（§7.5）。

239. **"音符位置乱 / 规律不好听" = 落点逐音独立采样，且细胞层修法有两处硬约束**（2026-09-22）。
     现场（用户原话）："音乐音符的位置有点乱没有规律或者规律不好听……这是现在通用的问题"。
     **先量再改**（同一把尺子：人类 midi2 223 首 vs 引擎 30 首；主奏取"非鼓轨中基本单声、
     中位音高最高"那轨，**必须排除鼓**——否则和声粗糙度全被鼓键污染）：
     | 指标 | 人类 | 引擎 | 读法 |
     |---|---|---|---|
     | 弱格（16 分 e/a）落点占比 | **12.9%** | **26.8%** | 引擎是人类 2 倍 = "乱" |
     | 落在 8 分格比例 | 85.0% | 73.2% | 人类旋律基本贴 8 分格 |
     | 相邻落点间隔（IOI）熵 | **0.310** | **0.646** | 人类就那么几种时距，引擎两倍杂 |
     | 与上一小节同节奏型 | 3.1% | **0.0%** | 引擎从不做"邻小节动机重述" |
     | 伴奏落正拍占比 | 39.2% | **56.2%** | 伴奏又太方 = "规律不好听" |
     逐段量（**必须分段**，全曲平均把"哪段乱"抹平）：引擎按段名聚合是
     **Intro 70/30 · A–B 75~76/24~25 · C 81/19 · Outro 70/30 · E 65/35**（on8/弱格）——
     系统性偏散，**引子/尾声最严重**。根因：`melody_gen` 的落点是**逐音从画像分布独立采样**
     （动机层只管音高图式）→ 出不了"同一个节奏细胞反复"这种人类规律；**画像本身也偏平**
     （sorrow 弱格 3.7–6.5% vs 人类 2.3–3.0%），而唯一相关的守卫 `melody_onset_spread`
     门松到 **0.65**（实测本曲各段 0.44~0.63 全过）⇒ **等于没管**。
     **修法**：`melody_gen --rhythm-cells`（`new_song` 对新歌**默认带上**，旧曲重跑不带 ⇒
     输出与旧版一致）：每小节把落点换成反复出现的细胞、**音高按原先后顺序贴上**（轮廓不变）。
     实测逐段 **on8 87.5–90.9% / 弱格 9.1–12.5%**（旧 66.7–81.8% / 18.2–33.3%），
     成绩单"低频节奏型与参考差" **15/16 → 6/16 格**、反拍重音 0 → 2 个。
     ⚠ **三处硬约束（都当场破门）**：
     ① **细胞不能每小节都从正拍起** —— 第一版这样写：`格 0 占比` 冲到 **40.3%**（门 ≤22%）、
        `末落点 ≥8 格的小节` **62%**（门 ≥65%）。改成"按音数选细胞 + 每条末落点都 ≥ 第 2 拍 +
        只有 ~1/6 变体带一个 16 分装饰"后：格 0 **7~9%**、末落点 **100%**、弱格 **9~12%**
        （装饰频率 1/4~1/5 时弱格冲到 18.8%，超人类 11% 太多，也不行）。
     ② **保留句末长音时，正身落点必须全部排在它之前** —— `melody_motif_rules` 的"句末收束"
        要求末音是长音 + 和弦音；一律套细胞会把它压到 1 拍内（收束率 85% → **0%**）；
        加了"每 4 小节窗口末音原样保留"后又踩第二条：正身落点若排到它后面，`sort()` 会把
        **音序换掉** → 跳后反向率 **0.81 → 0.44**（门 ≥0.60）。正解：正身落点先按 `< 末音位置`
        过滤，不够就均分在末音之前。
     ③ **别用"事后后处理"改既成交付曲**：对成曲套细胞再 `_enforce_strong` 改强拍音高，
        实测跳后反向 0.605 → 0.444（破门）；**正路是重跑 `melody_gen --rhythm-cells`**
        （和声/结构/编配全不动，只换旋律）。
     **可迁移**：① "乱"要拆成两半（**散** = 熵/弱格，**方** = 正拍占比）才量得动；
     ② 修"分布"不够，要修**"反复出现的型"**；③ 每次修完都要**把守卫口径一起量** ——
     本轮两条形态门 + 一条动机门都是被自己的修法当场破掉的。

240. **`import torch` 在本机 `.venv-ml` 会卡死 —— 是 CUDA 初始化，不是网络、不是模型下载**（2026-09-22）。
     现场：`probe_aesthetic.py`（CLAP）**两次各卡 13 分钟无输出**；我先归因"模型 1.6GB 下不动"、
     再归因"HF 直连不通"，**两次都不对**。**分阶段量**（每题 `timeout 120`）：
     `import librosa` **0.0s** ✓ · `import torch` **超时 rc=124** ✗ · CLAP 加载超时（它要 torch）。
     **解法**：`CUDA_VISIBLE_DEVICES=""` → `import torch` **4.1 秒**过；配
     `HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1` 吃本机缓存（模型早在
     `~/.cache/huggingface/hub/models--laion--clap-htsat-unfused`，1.2GB），CLAP **分段情绪当场跑通**。
     ⚠ 缓存里若出现 `.no_exist/<rev>/model.safetensors`（transformers 5.x 先找 safetensors、镜像没有），
     **不是**下载失败的证据 —— 别据此换 `use_safetensors`，先看是不是 torch 卡住。
     **可迁移**：与坑 237（ffmpeg 联网）同族 —— **"慢/卡"先分阶段量，别对着最显眼的环节猜**。

241. **假"同步"核对：`scripts/compose.py` 并不存在（它在每首歌目录里）**（2026-09-22）。
     现场：改完 `song.json` 后跑
     `python scripts/compose.py songs/<曲>/song.json > /dev/null 2>&1` 重生成 MIDI，再比 md5
     宣称"song.json 与 MIDI 同步"—— **报了 OK**。真相：`scripts/` 下**没有** compose.py，命令当场
     `No such file or directory`，却被 `>/dev/null 2>&1` 吞掉 ⇒ **MIDI 根本没重写**，比的是同一个旧文件。
     **正确入口**：`python songs/<曲>/compose.py`（它自己解析仓库根，见该文件里 `BGM_STUDIO_ROOT`
     那段），或直接 `make_song.py <曲>`（`[1/3] 作曲: compose.py` 跑的就是它）。
     **可迁移**：① **凡"用 md5 证明生效"的核对，必须同时看命令退出码** —— 重定向到 `/dev/null`
     会把证据一起吞掉；② 与坑 237（改了没效果）、坑 162（SHA 不同 ≠ 生效）同族 ——
     **"ok" ≠ "生效"**，这已是第三次以不同面貌出现。

242. **`sections[i].chords` 的数量必须**等于** `bars` —— 少给一个会让十几条守卫连环
     `IndexError`，而报错点离真因很远**（2026-09-24）。
     现场：把 72 小节的骨架扩成 12 段 / 100 小节，尾声从 8 小节改成 12 小节，`chords` 仍留着
     **8 个**。`check_song` 当场从"数据契约没问题"翻成 **16 项未通过**，而且大部分长这样：
     `song_json_buildable → IndexError: list index out of range` ·
     `track_balance → IndexError` · `density_dynamic_range → IndexError` ·
     `audio_semantics → IndexError` · `compose_dry_run → IndexError` ——
     **看着像引擎崩了 / 仓库坏了**，只有一条 `melody_within_sections` 说了人话
     （"和弦 8 个 ≠ 小节 12（多出的会被忽略）"，注意它的措辞是"多出"、方向正好相反）。
     **根因**：引擎逐小节取和弦（`sec['chords'][i]`），段级字段的长度契约是"**每小节一个**"。
     **修法**：`chords` 补齐到 `bars` 个。**可迁移**：① 段级字段长度契约不止这一个，
     改段长时要**一起**看（`chords` / `drum_grid.per_bar` 的全曲长度 / `melody_extra` 的小节号）；
     ② **同一条数据的长度契约，报告口径要统一** —— 这次"真因"那条说反了方向，
     所以先信了 `IndexError` 那一堆，白绕一轮；③ 与坑 223 同族：
     **报错离真因远时，先找那条"说人话"的**。

243. **段数 / 段长偏离主题包 `form.plan` 时，`basis` 里要**同时**有 `kind='theme_pack'`
     与 `structure_source='theme_pack-plan:<说明>'` —— 只写一个仍 FAIL**（2026-09-24）。
     现场：把 10 段 / 72 小节扩成 12 段 / 100 小节之后，`check_song` 报
     `imitate_path_marked`（"段数 12 ≠ 主题包 form.plan 10，且没有 `basis.structure_source`"）。
     我按提示补了 `structure_source='theme_pack-plan:cheerful-12sec-96bar'` —— 结果换成
     `theme_basis_whitelist` FAIL：**`basis.kind=None`**（"依据不是白名单模板"）。
     **两条守卫各管一半**：`t_imitate_path_marked` 只认前缀
     `^(?:imitate:|theme_pack-plan:)\S+$`（**不许有空格**，顺手写的说明文字会让它不匹配）；
     `t_theme_basis_whitelist` 只认 `basis.kind == 'theme_pack'`。
     **修法**：`basis = {'kind': 'theme_pack', 'structure_source': 'theme_pack-plan:<无空格说明>'}`。
     **为什么要留痕**：段结构改过就必须声明"依据是哪一版" —— 写 `imitate:` 是照着某首参考曲，
     写 `theme_pack-plan:` 是在主题包 plan 的基础上扩写，两者**溯源去向不同**。
     **可迁移**：一条报错给的"补丁指令"往往只覆盖**它管的那一半**（这里明说的只是
     `structure_source`）→ 补完**必须重跑同一条检查**，别假设"按提示补了就完事"。

244. **手写 `sections` 后写盘必须走 `json_io.save`** —— `json.dump(indent=1)` 会被
     `song_json_canonical` 判不合格**（2026-09-24）。
     现场：扩段脚本自己 `json.dump(d, open(p,'w'), ensure_ascii=False, indent=1)` → `check_song` 报
     `song_json_canonical → 60_carnival_days(5680 行 → 应 2076 行)`。
     **口径**：`song.json` 要按"**每小节一行**"排（`json_io.dumps`），理由是**读的成本** ——
     一个音符一行的写法，一首 40 小节的歌就是 868 行、读一遍 ≈2k token，而"读一遍 song.json"
     是写歌流程里最普通的一步（又是 **token 预算**问题，不是洁癖）。
     **修法**：`import json_io; json_io.save(path, data)`。
     ⚠ 这条**只查格式**、不查内容：判据是 `json_io.dumps(data) == 原文`，
     所以"重新格式化"是安全的（规范化后必须与原文**逐字节**相同，等价性另有断言守着）。
     **可迁移**：新增任何"写 `song.json`"的脚本，**别自己 `json.dump`** ——
     仓库里已有唯一出口 `json_io`（与坑 245 同一个病：绕过既有出口就多一份口径）。

245. **`song_engine.ARR_KEYS` 与"另外两个合法键"被抄了三份，第三份就漂了**（2026-09-24）。
     现场：新工具 `expand_sections.py` 按 `ARR_KEYS` 白名单校验 `arr` 的键，结果把本曲
     **合法**的 `glock_all` 判成"引擎不认的键"并拒绝写盘。真相：`glock_all` 引擎在用
     （`song_engine.py:1599` 决定钟琴是否**每小节**都补），只是被 `ARR_KEYS` 故意排除，
     靠**两处硬编码的例外**兜着 —— `song_engine.py:533` 的校验与
     `selftest.t_arr_role_variety` 各写了一份 `{'vel', 'glock_all'}`。**第三处一出现就漂了。**
     影响面：`glock_all` 出现在 **29 首** `song.json` 里（`"glock_all": true` 有 2 首）——
     真要按 `ARR_KEYS` 从严判，全库 29 首都过不了。
     **修法**：在 `song_engine` 里抽 `ARR_KEYS_EXTRA = ('vel', 'glock_all')` 作为**唯一出处**，
     三处（引擎校验 / 自检 / 新工具）都读它。
     **可迁移**：与坑 91/118/188「每份清单都会漂移」同族，但这次的**发现方式**可复用 ——
     不是靠"记得同步"，而是**新写一个消费方时它自己撞出来的**：
     所以"新增一个读白名单的工具"本身就是一次**一致性审计**，撞出不一致先别改白名单、
     先问"这个键到底该不该在名单里"（`ARR_KEYS` 的定义是"引擎**自己写进** `arr` 的键"，
     手写用的键本来就不该进去）。

246. **在"副本"上验证复现性 ≠ 在原目录验证 —— 我把副本的读数当成了工具的行为，
     还据此写进了交付文档**（2026-09-24）。
     现场：为了不动已交付的 `60_carnival_days`，我把它复制成 `songs/_bench_carnival` 再跑
     `melody_gen --avoid songs`，得到的结果 **≠ 交付版** → 我据此断言
     "**`--avoid songs` 会把库里这首曲子自己也当去重对象，所以跑不出交付版**"，
     并把这句话写进了两份交付 `notes.md`。
     真相：`melody_gen` **一开始就排除本曲目录** ——
     `load_lib(avoid, exclude=os.path.dirname(os.path.abspath(song)))`（`melody_gen.py:1627`）。
     副本的 `exclude` 是**副本目录**，于是**原曲仍在库里参与去重** → 候选改选。
     **是"副本"这个条件造成的，不是 `--avoid` 的缺陷。**
     正确说法：在**曲目自己的目录**里跑才是同口径；想在副本上试就**别拿它当交付版的复现**。
     **修法**：两份 `notes.md` 已改正；`--avoid` 的语义不变。
     **可迁移**：① "改着玩别动交付物"这个做法本身没错，错在我把**副本上的读数**直接
     升级成了**工具的行为结论** —— 结论必须带**适用范围**（"在副本上"）；
     ② 与坑 165/166/167（读数与归因的方法错）同族，但这次的教训更锋利：
     **结论若是"某工具会 X"，先去读它那一行代码** —— 这次只要 `grep -n avoid` 就能看到
     `exclude=`，成本不到 10 秒，而我先下了结论、后写进文档；
     ③ **写进交付文档的结论要额外审一遍**：交付物会被人照着跑，一条错结论比一句"没验证"贵得多。

247. **"丰富混音目标"别去重跑 `theme_pack.py` —— 它重算的是**整包**，不是那一个字段**（2026-09-24）。
     现场：用户要"混音目标丰富一点 / 重新写一下"，我跑了 15 个 `theme_pack.py <主题>`。
     混音目标确实更新了（成员 6→8、按 `source.file` 去重前移、排除 `BGM35`），
     但 `selftest --fast` 从 **152/152 掉到 146/152**，6 项 FAIL 全是**连锁**（与混音目标无关）：
       · `theme_basis_whitelist` ×3（07/12/61）：重建重算了**模板选择** → 老曲目声明的
         `theme.templates` 与包不一致
       · `melody_matches_profile` / `melody_form_rules` / `melody_onset_spread` ×6
         （07/02/08/11/19/14）：重建重算了**旋律画像** → 按旧画像生成的老曲目不再匹配
       · `imitate_path_marked` ×4（01/02/04/06）：重建重算了 **`form.plan`** → 段数不符
       · `theme_timbre_pool`（battle）：重建冲掉了注入的 `arrangement.prog_pool`
     **正确做法**：只改你要动的那**一个键** —— 写"读包 → 只替换该键 → 写回"的脚本，
     并在写盘前证明作用域（把该键摘掉后，其余字段与原版**逐字节相同**）。
     回退 + 精准改写后：15 个包其余字段全同、`selftest` 回到 **152/152**，耗时还更短。
     **可迁移**：① 与 238（一次只改一处）同族，但这条更硬 —— **"重建"= 作用域不受控**，
     想只动一个字段就得写"只动一个字段"的代码；
     ② `extract_theme_timbres.py` 的 docstring 与 README 早写过"重跑 `theme_pack.py`
     会整包重建冲掉注入物"—— 我读到过还是踩了，说明**写在"另一个工具的文档"里等于没写**：
     "重建会冲掉/重算什么"的清单，应该长在**重建工具自己的输出**上。

248. **引擎有多个"出口"时，改一个等于只修一半 —— 而且日志会骗你**（2026-09-24）。
     现场：31 首从头重写后，`track_ranges_musical` 报 3 首 Glock 出 **62**（下界 63）、
     1 首 Melody 出 **38**（下界 43）。逐条排查四条 Glock 写入路径
     （`glock_part` 的 `while m<63` 保护 / `glock_starved` 两层 / 旋律高八度层 / 出口 `range_fix`）
     **都不产 62** —— 因为它们确实都对，问题在**我加的后处理放错了位置**：
       ① 先放在 `range_fix` 之前（`build_events` 中段）→ **日志明明打印"音域夹取：移八度 1 个"
          （看起来生效了）**，但 `t_track_ranges_musical` 是**直接调 `build_events` 取 `ev`** 的
          （`selftest.py:3761`），仍读到 62/38；
       ② 再挪到 `write_midi` 里 → 同样只修一半（**MIDI 文件对了、内存事件还是错的**）。
     正解：`build_events` 的 **`return ev, bar0` 之前** —— `ev` 同时是 `write_midi` 的输入，
     一处修、两个入口都对。
     **可迁移**：① **判据读哪个入口，修复就得放在那个入口的上游** —— 动手前先
     `grep` 出"谁在调这个函数"（本例两个调用点：`selftest` 与 `write_midi`）；
     ② **"日志说改了 N 个"不等于"你量的那个量变了"**（与坑 200/230 同族）：
     工具自己的打印不能当证据，必须**读回你实际量的那个读数**（本例：`probe_build_events.py`
     直接打印 Glock/Melody 的音域，一眼看出 62 还在）。

249. **物理约束不该做成 opt-in 开关**（2026-09-24）。
     现场：`TR_RANGE`（每轨"乐器合理音域"）原来**只在 `patterns.range_fix` 打开时**才夹取，
     于是没开这个开关的曲子可以把 Glock 写到 62（下界 63）、Melody 写到 38（下界 43），
     而守卫 `track_ranges_musical` 照样变红 —— **守卫在报一个引擎自己不肯修的错误**，
     每次都得人肉兜。
     口径：**"这个乐器能不能弹到这个音"是物理事实，不是口味**。口味（响度/宽度/段间起伏/
     力度曲线）才配开关；物理约束（音域、同刻同音高、MIDI 值域）应**无条件生效**。
     所以把音域夹取从 opt-in 里独立出来、始终跑；`patterns.range_fix` 只留
     "弱起音提到 26"这项（那才是口味）。
     **可迁移**：写检查项时先问一句"**这是物理约束还是口味偏好？**" ——
     前者若挂开关，等于给守卫留了一个"合法但引擎不修"的洞。

250. **写死的"默认值"会在每个真实数据上现形：和弦低音恒为 A#1**（2026-09-25）。
     现场：`transcribe_to_song.py` 的 `tones()` 里 `return 34, [...]` —— 34 = A#1，
     于是**每个和弦的低音都是 A#1**。`check_song` 的 `chord_names_match_notes`
     一次性报出 `siren_end` **20 个和弦种全部**"低音 34 与根音不符"（Fsus4 / Dm7 / G#… 全中）。
     修法：低音跟着根音走 —— 与 `check_song._build_voicing` **同口径**
     （根音 pc 落进 28–45 音区、超出回退一个八度），并把 `tones` 提成模块级
     `chord_tones()`，让守卫能直接钉住那个函数（而不是只靠下游的数据契约）。
     **守卫**：`t_chord_bass_matches_root`（先自证"不同根音必须给出不同低音"，再逐曲核 chords）。
     **可迁移**：① **一个"看起来像常量"的返回值，先问它是不是从某个例子里抄来的** ——
     34 恰好是 A#1、也正是这首曲子的调（A# minor），说明它当年是照着某一首写死的；
     ② 判据抓到了不等于防线到位：`chord_names_match_notes` 只在 `check_song` 里跑，
     而**产生它的那个函数**是可以被守卫直接钉住的。

251. **判据缺"样本量"与"夹具前提"这两道闸，真实数据一定会把它打爆**（2026-09-25）。
     同一轮扒谱连踩三次，全是**检查自己**的毛病（不是被测数据错）：
     ① `probe_melody_health` 的强拍贴合 `fit` —— `siren_end` 只有 **1 个**强拍样本，
        照样判"强拍 0%"（那个 0% 完全由那一个音决定）。全库 32 首实测：**31 首正常曲目
        的强拍样本 ≥14**（最小 14），唯一样本不足的就是这首还原曲 → 加 `MIN_FIT_N = 8`；
     ② `t_midi_ops_semantics` 挑夹具只看**文件最大**，而它通篇用 `tracks[1]` 与
        `copy_range(0, 8, track_idx=0)`：挑到 `siren_end.mid`（体积最大、第 0 轨
        **前 8 拍一个音都没有**）→ `clip['tracks'][0]` **IndexError**。
        **崩掉 ≠ 通过**：夹具挑法脆 = 这条防线在真实数据上已经失效；
     ③ 第三次是同一个病的另一面：把"抽旋律门槛 4 → 2"当成"修数据"——
        **用户口径**（2026-09-25）："**如果是真的没有音要保留，重要的是符合原曲**"。
        **判据服从原曲**：密度低于下限就写**带实测数字**的 `patterns.melody_exempt`
        （理由空白 = 没写 = 不放行），不许放宽抽取窗口或补音去凑指标。
     **可迁移**：① 判据上线前先问两句 —— "**它有多少样本？**"、
     "**夹具凭什么满足它的前提？**"；② 夹具要用**检查真正依赖的性质**去挑
     （这里 = 第 0 轨前 8 拍有音符），不是"体积最大"；实在凑不出前提就
     **补齐前提并打印**（别让检查空转，也别让它崩）；③ 三条的共性：
     **它们都不会报"我测不到"**，只会给出一个像模像样的结论。

252. **Windows 文本模式写回 = 静默改字节；而"逐字节相同"的守卫用文本模式读，看不见它**（2026-09-25）。
     现场：`mutation_check.py` 为了注入故障会**改真曲库里的 `song.json`**，恢复时写
     `open(p, 'w', encoding='utf-8').write(old)` —— Windows 上 `\n` 变成 `\r\n`，
     于是"恢复"后的文件**每行多 1 字节**：实测 84835 → **89439** 字节（4604 行）。
     更糟的是 `t_song_json_canonical`（专门守"重新格式化后必须与原文逐字节相同"的那条）
     **照样 PASS** —— 因为它用 `open(p, encoding='utf-8').read()` 文本模式读，
     universal newlines 已经把 `\r` 吃掉了。
     修法：① 所有写回点显式加 `newline='\n'`（本轮给 `mutation_check.py` 补了 **21 处**）；
     ② 判据改**二进制读**（`open(p, 'rb')` 后查 `b'\r'`）—— 这才是根治：
     污染源可能来自任何脚本，逐个堵不现实。
     **可迁移**：① 判据若声称"逐字节相同"，就必须**用字节读** —— 文本模式读出来的
     "逐字节"是假的；② 与坑 200/248 同族（"日志说改了 N 个" ≠ 你量的那个量变了）：
     先问一句"**我这一刀砍下去，文件真的变了吗**"，再信读数。

253. **引擎的"物理约束保护"是整轨的：4% 越界 → 100% 的音被移八度**（2026-09-25）。
     现场：`siren_end` 扒谱产物里 Strings **11/278（4%）**、Melody **9/32** 越出 `TR_RANGE`，
     引擎的"自适应八度边界保护"于是把**整条轨**移了 **+12 / +24 半音** ——
     96% 本来正确的音被一起改掉。还原任务里这是硬伤（用户口径："重要的是**符合原曲**"）。
     修法：在**生成端**（`transcribe_to_song.range_fit`）把**越界音逐个**移到最近的合法八度、
     合法音一个不动 → 引擎不再触发整轨移位。实测重生成后各轨越界 **0**
     （Piano 23–101→29–94 · Strings 29–87→41–87 · Melody 24–78→46–78）。
     **守卫**：`t_transcribe_range_within_instrument`（自证"合法音必须不动" + 全库越界合计 0）。
     **可迁移**：① 与 249（物理约束该无条件生效）**不矛盾、是它的下一层**：
     约束生效的**方式**也要对 —— "整轨挪"对生成曲无害，对**还原曲**就是改内容；
     ② 与 248 同族：要修在**判据读的那个入口的上游** —— `t_track_ranges_musical` 读的是
     引擎夹取**之后**的事件，所以数据越界时它照样 PASS，生成端必须自己守。

254. **`render.json` 的元数据只有一条路径会写 —— 另一条路径的曲目必定缺字段**（2026-09-25）。
     现场：`mid/out/composer/ref` 原来**只由 `new_song.py` 写**，走**还原路径**
     （`transcribe_to_song.py`）的曲目一份都没有 → `t_render_json_schema` FAIL
     （实测 `siren_end` 的 render.json 只有渲染参数 + `last_bands`）。
     根因不在"谁忘了写"，而在 **`make_song.py` 写回时只写它读到的那份 `cfg`** ——
     缺什么就永远缺什么。修法：写回前用**内存里已有的值** `setdefault` 补齐
     （`composer` / `mid` / `out` / `ref` / `song`），幂等、不动已有键。
     **可迁移**：① 与 248/253 同族 —— **多个入口写同一份文件时别指望"上游会写全"**，
     要在**收敛点**（这里是 `make_song` 的写盘处）兜底；
     ② **把新路径（还原/模仿）接进来后，先跑一遍全量自检**：凡是"读某个文件/字段"的老检查，
     都会成为新路径的第一批报错点（这次一口气暴露 4 条：`render_json_schema` /
     `accompaniment_harmony` / `melody_chord_fit` / `midi_ops_semantics`）；
     ③ 新路径还会暴露**判据适用范围**问题：`accompaniment_harmony` 的 95% 门是为
     **引擎生成的编配**设的（当年抓 `TR_SHIFT` 改音级），而还原曲的伴奏是**抄来的真实演奏**
     （实测 Piano 58% / Strings 55% / Hook 44%）—— 处理与 `melody_exempt` 同构：
     写**带实测数字**的 `patterns.accomp_exempt.fit`，**不改音去凑门**（用户口径"符合原曲"）。

255. **还原曲的默认 `arr` 会"凭空补音"—— 用户听出来的正是这个**（2026-09-25，**同一轮里被用户第二次纠正**）。
     现场：`transcribe_to_song` 默认把 `piano/pad/strings/glock/arp/shimmer` **全开着**，那些是
     **引擎按和弦自己生成的层**；再加上默认按 `density` **抽样**（`notes_extra_full` 没开）——
     一边凭空补、一边砍真音。实测 `siren_end`：成品 **3565 音** vs 转录 **2981 音**，其中
     **Arp 1098 音**（转录里根本没有 Arp 轨）；**11 个转录为空的小节，成品却有 73 个音**。
     用户原话："**midi 该没有声音的地方出现了声音，不符合原曲**，我不是说过了吗"。
     修法（**三条必须一起做**）：① `patterns.notes_extra_full = true`（不抽样）；
     ② 逐段 `arr` 的生成层**全关**（`piano/pad/strings/glock/arp/shimmer/uku/ep/bass` = False，
     `perc` 保留 —— 鼓型来自转录的 `drum_grid`）；③ **melody 层清空**：它是从主奏轨高音区抽的
     **副本**，与那条轨**重复发声**（音留在原轨，不丢）。
     实测：凭空补音的小节 **11 → 0** · 成品 3565 → **2757** 音（与转录差 −224，来自引擎防吞音去重）·
     `drum_grid` 为 0 的小节成品**一个鼓都没有**；`Ending` 段 `perc=0` 会吃掉 9 个真实鼓点（改成 2）。
     **可迁移**：① **还原路径与生成路径的默认值取向是相反的** —— 生成要"引擎帮你编配"（默认对），
     还原要"一个音都不许多"；新路径接进来时**先把所有"生成类默认"显式关掉**再看效果；
     ② 判据要**对着用户听的那个量**：这次量的是"**转录为空的小节里成品有多少音**"（11→0），
     不是频谱、不是总分；③ 与坑 254 同族：`t_melody_breathing` 因 melody 为空报"没有可检查的曲目"，
     已改成"**单曲沙箱里为空 = 本曲不适用；多曲库全空才 FAIL**"（防空转的判据别误伤合法状态）。

256. **补漏也有反面：分轨在"近乎静音"处的残余，补进去就是"该没声音的地方有声音"**（2026-09-25）。
     现场：用户说"**有时候有音乐的播放没有了**" → 逐小节量出 **47/143 小节**"原曲有声、我们没响"
     （最极端：小节 17 原曲 −21.7dB、我们 **−61.3dB**）。用分轨转录补回来方向是对的，但
     **第一版补法在结尾翻车**：小节 139/140 原曲是淡出到 **−52 / −70 dBFS**（等于没声音），
     Demucs 的 bass 分轨却在那里留下残余 → 转录器把它变成 12 个连续低音 → 补进去后成品
     变成 **−9.5 / −12.4dB**，正好又犯了上一轮那个错（用户上一句刚说过"该没有声音的地方出现了声音"）。
     修法（`scripts/restore_gap_fill.py`，**对称的两半**）：
     ① **补**：只补"我们 0 音 或 < 分轨 `--ratio`"的小节；② **能量门槛**：原曲该小节 RMS
     < `--min-rms`（默认 −38dBFS）**不补**；③ `--prune-quiet`：原曲静音的小节里**我们已有的音也删掉**。
     实测：11 个完全没响的小节补回 **177 音**、结尾 2 个静音小节**正确跳过**；起音覆盖 **68% → 74%**。
     **可迁移**：① 与坑 255 是一对 —— **补与删要用同一把尺子**（原曲能量），否则一定在某个方向翻车；
     ② 分轨/转录的**假音最集中在"近乎静音"处**（信噪比最低）——任何"从分轨补音符"的流程都必须带能量门槛；
     ③ **判据要挑对量**：按"我们 < 分轨 50%"会补进 **10665 音**（假音/泛音），
     按"我们 0 音 + 原曲有声"只补 177 音 —— **宁可少补，不可乱补**；
     ④ 收尾处的"多出来的声音"要分清是 **MIDI 音符**还是**音源释音尾巴**：
     本例 MIDI 在 **171.4s 已结束**，172s 之后的 −28~−58dB 是 FluidSynth 的释音（原曲那里 −46~−99dB）。

257. **"记录"不是"修复"：坑 255 的三条修法只落在单曲数据上，重扒同一首原样再踩 —— 现已全部代码化**（2026-09-25 晚）。
     现场：换一条路径重扒同一首（`siren_end2`）时，`transcribe_to_song.py` 的 `arr` **仍然是段名规则**
     （`'arp': nm not in ('C', 'Ending')`），而它自己 `--auto` 生成的段名是 **`S01…S24` + `Ending`**
     → 该条件**恒为 True** → 引擎凭空生成的 `arp/pad/glock/shimmer` **段段全开**。
     用户这次的原话是"**midi 内容前面有点乱**"；量出来：引子第 1–4 小节**转录只有 0~1 个音**，
     成品却有 **Arp 13 + Pad 4**（合计 17 音）—— 开头的主角是原曲根本没有的琶音与垫子。
     **三条修法全部落进代码**（255 当时只改了那一首的 `song.json`）：
     ① `transcribe_to_song.gen_layer_on(src_count)`：生成层**按本段有没有该来源的转录音**开关
        （不再看段名）。⚠ `bass/piano/strings` **仍要恒开** —— 它们的 `ev` 会被 `notes_extra`
        整轨覆盖，但引擎是 `if _tr not in ev: continue`，**关掉 = 把转录的音整轨丢掉**。
        实测 `pad/arp/glock/shimmer` **25/25 段全开 → 0 段开启**。
     ② **melody 在 `--full`（还原模式）下默认清空**：它是 `--melody-from` 那条轨高音区的**副本**，
        实测 Melody **176 音里 174 音（99%）**与 Piano 轨同音高且时间重叠（两轨同响、音色不同 = 糊）。
        要保留须显式 `--keep-melody`；实测成品 Melody **176 → 0 音**。
     ③ **"全关"之后必须回头看真内容有没有被一起关掉**：全关后引子变成 **−50.6dB**（原曲 **−29.2dB**）。
        查证那层是**明确的 A# 和弦垫**（0.5–4.5s 谱峰 A#2/F2/A#1/D3、谱平坦度 **0.057**、
        自相关 **0.925**），落在 Demucs 6s 的 `other` 分轨上；**YMT3 在 0–5s 一个音都没转出来，
        而 Basic Pitch 转出了 13 个**。→ 合并器新增 **Pad 轨**（主来源 = BP）+ 长音层整形
        （同音高碎片并成长音、时值下限 2 拍）。实测引子 **−50.6 → −41.7dB**（剩 12.6dB 如实记账）。
     **可迁移**：① **"记录"≠"修复"** —— 写成文档却只改数据的坑，下一首原样复现；
        **凡是"默认行为错"的坑必须改默认值 + 配守卫**，别指望每次手工改数据；
     ② **"关掉多余"和"关掉真内容"只差一步**，判据要**对着原曲量那个窗口**（逐秒 RMS + 逐带绝对差），
        不是看代码逻辑自洽；
     ③ 守卫 `t_transcribe_arr_by_source` + 变异用例"引擎生成层退回段名规则" —— 判据抽成
        `gen_layer_on` 做**行为测试**（读源码字面串太脆：注释里提一句旧写法就误判）。

258. **"文档写了、代码没接"在工具侧连中三次 —— 加 `scripts/pyenv.py` 统一兜底**（2026-09-25）。
     同一轮里三个工具的**文档与实现不一致**，症状都指向第三方库、看着像库坏了：
     ① `transcribe_audit.py`：`__doc__` 教的是 `python scripts/transcribe_audit.py …`，而它把
        `.venv-ml/Lib/site-packages` **插进 sys.path** 再 import librosa —— 主 venv 是 **Python 3.14**、
        而 scipy 是 **cp313 轮子** → 崩在 `scipy._lib._ccallback` → `_ccallback_c`；
     ② `bp_transcribe.py`：`__doc__` 写着"独立 bp-venv、`BP_PY` 可覆盖"，代码用的却是
        `sys.executable` → **`BP_PY` 从未被读取**，主 venv 跑必崩 `ModuleNotFoundError: basic_pitch`；
     ③ `measure_velocity.py`：`__doc__` 写着"加 `--self-test` 打印本轨峰值分布"，而 argparse
        **根本没有这个参数** → 按文档敲就是 `unrecognized arguments`。
     修法：新增 `scripts/pyenv.py` 的 `ensure(module, venv, why)` —— 当前解释器 import 不到目标模块时
     **自动 `os.execv` 换到目标 venv 重跑本脚本**（`sys.argv` 原样透传 + 防重入标志），都不行才打印
     **可复制的正确命令**并 `exit(3)`；三个工具各接一行；`--self-test` 真做出来（分轨为空时
     `calibrate()` 会把噪声底映射成"看起来正常的力度"，这一步是必需的）。
     **可迁移**：① **用法写进 `__doc__` ≠ 生效**（同族：209 判据要印在运行时输出、213 工具静默失效）；
        **能被代码回答的别只写文档**（CONVENTION §1）；
     ② 报错要**指向正确命令**，别让 traceback 停在第三方库里 —— 那会让人往错方向查。

259. **"读"也要分段：全曲中位数会把两头抹平**（2026-09-25，**用户当场纠正两次**）。
     原话："**不要直接看全曲，分段看，不是说过了吗**" → "**以后要全曲读的先看看要不要分段**"。
     （§20 / 坑 255/256 只管了"**改**"，"**读**"是这次新犯的。）
     现场：我按"全曲 chroma **0.904**、质心差 **−25%**"判定"音符基本对、音色偏低"，准备做**整曲**补偿。
     逐段量后完全是另一幅图：质心差 **+43Hz（引子）→ −944Hz（尾段）**、chroma **0.77~0.96**，
     **S02/S03 我比原曲亮 +17~21dB（该降）**、**S07–S24 反而低 25%（该升）**
     —— 整曲一套补偿必然在某一头过头（与 214 同型）。
     修法：`band_match.py --segments`（25 段各一套 + 交界 **±0.3s** 线性混合）实测平均带差：
     S10 **3.06→0.23** · S20 **4.35→0.64** · S02 10.32→5.86 · Ending 14.65→9.62
     （Ending/S01 是**缺层**，`--max-gain 6` 到顶也补不回来 —— 该回去改编配）。
     ⚠ 同轮修了 `band_match` 一个真缺陷：段边界给到接近末尾（`176.7` vs 实际 **176.701s**）
     会切出 **36 采样**的碎段 → `sliding_window_view` 当场崩、**25 段全算完才死**；
     现在相邻边界间隔 < 0.1s 的合并、末段延伸覆盖到结尾，窗口也随信号长度退让。
     同轮逐段表还暴露了**真·识别问题**：**S15（97–104s）我比原曲轻 9.4dB** →
     **YMT3 在 h6_bass/h4_bass 上那段整段 0 音**（原曲那段 bass **−24.3dB**，是全曲最响的低音段之一），
     而 **BP 转出了 7+7 个音**却因**单来源**被"≥2 支持"整批滤掉 → 合并器改成支持**多个并列主来源**。
     实测（pyin 基准）：Bass 一致率 **48.3%→59.1%**、漏检 **24.0%→7.2%**、音数 363→669。
     **可迁移**：① "读"与"改"是**同一条纪律**，标准入口 `scripts/report_sections.py`（逐段一张表：
        RMS/chroma/质心/最差带/轨构成）；② **"段间是否同向"本身要量** ——
        chroma/质心差的**跨度**就是判据（跨度大 = 不能一刀切）；③ 逐段表还是**症状入口**：
        这次的低音漏检就是"哪一段轻了 9dB"暴露出来的。

# 坑台账（当前：144–155）

> 归档的坑见 `PITFALLS-ARCHIVE.md`（1–40、81–88、89–92、93–104、105–108、109–113、114–120、121–126、127–130、131–132、133–143，**编号不变**）。
> 本文件只留"新近 + 高频"的坑 —— 台账会一直长，按 token 预算定期归档。
> 最近一次归档：141–143（并入 ARCHIVE）—— 加入 154~155 条时超预算，按 LIMITS 注释拆（不许抬上限）。

144. **`S.player` 从来没被赋值 → 任何任务完成后 `pollJob()` 都抛异常**（"试听某一轨"没反应）。
     用户报"音轨的试听按钮没有用"。三层根因，一层比一层深：
     ① **前端抛错**：`pollJob()` 的 done 分支对所有任务一律 `S.player.src = '…kind=mix…'`，
        而 `S.player` **从未被赋值**（全仓库只有这一处写属性、`btnStop` 里一处 `=null`）→
        `TypeError: Cannot set properties of undefined`，且它发生在 `await loadSong()`
        **之前** → 试听不播、连"任务完成后刷新界面/重载音源"也一起没了。
        （"⚡ 试听本段"看着正常，只因为它的分支写在这一行**之前**就 return 了。）
        反证：旧版跑 smoke 直接崩在 `pollJob` 的未捕获 rejection。
     ② **还缺分派**：服务端早就备好了 `/api/audio?kind=solo&track=`（产物落在 `TMP_AUDIO`，
        与 `jobs_dir` **同一个目录**），前端**从没请求过它**。修法：`playSolo(tr)` 把该轨音频
        当主音源接进 ENG 的完整播放通道（进度条/波形/播放头/⏹ 都能用），
        按 ▶ 或「↩ 回整曲」换回整曲；顺带删掉那个谁也读不到的 `<audio id="player">`。
     ③ **`loadMaster()` 没有返回值**：调用方写 `if(!ok)` 会**永远早退**（我第一版就踩了）。
        现在它如实返回"载进来了没有"（老调用方忽略返回值，不受影响）。
     **验收侧同一族的坑**：等待逻辑写成"job 为空就退出"→ 明明点成功了却判成"没点到"→
     又点第二次 → **两个任务并发渲染同一轨、互相删 `<base>.raw.wav`**
     （FileNotFoundError，与坑 140 ② 同一族）。所以：点击后**先等固定时间**再读，
     且**只有"从头到尾没建过任务"才回退**到 DOM `click()`。
     判据：真实鼠标点一次 → `S.soloTrack` = 点中那一轨 · `ENG.playing=true` ·
     时长 = 该轨渲染值 · `kind=solo` 请求 **1 次** · 任务 `rc=0`；按 ▶ 回到整曲。

145. **听感不对时"先量"是对的，但别把"忠于画像"当成"写错了"** —— 我连推翻自己三次。
     用户听 42 号 A 段说"旋律怎么乱乱的"。我依次怀疑并"证实"了三个问题，**每个都被真值推翻**：
      ① "正拍落点 44% 偏低" → 画像 `onbeat_pct` = **41.14**，44% 是**忠实的**；
      ② "级进 15% vs 画像 55.6%，差 3.7 倍" → 那个画像是 **F0 跟踪的产物**，同一文件
         `persona` 的注释早就写明"手写曲实际只有 6~15%" → **我拿错口径当了真值**；
      ③ "格 0 只有 9% 偏低" → 真实模板 **25% 分位的 8 小节窗就是 0.000**、中位 0.087；
         我们全库中位 **0.176** —— 比真实商业 BGM 还"规矩"。
     和声也被怀疑过（`Gsus4` 连续两小节、"悬留没解决"）→ 它是主题包的 `primary` 进行，
     出自两首真实模板（`II7|Vsus4|i7|i`）。**四条教训**：
      ① **口径必须同源**：F0 画像（音频跟踪）与 MIDI 真值不可直接比，差 2~4 倍是常态；
         要比就拿真实模板的 MIDI 算同一口径（`refs/midi2/` 218 首 → 1685 个 8 小节窗）。
      ② **"统计正常"≠"好听"，"统计偏离"≠"写错了"** —— 前者只能靠耳朵（README 第 1 条），
         后者要同源对照；两件事都不该拿单一指标下结论。
      ③ 真正定位到问题时靠的是**物理量**：把伴奏与旋律的**音域重叠**量出来（我的实验版
         钢琴 51% 压在旋律音域、引擎只有 5%）—— 那才是可行动的证据。
      ④ 连推翻三次说明"先猜后证"的顺序本来就慢：**先量、再猜**，别拿直觉当判据。

146. **"多生成几条挑一条"里，那个"挑"从来没看过听感**（`--step-bias` 的由来）。
     用户听同一骨架的 4 条候选旋律，结论很干脆："**级进 17% → 52% 越来越顺，
     202 之后两条都比原版好**"。回头查生成器，两条根因：
      ① `persona` 的 `leap = rng.uniform(0.70, 1.40)` —— **跳进率每首随机、两倍范围**，
         同一份画像下有的顺有的跳，纯抽签；
      ② 候选挑选用 `score = 形状共享×2 + 语言重合 + 复用冲突×0.5` ——
         **只衡量"与库里不像"**，完全没有听感维度（生成 8 条也永远不会优先选更顺的那条）。
     修法（opt-in，`step_bias=0` 时**逐字不变**、有自检断言）：把级进率作为候选之间的
     **相对偏好**（`melody_gen.cand_score` / `--step-bias`；`new_song` 默认 1.0，
     `BGM_STEP_BIAS=0` 可关）。实测同 seed 同 4 候选：关 → 选中级进 **44%**；
     开 → 选中 **68%**（候选 44/57/63/68）。
     **两个分寸**（`selftest.melody_step_bias` + `mutation_check` 两条注入用例守着）：
      ① 级进**只能当排序轴，不能当门槛** —— 画像口径不可比（见 145①），设绝对下限会把
         一批忠于画像的旋律判成不合格；
      ② 偏好量级必须**小于"去重"项**，否则会把"不与库里重复"这个主要目标挤掉。

147. **song.json 合规 ≠ 渲染输出合规：旋律被引擎整体 +12 顶出模板画像音区**（用户："8~11 秒有点奇怪"）。
     症状：A 段第一小节那段旋律发尖、刺耳。量法：**模板旋律画像的 `range` 才是"旋律该在哪儿"的权威**
     （cheerful [64,81]、tender [59,93]，`mean_pitch` 76.2）。实测：全库 9 首带旋律的曲子，
     `song.json` 的原始音域**全部落在各自画像内**，渲染后却被 +12 顶出去 ——
     43 号 66~81(A#4~A5) → 78~93(F#5~A6)，中位 D5 → D6（比画像均值高 10 个半音）。
     根因：`TR_SHIFT['Melody'] = 12`（2026-09-14 为修"伴奏和弦外音"引入：伴奏整体 −12 让位、
     **旋律 +12 独占最高**）。它修好了纵向配合，但**边界用的是"乐器物理音域 43~103"
     而不是"模板画像音域"**——后者宽得多，等于没保护。
     修：`Melody: 12 → 0`（伴奏 −12 保留：旋律回到原音区后仍是最高的非打击轨，
     Hook 最高 67 < 旋律 70，纵向配合不变）。副产物：Melody 与 Hook 的电平差 +13.1 → +9.2dB。
     **教训**：引擎里的"音区保护"要拿**模板真值**当边界，不能拿"乐器能响的范围"当边界。

148. **`perc_style: light` 是"无鼓组"风格 —— 欢快段用它 = 整首歌没有节奏骨架**。
     实测 43 号 A 段每小节 8 个鼓点、**全是同一个键位**（midi 82 = shaker），0 底鼓 0 军鼓；
     而 cheerful 模板池 8 首有鼓的里 **7 首有 kick+snare**、中位 22 点/小节。
     cheerful 主题包给的本来就是 `perc_style: dance`（`perc_density` 18.99）。
     全库扫描：**21/39 个 midi"有鼓点但无底鼓+军鼓"**，全部是 light 的歌 ——
     用户"所有你生成的快乐的都有这个问题"指的就是它。
     修：43 号 `light → dance`（鼓点 82 → 106：kick 40 / snare 20 / hihat 40）。
     **读源码注释**：`light` 的定义就是"想要'无鼓组'请用 light + perc=1（只留沙锤）" ——
     它不是"轻一点的鼓"，是"没有鼓组"。

149. **`bass_style: offbeat` 的 sub 层是"无条件"生成的**（`sub_gain: 0` 拦不住）。
     `out.append((0.25, sub_dur, bass - 12, int(72 * sub_gain)))` 只看力度乘法、
     **没有 `if sub_gain:` 判断**（其他风格分支早就写了）。于是 `sub_gain: 0` 只把力度乘成 0，
     音高照样写进 MIDI。实测 43 号 Bass 低到 F0(21.8Hz)、28% 的音低于 28Hz，
     而真实模板低音线（每 0.25 拍最低音）cheerful 最低 C1(32.7Hz)/中位 G1(49Hz)。
     修：加 `if sub_gain:` 门 + 43 号 `sub_gain: 0` → Bass F1~D#2(43.7~77.8Hz)，低于 28Hz 的 28% → 0%。
     同族（坑 81 的姊妹）：**参数写成"乘法系数"时，0 往往只把音量乘没、结构还在** ——
     要拦就拦在"生不生成"那一层。

150. **引子"一次性全开"= 4 个编配变量同时跳变**（听感：第 8 秒突然变亮）。
     43 号原引子 4 小节 `perc=0` + uku/glock 关 + `vel 0.85`，第 5 小节全开 →
     频谱质心 788 → 4907 Hz（`probe_peaks` 逐小节）。模板真值：b1–b2 安静、
     **b3–b4 鼓组进来**（cheerful 10 首里 7 首前 4 小节有鼓、合计中位 18 点；单看 b1 多数是 0）。
     修：拆 `Intro`(2 小节, perc=0) + `Intro2`(2 小节, perc=1, uku 开) → 引子鼓点 0 → 23。
     **副作用**：两半引子必须各写一支旋律（引擎按**段内相对**小节取旋律、没有段内偏移），
     而 `theme_melody_reuse` 按**角色**判"同名段落"（`Intro`/`Intro2` 都归 `intro`）→
     需要逐曲声明 `melody_reuse_exempt`，**理由必须非空**（空话不算，同 `align_exempt`；
     两条 mutation case 守着：拆掉豁免 / 理由写空白，都必须被抓住）。


151. **听感问题修完不算完 —— 要落到"引擎默认值 / 生成流程 / 自检守卫"三层，否则下首歌照犯**。
     用户口径："确保上面修改的内容之后的音乐生成也能遵守"。43 号这一轮的四条修复落点：
       · **旋律音区**：`song_engine.TR_SHIFT['Melody'] 12 → 0`（引擎默认值，所有新歌自动继承）
       · **贝斯次声波**：`song_engine.SUB_FLOOR = 24`，8 处 sub 层改写 `max(bass-12, SUB_FLOOR)`
         （引擎默认值）+ `bass_register` 守卫（改回去会被抓）
       · **引子渐入**：引擎加 `perc_part(inbars=…)` 读 `arr.perc_in`；`new_song.py` 给引子段
         自动写 `perc_in: 2`（**生成流程**）—— 比 43 号那种"拆段 + 豁免"干净，不必碰
         `melody_reuse_exempt`
       · **落点集中**：`melody_gen.cand_score` 加一项"落点分布与画像的 TVD"（生成流程，候选
         之间排序）+ `melody_onset_spread` 守卫
     判据：**每条守卫都有 mutation case**（拆掉 SUB_FLOOR / 落点门归零），且要有判据自证。

152. **判据的门不能拿"被检查对象自己的模块常量"** —— 否则 mutation 一注入，门跟着变，判据自废。
     实测：`bass_register` 第一版写 `if lo < SE.SUB_FLOOR`，mutation 注入 `SUB_FLOOR=0` 时
     门也变成 0 → **漏抓**（`mutation_check` 直接报"漏掉的故障意味着对应的自检项是坏的"）。
     改法：门用**独立真值常量** `BASS_FLOOR = 24`，另外**断言引擎常量与真值一致**
     （`assert SE.SUB_FLOOR == BASS_FLOOR`）——
     注入常量 → 一致性断言抓住；从别的路径让贝斯变低 → 独立门抓住。两条路都堵上。

153. **同一处"故障"被引擎挡住后，老的 mutation 会静默失效**。
     `低音整体再降八度` 这条 case 原先把夹具的和弦 bass 全 −12，靠 `track_ranges_musical`
     抓次声波；加了 `SUB_FLOOR` 之后本体 −12、sub 又被抬回 C1 → **故障其实不存在了**，
     于是"检查通过"被报成漏抓。改法：注入改成 **−24**（绕开被挡住的那一段），
     并在注释里写明为什么不是八度。
     **教训**：mutation 失效有两种 —— 判据坏了（要修检查）、故障被别的机制挡住了（要改注入）。
     两者处理方式相反，得看注入到底还改不改得动行为。



154. **判据的门必须用真值校准 —— 三条门"比真实音乐还严"，把忠于真实写法的曲子判成缺陷**。
     用户口径："限制的条件也有可能出错"。实测（10 首模板的 MIDI 与画像比）：
       · **时值交叠** `MELODY_ACCEPT_MIN = 0.55`：模板自身 cheerful **min 0.42 / 中位 0.52**、
         sorrow min 0.49 —— **门 0.55 会误伤 6/9 首模板**。根因是口径差异：画像的
         `dur16_hist` 是 **F0 跟踪的"发声时长"**，与 MIDI 的 note-off 不是一回事
         （`melody_gen._make_cell` 的注释里早就写明了）。
       · **span ≥ 画像 × 0.85**：模板自身 cheerful min 0.47 / 中位 1.12、sorrow min 0.45 /
         中位 0.76 —— **误伤 4/9 与 6/10 首**。"单曲音域比聚合画像窄"是常态。
       · **`FORM_SPAN_MIN = 12`**：模板最小 span = **8**（Disco Citizens）—— 误伤 3 首。
     改：0.40 / 0.45 / 8，并把真值依据写在常量旁边。
     **教训**：门的来源必须是"真值的分布"（分位数），不是"看着合理"。三条一起放宽后
     `mutation_check` 仍 **128/128** —— 放宽不等于失效，前提是**判据自证**还在。

155. **"引子不许上打击"这条设计规则与真值冲突；而且 `form.plan` 是硬编码段名表**。
     两件事叠在一起，导致新歌**从来没有引子**：
       · `theme_pack` 的 `form.plan` 直接写死 `['A','A2','B','A3','C','A4','B2','A5']`
         —— 段落长度来自模板统计，**段落结构从没分析过模板**。
       · `song_engine.arr_pack_idx` 给引子/尾声返回档 0（`perc=0`），注释写着
         "从简进入、留白收尾"，还被 `arr_role_variety` 断言守着。
     真值：cheerful 10 首里 **7 首前 4 小节有鼓**，合计**中位 18 点**（主段约 22 点/小节），
     模式是 **b1–b2 安静、b3–b4 鼓组进来**；sorrow 池鼓点中位 0（尾声确实不收打击）。
     改：`plan` 前后各补 `Intro`/`Outro`(4 小节)；引子给 `perc=1` + `perc_in: 2`
     （前 2 小节不敲，`perc_part(inbars=…)`）；**尾声保持 0**。断言与自证同步改。
     实测新歌引子鼓点 `[0, 0, 10, 12, …]` ✓ 与模板同形。


156. **`pwsh -Command "<整条命令>"` 这一层会吃掉命令里的半角双引号** —— 凡是要把
     "一段含引号的文本"当参数传给别的程序（`git commit -m`、`gh release --notes`、
     `python -c`、`--desc`/JSON 片段…），**别写在命令行里，落到文件再引用**。
     实测（PowerShell 5.1.26100）：
       · `git commit -m '测试 引号 " 和 反引号'`
         → `error: pathspec '和' did not match any file(s) known to git`
         （`-m` 的值被拆成多个参数，git 把后半段当成路径；commit 根本没执行）
       · `git commit -F <文件>` → 正常接收，`"` `'` `` ` `` `$` `%` `;` `&` `|` `( ) { }` 全部原样
     做法（**顺序很重要**）：① 用**文件工具**把 message 写盘（完全不经过 shell）；
     ② `git commit -F <绝对路径>`。写盘这一步不能省 —— 用 `Set-Content`/`echo >` 写
     同样会经过 shell，等于没绕开。
     同族坑：`powershell` 里 `>' 重定向写出来的是 UTF-16`，读的时候要用 `-Encoding unicode`；
     `Get-Content` 按默认编码读 UTF-8 中文会变 GBK 乱码 —— 文本读写一律指明编码，
     或者干脆走文件工具（见坑 156 与 `cli_utf8.py`）。

